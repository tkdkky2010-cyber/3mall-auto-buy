# WORKLOG 2026-08-26 — step1/step2 / Hmall clear_cart 품절 폴백 / n 상품번호 후보 리스트 / 롯데 #15~20 조합25 담기·결제 5/5

## 0. 사용자 지시 (원문)

1. "오늘 스텝 1,2 시켜"
2. "지금현대 조합이 안구해지는 사유? 어떤제품이없어서?"
3. (현대몰 6개 상품 URL 제공 — 자음2종/탄력3종/자음생2종/본윤2종/윤조3종/자음생크림리치)
4. "설화수 탄력크림 75ml - https://www.hmall.com/md/pda/itemPtc?slitmCd=2228732430&searchTerm=… 이거랑 이전꺼랑 왔다갔다함 현대도 롯데랑동일하게 탄력크림 2개 상품id 놓고 어떤거하나없으면 다른거 하나 들어가보면됨"
5. "롯데몰 설화수 최근 구매 14번아이디까지 했었는지 체크"
6. "롯데 14번까지 구매햇음?"
7. "15-20까지 18번빼고 25번조합 담아줘"
8. "지금까지 2번제품 몇번 아이디 사용했는지 알려줘"
9. "10-12번 아이디 알려줘 현대몰"
10. "2번제품 10~12번 아이디로 2개씩, 25번제품 1~9번 아이디로 2개씩 장바구니에 담아줘"
11. "406325 172.30.1.68:45227 페어링코드고 포트번호 36315야 / 롯데몰15-20번아이디 결제시작해줘"
12. "워크로그써줘 커밋푸시해"
13. (현대몰 카트플랜 캡처 제시) "현대몰 오늘 카트플랜이야"
14. "아냐 구매는 내가다른컴퓨터에서할게 오늘워크로그잘쓰고 커밋푸시해"
15. "62087 172.30.1.68:37001 페어링코드 / 포트번호는36315 , 현대몰 토스페이인데 결제가능해?"
16. "지금 여기서 물어보는거윈도우야,첫시작을 맥에서 한거고 원격을 켜놔서 어떤컴퓨터에서든지 다 공유되니까. 윈도우에서하려면 이 세션 버리고 다른데서해야해?"
17. "그럼 내가지금 여기서 치나 맥에서 치나 둘다 똑같다는거같은데 … 구매진행해 하면 바로가능한거네? 토스페이로해야함"
18. "ㅇㅋ 그럼 일단 이 맥 켜놓을거야. 다른 맥에서 이 세션연결해서 할거니까 워크로그잘써놔 커밋푸시잘해놔"

## 1. Step 1 — 1차 실행에서 **Hmall 27/27 ERR**

| 단계 | 결과 |
|---|---|
| 갤러리아 | 27조합 전부 측정. 추증 b~h 값 전부 상이(리디렉트 오염 없음), GWP 지문 일치(8/23 판독 재사용) |
| 재고관리 | 당일 구성 27조합이 활성 버전 `k` 와 전체 일치 — 새 버전 없음 |
| **Hmall** | **27/27 ERR** — `clear_cart 실패 — 카트 잔여물 ['스키니랩 시서스 다이어트…'] (2회 시도). 합산 오염 방지로 중단` |
| 롯데 | 27조합 측정. 쿠폰 10~13%, 카드 = **롯데카드 7% 청구할인**, 적립 tier 150,000↑ → 15,000원 |
| 카트플랜 | channel=lotte, N=7 |

### 원인 — 품절 상품은 `선택삭제` 버튼이 아예 없다

카트 DOM 실측:

```
nCbs=1, rows=[{checked:false, disabled:true, text:['[우수고객전용] 스키니랩 … 11박스','삭제','품절','169,000']}]
btns=[{txt:'품절/불가 삭제'}, {txt:'삭제'}]      ← '선택삭제' 없음
```

- 품절 상품은 체크박스가 **`disabled`** → `_JS_SELECT_ALL` 이 못 켠다.
- 그 상태의 카트엔 `선택삭제` 대신 **`품절/불가 삭제`** 버튼만 뜬다.
- `clear_cart` 는 `선택삭제` 를 못 찾으면 **그냥 `return`** → 잔여물 1건이 남고,
  `hmall.py` 가 그걸 감지해 **27조합 전부를 중단**시켰다.

### 수정 — `buy/run.py` clear_cart 에 `품절/불가 삭제` 폴백 1줄

```python
if delete_btn.count() == 0:
    # ★품절/구매불가 상품은 체크박스가 disabled 라 전체선택에 안 걸리고,
    #   '선택삭제' 대신 '품절/불가 삭제' 버튼만 뜬다 (2026-08-26 실측 …)
    delete_btn = page.locator("button").filter(has_text="품절/불가 삭제").first
```

**실검증**: 재실행 1회차에 `[cart] 삭제 시도1 — 선택 1/1건` → `[cart] 비우기 완료 (검증 0건)`,
2회차부터 `이미 비어있음`. 그 뒤 조합 측정이 정상 진행됐다.

## 2. Hmall ERR 19건은 **버그가 아니라 판매 조건** (지시 2)

수정 후에도 19개가 ERR. 실제 페이지를 열어 확인:

| 코드 | 상품 | 현대몰 상태 (실측) | 막힌 조합 |
|---|---|---|---|
| `f` | 윤조에센스90 | 본문에 **"현재 판매가 중단된 상품입니다"**, `btn-purchase` 0개 | 16개 — 1·3·4·5·6·7·8·9·10·11·18·19·20·21·22·26 |
| `n` | 탄력크림EX 75ml (구 `2228732274`) | **"방송중에만 구매가능"** 버튼, `btn-purchase` 0개 | 3개 — 24·25·27 |

사용자가 준 6개 URL 의 `slitmCd` 는 config(SoT) 와 **전부 일치**했고 정상 판매 중이었다.
즉 못 구하는 건 `f`·`n` 딱 둘. **`f` 하나만 살아나면 27개 중 24개가 나온다.**

## 3. ★ `n` 상품번호 **후보 리스트** — 데이터만 고쳐 3조합 복구 (지시 4)

사용자가 신번호 `2228732430` 제공("이전꺼랑 왔다갔다함").

**후보 리스트 메커니즘은 이미 있었다**(2026-08-19 신설, `_common.id_candidates` + `hmall.py:221` 폴백).
그런데 `sulwhasoo-ids.json` 의 값이 전부 문자열이라 **아무도 쓰지 않고 있었다**
(ERR 문구 `(후보 1개 전부)` 가 그 증거). → 코드 수정 없이 **데이터만** 리스트로 바꿨다.

```json
"hyundai": ["2228732274", "2228732430"]
```

### 바꾸기 전 가격 대조 — 다른 상품이면 조용한 오측정이 된다

구번호는 `기획세트(쇼핑백)`, 신번호는 `단품` 이라 같은 SKU 인지 확인이 필요했다:

| | 구 `2228732274` | 신 `2228732430` |
|---|---|---|
| 상품명 | `[공통][쇼핑백]탄력크림EX 75ml 기획세트` | `[공통]탄력크림EX 75ml` |
| 구매버튼 | 0개 (방송중에만 구매가능) | **2개 (구매하기)** |
| 정가 → 할인가 | 135,000 → 121,500 | 135,000 → 121,500 |
| KB국민 7% | 112,995 | 112,995 |

**가격 완전 동일** → 폴백해도 오측정 없음을 확인하고 적용.

**실검증**:
```
[STEP] add_to_cart n × 2  [후보 1/2 2228732274]   → 실패
[STEP] add_to_cart n × 2  [후보 2/2 2228732430]   → 성공
[WARN] n 1순위 2228732274 실패 → 후보 2 2228732430 로 성공
```
조합 24·25·27 복구 → **Hmall 측정 11/27** (2·12·13·14·15·16·17·23·24·25·27).

- ⚠️ 구번호가 1순위라 `n` 조합마다 25s 타임아웃(3조합=약 75초). **순서는 그대로 뒀다** —
  사용자가 "왔다갔다한다"고 했으므로 오늘 기준으로 뒤집으면 바뀌는 날 다시 느려진다.
  (READ_FIRST "사용자가 정한 상수를 stale 해 보인다고 자동으로 바꾸지 말 것" 과 같은 계열)
- ⚠️ `hsmaster/config/sulwhasoo-ids.json` 은 **gitignore** — 집 PC 에서 수동 동기화 필요.

## 4. cart_plan / 미러 재실행 — 채널 결정은 불변

Hmall 값을 채운 뒤 `cart_plan.py` 재실행 → **channel=lotte, N=7 동일**.
측정된 Hmall 11행 전부 롯데가 최저였다.

| 조합 | 갤러리아 | Hmall | 롯데 |
|---|---|---|---|
| 2 | 0.4763 | 0.4755 | **0.4468** |
| 24 | 0.4843 | 0.4834 | **0.4553** |
| 25 | 0.5052 | 0.5043 | **0.4756** |
| 27 | 0.4946 | 0.4937 | **0.4653** |

플랜: 조합 3·8·12·22·24·25·27 각 1개, 2,417,458원, 평균 0.4789.
**원본(`8.26` 탭) + 미러(`당일 설화수`) 양쪽 실측 확인** — 미러 J1:M28 에 Hmall 열이 들어간 것 확인.
(1차 미러는 Hmall ERR 상태로 복사돼 있어서 `mirror_daily.py sulwhasoo` 재실행함.)

## 5. Step 2 — 현대몰 10% 적립 체크

계정 `tkdkky2002` / **10%적립 10건 / 적립없음 7건 / ERR 2건**.
`8.26 식품` 탭 신규 생성(A1:V21), `당일 식품` 미러 21행 반영(양쪽 실측 확인).

- 10% 적립: #2·3 하루견과, #8·9 이디야, #11 라벤더꿀, #16 루솔, #17 원데이알파, #25 석류젤리, #34 파라다이스버닝, #35 프리바이오틱스
- ERR 2건 = **판매중단**: #28 스키니랩 시서스(= §1 의 그 카트 잔여물), #39 서정희pick 올리브오일

## 6. 롯데 구매 이력 조회 (지시 5·6) — #14까지 완료

| 날짜 | 계정 | 조합 | 금액 |
|---|---|---|--:|
| 8/24 | #1~#7 | 조합3 `e탄력3 2 + f윤90 2` | 513,565 |
| 8/25 | #8~#14 | 조합25 `e탄력3 2 + n탄력EX75 2` | 504,636 (KB) |

#15~#20 은 대장상 마지막이 8/05 (#18 `nami8225` 는 기록 자체 없음).

- ⚠️ **대장은 best-effort 가 또 확인됐다** — #15 는 대장 마지막이 8/05 인데
  실제 롯데 주문내역엔 **8/21 주문(2026-08-21-J62925)** 이 있었다(§9 에서 발견).

## 7. 롯데 #15·16·17·19·20 조합25 담기 (지시 7) — 5/5

18번은 지시대로 제외. 전 계정 **`[OK] 담기 검증 — 카트 2건`** 로그 확인.
쿠폰도 e(2923406968)·n(2923426629) 양쪽 `downloaded`.
비번변경 캠페인 팝업은 5계정 모두 미발생.

## 8. 현대몰 담기 (지시 8·9·10) — 12/12

지시 8 답변용으로 대장을 상품별·계정번호별로 집계:
**2번(하루견과 초록색)** = 7/13 에 #2~#8·#10, 8/24 에 #2·3·7·8·9. → 8/30 까지 재구매 불가 5계정 파악.

담기 실행 (`buy/cart_plan.json` 재작성 후 `run.py 1-12`):
- **#1~#9** → 25번 `유기농 석류젤리 9박(90개)` ×2
- **#10~#12** → 2번 `하루견과 초록색 100봉` ×2

### ★ Hmall `add_to_cart` 의 `[OK]` 는 검증이 아니다 → 카트 실조회로 확인

`buy/run.py add_to_cart` 는 **장바구니 버튼 클릭 직후** `[OK]` 를 찍는다. 담겼는지 확인하지 않는다
(롯데 `lotte_add_combo` 엔 `담기 검증 — 카트 N건` 이 있는데 Hmall 엔 없다).
→ 12계정 전부 **재로그인해서 실제 카트 행을 읽었다**:

```
# 1~ 9  카트 1건  · [우수고객전용] 빨간스캔들 유기농 석류+콜라겐 젤리 90포  x2
#10~12  카트 1건  · [우수고객전용][하루견과] 순수견과100% 썬넛트 … 23gx100봉  x2
```
전 계정 정확히 1건 / 수량 2, 잔여물 없음.

## 9. 롯데 #15~20 결제 (지시 11) — **5/5 완주, 실돈 2,497,690원**

폰 페어링: `adb pair 172.30.1.68:45227 406325` → `adb connect 172.30.1.68:36315` (SM-G9960).

`today_carts.json` 이 8/24 자라 preflight 하드스톱 → **8/18·8/24 선례대로 우회 대신 오늘 실제 내용으로 갱신**
(ALLOW_STALE_CART 안 씀).

| # | 계정 | 주문번호 | 금액 | 뷰티 | 구매사은 |
|--:|---|---|--:|---|---|
| 15 | joyoungjin1220@gmail.com | 2026-08-26-B31965 | 499,538 | ✓ | ✓ |
| 16 | kimmk2008 | 2026-08-26-B31294 | 499,538 | ✓ | ✓ |
| 17 | miu1838 | 2026-08-26-B31357 | 499,538 | ✓ | ✓ |
| 19 | ybkim9960 | 2026-08-26-B31812 | 499,538 | ✗ | ✓ |
| 20 | yeonsuk0428@naver.com | 2026-08-26-B31882 | 499,538 | ✓ | ✓ |

당일카드 = **롯데(7%)** 자동감지 → `pay_loca`. **구매대장 시트 5건 전부 확인.**

### #15 1차 PAY_FAIL → 주문내역 실조회 후 재시도 완주

- 1차: `PAY_FAIL@loca_app:wait_for_activity 'SimpleNumberAuth' timeout`
  — LOCA 앱이 `PayRequestActivity` 에서 간편번호 화면으로 안 넘어감.
- 8/24 #1 선례대로 **재시도 전 PC 주문내역 실조회**: 최근 주문 = 8/21·8/05·8/02, **오늘 주문 없음** → 미결제 확정.
- 재시도 완주. **PAY_FAIL 이어도 "결제 안 됨"이 아니다 — 주문내역 확인이 표준 절차** 를 다시 확인.

### #19 뷰티포인트는 재시도해도 안 된다 (근본원인 확인)

스샷(`/tmp/_lt_beauty_19.png`) 실판독:
> "고객님의 정보와 일치하는 뷰티포인트 카드 등록 고객이 없습니다."

→ 계정에 뷰티 멤버십 자체가 없음. 코드 문제 아님, 재시도 무의미. 구매사은 적립은 정상.

## 10. ★ 핸드셰이크 `request.json` **잔재 오탐** — 하마터면 딴 화면을 두드릴 뻔

`CLAUDE_OCR_ONLY=1` 로 결제하며 `phone_auto/_tmp/claude_ocr/request.json` 감시를 걸었는데,
감시 시작 즉시 요청이 뜬 것처럼 보였다. 이미지를 열어보니 **Hmall 92,069원 일반결제 카드번호 키패드** —
지금 돌리는 롯데(499,538원)와 전혀 다른 화면이었다.

파일 mtime 확인: **2026-08-06 18:34** (id `1786008893304` = 같은 시각). **20일 전 잔재**였다.

- 그대로 판독해 좌표를 응답했으면 **엉뚱한 화면 좌표를 두드렸다.**
- 조치: 잔재를 scratchpad 로 치우고 감시 재기동. 이후 실제 요청은 한 번도 오지 않았다
  (롯데카드=`pay_loca` 는 간편번호 자동입력이라 핸드셰이크 불필요).
- **교훈: `_tmp` 감시는 "파일이 있다" 가 아니라 "이번 실행 이후에 생겼다" 로 판정해야 한다.**
  이미지 내용(몰 이름·금액)이 지금 작업과 맞는지 대조하는 것도 마지막 방어선이다.

## 11. 현대몰 결제 인수인계 — **이 맥에서 실행 + 다른 맥에서 원격 접속** (지시 14·16·17·18)

사용자가 카트플랜을 캡처로 확인해줬고(지시 13), 담긴 내용과 **정확히 일치**했다:

| 플랜 | 계정 | 실제 담긴 것 (카트 실조회) |
|---|---|---|
| 25 유기농 석류젤리 9박(90개) | 1-9 | `빨간스캔들 유기농 석류+콜라겐 젤리 90포` ×2 |
| 2 하루견과 초록색 100봉 | 10-12 | `썬넛트 하루견과 넛츠시그니처 23g×100봉` ×2 |

### ★ Remote Control 로는 **실행 위치가 안 바뀐다**

지시 14 시점엔 "다른 PC 에서 결제" 로 알아듣고 git 인수인계를 준비했으나, 지시 16~18 에서
실제 계획이 밝혀졌다: **이 맥(Jasons-Mac-mini)을 켜둔 채 다른 맥에서 이 세션에 원격 접속**해서 친다.

- 세션 명령은 **세션을 띄운 호스트에서 실행된다**(`uname` = Darwin / `hostname` = Jasons-Mac-mini.local).
  Remote Control 은 **대화·화면만** 공유한다. 어느 기기에서 타이핑하든 `adb`·`python` 은 이 맥에서 돈다.
- 따라서 **git pull / 재페어링 / 파일 동기화가 전부 불필요**하다. 그냥 여기서 이어서 치면 된다.
- 부수효과로 **윈도우 OCR 취약점(2026-08-19 5곳 파손)도 해당 없음** — 판독은 맥 Apple Vision 이 한다.
  (지시 15~16 답변 중 윈도우 실행을 전제로 `pay_toss` 위험을 경고했는데, 맥 실행이면 그 전제가 사라진다.)

### 그래도 남겨두는 것 — 진짜로 다른 PC 에서 돌릴 때

| 항목 | 넘어가나 | 비고 |
|---|---|---|
| **장바구니 내용** | ✅ 자동 | 카트는 PC 가 아니라 **Hmall 서버**에 있다. 단 **당일 만료** |
| `cart/today.json` · `cart/today_carts.json` | ✅ git | 결제 preflight + prmo(적립 이벤트) 정본 |
| `hmall_config.json` | ❌ gitignore | 계정 정보 — 그 PC 에 이미 있어야 함 |
| `buy/cart_plan.json` | ❌ gitignore | **결제엔 불필요**(담기 전용). `hmall_hyundai_buy.py` 는 안 읽는다 |
| `hsmaster/config/sulwhasoo-ids.json` | ❌ gitignore | 설화수 전용 — 현대몰 식품 결제엔 불필요 |
| **폰 adb 페어링** | ❌ | 기기별. 그 PC 에서 다시 페어링 필요 |

## 12. 토스페이 결제 가능 여부 (지시 15·17) — **가능, 지정도 불필요**

사용자 요구: "토스페이로해야함". 오늘 Hmall 카드할인 = `KB국민 7%` + `토스페이 NH 7%` (동률).

### 자동으로 토스가 선택된다 — CLI 강제는 오히려 안 먹는다

`detect_card()` 는 카드할인 영역에 `토스` 문자열이 있으면 **다른 판정보다 먼저 무조건 `"토스"` 를 반환**한다.
토스페이는 삼성카드를 감싸는 구조라 `pay_samsung` 과 경로가 완전히 달라 반드시 먼저 분기시켜 둔 것
(2026-07-15 실측 DETECT_CARD_FAIL 대응). 이 early-return 은 "가장 싼 카드" 로직보다 앞선다 —
오늘은 KB 와 토스가 둘 다 7% 라 손해 없음.

⚠️ **반대로 CLI 로 `토스` 를 강제하는 건 안 된다.** `card_override` 는 `CARD_GRID_NAME` 에서만 찾는데
거기엔 토스 키가 없다(`현대/롯데/하나/KB/삼성/NH/BC`) → 인자로 줘도 **조용히 무시**되고 자동감지로 떨어진다.
결과적으로 토스가 잡히므로 문제는 없지만, "강제했다"고 믿으면 안 된다.

### `pay_toss` 전제와 함정

- **폰 토스앱(`viva.republica.toss`) 로그인 전제.** 미로그인이면 게스트 본인확인(휴대폰+SMS/PASS)이 떠
  자동화 불가 → `pay_toss` 가 감지해 **카드 인증 전에 안전정지**(미결제라 재시도 안전).
- **승인 후 '완료' 필수.** 7/15 #2 실측: 카드 94,005원 승인됐는데 **Hmall 주문이 생성 안 됨**.
  `완료해주세요` 화면의 하단 '완료' 를 눌러야 주문이 최종 생성된다(OCR 실패 시 고정좌표 `(540,2130)` 폴백).
  → 결제 후 주문번호가 안 나오면 **이 지점부터 의심**하고, 재시작 전에 주문내역을 실조회할 것.
- PIN 은 셔플 + `FLAG_SECURE`(screencap 검정) → `text_dump` 로 입력. 핸드셰이크 대상 아님.
- ⚠️ `pay_toss` 는 탭 4곳 중 3곳이 **순수 `ocr_tap`**(dump 폴백 없음) — 2026-07-15 작성이라
  2026-08-19 윈도우 OCR 교훈(`ocr_or_dump_tap`)이 반영돼 있지 않다. **맥 실행이면 무해**하지만,
  언젠가 윈도우에서 돌린다면 여기부터 고쳐야 한다.

## 13. 미결

- [ ] **현대몰 #1~12 결제** — 카트만 담긴 상태. 이 맥에서 실행하고 사용자가 다른 맥에서 원격 접속해 진행(§11).
      **블로커: 폰 adb 연결.** `ping 172.30.1.68` 은 0% loss 인데 `connect …:36315` 은 `No route to host`
      = 무선 디버깅이 꺼졌거나 포트가 재배정됨. 폰 '무선 디버깅 > IP 주소 및 포트' 값 필요
      (페어링 포트 37001 과 다른 값). 카트는 **당일 만료**라 오늘 안에 해야 함.
- [ ] 결제 전 확인: **폰 토스앱 로그인 상태** (미로그인이면 게스트 본인확인으로 정지)
- [ ] `f 윤조에센스90` 현대몰 판매중단 — 대체 상품번호 확보되면 후보 리스트에 추가하면 16조합 복구
- [ ] 롯데 #18 `nami8225` — 이번 지시에서 제외됨(담기·결제 안 함)
- [ ] 롯데 resume 라이브 미검증 (8/24 이월) — 이번에도 발동 상황 없었음
- [ ] 8/23 H.Point 이벤트(P202608074038) 미신청 16계정 — 사용자 결정 대기
- [ ] 8/18 #8 구매대장 `(조합 미지정) x1 ?원` 행 보정
- [ ] `sulwhasoo-ids.json`(n 후보 리스트) · `buy/cart_plan.json` 은 gitignore — 집 PC 수동 동기화 (둘 다 **결제엔 불필요**, 담기·rate-check 용)
- [ ] `phone_auto/hmall_hyundai_buy.py` 의 `HMALL_STOP_BEFORE_PAY`(8/24 작성분)는 **라이브 미검증**
- [ ] `pay_toss` 의 `ocr_tap` 3곳 → `ocr_or_dump_tap` 전환 (윈도우 실행 대비, 맥에선 불필요)
