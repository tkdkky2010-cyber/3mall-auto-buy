# WORKLOG 2026-09-09

## 사용자 지시 (문장 그대로)

> "1. 이 폴더를 다른 ai한테 맡기니까 완전히 맛탱이가 가버려. 결제하기 페이지에서 이거저거 버벅거리는게
> 정말 많고 결제단계에서도 많이 버벅거려. 코드가 중복되는게 많고 이것저것 겹치는게 많아서 제대로 읽지를
> 못하나봐. 지시내용이 다른데 그 다른 지시들이 공존해서 그런건데 폴더 전체 읽고 어떤 것들이 필요 없는지
> 나랑 수정하자."

> "2. NS홈쇼핑(내가 이용하는건 갤러리아, 롯데, 현대잖아) 거기서 반품이 들어왔는데 어떤 계정으로
> 신청했는지 모르겠어. 자음생 크림리치 50ml 반품이었어, 이거 어떤 홈쇼핑에서 어떤 아이디로
> 반품신청했는지 너가 찾아줘; 하나씩들어가서 마이페이지 들어가면 "반품" 탭 찾아보면돼
> 자음생크림리치 50ml 있으면 그거 어딘지 날짜어딘지 어떤계정인지찾아줘"

> "롯데 순서 "★순서 확정: 할인쿠폰 → 플러스쿠폰 → 적립금""
> "그리고 카트플랜에 대해서도 두가지 코드파일이 있다던데 최근에 내가 카트플랜 주고 나면
> 어떤 파일들로 사용하고 있었는지 확인해"

**사용자 결정 (질문 응답):** 반품 시점 = 최근 2주 이내(8/26~) / 정리 범위 = **충돌 지시 8건만 해소**
(파일 삭제·워크로그 아카이브·PIN 평문 제거는 이번엔 안 함).

---

## 1. 다른 AI 가 헤맨 구조적 원인 (전수 조사 결과)

### 1-1. 루트에 `CLAUDE.md` 가 없다
`READ_FIRST.md` 는 **자동 로드되지 않는다**. 반대로 `hsmaster/CLAUDE.md` 는 자동 로드되는데
내용이 5월 스펙(「고정 11개 조합」·「Track B 미구현」)이고, 첫 줄이 `../CLAUDE.md 를 상속한다` 인데
그 파일이 **없다**. → AI 가 가장 먼저 읽는 문서가 가장 낡은 문서다.

### 1-2. "폴더 전체를 읽어라" 가 컨텍스트 폭탄
마크다운 총 799KB 중 **워크로그가 594KB(74%)**. 28개 파일, 8/05 하나만 57KB.

### 1-3. 같은 주제를 여러 문서가 다르게 말한다 → 아래 8건

---

## 2. 고친 것 — 결제 경로 충돌 지시 8건 (코드 실측 기준으로 통일)

| # | 주제 | 무엇이 틀렸나 | 고친 파일 |
|---|---|---|---|
| 1 | **키패드 판독** | READ_FIRST 가 "로컬 OCR 은 코드에서 삭제됨 / 플래그 없음" 이라 했으나 **거짓**. `ocr_keypad.py:46` 기본값은 지금도 `ENGINE_FAST=("vision","easyocr")` 로컬 사다리다. 그 서술을 믿고 `CLAUDE_OCR_ONLY=1` 없이 돌리면 **키패드마다 120초를 태우고 실패** → 이게 사용자가 말한 "결제단계 버벅거림" 의 정체. 게다가 "이 **두 개**를 켠다" 라고 써 놓고 하나만 적어 둬서 없는 플래그를 찾게 만들었다 | `READ_FIRST.md` |
| 2 | **CDP 포트** | `RULES.md §9` 가 "Step1=9224 / Step2=9223 분리" 라 했는데 2026-07-16 **단일 CFT 원칙(9222 하나)** 으로 폐기된 서술. 따라 하면 미로그인 Chrome 이 떠 조용히 실패하고, 9223 은 폰 webview `adb forward` 와 충돌해 **적립이 통째로 날아간다** | `rate-check/RULES.md` |
| 3 | **롯데 결제설정 순서** | STEPMAP `C` 절이 "①적립금 →②현금영수증 →③주소 →④할인쿠폰…" 인데, **같은 문서 하단**이 "★순서 확정: 할인쿠폰 → 플러스쿠폰 → 적립금(쿠폰이 포인트를 리셋)" 이라 정반대. C 순서로 하면 **포인트 미적용 결제** | `LOTTE_HOMESHOPPING_STEPMAP.md`, `LOTTE_PAYMENT_RUNBOOK.md` |
| 4 | **`buy/run.py` 게이트** | PM 문서가 "기본값 `CART_ONLY=false` 라 결제하기까지 클릭됨 — 절대 금지" 라 경고. 실제는 `run.py:62` 기본값 **`true`** 이고 `do_checkout`/7자리/폰트리거는 **삭제됨**(`run.py:4`) = **존재하지 않는 위험** | `.claude/agents/project-manager.md` |
| 5 | **결제 진입점** | PM Step5 가 `python3 -m phone_auto.hmall_combo_checkout` 을 시키는데 **그 모듈이 없다**. 실제 정본은 루트 `buy.py` (몰별 디스패처) | `.claude/agents/project-manager.md` |
| 6 | **롯데/갤러리아 상태** | PM 이 "PM 통합 X / **작동 검증 X**" 라 했으나 8~9월 내내 라이브 결제가 돌았다(구매대장 기록). 되는 걸 안 된다고 보고하게 만듦 | `.claude/agents/project-manager.md` |
| 7 | **롯데 구매사은 적립** | READ_FIRST 의 이 절이 시간순으로 쌓이기만 해 어느 게 현행인지 알 수 없었다. ★**rebase 중 발견**: 다른 PC 의 9/7·9/8 커밋이 이미 **「CFT 먼저 → 앱 전용 행사만 폰」** 으로 결론을 바꿔 놨다. 내가 먼저 쓴 "경로는 폰이다" 는 **틀린 결론**이라 걷어내고, 「뒤로 갈수록 최신, 맨 아래 규칙이 현행」 이라는 읽는 순서만 명시 | `READ_FIRST.md` |
| 8 | **토스 PIN source** | 문서 상단 `text_dump` / 하단 `dump` 로 자기모순. 코드 정본은 `text_dump` | `TOSS_PAY_NOTES.md` |

부수 정정: PM Step2 의 "CDP 9223" → 단일 CFT 9222, PM 「미구현 모듈」 절(2026-05 상태, 없는
`rate-check/run.py` 를 실행기로 안내) → 2026-09-09 실측 현황표로 교체.

**원칙**: 낡은 서술을 조용히 지우지 않고 **"종전엔 X 라고 돼 있었는데 사실은 Y"** 로 남겼다.
같은 문서를 다시 읽는 AI 가 옛 서술을 어디선가 다시 만나도 재감염되지 않게 하기 위함.
사용자 지시 원문(#7 의 "cft에서")은 **문장 그대로 보존**했다.

### ★ 두 PC drift — 이번에 실제로 밟았다

작업 후 push 가 거부됐다(원격에 9/7·9/8 커밋). rebase 해 보니 다른 PC 가 **같은 세 문서**
(`READ_FIRST` / `STEPMAP` / `RUNBOOK`)를 이미 고쳐 놨고, **#7 은 나와 반대 결론**이었다.
그대로 push 했다면 최신 결정(CFT 우선)을 옛 결론(폰 우선)으로 **덮을 뻔했다.**
→ **문서 고치기 전에 `git fetch` 로 원격 확인부터 할 것.** 이 레포는 두 PC 를 오간다.

## 2-1. 아직 안 한 것 (사용자가 이번엔 제외)

- 루트 `CLAUDE.md` 신설 + 워크로그 아카이브(`WORKLOG/` 이동)
- 죽은 파일 삭제: `hsmaster/` TS 전체(node_modules 없어 **실행 불가**, 실제 쓰는 건
  `config/sulwhasoo-ids.json` + `scripts/launch-hmall-chrome.sh` 2개뿐) /
  `step1.sh`↔`step1.py`·`step2.sh`↔`step2.py` 사본 이중화(과거 2회 drift) /
  `step1_parallel.sh`·`launch-check10-chrome.sh` 폐기 스텁 /
  `buy/checkout_kakao.py`·`buy/recollect_coupons.py`·`phone_auto/adb_fwd.py` (호출자 0) /
  `coords/apps/lotte_homeshopping.json` + `flow_runner` 의 `lotte_*` 핸들러 5개(로더 없음) /
  `GOAL.md`(2026-05 스냅샷) · `NH_GENERAL_PAYMENT_VISION.md`(수동 절차서)
- **PIN `137601` / NH 카드번호·CVC 가 git 추적 문서·코드 40여 곳에 평문** — `GOAL.md` 가 스스로
  정한 "PIN 평문 금지" 규칙 위반. git 히스토리에도 남아 있어 별도 작업 필요.

---

## 3. 카트플랜 파일 — "두 개" 가 아니라 넷이고 서로 다른 물건 (사용자 질문)

| 파일 | 정체 | 최근 실사용 |
|---|---|---|
| `rate-check/cart_plan.py` | **설화수** 조합 분배기. K2:M 공급률 읽어 채널·N 정하고 시트 **O열** 에 씀 | ✅ 9/6 step1 5/5 단계 (`channel=lotte, N=7`) |
| `cart/combo_plan.py` | **식품** 적립구간 맞추는 수량 계산기(보조) | 최근 실행 흔적 없음 |
| `buy/cart_plan.json` | **식품 담기 계획**. `buy/run.py:41` 이 읽음 | ✅ 9/4 최종 |
| `cart/today_carts.json` | **결제 매니페스트**. `buy.py`·`hmall_hyundai_buy`·`nh_enter`·`verify_cart` 가 읽음 | ✅ 9/4 20:06 |

**사용자가 카트플랜을 주면 실제로 도는 경로 (9/3~9/4 로그 실측):**
```
자연어 플랜 → buy/cart_plan.json → buy/run.py (CART_ONLY 기본 true) → PC 카트 담기
           → cart/today_carts.json (손으로 작성) → buy/verify_cart.py 대조
           → buy.py → phone_auto/hmall_hyundai_buy.py (폰 결제)
```
- ⚠️ `rate-check/cart_plan.py` 는 `buy/cart_plan.json` 을 **만들지 않는다.** 이름만 같고
  설화수↔식품으로 갈래가 다르다. `GOAL.md:19` 가 "Step 3 → buy/cart_plan.json 변환" 이라 써서
  둘을 한 흐름처럼 보이게 만든다(미수정 — 다음 세션 후보).
- ⚠️ 담기 계획과 결제 매니페스트를 **손으로 두 번 적는 구조**라 어긋나면 조용히 딴 걸 결제한다.

---

## 4. 자음생크림리치 50ml 반품 계정 추적

### 방법
구매대장(구글시트) 로 후보를 좁힌 뒤 **각 몰 마이페이지를 계정별 실측**. 대장만 믿지 않은 이유 =
**대장이 불완전**했다(롯데 8/11·8/17·9/7 주문이 대장에 없음).

- 롯데: `mypage/searchOrderList.lotte?start_date=…&end_date=…&m_arg=365` → 상품행 진행상태 직접 파싱
- 갤러리아: `mypage/initOrderClaimList.action` (취소/교환/반품 전용) + `전체/취소/교환/반품(N)` 카운터
- 현대: `mo/mpa/selectOrdDlvCrst?selectTypeGbcd=**P2**` (**P2=반품** / P1=교환 / C1=취소)

### ★ 함정 2개 (다음에도 걸린다)

1. **현대몰 반품 탭 파라미터** — 1차 스윕에서 `selectTypeGbcd=P1` 로 돌렸는데 그건 **교환 탭**이다.
   페이지가 "검색 기간 내 **교환** 내역이 없습니다" 를 뱉는데 내 파서는 "내역 없음" 으로 읽어
   **19계정 전부 반품 0건으로 오판**했다. 헤더 카운터가 "취소3" 인데 목록이 비어 있는 **모순**을
   보고 재검증해서 잡았다(READ_FIRST 「관측이 도메인 상식과 모순되면 관측부터 의심한다」).
   → **반품 = `P2`**.
2. **갤러리아 강제 비번변경 본인인증** — `linguist01` 로그인 시
   `member/ForcePwdChangeAuth.action` 이 뜨는데, 안 닫으면 **그 뒤 모든 계정이 같은 화면에 갇힌다**
   (`#login_id` 가 3개로 늘어 strict-mode 위반). 롯데 비번변경 캠페인과 같은 유형.
   → **`취소` 로만 탈출.** 본인인증하기/비밀번호 변경 절대 금지.

### 결과

- **롯데 20/20 계정 완주** — 최근 2주 내 크림리치 **반품 없음**.
  8/28 `주문취소` 2건(`kms3945` `2026-08-28-C28434`, `kgi5907` `2026-08-28-C28384`)뿐인데
  이건 배송 전 취소지 반품이 아니다.
  (참고 — 과거 반품은 있다: 6/30 `반품완료` 7계정 일괄 / 6/11~6/14 `교환반품완료` 다수.)
- **갤러리아 8/12 계정 확인 — 전부 `반품(0)`.** 8/23 크림리치 구매 9계정 중 **8계정 전부 확인 완료**.
  - 미확인 4: `1plu1mall1`·`linguist01`(강제 본인인증), `ybkim8225`·`kimmk2017`(로그인 실패)
  - ★**`tkdkky2008@naver.com` 은 `galleria.json` 에 없어 조회 불가** — 8/23 크림리치를 산 계정인데
    설정 파일에 빠져 있다. **자동화 사각지대**다.
- **현대몰 19/19 계정 완주 (반품 탭 P2)** — **전 계정 반품 없음.** (로그인 전 계정 성공.)

### 결론 — **최근 2주 내 자음생크림리치 반품은 3사 어디에도 없다**

| 몰 | 확인 | 결과 |
|---|---|---|
| 롯데홈쇼핑 | 20/20 | 최근 반품 0 (8/28 주문취소 2건은 반품 아님) |
| 갤러리아 | 8/12 | 확인분 전부 `반품(0)`. **크림리치 구매 9계정 중 8계정 확인 완료** |
| 현대Hmall | 19/19 | 전부 반품 없음 |

### ★ `tkdkky2008@naver.com` 추가 조회 (사용자가 계정 제공) — **반품 2건 발견, 단 상품이 다르다**

8/23 크림리치를 산 이 계정이 `galleria.json` 에 없어 못 봤던 것을, 사용자가 계정을 줘서 #13 으로
추가(★기존 계정번호가 밀리지 않게 **맨 뒤**에 append)하고 조회했다.

```
갤러리아 tkdkky2008@naver.com — 취소/교환/반품 카운터: 전체(2) 취소(0) 교환(0) 반품(2)
  · 반품접수일 2026-08-25  주문 202608218234511  설화수[공통]에센셜 탄력케어 세트(탄력3종)  반품수량 4개  634,680원  [반품접수]
  · 반품접수일 2026-08-25  주문 202608218234512  설화수[공통]에센셜 탄력케어 세트(탄력3종)  반품수량 4개  634,680원  [반품접수]
```

- **둘 다 `탄력3종`(에센셜 탄력케어 세트)이고, 자음생크림리치가 아니다.**
- 이 계정은 크림리치를 **많이 샀다**(8/23·8/28·8/31×2·9/2×2 …) — 다만 그 중 반품된 건은 없다.
- 이 계정의 8/28·8/31·9/2 갤러리아 주문은 **구매대장에 없다**(대장 누락 재확인).

### 결론 (2026-09-09 시점)

**최근 2주 내 "자음생크림리치 50ml" 반품은 3사 어디에도 없다.**
같은 기간 확인된 설화수 반품은 위 **갤러리아 tkdkky2008@naver.com 의 탄력3종 2건(8/25 접수)** 이 유일하다.
→ 사용자가 상품을 혼동했을 가능성. **확인 필요.**

미확인으로 남은 갤러리아 4계정 (전부 **갤러리아 구매 이력이 대장에 없는** 계정이라 우선순위 낮음):
- `1plu1mall1`·`linguist01` — `ForcePwdChangeAuth` 강제 본인인증. 사람이 휴대폰/아이핀 인증해야 진입 가능.
- `ybkim8225`·`kimmk2017` — 로그인 거부(캡차 아님, 예외 아님 — 자격증명 불일치 또는 갤러리아 미가입 추정).

### 계정 설정 대조 (신규 점검)
구매대장에 등장하는 계정 ↔ 설정 파일을 대조했다. `tkdkky2008` 추가 후 **3사 모두 누락 0**.
`lotte.json` 20 / `galleria.json` 13 / `hmall_config.json` 19.
⚠️ 단 대장 자체가 불완전하므로 "대장에도 설정에도 없는 계정" 은 이 방법으로 못 잡는다.

### 부수 발견 — 구매대장이 불완전하다
롯데 실제 주문내역에 있는 8/11·8/17·9/7 크림리치 주문이 **구매대장 시트에 없다.**
"대장에 없으니 안 샀다" 는 판단은 위험하다.

---
━━━ 2차 세션 ━━━

## 5. 롯데 조합12 담기 — 계정 8~14 (사용자 지시)

> "12번 조합 오늘 8-14번 아이디에 담아줘"

오늘 탭 `9.9` 카트플랜 = **channel=lotte, N=7**. 조합12 = `e 탄력3종 ×2 + h 자음생크림리치 ×1`
(공급률 0.4927, 수익금 72,984 — 오늘 7개 조합 중 최고).

`buy/sulwhasoo.py lotte <idx> 12` × 7 (LOTTE_CART_ONLY=true → 담기만).
**7/7 전부 rc=0 + `[OK] 담기 검증 — 카트 2건, 수량 [1, 2]`** 실측 확인.
대상: #8 jye40323 / #9 wlstmdlsfk / #10 kim455307 / #11 diinamic / #12 leenamsu0318 / #13 Lee0128 / #14 johwajeong2762.
로그 `logs/cart_20260909_lotte_combo12_8-14.log`.

## 6. Step 2 (check10) + 「10% 1만원 적립」 검증 (사용자 지시)

> "2026.9.9(수) ~ 2026.9.10(목) 10% 1만원 적립 이거 잘 체크해 / 그리고나서 스텝2 해"

### 6-1. 행사 파싱은 정확했다

`cart/today.json` 실측 — 행사 **「잘사는 추석 10%적립」** (`prmo P202608247695`), **종료 9.23 02:00**,
`simple_range=None` + **7단 tier**:

```
10만↑ 1만 / 20만↑ 2만 / 30만↑ 3만 / 50만↑ 5만 / 100만↑ 10만 / 300만↑ 30만 / 1000만↑ 100만
```
정확히 10% 정률. **사용자가 말한 "9.9~9.10" 과 시트상 종료(9.23)가 다르다 — 확인 필요.**

### 6-2. ★사전에 의심했던 `simple_range` 캡 버그는 이 행사엔 해당 없음

`_compute_reward`(check10.py:156)의 simple_range 경로는 **적립 상한(cap)이 없고**, 파싱 패턴 C
(check10.py:472)는 "최대 Y원"을 **주문금액 상한 `max_won`** 에 넣는다. 그래서
`10만원 이상 10% (최대 1만원)` 류 문구가 오면 `100000 ≤ price ≤ 10000` 이 되어 **적립 0 으로 조용히 누락**
되거나, 캡이 본문 다른 곳이면 **상한 무시로 과대계상**된다.
→ **이번 행사는 tier 구조라 그 경로를 타지 않아 무사했다.** 다만 **버그는 그대로 남아 있다**
(다음에 캡형 문구가 오면 터진다). 미수정 — 실제 그런 행사를 만나면 그때 원문 보고 고칠 것.

### 6-3. ★계단식이라 실적립률이 10%에 한참 못 미친다 (오늘 수량 기준)

| # | 상품 | 즉시할인가 | 적립 | 실적립률 | 다음 구간까지 |
|---|---|--:|--:|--:|--:|
| **17** | 데이즈온 원데이 알파 8개 | 95,840 | **0** | **0.0%** | **4,160** |
| 16 | 루솔 | 46,350 | 3,000 | 6.5% | 3,650 |
| 35 | 데이즈온 프리바이오 트리플 | 165,600 | 10,000 | 6.0% | 34,400 |
| 2 | 하루견과 초록색 | 161,820 | 10,000 | 6.2% | 38,180 |
| 3 | 하루견과 갈색 | 159,300 | 10,000 | 6.3% | 40,700 |
| 11 | 라벤더 천연꿀 | 262,400 | 20,000 | 7.6% | 37,600 |
| 9 | 이디야 카페인 | 131,920 | 10,000 | 7.6% | 68,080 |
| 22 | 올바른건강식품 와이 | 132,680 | 10,000 | 7.5% | 67,320 |
| 33 | GRN 붓기캔디 | 118,840 | 10,000 | 8.4% | 81,160 |
| 25 | 석류젤리 | 103,360 | 10,000 | 9.7% | 96,640 |
| 1-1 | 이너플로라 30정 | 102,240 | 10,000 | 9.8% | 97,760 |

- **#17 이 제일 아깝다 — 4,160원 모자라 적립 0.** 단가 47,920 → qty 3(143,760)이면 10,000 적립.
- **#16 루솔도 3,650원 차이**로 5만 구간(5,000) 미달.
- ⚠️ **수량은 사용자 지정 상수다**(`cart/STEP2_PLAN_2026-09.md`). 임의로 안 바꿨다 —
  READ_FIRST 「사용자가 정한 상수를 자동계산으로 바꾸지 말 것」. 보고만 하고 사용자 판단을 받는다.

### 6-4. 데이터 없는 상품 5개 (판단 불가)

- **#1 이너플로라 60정 / #34 데이즈온 파라다이스 버닝 9박스** — 10% 대상은 맞는데 **바로구매 실패**로
  금액 미수집. #34 는 혜택가 210,800 이라 20만 구간(2만) 후보인데 놓치고 있다.
- **#28 스키니랩 / #39 올리브오일 / #40 뉴케어 당플랜** — `ERR 판매중단`.
  ★#40 은 9월 정본표에 1~12번 9/2~9/17 로 잡혀 있는데 **9/3 이래 계속 판매중단**이다.
- #8 이디야 디카페인 — 10% 대상 아님(정상).

시트 입력 완료: `'9.09 식품'` 탭 + 재고관리 `당일 식품` 미러(19행).

## 7. 결제 안전장치 2건 신설 (사용자 지시 "1번 2번 먼저 해줘")

### 배경 — 코드 실측으로 확인한 구멍

- `money_guard`(현대) / 롯데 결제 직전 블록의 `MAX_PAY` 는 **환경변수를 켜야만** 동작한다
  (`if _max:` 안 켜면 통째로 skip). 주석엔 "사람이 로그를 보는 것에 의존하지 않고 코드가 막는다"
  라고 돼 있지만, 실제로는 **사람이 env var 를 기억하는 데** 의존했다.
- 롯데 `_from_order_sheet` 는 쿠폰이 실패해도 **재시도 1회 후 그대로 결제로 진행**했다.
- ★`set_plus_coupons` 는 **`err` 를 한 번도 세팅하지 않는다** → 호출부의 플러스쿠폰 재시도
  조건(`applied==0 and err`)은 **발동하지 않는 죽은 코드**였다. '받은 쿠폰 X' skip 과
  "정말 쿠폰이 없는 날" 이 구분되지 않는다.
- ★**정답은 이미 코드 안에 있었다** — `purchase_ledger.combo_info()` 가 rate 시트에서 그 조합의
  **최종구매가**를 읽어온다. 두 결제 모듈에 이미 import 돼 있었는데 **결제가 끝난 뒤 대장 기록에만**
  쓰고 있었다.

### 신설 ① 시트 조합가 대조 — `purchase_ledger.check_amount()`

`{"ok","expected","limit","reason"}` 반환. **한 군데에만 구현**하고 두 결제 모듈이 같이 쓴다
(가드를 두 벌 만들면 그게 다음 버그다).

- 기준 = rate 시트 오늘 탭의 그 조합 **최종구매가**.
- ★**상한 한 방향만** 본다 — 적립금·L.POINT·H.Point 사용은 결제액을 **낮추기만** 하므로
  "예상보다 낮다"는 정상. 막을 것은 "예상보다 높다"(=혜택 미적용) 뿐이다.
- 허용오차 기본 **3%** (`AMOUNT_TOLERANCE_PCT` 로 조정).
- **환경변수 없이 항상 동작한다.** 탈출구는 `SKIP_SHEET_GUARD=1`.
- 시트값 없음(오늘 탭 없음/Step1 미실행/조회 실패) · 금액 판독 실패 → **차단**
  ("모르는 금액은 결제하지 않는다" — 기존 `AMOUNT_UNREADABLE` 과 같은 원칙).
- `combo_idx=None`(식품)은 대상 아님 → 통과.

붙인 곳: `hmall_hyundai_buy.money_guard` (combo_idx 는 `res["combo_idx"]` 재사용 — 시그니처 무변경) /
`lotte_homeshopping_buy._order_sheet_tail`.

⚠️ **알려진 구멍**: 현대몰 `resume` 경로는 `res` 에 `combo_idx` 가 없어 이 대조가 건너뛰어진다.
그 경로는 여전히 `MAX_PAY` 가 유일한 상한이다.

### 신설 ② 할인쿠폰 재시도 실패 시 결제 중단

`_from_order_sheet` 에서 재시도 후에도 `applied==0 and err` 면 `COUPON_FAIL` 로 **결제하지 않는다**.
`err` 는 '보유 N장인데 0장 적용' 처럼 **적용됐어야 하는데 안 된** 경우에만 붙으므로,
쿠폰이 아예 없는 날은 걸리지 않는다.
플러스쿠폰은 위 이유(err 미세팅)로 중단 조건을 만들 수 없어 **①번 금액 가드가 유일한 backstop** 임을
코드 주석에 명시했다.

### ★ 최초 구현은 틀렸다 — 실측으로 잡음 (같은 세션 안에서)

사용자 질문 "한도는 그럼 누가 정하는거야?" 에 답하려고 근거를 캐다가 **내가 방금 넣은 가드의 버그**를 찾았다.

`_AMOUNT_LABEL`(대장용 컬럼)을 그대로 기준으로 삼았는데, **몰마다 그 값의 의미가 다르다**:

| 몰 | 대장용 컬럼 | 결제화면 금액과의 관계 |
|---|---|---|
| 갤러리아 | `네이버최종구매가` | 그대로 (카드할인 미운영) |
| 현대Hmall | `구매가격` | = **`미리보기가`** × (1−페이백) → **더 낮다.** 결제화면 = `미리보기가` |
| 롯데 | `최종구매가` | `정가×0.9×(1−쿠폰)×(1−청구할인)×(1−페이백)` → **청구할인·페이백이 이미 빠져** 결제화면보다 6~8% 낮다 |

청구할인·페이백은 **결제화면에서 안 빠진다**(나중에 청구/환급). 그대로 한도를 걸었더니
8/28 실제 로그(540,486~543,857원)가 전부 한도(518,409원)를 넘어 **정상 결제가 전부 차단**되는
상태였다. → `expected_pay_amount()` 신설: 몰별로 **결제화면에 실제로 뜨는 금액** 컬럼을 쓰고,
롯데만 섹션 위 `카드 청구할인: … N% (페이백 M%)` 줄을 읽어 되돌린다.

**교훈**: "시트에 정답이 있다"까지는 맞았지만 **어느 컬럼이 무슨 뜻인지 확인 안 했다.**
로그의 실측 결제금액과 대조하고서야 드러났다 — 가드는 만든 즉시 **과거 정상 건으로 회귀검증**해야 한다.

### 한도 = 예상금액 + 20,000원 (사용자 지정)

> 사용자 지시 2026-09-09 (문장 그대로):
> "max 를 좀 넉넉하게 잡아놔줘야해 너가 계산한 수치 + 2만원"

처음엔 내가 임의로 정한 **3%** 였다. 사용자가 정액 2만원으로 지정 → `HEADROOM_WON`(기본 20,000,
`AMOUNT_HEADROOM_WON` 로 조정). **사용자가 정한 상수이므로 자동계산으로 바꾸지 말 것.**

### 검증 (실측)

정상 건(8/28 실제 로그 6건, 540,486~543,857원) **전부 통과** — 예상 546,660 / 한도 566,660.

과거 실사고 3건이 전부 새 가드에 걸린다:

| 사고 | 기대(시트) | 한도 | 실제 | 결과 |
|---|--:|--:|--:|---|
| 08-25 롯데 쿠폰 0장 | 541,800 | 558,054 | 602,000 | ⛔차단 |
| 08-26 롯데 10% 2장 누락 | 545,000 | 561,350 | 606,181 | ⛔차단 |
| 08-28 #5 플러스쿠폰 skip | 546,660 | 566,660 | 627,197 | ⛔차단 |

쿠폰 1장 누락 규모별 감지력(예상 546,660 기준):
크림리치 10%(+27,000) ⛔감지 / 탄력3종 10%(+43,000) ⛔감지 / 플러스 12%(+32,400) ⛔감지 /
**최저가 상품 10%(+13,500)는 2만원 여유 안에 묻혀 못 잡는다** — 알고 두는 한계.
세 모듈 컴파일·import 정상.

### ★운영상 달라지는 점 (중요)

**오늘부터 설화수(조합) 결제는 rate 시트 오늘 탭이 있어야 진행된다.** Step1 을 안 돌린 날이나
시트 조회가 실패하면 결제가 **차단**된다(종전엔 그냥 진행). 의도한 동작이며, 확인 후 강행하려면
`SKIP_SHEET_GUARD=1`.

### 안 한 것

`buy/run.py` 의 `_JS_CART_ROWS` 이중 정의(284·303행, 앞엣것 죽은 코드) 제거와
dump 구현 3벌 통합(`_dump_texts` 가 `content-desc` 를 못 읽어 1661·1696행에 우회 정규식이 따로 있음)은
사용자가 3·4번으로 미뤄 이번엔 손대지 않았다.

### 별건 — 오늘 담기에서 내가 놓친 것

`buy/_lotte_cart_fill.py` 가 **계정 간 30초 페이싱이 들어간 배치 러너**인데(어느 문서에도 없다),
나는 이걸 안 쓰고 bash for 루프로 7계정을 **페이싱 없이** 연속 로그인시켰다. 결과는 7/7 정상이나
리셀러 탐지 회피 관점에선 나빴다. → 다음부터 `python3 buy/_lotte_cart_fill.py <combo> <start> <end>`.
