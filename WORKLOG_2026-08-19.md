# WORKLOG 2026-08-19 — 윈도우 첫 step1/step2 완주 / **check10 실비 계산 2건 정정** / 상품번호 후보 구조 / **Gemini 탭 오독 사고**

## 요약 (한 줄)
윈도우에서 step1·step2 **둘 다 첫 완주**. 사용자 지적으로 **check10 의 실비 계산이 두 군데 틀렸음을 확인·수정**했고,
`n`(상품번호 수시 변경) 대응으로 **몰별 상품번호 후보 리스트 + 실패 시 폴백** 구조를 넣었다.
재실행 중 **selenium 이 Chrome 내장 Gemini webview 를 잡아 롯데 전 상품을 빈 값으로 오독**하는 사고를 발견·수정(§8).

## 1. 사용자 지시 (원문 요지)

1. "여기윈도우, 스텝1,2 진행해"
2. "n 제품의 url이 수시로 바뀜 … n을 후보를 이거랑 이전거랑 같이 놔두고 만약 하나로 실패하면 다른 url로 시도하면 돼"
   → 현대 `2228732430` / 롯데 `2923426629` / 갤러리아 `2502913443`
3. "롯데g 는 한동안 2834421446 … 저게 한정품이라 추후에 2719761525 로 이동될거야 … 지금은 한정품이 더 좋은
   샘플을 주기때문에 한정품으로 1446 … 추후에 중단되면 1525로 봐 **중단여부는 해당 링크들어갔을때 파악해봐**"
4. "step2 에 17제품은 18개가 아니고 8개임 … 17번 2개 담으면 현대 5% 할인받고 81548원이고 10% 적립 받으면
   81548*0.9 가 실비 아니야? **다른애들도 다 제대로 계산이안된거같은데?**"
5. "8 이디야 디카페인은 29070 원 맞음 다만 구매해보려하니 **일시중단상품**이라고 알람이뜸"
6. "만원차이나는거는 **결제금액할인**이라 실제 결제창 들어갈 때 나오는 거임 … 장바구니단계에서도 안뜸, 결제단계에서만뜸"
7. "**현대홈쇼핑 현대카드 Ed2 7% 청구할인 이거는 무조건 무시** / 현대홈쇼핑 현대카드 Ed2 **7% 적립 은 인정**"
8. "상품명 고치고 step2 다시 돌려 / 오늘 수정내용 커밋 푸시 워크로그 쓰고"

## 2. step1 (탭 `8.19`) — 윈도우 첫 완주, 18분

| 단계 | 결과 |
|---|---|
| ① 갤러리아 | ✅ 27조합 `A1:K62`, REDIRECTED 0건, 신규 0개 |
| ② 재고관리 | ✅ 새 버전 **`f`** (`1f 8/19`…`27f 8/19`) |
| ③ Hmall | ⚠️ **23/27** — 24·25·26·27 실패 (전부 `n` 조합) |
| ④ 롯데 | ✅ 27조합 (단 `n` 조합은 **죽은 번호로 측정** — §4) |
| ⑤ 카트플랜 | ✅ `O1:V32` **channel=galleria N=36**, 13,220,242원, 평균 0.5046 |

- 색칠 `[3,5,12,13,14,23,24,25,27]` — 8/7 고정세트 유지(색칠 사고 재발 없음).
- GWP 판독: 기간 **7.31 - 8.31**, 1세트 8품목, 8품목 전부 단가 매칭(신규품목 경고 0).
  27조합 전부 70만↑ 이라 GWP 균일 81,600원(=20,400×4세트).
- ⚠️ **갤러리아 재고 부족** — 재고 `c=2` 인데 플랜 `c=21`, `b=1` 인데 `2`. 로그는 "참고용 — 분배엔 미반영".
  8/7 에 지적된 것과 **같은 상황이 그대로 재현**됐다(8/10 이월 TODO).
- ⚠️ **현대카드 페이백 미등재** — Hmall 27조합 전부 `[WARN] … 카드 '현대' → 0% 적용`.
  8/02 와 동일 재현. `_common.CARD_PAYBACK` 에 값 미확정이라 **손대지 않았다**(RULES: 추측 하드코딩 금지).

## 3. step2 (탭 `8.19 식품`) — 로그인 차단 없음

`[OK] 로그인된 CfT 재사용 → 포트 9222` / `[OK] 로그인`.
**새 윈도우 CFT 프로필인데도 `[BLOCKED] 다른 로그인 수단` 이 안 떴다** — 7/05 CfT 안티봇 차단도,
8/10 해외 IP SMS 본인확인도 재현되지 않았다(출구 IP 한국).

1차 결과: 10%적립 14 / 적립없음 3 / 에러 2(`#28`·`#39` 판매중단).

**★ 8/10 이월 TODO 해소** — `1-1` 이너플로라 30정(`2201017629`), *"4일 연속 ERR"* 로 남아 있던 것이
**오늘 정상 판독**(51,120 / 실비 46,008).

## 4. ★★★ 롯데가 **죽은 상품을 조용히 측정**하고 있었다 (실측)

Hmall 은 24~27 이 `add_to_cart 실패: n×N` 으로 **드러났는데** 롯데는 27조합 전부 "성공"했다.
사용자 지시 3 에 따라 **링크를 직접 열어 관측**한 결과:

| 대상 | 가격 표시 | 신호 | 판정 |
|---|---|---|---|
| g 한정품 `2834421446` | 22% 176,170 | 없음 | ✅ 살아있음 |
| g 공통 `2719761525` | 22% 176,170 | 없음 | ✅ 살아있음 |
| n 신규 `2923426629` | 22% 105,700 | 없음 | ✅ 살아있음 |
| **n 구번호 `2923418727`** | **13% 117,450** | **`일시품절` `재입고알림`** | ❌ 품절 |

**죽은 번호도 페이지가 열리고 가격까지 멀쩡히 나온다.** 리디렉트도 404도 아니라 감지 수단이 없었다.
→ step1 롯데의 `n` 조합(24~27) 공급률은 **판매불가 상품 기준**이라 신뢰할 수 없다.

> ⚠️ **문구가 기록과 다르다** — 8/18 워크로그는 `판매중단` 이라 적었으나 실제 페이지는 **`일시품절`** 이다.
> `판매중단` 으로 감지 코드를 짰으면 못 잡았다. **기록된 단어를 그대로 셀렉터에 쓰지 말 것.**

## 5. ★★★ check10 실비 계산 — 오류 2건 (사용자 지적 → 실측 확인)

사용자 계산: `#17` 2개 = 현대 5% 할인 후 **81,548원**, 실비 = `81,548 × 0.9` = **73,393원**.
코드 출력: 즉시할인가 **91,048** / 실비 **75,570**. → 원데이터를 파보니 **둘 다 코드가 틀렸다.**

### 오류 ① 주문서가 알려준 실제 금액을 버리고 재계산했다

`card_slides` 는 **바로구매로 주문서까지 들어가** `_extract_order_page()` 로 읽은 것이고,
거기 `{"text": "5% 즉시할인", "percent": 5, "price": 81548}` 로 **정답이 이미 들어 있었다.**
코드는 `percent` 만 쓰고 `price` 를 버린 뒤 `member_price × (1-pct)` 로 재계산했다.

차이의 정체 = **결제금액할인**(사용자 지시 6). 장바구니엔 안 뜨고 **결제 단계에서만** 뜬다.
→ 코드는 이미 그 화면까지 갔으므로 **추가 탐색 없이 값을 갖고 있었다.** 실측 6건 전부 정확히 **−10,000원**:

| # | 우수가(재계산) | 슬라이드 price | 역산 기준액 | 차이 |
|---|--:|--:|--:|--:|
| 2 | 161,820 | 144,229 | 151,820 | −10,000 |
| 3 | 159,300 | 141,835 | 149,300 | −10,000 |
| 11 | 262,400 | 239,780 | 252,400 | −10,000 |
| 17 | 95,840 | 81,548 | 85,840 | −10,000 |
| 34 | 198,400 | 178,980 | 188,400 | −10,000 |
| 35 | 82,800 | 69,160 | 72,800 | −10,000 |

⚠️ 단 `price` 는 **즉시할인 슬라이드만** 신뢰해야 한다 — 토스페이/네이버페이 슬라이드의 숫자는
주문총액이 아니라 **'최대 N원 혜택' 상한액**(13,000원)이다. 그대로 쓰면 13,000원짜리 주문이 된다.

### 오류 ② 청구할인을 즉시할인과 **겹쳐** 적용했다

`I = H × (1 - 청구할인 7%) - 적립` 으로 `현대홈쇼핑 현대카드 Ed2 7% 청구할인` 을 항상 적용했다.
**사용자 지시 7: 청구할인은 무조건 무시.** (구매혜택 배너의 제휴카드 안내일 뿐 실제로 못 받는다.)

> 내가 처음에 "당일 즉시할인 카드가 현대카드면 계산" 으로 이해했으나 **사용자가 정정** —
> 그건 `7% 적립` 쪽 얘기고, `7% 청구할인` 은 조건 없이 무시다. **청구할인과 적립을 뭉뚱그리지 말 것.**

### 오류 ③ (부수 발견) alias 경로가 청구할인을 즉시할인으로 오인할 수 있었다

alias 상품(`1-1` 등)은 `max(percent)` 로 슬라이드를 골랐다 → **7% 청구할인이 5% 즉시할인을 이겨**
즉시할인으로 둔갑할 수 있는 구조였다. 오늘은 토스페이 슬라이드라 발현 안 됨.

### 수정 — `_settle()` 로 계산 단일화

두 곳(메인 `_extract_payment` / alias)이 **같은 계산을 각자 구현**하고 있었다(READ_FIRST 가 경고하는 중복).
`cart/check10.py` 에 `_settle()` 신설 후 양쪽이 호출하도록 통합.
alias 는 `trust_price=False` — base 의 슬라이드 절대금액은 **alias 수량에 안 맞아** % 만 빌린다.

### 재계산 결과 (14건)

| # | 실비 기존 → 신규 | 변화 |
|---|---|--:|
| 2 | 127,968 → 134,229 | +6,261 |
| 3 | 125,742 → 131,835 | +6,093 |
| 11 | 206,902 → 215,802 | +8,900 |
| **17** | 75,570 → **73,393** | −2,177 |
| 34 | 156,438 → 161,082 | +4,644 |
| 35 | 65,288 → 62,244 | −3,044 |
| 나머지 8건 | 변화 없음 | 0 |

**두 오류가 반대 방향이라 서로 상쇄되고 있었다** — 결제금액할인 누락(실비 과대)과 청구할인 과대적용(실비 과소).
그래서 상품에 따라 오르기도 내리기도 한다. 한쪽만 고쳤으면 오히려 더 틀렸을 것이다.

## 6. 상품번호 **후보 리스트 + 폴백** 구조 (사용자 지시 2·3)

`hsmaster/config/sulwhasoo-ids.json` 의 몰별 값이 **문자열 또는 후보 리스트**를 받도록 확장.
앞에서부터 시도하고 실패하면 다음 후보. `galleria_url` 은 `galleria` 와 같은 순서·길이.

| 코드 | 몰 | 1순위 | 2순위 |
|---|---|---|---|
| n | 현대 | 2228732430 | 2228732274 |
| n | 롯데 | 2923426629 | 2923418727 |
| n | 갤러리아 | 2502913443 | 2502913437 |
| g | 롯데 | **2834421446** (한정품·샘플 좋음) | 2719761525 (공통) |

### 실패 신호 — 몰마다 다르다

| 몰 | 신호 | 파일 |
|---|---|---|
| Hmall | `add_to_cart()` → False | `rate-check/hmall.py` |
| 갤러리아 | `scrape_product()` → `redirected` | `rate-check/galleria.py` |
| 롯데 | **`재입고알림`/`일시품절` 버튼 존재** (신설 `_sold_out()`) | `rate-check/lotte.py` |

폴백으로 성공하면 `[WARN] 1순위 … 실패 → 후보 N 사용` 을 찍어 **순서 갱신을 유도**한다(조용한 폴백 금지).

### ★ 같은 모양 버그 하나 더 — `buy/sulwhasoo.py` 갤러리아 하드코딩

롯데는 8/17 에 config(SoT) 를 읽도록 고쳤는데 **갤러리아는 하드코딩 사본이 그대로 남아 있었다**
(`n = 2502913437`). config 만 고치면 담기는 구 번호를 계속 쓰는, 8/17 과 **완전히 같은 사고 구조**.
READ_FIRST *"버그 하나를 고치면 같은 모양을 폴더 전체에서 찾는다"* 에 따라 같이 정리
(`_mall_products()` 로 롯데·갤러리아 공통화).

## 7. 미해결 / 다음

- [ ] **`7% 적립` 미수집** — 사용자: "10% 적립 한 칸 아래에 뜨고 10%와 중복 인정".
      **단 오늘은 그 행사 자체가 없다**(사용자 확인) → 관측 대상이 없어 이번엔 손대지 않았다.
      그런데 `#17` PDP `구매 혜택` 패널을 관측하니 4줄뿐이고 그 문구가 **없다**:
      `H.Point 10% 적립` / `PAY 토스·네이버` / `적립 기본 50P` / `제휴카드 7% **청구할인**`.
      → **어느 화면에서 보이는지 확인 필요**(주문서? 다른 상품?). 확인 전엔 정규식 손대지 않는다 —
      `기본 50P 적립` 과 뒤섞여 이중계상될 위험(`check10.py:317` 주석이 경고하는 그 혼동).
      참고: 행사 수집은 `a[href*="evntHPointDtl"]` **앵커가 있는 줄만** 잡는다(`check10.py:335`).
      `제휴카드` 줄은 `searchSpexSectItem` 이라 애초에 수집 대상 밖이다.
- [ ] **`#8 이디야 디카페인` 일시중단 라벨 없음** — 29,070 은 맞으나 **구매하기 버튼을 눌러야** 일시중단이 뜬다(사용자 확인).
      `#28`·`#39` 는 `ERR 판매중단` 이 붙는데 `#8` 은 금액만 `—` 로 비어 **이유를 알 수 없다.**
- [ ] **`hsmaster/config/sulwhasoo-ids.json` 은 gitignore** — 오늘 넣은 후보 리스트가 **git 으로 안 간다.**
      맥에도 같은 값을 넣어야 폴백이 동작한다. (이 PC 로 온 번들의 config 는 `lastUpdated: 2026-08-05` 로
      8/17 맥 수정본보다 낡아 있었다 — 이번 사고의 뿌리.)
- [ ] 갤러리아 `c` 재고 2 vs 플랜 21 (8/7 부터 이월, 오늘 재현)
- [ ] 현대카드 페이백 값 미확정 (8/02 부터 이월)
- [ ] 롯데 `_sold_out()` **여전히 라이브 미검증** — 재실행에서도 1순위가 전부 살아있어 발동 안 함
- [x] ~~step1 재실행~~ — 완료 (§9). 단 도중에 Gemini 사고 발견 (§8)
- [ ] 오늘 재고 버전 `f`(아침, 죽은 n 기준) 정리 여부 — 사용자 확인 대기
- [ ] `.gitignore` 에서 `sulwhasoo-ids.json` 제외 해제 여부 — 사용자 확인 대기

## 8. ★★★ Gemini 패널 때문에 롯데가 통째로 오독됐다 (step1 재실행 중 발견)

새 상품번호 반영 후 step1 을 재실행했더니 **롯데 8개 상품 전부** `쿠폰: None (popup=False, lines=[])`,
`카드: lines=[]`, `#eventBanner 슬라이드 0개` 로 나왔다. 번호를 안 바꾼 `b` 까지 실패 → 후보 변경 탓이 아니다.

### 원인 (실측)

CDP 타겟을 찍어보니 CFT 에 7개가 떠 있었고, **selenium 이 `gemini.google.com/glic` webview 를 기본 핸들로 잡았다.**

```
handles 3개:
  https://www.hmall.com/mo/oda/order   ← 진짜 콘텐츠 탭
  chrome://glic/                        ← Gemini 패널
  https://gemini.google.com/glic        ← selenium 이 잡음
```

`driver.get()` 을 해도 그 webview 는 안 움직인다 → **모든 판독이 Gemini 화면을 읽었다**
(`bodyLen: 11`, `head: "Gemini와의 대화"`). 콘텐츠 탭으로 바꾸자 즉시 정상(`bodyLen 2270`, `쿠폰받기` 발견).

- **CFT 프로필이 구글 로그인돼 있어서** Chrome 내장 Gemini 패널이 살아 있고 CDP 타겟으로 노출된다.
- **에러가 안 난다.** 빈 값으로 조용히 통과해 공급률이 0.48 → **0.66 으로 시트에 잘못 기록됐다.**
- **아침 실행은 정상이었다** — 패널이 뜨기 전이었을 뿐. **재현이 간헐적이라 더 위험하다.**

### 수정

`_common.use_content_tab(driver)` 신설 — attach 직후 핸들이 http(s) 가 아니거나 gemini 면 콘텐츠 탭으로 전환.
**`lotte.py` 와 `galleria.py` 둘 다** 적용했다(같은 방식으로 attach 하므로 갤러리아도 언제든 당한다.
READ_FIRST *"버그 하나를 고치면 같은 모양을 폴더 전체에서 찾는다"*).

> ⚠️ **추정(미확증)**: 아침 실행의 롯데 `적립 0건` 도 같은 문제였을 수 있다. 재실행에서는
> `적립 후보 2건 → 최고 tier 80만↑ 5만원`(raw 임계 5만/10만/30만/80만) 이 정상 판독됐다.
> 아침엔 쿠폰·카드는 정상이었으므로 완전한 오탐은 아니었다. **관측으로 확정한 것이 아니다.**

## 9. step1 재실행 최종 (시트 `8.19`)

| 단계 | 결과 |
|---|---|
| 갤러리아 | ✅ 27조합, `n` 새 번호 `2502913443` 정상, REDIRECTED 0 |
| 재고관리 | ✅ 새 버전 **`g`** (`1g 8/19`…`27g 8/19`) |
| **Hmall** | ✅ **27/27** — 아침 전멸했던 24~27 측정 (0.5163~0.5364) |
| 롯데 | ✅ 27조합, 쿠폰 10~13% + KB국민 7%, 적립 tier 80만↑ 5만원 |
| 카트플랜 | ✅ `O1:V32`, channel=galleria N=36, **13,234,198원 / 평균 0.5051** |

- `n` 후보 **1순위가 3사 전부 성공** — 폴백 미발동(후보 구조는 로그로만 확인: `[후보 1/2 2228732430]`).
- 롯데 `g` 한정품 `2834421446` 정상 판독.
- 갤러리아 `n` 추가증정이 17,300 → **16,300원** 으로 변함(상품이 바뀌었으니 사은품도 바뀐 것).
  `inventory.py` 가 독립적으로 이 차이를 잡아 새 버전 `g` 를 만들었다(교차검증).
- ⚠️ **오늘 버전이 `f`(아침) → `g`(재실행) 두 개 생겼다.** 아침 `f` 는 죽은 `n` 기준이라 쓰면 안 된다.
  **구매 입력은 `g` 행에.** `f` 정리 여부는 사용자 확인 대기.
- ⚠️ 롯데 `_sold_out()` 은 **이번에도 발동하지 않았다**(1순위가 전부 살아있음) → **여전히 라이브 미검증.**

## 10. 오늘 배운 것

1. **"성공했다"가 곧 "맞다"가 아니다.** 롯데는 27/27 성공했지만 4건이 죽은 상품이었다.
   Hmall 은 담기를 실제로 해봐서 실패로 드러났다. **행위(담기)까지 해보는 쪽이 조용한 오측정을 잡는다.**
2. **기록된 단어를 그대로 셀렉터에 쓰지 말 것.** 워크로그의 `판매중단` vs 실제 `일시품절`.
3. **두 오류가 반대 방향이면 겉으론 그럴듯해 보인다.** 사용자가 실제 결제창 숫자를 대보지 않았으면
   못 찾았다. **한쪽만 고쳤으면 더 틀렸다.**
4. **코드가 이미 그 화면에 가 있었다.** "결제단계에서만 뜨는 값"을 못 얻는 줄 알았는데,
   `_extract_order_page` 가 이미 읽어와서 버리고 있었다. **없는 줄 알기 전에 수집된 원데이터부터 본다.**
5. **자동화 브라우저에 사람용 패널이 붙으면 조용히 다 틀린다.** Gemini webview 를 잡은 selenium 은
   에러 없이 빈 값만 돌려줬다. **판독 결과가 전부 None/0 이면 페이지가 아니라 '어느 탭을 보고 있나'를 의심한다.**
6. **사용자 정정을 넓게 해석하지 말 것.** "청구할인 무시"를 내가 "카드가 같으면 적용"으로 조건화했다가
   정정받았다. 지시에 없는 조건을 만들지 않는다.

━━━━━━━━━━━━━━━━━━━━ 2차 세션 (맥, 현대몰 1:1 문의 접수) ━━━━━━━━━━━━━━━━━━━━

## 지시 (사용자 문장 그대로)

> 현대몰 아이디 별로 로그인 / 우측 하단 "마이페이지" 클릭 / 스크롤내리면 좌측에 "고객센터" /
> 우측상단에 "1:1문의하기" / 상담유형 팝업 > "시스템오류" 찾아서 클릭 / 그 아래 팝업은 자동 선택됨
>
> 문의내용에
> 추후 아모레 주문건들에 대해 다음 이름, 뷰티포인트 적립번호로 뷰티포인트 적립부탁드립니다
> 라고 글 써줘 … (계정별 이름/적립번호 아래 표)
>
> 그 아래 답장방법에는 SMS 클릭 그 아래 01026541598 를 붙여넣고 동의합니다 누르고 확인버튼 누름

> **폰으로하는거아님 CFT로해**  ← 처음에 폰(ADB) 경로로 잡았다가 정정받음

## 결과 — 19/19 접수 완료

전 계정 `시스템 시스템오류 2026.08.19` 목록 최상단 확인. 성공모달 `시스템오류 신청이 완료되었습니다.`

| 계정 | | 계정 | |
|---|---|---|---|
| 1 tkdkky2002 이예나 | ✅ | 11 kimgaeun04 김미경 | ✅ |
| 2 1plu1mall1 전영미 | ✅ | 12 Jinhwa4553 김예린 | ✅ |
| 3 kgi5907@daum.net 이예진 | ✅ | 13 gcd0327 이남수 | ✅ |
| 4 1plus1mall0 조영진 | ✅ (1차 실패 → 재시도) | 14 johwajeong 김진화 | ✅ |
| 5 1plus1mall1 김진화 | ✅ | 15 jyj041220 조영진 | ✅ |
| 6 1plus1mall4 이남수 | ✅ | 16 miu1838 이예진 | ✅ |
| 7 linguist01 김예린 | ✅ | 17 skykow 전영미 | ✅ |
| 8 ybkim8225 김미경 | ✅ | 18 ybkim9960 이예나 | ✅ |
| 9 lee0503031 조영은 | ✅ | 19 yeonsuk0428@naver.com 이예나 | ✅ |
| 10 jye40323 조영은 | ✅ | | |

로그: `logs/hmall_inquiry_20260819.log`

## 만든 것 — `hmall_inquiry.py`

`buy/run.py` 의 `login`/`logout_if_needed` 재사용 (새로 만들지 않음).
`python3 hmall_inquiry.py --dry 1` / `1` / `2-19`.

### DOM 실측 (2026-08-19, CFT 9222)

- **1:1문의하기 = `https://www.hmall.com/mo/ccd/cancelCall`** (마이페이지 → 고객센터 경유 없이 직행 가능)
  - 도달 경로: 마이페이지 → `1:1상담 내역`(`/mo/ccd/myMtoMReview`) → 우측상단 `게시판 상담하기` → cancelCall
- 상담유형/상담사유 = `<select>` **id·name 없음** → 첫 옵션 텍스트(`상담 유형`)로 식별
  - 시스템오류 = `0712` → 상담사유 `071201`(시스템오류/불편사항) **자동 선택**(옵션 1개). 지시대로.
- 문의내용 `textarea#cnslCntn` (500자 제한, 본문 115자)
- 답변방법 `input[name=answReqnGbcd]` value=1 SMS / 2 전화 — **기본값이 이미 1(SMS)**
- 휴대폰 `input[name=hpNum]` — **계정 등록번호가 프리필됨** → 반드시 덮어쓴다
- 동의 `input#deliverCheck` / 제출 `button` 텍스트 `확인`
- 접수 성공 → `#confirm-portal` 모달 → 닫으면 `/mo/ccd/insertCnslAcptSuccess`

## 발견 — 오늘 배운 것 (재발방지)

### 1. `hpNum` 은 `fill()` 로 안 들어간다 (React controlled input)

`page.fill()` 후 DOM `value` 를 되읽으면 `01026541598` 로 **정상으로 보인다**. 그런데 확인 누르면
`alert: 휴대폰 번호를 입력해주세요.` 로 접수가 막힌다 — React state 에 안 붙었다.
→ `click` → `Meta+A`/`Backspace` → `press_sequentially` 실제 키입력으로 해결.
**교훈: DOM value 되읽기는 controlled input 의 검증이 아니다.**
(같은 페이지의 `textarea#cnslCntn` 은 `fill()` 로 정상 동작 — 전부 그런 건 아니다.)

### 2. 성공 신호는 alert 이 아니라 모달 — 알럿이 뜨면 그게 **실패**다

처음엔 `확인` 클릭 후 "알럿 없음 = 성공" 으로 짰다. 실제로는 정반대:
- 실패 → `window.alert` (`휴대폰 번호를 입력해주세요.`)
- 성공 → `#confirm-portal` 모달 `시스템오류 신청이 완료되었습니다.`
- 모달 안에도 `확인` 버튼이 있어 `button:has-text("확인")` 이 **2개로 strict mode violation**.
  → 폼 버튼은 `!e.closest('#confirm-portal')` 로 걸러야 한다.

### 3. 검증이 헐거워서 **가짜 OK** 가 났다

1차 검증 = `'시스템오류' in 최신 상담내역`. 1번 계정은 8/6·8/11·8/17 에 이미 시스템오류 문의가 있어
**접수가 막혔는데도 OK 로 찍혔다.** → `오늘 날짜(2026.08.19)` 까지 맞아야 성공으로 판정하도록 수정.
READ_FIRST 「성공 메시지는 검증이 아니다」 의 재현.

### 4. 계정 인덱스 drift 방어

`ROWS` 에 지정 id 를 같이 넣고 `hmall_config.json[idx-1]['id']` 와 **불일치 시 즉시 abort**.
config 순서가 바뀌면 딴 계정에 딴 사람 적립번호를 접수하게 된다. (자동메모리 `inventory-sheet-row-drift` 와 같은 모양)

### 5. #4 실패 원인 = 로그인 폼 미로딩 (일시적)

`Locator.fill 30s timeout`, `url=/md/dpl/index`. 비번 문제 아님. 단독 재시도로 성공.
8/10 워크로그의 **해외 IP SMS 본인확인 차단은 오늘 재현되지 않았다** — 19계정 전부 정상 로그인.

## ★ 상담원 답변 — 1:1 게시판으로는 안 될 수 있다 (사용자 판단 필요)

1번 계정 8/17 문의에 **8/18 자 답변**이 이미 와 있었다:

> 아모레퍼시픽은 아이디당 **본인 이름으로 구매된 내역만** 아모레뷰티포인트 적립되게 되어 있습니다.
> 문의주신 아모레 뷰티포인트 관련 **발송 이메일** 안내입니다.
> 고객명/ID/변경하시고자 하시는 뷰티포인트 기재하시어 발송 부탁드립니다.
> 이메일: **dongyang05@hyundaihmall.com** / 발송후 당사로 연락 부탁드립니다.

→ 사용자에게 보고했고, 지시대로 1:1 접수는 그대로 19건 진행. **이메일 발송 건은 별도 지시 대기.**

## 다음 할 일

- [ ] `dongyang05@hyundaihmall.com` 이메일 발송 여부 사용자 확인 (19계정 고객명/ID/뷰티포인트)
- [ ] 8/10 이월분 그대로: 조합 26 미측정 / step2(check10) 미실행 / `1-1` 이너플로라30정 URL `2201017629`

━━━━━━━━━━━━━━━━━━━━ 3차 세션 (윈도우, 담기 19계정 + 현대 결제 1~10) ━━━━━━━━━━━━━━━━━━━━

## 지시 (사용자 문장 그대로)

> 현대몰 1-19번 아이디에, 식품 22번,34번 제품 2개씩 담아
> 1-10번까지 결제진행해 554153 172.30.1.99:38897 페어링코드고 46847 포트번호야
> 앱카드결제하면안되잖아 무조건 일반결제야
> 2-10번 돌리기전에 뭐가문제였었는지 정확히 확인해
> 해당 ocr 로 실패하면 처음부터 다시시작이아니라 그 자리에서 핸드세이크로 해결하는 방법 고안해내서
> 앞으로 무조건 이런식으로하는거, 다른카드사 쓸때도, 다른 홈쇼핑에서도 이렇게 해줘야돼
> 카드비번 pin 부분 말고도 다른부분에서도 resume기능써. 무조건 이거 해야 시간 절약됨
> 홈쇼핑 창 결제하기 이후 모든 카드번호 및 pin 번호 입력하는 부분의 ocr 은 핸드세이크로 바꾸는 거…
> nh카드 그렇게 하고 있잖아
> 현대카드 무조건 앞으로 핸드세이크로 처리해 / 현대몰 설화수 현대몰 식품 롯데몰 설화수 모두

## 1. 결과 — 담기 19/19, 결제 10/10

담기: 계정 1~19 전부 **22번 올바른 와이 9박스 ×2 + 34번 데이즈온 파라다이스 버닝 6+3박스 ×2** = 38/38 성공.

결제(계정 1~10, 현대카드 일시불 + H.Point 전액사용 + 현대 5% 즉시할인):

| # | 계정 | 주문번호 | 적립 |
|---|---|---|---|
| 1 | tkdkky2002 | 20260819078146 | ✅ |
| 2 | 1plu1mall1 | 20260819081549 | ✅ |
| 3 | kgi5907@daum.net | 20260819084728 | ✅ |
| 4 | 1plus1mall0 | 20260819085523 | ✅ |
| 5 | 1plus1mall1 | 20260819085718 | ✅ |
| 6 | 1plus1mall4 | 20260819085926 | ✅ |
| 7 | linguist01 | 20260819086122 | ✅ |
| 8 | ybkim8225 | 20260819086248 | ✅ |
| 9 | lee0503031 | 20260819086487 | ✅ |
| 10 | jye40323 | 20260819086886 | ✅ |

- 주문서 합계 **529,478원** → H.Point 전액사용 → 현대 5% 즉시할인. 계정별 결제액은 포인트 잔액에 따라 다름
  (#1 = 51,500P 사용 → **454,080원** / #3 = 1,560P → 501,523원).
- 구매대장 **20건**(주문 10 × 2줄), 매니페스트 `paid=True` 10/10, 적립 10/10.
- #1·#10 마이페이지 교차확인: `결제완료 2`(한 주문의 2줄) — **중복주문 없음**, 포인트 0P.

## 2. ★★ 사용자 지시로 바뀐 정책 — 결제 후 키패드는 **핸드셰이크 1순위**

`CLAUDE_OCR_ONLY=1` (기존 플래그를 **표준으로 승격**, 새로 만들지 않음).
`vote_digits` = `input_pin` 의 단일 판독 관문이라 **한 곳만 고쳐 전 카드·전 몰에 적용**된다.

### 카드사별 실측 — "모든 카드사 수정"이 아니라 이미 다 되거나 불필요했다

| 카드 | 판독 방식 | 핸드셰이크 |
|---|---|---|
| 현대·BC·삼성 | `preset`/`kind` → `vote_digits`(OCR) | ✅ 이 수정으로 **자동 적용** |
| 롯데·KB | `source: "dump"` (content-desc) | ➖ OCR 안 씀 |
| 토스 | `source: "text_dump"` | ➖ OCR 안 씀 |
| 하나 | `source: "sequential_logo"` | ➖ 8↔0 혼동 회피로 **일부러** OCR 배제 |
| NH | `nh_vision_input.py` | ✅ 원래부터 핸드셰이크 |

### 같이 고친 것
- 타임아웃 45s → 플래그 ON 이면 **240s** (45s 는 백그라운드 실행에서 무조건 놓친다 — 실측).
- 한 번 무응답 시 `_claude_unavailable=True` 로 프로세스 내내 skip → **플래그 ON 이면 끄지 않는다**
  (안 그러면 지시가 조용히 무력화된다).
- ⚠️ **무인/크론 실행에선 켜지 말 것** — 응답할 사람이 없어 키패드마다 240s 를 태운다.

### 효과 (실측)
`#3` 카드비번에서 로컬 사다리가 `0,1,2,3,4,5,6,8,9` 를 잡고 **'7' 만 못 잡아** 9/10 실패 →
3회 재시도 **120초 낭비** 후 핸드셰이크가 10/10 성공. 처음부터 부르면 그 120초가 없다.
전환 후 **#4~#9 6연속 무실패**(계정당 ~4분). 전환 전엔 #3 한 계정에 4번 실패했다.

## 3. ★★ 윈도우 OCR 이 맥보다 약하다 — 판독은 OCR + dump 를 **겹쳐** 쓴다

오늘이 윈도우에서 폰 결제를 처음 완주한 날이고, **한 곳도 아니고 다섯 곳**이 이 때문에 순차로 터졌다.
"같은 실패의 반복"이 아니라 고칠 때마다 한 단계 더 가서 다음 벽이 드러난 것.

| 대상 | OCR | dump |
|---|---|---|
| PIN/카드비번 `확인`(검은 버튼+흰 글씨) | ❌ | ✅ (540,1312) |
| 토스트 `현대 5% 즉시할인이 적용되었어요.` | ❌ | ✅ y=2085 |
| `안전한 결제를 위해 추가 인증` 팝업(딤) | ❌ | ✅ |
| 주문서 텍스트 | `口토사OI그`·`丁 0 曰` 깨짐 | 깨끗 |
| 로그인 chooser(WebView) | ✅ | ❌ **node 16개·text 0개** |

→ `ocr_or_dump_tap()` / `_dump_texts()` 신설, 판독은 `_ocr_texts(cap()) + _dump_texts()` 병합.
**dump 가 비는 화면도 있으니 OCR 을 버리는 게 아니라 겹친다.** 키패드는 dump 에 안 잡혀 핸드셰이크 담당.

## 4. 고친 버그 (전부 라이브 재현·검증)

1. **`logout()` 팝업 미처리** — `buy_one` 이 로그인 전 1회 닫아도 `logout()` 이 루프마다 `_launch()` 로
   앱을 다시 띄워 **다른 팝업**이 새로 뜬다(`7일간 보지 않기` → `오늘의 최저가`/`오늘 그만 보기`).
   → `hmall_webview.close_ad_popup()` 신설 + `POPUP_KEYS` 를 OCR 판독본과 **공유**.
2. **`_is_mypage_loggedin()` 전체 target 훑기** — 홈 광고 `VIP 멤버십 제공` 이 `"멤버십"` 마커에 걸려
   마이페이지 미도달인데 True → 홈에서 톱니바퀴·로그아웃 좌표 **맹탭** → `logout 실패`.
   → `/mpf/` 타깃만 읽고 마커도 마이페이지 전용으로 교체. READ_FIRST 「스코프를 좁혀라」 재현.
3. **`HMALL_LOGIN_LINK` 좌표 stale** — 주석엔 `2026-07-07 버튼 y1310→1454` 라고 **적어놓고 상수는 1310**.
   게다가 chooser 는 WebView 라 dump 에 텍스트 0개 → `_tap_text` 폴백도 원리적으로 불가.
   → **CDP 텍스트 매칭 클릭**(`_CHOOSER_LINK_JS`, 좌표 무관)을 1순위로. 버튼은 `<a>` 가 아니라
   `<button>` 안의 `<p>` 이고 클래스는 CSS-in-JS 해시(`_1nyufc21m`)라 셀렉터로 못 쓴다.
   **교훈: 관측을 주석에만 적으면 코드는 안 바뀐다.**
4. **현대카드 앱카드 인증벽** — 사용자 지시로 **무조건 일반결제**. `GENERAL_PAY_IDS` 계정 선별 구조 **삭제**
   (임계가 계정별 누적이라 어제 되던 계정이 오늘 막힌다 — tkdkky2002 가 454,080원에서 첫 발현).
   탭 없는 계정(앱카드 미등록)은 `pay_hyundai()` PIN 경로로 위임.
5. **PIN `확인` OCR 미판독** → `ocr_or_dump_tap` dump 폴백. **#2 로그에서 발동 확인**:
   `✗ ocr_tap 미발견(4회)` → `✓ '확인' tap @ (540,1312)`.
6. **`_verify_pay_method` OCR 전용** — 토스트를 못 읽어 대기 가드가 안 걸리고 즉시 실패 판정
   (`SELECT_CARD_FAIL`) → OCR+dump 병합.
7. **`추가 인증` 팝업을 `handle_after_pay` 맨 앞에서 1회만 검사** — 팝업이 본인인증 화면 **뒤에** 늦게 떠
   키패드를 덮은 채 판독 → 배경 글자 `['1','4','8']`. → `_dismiss_extra_auth_popup()` 를 카드비번 직전에도 호출.
   `enter_card_password` 안의 `screen_has` 검사도 같은 이유로 못 잡고 있었다(같은 모양 버그 2곳).
8. **`enter_card_password` 가 '확인' 실패를 무시하고 `ok=True`** — 4자리 넣어놓고 제출 못 한 채 성공 보고.
   → 결과 반영. READ_FIRST 「성공 메시지는 검증이 아니다」.
9. **`buy.py` 가 성공 시 로그를 버림** — 어느 경로로 성공했는지 확인 불가라 실돈 9계정 앞에서
   증거가 없었다 → 성공에도 `--- log(성공) ---` 출력.
10. **`resume` 이 뒷기록을 안 남김** — 구매대장·적립·paid 가 `buy_one` 에만 있어 resume 완주분이
    **조용히 0건**(실측 #3, 사람이 손으로 메꿨다). → `_record_after_done()` 로 뽑아 **양쪽에서 호출**.

## 5. 실패했지만 자동화 문제가 아니었던 것

- **#2 `[A001] 세션이 만료`** — 코드 수정 없이 재시도로 통과. 일시적. 실패 시 주문 미생성 확인.
- **#10 KCP 승인요청 실패** — 화면 원문 `Failed to resolve 'spl.kcp.co.kr'` (`UnknownHostException`).
  **카드 한도·FDS 아님, 폰 DNS 일시 실패.** 직후 확인 시 `spl.h.kcp.co.kr(157.119.32.149)` 정상 해석.
  ⚠️ 코드가 KCP 시스템오류까지 `결제거절` 로 뭉뚱그려 분류해 **처음에 카드사 거절로 오판했다** —
  로그 문자열만 보지 말고 **화면을 볼 것**.

## 6. 윈도우 이식 구멍

- `C:\tmp` 생성 — 코드 전반의 `/tmp/...` 하드코딩(30여 곳)이 윈도우에선 `C:\tmp` 로 간다.
- `BROWSER_PY` 기본값 `python3` 이 윈도우 스토어 스텁 → **적립 신청이 조용히 실패할 자리**. 실경로 지정 필수.
- `cart/today_carts.json` 이 **8/6 자 stale** — 그대로면 preflight 차단(정상 동작) 또는 엉뚱한 상품으로
  대장·적립 기록. 오늘자로 재생성했다.
- 폰 무선 ADB 최초 페어링 완료: `172.30.1.99:46847` (SM-G9960).

## 7. 검증 중 있었던 내 실수

`login=False` 인데 페이지를 읽어 **남의 계정(김미숙 31,530P)을 #1 것으로 오독할 뻔**했다.
`_hmall_clean(deep=True)` 가 로그아웃을 못 시켜 이전 세션이 남은 상태. → `login=False` 면 **판독 중단**
하도록 바꿔 재확인(#1 = 김건엽님 51,500P→0P). 8/11 워크로그의 「#1·#2 가 같은 계정을 두 번 읽었다」 재현.

## 8. 다음 할 일

- [ ] **11~19번 결제 미실행** — 카트는 담겨 있음(22번×2 + 34번×2). 결제하려면 `cart/today_carts.json`
      에 11~19 를 추가해야 한다(현재 1~10 만 기재 — 무인자 `buy.py` 가 기재분 전부를 결제하므로 의도적).
- [ ] `phone_auto/_tmp/claude_ocr/` 핸드셰이크를 **롯데홈쇼핑에서도** 라이브 검증(오늘은 현대몰만).
- [ ] `enter_card_password` 의 팝업 검사 중복(신설 헬퍼 + 호출측) 정리 여부.
- [ ] 8/19 1차 세션 이월분: 7% 적립 미수집 / #8 이디야 일시중단 라벨 / `sulwhasoo-ids.json` gitignore /
      갤러리아 `c` 재고 / 현대카드 페이백 / 롯데 `_sold_out()` 라이브 미검증 / 재고 `f` 버전 정리.

## 9. 적립(H.Point) 계정별 재확인 — **10/10 확정**

사용자 지시 "1-10 적립신청만 잘 확인하고". 매니페스트 플래그를 믿지 않고 **계정별 재로그인 후 조회**했다.
(`buy.py reward N` = `apply_hpoint` 멱등 — 이미 신청이면 `이미신청`, 아니면 그 자리에서 신청)

| # | 계정 | 결과 | 판정 |
|---|---|---|---|
| 1 | tkdkky2002 | 화면 `김건엽님` + `already_done=True` | ✅ |
| 2 | 1plu1mall1 | 이미신청 (login True) | ✅ |
| 3 | kgi5907@daum.net | 화면 `김교일님` + `already_done=True` | ✅ |
| 4~9 | 1plus1mall0 … lee0503031 | 이미신청 (login True) | ✅ |
| 10 | jye40323 | 쿨다운 후 3번째 시도 login=True, 화면 `조영은님` + `already_done=True` | ✅ |

prmo = `P202608145877` (건강식품 브랜드 특별전 10%적립, ~8.20 23:59) 단일.

### #10 이 미확정인 이유 = 적립이 아니라 **로그인 차단**
```
[BLOCKED] jye40323 — 로그인에 실패하였습니다. 다른 로그인 수단을 이용바랍니다.
```
Hmall **reCAPTCHA v3 봇 탐지**(8/18 워크로그 오탐표에 이미 있던 그 증상). #10 은 오늘
KCP 실패 → 재결제 → 적립까지 **짧은 시간에 로그인을 여러 번** 해서 걸린 것으로 보인다.
1차 일괄 조회에서 `신규신청` 이 떴으나 그때 `login_ok=False` 라 **어느 세션에서 나갔는지 불명 → 무효 취급.**
→ 15분 쿨다운 후 재확인 결과 **`already=True` 로 확정**(시도1·2 는 여전히 차단, 시도3 성공).
  1차의 `신규신청` 은 역시 오판이었다 — 실제로는 이미 신청돼 있었다. **login=False 를 무효 처리한 판단이 맞았다.**

### 재확인하다 드러난 것 — 판정 로직은 맞게 동작했다
`login=False` 인데 `이미신청` 이 뜨는 조합이 실제로 나왔다(#1 1차). 코드 주석의
「login False + already_done=True 는 전부 오판이었다(2026-07-13)」가 오늘도 그대로 재현.
**apply_reward 가 login True 를 요구하는 설계가 옳다** — 안 그랬으면 3계정을 성공으로 오보고했다.

### 남은 주의
- `PowerShell` 로 한글 정규식이 든 `.ps1` 을 heredoc(UTF-8)으로 만들면 **ANSI 로 읽혀 깨진다**
  (`Select-String -Pattern "적립\]"` → InvalidRegex). 반복 실행 드라이버는 **파이썬으로 쓸 것.**
  이 사고로 `buy.py reward 1..10` 이 결과 없이 한 번 더 돌았다(멱등이라 피해는 없음).
