# WORKLOG 2026-07-15 (토스페이 결제 자동화 pay_toss 코드화·라이브검증 + 식품 데일리런 14계정)

## 요약 (한 줄)
당일카드가 **토스페이 삼성 7%**(자동화 미지원)로 떠서, **토스페이 간편결제를 그날 라이브로 코드화(pay_toss)+검증** 후 식품 14계정 결제·적립 완주. 토스앱 로그인 전제 PIN(137601) 방식.

## 데일리런 (2026-07-15)
- **cart plan**: 33 GRN붓기캔디→#1-5, 28 스키니랩시서스→#6-10, 22 올바른와이→#11-14 (각 ×2). 담기 14/14.
- **결제 14/14 완료** (전부 토스페이, H.Point 전액사용). **적립 14/14 검증 완료**(전수 login True already_done).
- 원장 14건 기록(금액=즉시할인가; 실 토스청구액은 포인트 차감분만큼 낮음, 예 #7 실청구 242,442 vs 원장 287,300).

## 토스페이 자동화 — 신규 (커밋 82ef608)
### ① detect_card 토스 우선인식 (hmall_hyundai_buy.py)
- '토스페이 삼성'은 카드명 행과 금액 행이 cy 40px 넘게 떨어져 기존 same-row 매칭서 누락(DETECT_CARD_FAIL).
- has_amt 확인 후 영역에 '토스' 있으면 `return "토스"` (★'삼성' substring보다 먼저 — pay_samsung과 경로 완전 다름).

### ② _select_toss_card (신규) — 700px 캐러셀 정본 미지원 대체
- `hmall_select_card_discount`(700px)는 토스 카드 금액을 못 읽음('캐러셀 None') → 검증 실패.
- 대체: **토스 카드박스 직접 탭** + 결제버튼금액==토스카드금액 검증(+'적용되었어요' 토스트 폴백). 이미 선택시 탭 안 함.
- `_won`은 '원' 접미사 없이 콤마숫자 파싱(OCR이 '94,005'와 '원'을 분리하므로).

### ③ pay_toss (신규) — 토스앱 로그인 전제
- 경로: 원결제하기(OCR) → 토스 '결제진행 다음'(OCR) → 토스앱 진입 → **게스트 본인확인('휴대폰 번호'/CertifyGuest) 감지시 안전정지**(SMS/PASS=자동화 불가, 미결제) → OnlinePayActivity '결제하기'(OCR) → PIN(text_dump 137601) → **★OnlinePayApproveCompleteActivity '완료' 탭** → hmall 복귀.
- **★완료 탭 필수**: 안 누르면 카드는 승인됐는데 hmall 주문 미생성(#2 실측 — 수동 완료해야 했음). pay_toss 오탐실패 원인이었고 이걸 추가해 fix.

### ④ input_pin source=text_dump (flow_runner.py, 신규)
- 토스 PasswordActivity 셔플 키패드 숫자는 content-desc 아닌 **text="N" 노드** → 기존 `source=dump`(content-desc) 실패 → text 기반 변형 추가. FLAG_SECURE 무관(dump O). PIN=137601로 앱잠금 해제 검증 = 토스 로그인+PIN 확정.

### ⑤ _webview_socket hmall PID 우선 (hmall_webview.py) — 근본 fix
- 카드앱(KB 등)이 백그라운드 웹뷰 devtools 소켓을 동시에 열면 `/proc/net/unix` 첫 매칭이 카드앱 빈 웹뷰 → CDP 타겟 0개 → **login 타임아웃**(실측: KB카드 21688 vs hmall 24423). → hmall PID 소켓 우선 선택.

## 카드/토스 특이사항 (다음 토스날 참고)
- '토스페이 삼성 7%'의 '삼성'은 hmall-side 프로모명 — 실제 청구는 **토스 기본카드(현재 Amex)**, 7%는 그대로 적용(#11 실측 109,441).
- 토스 결제화면에 '신한카드 4,000원 할인' 옵션도 있었음(미선택, Amex 기본 유지). 추가 절감 원하면 신한 선택 스텝 필요.

## 포인트 정책 뒤집힘 (사용자 지시 2026-07-15)
- 예전 [[hmall-no-points-food-tier]](7/13, HMALL_NO_POINTS=1로 포인트금지, 15만원 tier 보존)을 **폐기**.
- **이제 포인트 남은 거 무조건 전액사용**(소진 타이밍). HMALL_NO_POINTS 설정 안 함(코드 기본=전액사용). 자동메모리 갱신.

## 미커밋 잔존(의도)
- `cart/today.json`·`today_carts.json`·`buy/cart_plan.json`(일일 데이터), logs, backup/temp. TOSS_PAY_NOTES.md 갱신은 별도 커밋 권장.
