# WORKLOG 2026-08-07 — 삼성 현대몰 **라이브 첫 검증** + 8/6 실패의 두 번째 원인 규명 + 로컬 OCR 완전 제거

## 1. 8/6 삼성 실패 원인 조회 — 워크로그가 이 맥에 없었다

사용자 질문("왜 삼성 결제 실패함? 이전엔 잘 됐는데"). 로컬 워크로그·로그·구매대장 어디에도 8/6 삼성 기록이 없었다.
원인은 **8/6 세션이 다른 PC에서 돌고 origin 에만 푸시**돼 있었기 때문(로컬 main 이 11커밋 뒤처짐).
`git pull` 로 `WORKLOG_2026-08-06.md` §31 확인 후 답변.

- 8/6 실패 = `SAMSUNG_FAIL@card_cvc: '다음' 비활성` 로 #11~#19 **9계정 전멸**(#10 만 현대카드라 성공).
- **삼성을 핸드세이크 없이(로컬 OCR) 결제한 마지막 날 = 2026-08-02**, 그날 앞의 2건뿐
  (15:20 #2 kms3945 / 15:34 #4 owyura, 롯데홈쇼핑). 같은 날 **18:58 커밋 `b2f1f46`** 에서
  `PAY_VISION_MODE`(정지-인계)가 처음 생겼고, 17:44~18:21 의 4건은 **이미 핸드세이크로 4/4 성공**이었다.
  그 이전(7/28 롯데 6건, 7/9~10 롯데 2건, 6/18 현대몰 16건)은 핸드세이크가 없던 시기 = 로컬 OCR.
  → "이전엔 잘 됐다"의 실체는 **핸드세이크로 잘 된 것**이었다. (근거: 구매대장 30건 + 커밋 시각 + docstring)

## 2. 폰 연결 — adb 만 LAN 이 막혀 있었다 (환경 문제, 30분 소모)

`ping`·`nc`·python `socket.connect` 는 폰(192.168.0.8)에 다 붙는데 **`adb` 만** `No route to host`.
Bonjour 로 `_adb-tls-connect`(40957)·`_adb-tls-pairing`(45433) 둘 다 보이고 포트도 열려 있었다.
샌드박스 해제·데몬 재기동·mDNS openscreen 모두 무효.

→ **루프백 TCP 포워더로 우회**(**`phone_auto/adb_fwd.py`** — scratchpad 는 gitignore 라 레포로 옮겨 커밋했다):
`127.0.0.1:41433→폰:45433`, `127.0.0.1:40957→폰:40957`
후 `adb pair 127.0.0.1:41433 <코드>` / `adb connect 127.0.0.1:40957` → `SM_G9960 device` 연결 성공.

그 뒤 `device offline` 이 반복돼 두 번을 더 날렸는데 원인이 둘이었다:
- **포워더 upstream 소켓에 `timeout=5` 가 남아 있었다** → 5초만 idle 하면 `socket.timeout` 으로 파이프를 닫아버림. `settimeout(None)` 으로 해결.
- **진단하다 띄운 2번째 adb 데몬(`-L tcp:5038`)** 이 같은 기기를 두고 5037 데몬과 다툼 → 종료.

자동메모리 `adb-phone-wireless-connect` 에 추가해둠.

## 3. ★ 삼성 현대몰 **라이브 첫 검증 성공**

```
python3 -u -m phone_auto.hmall_hyundai_buy 1 삼성     (ALLOW_STALE_CART=1)
```

| 항목 | 값 |
|---|---|
| 계정 | #1 tkdkky2002 |
| 주문번호 | **20260807004446** |
| 결제 | **198,400원 (삼성카드 일시불)** — 주문금액 248,000원 |
| 상품 | [우수고객전용] 데이즈온 파라다이스 그레인 버닝 6+3박스 |

- 오늘 캐러셀에 삼성이 없어(당일카드=KB) **그리드 강제선택**으로 갔다: `[grid] ✓ '삼성카드' 탭` (할인 없음).
- `pay_samsung` 이 로컬 OCR 시도 **0회**로 카드번호 화면에서 정지 → `samsung_enter` 로 완주.
- 구매대장 기록 완료(JSON+시트). **H.Point 적립은 사용자 지시로 생략**("날짜 바뀌어 어차피 삼성 아님").

### ★ 화면 발급사 '롯데' 표기는 정상 (사용자 확인)

비번 화면에 `3779-89****-**897` 아래 **'롯데'** 로 뜬다. 실제 승인은 삼성카드
(주문완료 결제정보 `198,400원 (삼성카드 일시불)` 로 실측). **롯데홈쇼핑에서도 동일하게 뜬다.**
→ 오결제로 오인해 멈추지 말 것. 코드 주석 + 자동메모리 `samsung-payment-handshake` 에 박아둠.

## 4. ★★ 8/6 실패엔 **두 번째 원인**이 있었다 — `next_button_enabled` 좌표 하드코딩

8/6 워크로그는 원인을 "로컬 OCR 매핑 실패"로만 적었다. 그런데 **오늘 완벽하게 입력한 상태에서도**
`[next] ✗ '다음' 비활성` 이 그대로 재현됐다. 화면을 보니 **'다음' 은 진한 파랑 = 활성**이었다.

원인: `next_button_enabled(y_hint=1377)` 이 **롯데 화면 기준 하드코딩**인데
**현대몰 삼성 화면은 버튼이 y≈1173** → 흰 배경을 찍어 **무조건 '비활성'** 판정.
= 입력이 100% 맞아도 그 자리에서 결제가 막힌다. 8/6 9계정의 `'다음' 비활성` 중 최소 일부(어쩌면 전부)가 이것.

> 교훈은 8/6 과 같다 — **좌표·플래그 하드코딩은 화면이 바뀌면 조용히 틀린 답을 준다.**
> 이번엔 "가드가 결제를 막는" 방향이라 돈은 안 나갔지만, 원인을 못 찾으면 매번 전멸이다.

## 5. 고친 것 (전 폴더)

| # | 파일 | 내용 |
|---|---|---|
| 1 | `hmall_hyundai_buy.next_button_enabled` | y 하드코딩 제거 → **버튼을 OCR 로 찾아 그 자리 색을 본다**. `label=` 추가('다음'/'결제') |
| 2 | `samsung_enter next` | 화면에 있는 **'다음'/'결제' 를 자동 선택**(완전일치 — '결제 비밀번호' 오탭 방지). 현대몰 PIN 화면 버튼이 '결제'다 |
| 3 | `samsung_enter` docstring | 몰별 분기 명시 — **현대몰은 인증서 단계 없음**(pin6→'결제'→주문완료) / 롯데만 cert·certpw |
| 4 | `hmall_hyundai_buy._HANDOFF_STEPS` | 삼성 안내를 현대몰 실제 순서로 정정(인증서 없음) |
| 5 | **`nh_enter finish` / `finish_lotte`** | ★`card="NH"` **하드코딩 제거** → `card=` 인자. 안 고쳤으면 **삼성 결제가 대장에 NH 로** 남았다 |
| 6 | `nh_enter finish` | ★`combo=` 추가 — **현대몰 설화수**는 `record_food` 가 아니라 `record_combo` 로 가야 한다(분기 자체가 없었다) |
| 7 | `nh_enter finish` | `order=` 추가 — 주문완료 화면을 이미 벗어났을 때 기록을 살린다(가드는 유지, 로그로 크게 남김). 오늘 실제로 알림 배너 때문에 화면을 놓쳐 필요했다 |
| 8 | `nh_enter finish` | 대장 0건이면 **경고 출력**(종전엔 조용히 통과 — 8/5 누락사고와 같은 모양) |
| 9 | `samsung_enter finish` 위임 | `card=삼성` **자동 주입**(직접 준 값이 있으면 그게 이긴다) |
| 10 | `hmall_hyundai_buy._handoff_stop` | 설화수면 `combo=N`, 식품이면 키워드로 **명령을 완성**해 출력(남은 계정 명령에도 combo 유지) |
| 11 | `lotte_homeshopping_buy` 핸드세이크 | 롯데도 **다음에 칠 명령을 완성해서 출력**(`finish_lotte <n> combo=N card=삼성` + 뷰티 경고) |
| 12 | **로컬 OCR 셔플 입력 전멸** | `_tap_shuffle`(hmall)·`_tap_shuffle_retry`(lotte)·`_kp_read`·`_tap_secure_each` **삭제**. 전부 호출자 0개였고, 되살리면 8/6 이 재현된다 |

## 6. 검수 (오프라인, 실제 코드 경로에 스텁 주입 — 폰·시트 미접촉)

`scratchpad/verify_fixes.py` **29항목 전부 통과**:

- 현대몰 식품 `finish 1 데이즈온 card=삼성` → `record_food` 1건, **card=삼성**, 키워드로 다른 상품 제외 ✓
- 현대몰 설화수 `finish 1 combo=24 card=삼성` → `record_combo(24, card=삼성)`, `record_food` 미호출 ✓
- `order=` → 주문완료 화면이 아니어도 기록, 값 그대로 전달 ✓
- **가드 유지** — `order=` 없고 주문완료도 아니면 `rc=1` + 대장·적립 호출 **0건** ✓
- `samsung_enter` → `card=삼성` 자동주입 / 직접 준 `card=NH` 는 유지 ✓
- 롯데 `finish_lotte` → `record_combo("롯데홈쇼핑", card=삼성)` + **뷰티 → 적립 순서 유지** ✓
- NH 는 기본값 `card=NH` 그대로(회귀 없음) ✓
- `_handoff_stop` 출력(설화수/식품) ✓ · `next_button_enabled(label=)` ✓ · 로컬 OCR 0건 ✓

## 6-2. 전 폴더 검수 — 같은 유형 훑기 (사용자 지시 "폴더 전체 검색해서 실수할 곳 없는지")

오늘 터진 것과 **같은 유형**을 기준으로 전 폴더를 훑었다: ①좌표 하드코딩 ②카드·몰 하드코딩
③플래그 뒤에 숨은 정답 ④조용한 실패 ⑤`contains` 오탭 ⑥죽은 코드.

**깨끗한 것**
- 환경변수 게이트 전수조사 — `PAY_VISION_MODE` 류(정답이 플래그 뒤에 숨는 것)는 **0건**.
  남은 건 전부 **안전한 기본값**이다: `HMALL_NO_POINTS`(기본=포인트 사용) · `HMALL_NO_REWARD`(기본=적립함) ·
  `ALLOW_STALE_CART`(기본=차단, 쓰면 크게 로그) · `DRY_PAYMENT`(기본=true).
- 카드 하드코딩 — `card="NH"` 는 §5 에서 제거, 이제 주석에만 남는다. 현대몰 디스패치는
  현대/롯데/KB/하나/BC/삼성/NH/토스 전부 매핑돼 있다.
- 구매대장 `combo_info("현대Hmall", …)` → `_MALL_ALIAS['현대Hmall']='hmall'` → 라벨 '구매가격' 로 정상 조회.
  **현대몰 설화수 조합기록 경로가 살아 있다**(§5-6 수정과 짝).
- 절대좌표 탭은 대부분 `tap(고정x, OCR로 얻은 cy)` 패턴이라 화면이 밀려도 따라간다.

**남은 위험 — 보고만 하고 좌표는 안 건드림**
- `phone_auto/nh_vision_input.py` 의 `ROW_CARD/ROW_CVC/ROW_PIN6` 는 **2026-07-31 롯데 실측 절대좌표**다.
  키패드가 조금만 움직이면 **엉뚱한 키를 누르고, 자릿수는 맞아서 `_verify_len` 도 못 잡는다**
  (→ 카드사 입력오류 3회면 카드 잠김). 실제로 삼성 키패드는 칸마다 위치가 바뀐다(8/7 실측 962/1079 → 612/727).
  → **좌표는 바꾸지 않았다**(NH 라이브 화면 없이 바꾸는 게 더 위험). 대신 `nh_enter._rows_plausible` 신설:
  dump 로 **키패드 컨테이너 bounds** 를 읽어 가정한 행이 그 안에 있는지 보고, **밖이면 탭하지 않고 중단**한다.
  판정 불가(dump 실패/후보 없음)면 기존 동작 그대로. 오프라인 3케이스 통과(정상/이동/판정불가).
- `hmall_hyundai_buy` 에 **컨테이너 상대좌표** 방식(`KP_COLS`/`KP_ROW_OFF` + `_grid_tap`)이 있는데
  **호출자가 없다.** 정작 살아 있는 NH 경로는 절대좌표를 쓴다 — 즉 **더 튼튼한 쪽이 죽어 있다.**
  다음 NH 결제 때 화면을 보고 상대좌표로 옮기는 게 근본이다(오늘은 검증 못 해 그대로 둠).
- `pay_nh`(PAYCO)·`pay_samsung_payco` 는 **호출자 0개인 폐기 경로**였다(docstring 에 DEPRECATED 명시).
  → **§6-3 에서 삭제함.**
- `pay_samsung` 의 `'다른'`·`'일반'` contains 탭은 **정밀매칭 실패 시 폴백**이고, 오늘 라이브는 정밀매칭으로 통과했다.
- `lotte_homeshopping_buy:848` ARS 다이얼패드 좌표는 **추정 폴백**이라 코드에 이미 경고가 있다(삼성 일반결제는 ARS 를 안 탄다).

## 6-3. 폐기 경로 삭제 (사용자 지시 "폐기 경로 정리해")

**PAYCO/ARS 경유 결제 3종 + 부속 1종, 총 210줄 삭제.** 넷 다 **호출자 0개**였고 디스패처에도 연결이 없었다.

| 삭제 | 파일 | 왜 |
|---|---|---|
| `pay_samsung_payco` | hmall (39줄) | PAYCO 는 금액 크면 **ARS 전화인증** 요구 → 무인 불가. 정본=`pay_samsung`(카드번호 직접) |
| `pay_nh` | hmall (46줄) | 〃. 정본=`pay_nh_general` |
| `pay_lotte_payco` | lotte (79줄) | 〃. 정본=`pay_lotte_samsung_general` → 3사 공용 `pay_samsung` |
| `handle_ars_call` | lotte (46줄) | 위 함수 전용(다른 호출자 없음). **다이얼패드 '1' 좌표가 실측 아닌 추정값**이라 남겨두면 언젠가 오탭한다 |

- 삭제 자리엔 **왜 폐기됐는지 + 정본이 무엇인지** 주석으로 남겼다(git 이력에서 꺼낼 수 있음도 명시).
- 이들만 갖고 있던 `ocr_tap("확인", contains=True)` 오탭 위험도 같이 사라졌다.
- 스테일 주석 3곳(`구 PAYCO=pay_nh 폴백` 등) 갱신.
- 삭제 후 확인: 문법·import OK / **살아있어야 할 함수 전부 존재**(`pay_samsung`·`pay_nh_general`·`_card_no_boxes`·`_wait_keypad`·`pay_lotte_samsung_general`·`pay_loca`·`buy_one`) / **죽은 `pay_*` 0개** / 검수 29항목 재통과 / 버튼판정 실화면 3/3 재통과.

**★지우지 않은 죽은 코드 1건 (의도적)**: `KP_COLS`/`KP_ROW_OFF`/`_kp_top`/`_grid_tap`.
폐기물이 아니라 **더 튼튼한 방식**(컨테이너 top + 오프셋)이라서다 — 살아있는 NH 경로가 절대좌표를 쓰는
문제(§6-2)의 **정답이 이쪽**이다. 주석으로 "지우지 말 것 + 다음 NH 결제 때 이쪽으로 옮길 것" 명시.

## 7. 안 한 것 · 남은 것

- **H.Point 적립** — 사용자 지시로 생략(날짜가 바뀌어 오늘 당일카드는 삼성이 아님).
- **`flow_runner` 의 카드앱 PIN OCR(`vote_digits`) 은 그대로 뒀다.** 현대·KB·하나·롯데 **앱 PIN** 경로라
  셔플 카드번호 키패드와 성격이 다르고, 여기가 **무인 완주의 핵심**이다(8/5 KB 9계정 무인 성공).
  이걸 걷어내면 매 계정 핸드세이크가 되어 무인 실행이 불가능해진다. 필요하면 별도 지시로.
- `cart/today.json`·`today_carts.json` 은 **8/6자** — 오늘 실행은 `ALLOW_STALE_CART=1` 로 우회했다.
  정상 절차는 step1/step2 재실행. (그래서 `finish` 의 식품 대장이 0건이었고, 오늘 건은 실측값으로 직접 기록했다.)
- BC 카드는 여전히 코드완성·실승인 미검증(8/5 이전 상태 그대로).

━━━━━━━━━━━━━━━━━━━━ 2차 세션 ━━━━━━━━━━━━━━━━━━━━

# 2차 세션 — step1/step2 8/7 실행 + **조합번호 색칠 회귀 규명·원복** + 8/6 현대몰 설화수 추적

## 8. 사용자 지시 (원문 그대로)

1. "git pull 오늘 새벽에 작업한거 보이나?"
2. "스텝1,2 실행해 8/7"
3. "끝나면 스텝2도 바로 돌려"
4. "왜 설화수 모든 조합에 색칠을햇니?"
5. "공급률 가장 높은애만 초록색칠해야지 왜 다 해놨냐"
6. "조합번호 원래 뭐 칠해야하는지 파일 전체 훑어봐 이 부분 너가 놓친거같으니 찾은 다음에 다시한번 안까먹도록 문서상단에 올려놔."
7. "3 5 12 13 14 23 24 25 27 항상색칠해 그리고 우측 결과에 3사 몰 중에 가장 할인율 높은것만 초록색칠해"
8. "1-20 계정 들어가서 현대몰에서 8/6 15:18분에 설화수 조합 어떤 계정으로 샀는지 체크좀해줘"
9. "스텝1 다시실행해줘 결과물에 색칠 제대로해"

## 9. git pull — 1차 세션(새벽) 커밋 3개 이어받음

`5f8b411 → fc3e0ba` fast-forward, 7파일 +451/−355. `b1c508c`(삼성 현대몰 라이브검증 + '다음'
비활성 오판 수정) · `06f2d54`(전 폴더 검수) · `fc3e0ba`(폐기 경로 210줄 삭제).

## 10. step1 실행 (13:08 → 13:23:32, rc=0)

6단계 전부 통과. **갤러리아 오염 없음** — `REDIRECTED` 0건 + b~n `goods_no` 전부 상이.
GWP 이미지 지문 일치로 재판독 불필요. **재고관리 전체 일치 → 활성 버전 `c` 유지.**

**★ 채널이 갤러리아로 바뀌었다 (8/6 은 롯데)**

| 채널 | 공급률 범위 | 당일카드 |
|---|---|---|
| **갤러리아 (선택)** | 0.4882 ~ 0.5336 | — |
| Hmall | 0.4821 ~ 0.6173 | KB국민 5% |
| 롯데 | 0.5064 ~ 0.5488 | 현대카드 5% |

롯데 조건이 8/6 대비 나빠진 것이 원인:

| 항목 | 8/6 | 8/7 |
|---|---|---|
| 쿠폰 | 13% (f·h 만 10%) | b·f·h 12% / c·d·e·g·n 13% |
| 카드 | 롯데카드 7% | **현대카드 5%** |
| 적립 | 30만↑ → 30,000원 | **0원 (tier 없음)** |

추증(당일): b 20,700 / c 10,900 / d 15,500 / e 19,500 / f 16,500 / g 26,800 / h 28,400 / n 16,500.
f 와 n 이 16,500 으로 같지만 **오염 아님**(URL·쿠폰율 상이). 8/6 대비 c 10,800→10,900, n 16,400→16,500.

**카트플랜**: channel=galleria, N=36, 25조합, 총 13,404,632원, 평균 공급률 0.5116. 보조코드 `d` 사용 0.

### ⚠️ 재고가 플랜을 못 받친다 — b·c

`cart_plan` 은 재고를 **분배에 반영하지 않는다**(참고 출력 전용).

| 코드 | 재고 | 플랜 사용 |
|---|--:|--:|
| b | 1 | 2 |
| **c** | **2** | **21** |
| e/f/g/h/n | 38~110 | 여유 |

`c자음2` 21개 필요 / 재고 2. 담기 단계에서 대량으로 막힌다. 8/6 의 `n`(4/6) 과 같은 유형인데 규모가 크다.
**c 를 쓰는 조합(6·7·8·9·10·11·15·16·17·20·21) 처리 결정 필요.** 플랜은 사용자 판단 전이라 그대로 뒀다.

## 11. step2 실행 (13:23:50, rc=0) — 10%적립 **6** / 적립없음 10 / 에러 3

시트 **`8.07 식품` 신규 생성** A1:T21(19개), 재고관리 `당일 식품` 미러 21행. 계정 #1 tkdkky2002.
`ten_percent_ids = ['1','17','22','33','34','35']`

| # | 제품명 | qty | 혜택가 | 즉시할인가 | 실비 | 구간 |
|---|---|--:|--:|--:|--:|--:|
| 1 | 이너플로라 유산균 60정 | 1 | 125,910 | — | — | 7단 |
| 17 | 데이즈온 원데이 알파 18개 | 2 | 47,920 | 91,048 | 79,675 | 6단 |
| 22 | 올바른건강식품 와이 9박 | 1 | 66,340 | — | — | 7단 |
| 33 | GRN 붓기캔디 | 1 | 62,910 | — | — | 7단 |
| 34 | 데이즈온 파라다이스 버닝 9박스 | 1 | 198,400 | 188,480 | 165,286 | 6단 |
| 35 | 데이즈온 프리바이오틱스 트리플 | 1 | 73,600 | 69,920 | 60,026 | 6단 |

8/6 대비 **`11` 라메종드미엘 · `25` 석류젤리가 ✓ → ✗** 로 빠져 8건 → 6건.

### ⚠️ 조용히 넘어간 것 3가지 (rc=0 이었다)

1. **6건 중 3건이 `[바로구매 실패]`** — `1`·`22`·`33` 이 결제 페이지를 못 열어 **즉시할인가·실비가 `?`**
   로 비었고 시트에도 빈칸으로 들어갔다. 8/6 엔 셋 다 값이 있었다(1: 125,910/115,910).
   실패한 3건이 **전부 7단 '건강식품 위크 10%적립'** 구간 — 우연이 아닐 수 있다. **원인 미규명.**
2. **페이백 전 상품 `[-]`** — 8/6 엔 1·22·25·33 이 삼성 페이백이었다. 이벤트 종료인지 판독 실패인지 **미확인.**
3. **`1-1` 이너플로라 30정 — 3일 연속 ERR 판매중단**(8/5·8/6·8/7). 8/5 워크로그가 적어둔
   "계속 ERR 이면 URL 재확인 필요" 조건에 걸렸다. **URL `2201017629` 점검 대상.**

## 12. ★★ 조합번호 색칠 — 내가 만든 회귀였다 (사용자 지적)

사용자: "왜 설화수 모든 조합에 색칠을햇니?" → 오늘 **25개 조합이 전부 칠해졌다.**

`git log -S"HIGHLIGHT_COMBOS"` 로 추적한 경위:

| 날짜 | 커밋 | 무슨 일 |
|---|---|---|
| 07-29 | — | **사용자 지시**: "주로 사는 조합번호를 색칠" |
| 08-01 | `8019f52` | 구현 — `HIGHLIGHT_COMBOS = {3,5,12,13,14}` **고정 세트** |
| **08-02** | `67f4fa2` | **클로드가 지시 없이** "하드코딩이 stale" 이라 판단 → **오늘 플랜 조합**으로 변경 |
| 08-05 | `cbf8342` | 사용자 지시 "14번도 색칠" → `HIGHLIGHT_COMBOS_ALWAYS = {14}` |

08-02 커밋 메시지에 사용자 지시 언급이 **없다** — 단독 판단이었다.
8/6 은 채널=롯데(N=7)라 7개만 칠해져 티가 안 났고, **8/7 채널이 갤러리아(N=36)로 바뀌자
tier_a 25개가 전부 칠해져** 발현됐다. `CHANNEL_DEFAULT_N = {galleria:36, hmall:36, lotte:7}` 이라
**플랜 기준 색칠은 갤러리아/Hmall 날엔 구조적으로 "전부 칠하기"가 된다.**

- 조합번호는 **안 밀렸다** — 07-29 시점 COMBOS 1~23 과 현재 1~23 동일(24~27 만 추가).
- 교훈을 **`READ_FIRST.md` 상단**(사고 사례 바로 뒤, "시작 전 반드시 읽을 것"보다 위)에 박았다
  — 사용자 지시 6. 자동메모리 `user-constants-not-stale-dont-auto-replace` 도 신설.

### 고친 것 (`rate-check/cart_plan.py`)

| # | 내용 |
|---|---|
| 1 | `HIGHLIGHT_COMBOS = {3,5,12,13,14,23,24,25,27}` **고정** (사용자 지시 7). `_FALLBACK`·`_ALWAYS` 제거 |
| 2 | `highlight_combo_numbers` — **세트 밖 번호는 흰색(`CLEAR_BG`)으로 되돌린다.** 안 고쳤으면 오늘 이미 칠해진 25개 중 16개가 그대로 남았다 (repeatCell 은 덮어쓰기만 하고 지우지 않는다) |
| 3 | 호출부 — 플랜(`set(cart)`) 대신 고정 세트 전달 |

### 색 두 가지 (혼동 금지)

| 색 | 위치 | 규칙 |
|---|---|---|
| 연노랑 `(1.0,0.9,0.6)` | 3사 A열 + 차트 J열 조합번호 | 주로 사는 조합번호(고정) |
| 연두 `(0.72,0.92,0.72)` | 차트 K2:M28 | 행별 **최저** 공급률 = 3사 중 최고 할인 |

**공급률은 낮을수록 좋다**(`순 ÷ 소비자가`) — 연두가 `MIN` 을 쓰는 이유.
**초록은 손댈 게 없었다** — 규칙·범위·수식 전부 이미 정확했고, 27행 모두 최저값이 1개씩(동점 없음)이라
행마다 정확히 1칸만 칠해진다. 오늘 기준 갤러리아 21행 / Hmall 6행(조합 2·15·16·17·24·25·27).

## 13. step1 재실행 (14:14 → 14:29:33, rc=0) + **색칠 실측 검증**

사용자 지시 9. `→ 조합번호 색칠: [3, 5, 12, 13, 14, 23, 24, 25, 27]`

시트 배경색을 API 로 직접 읽어 검증 — **갤러리아 A열 / Hmall A열 / 롯데 A열 / 차트 J열 4곳 전부 일치.**
고정 9개만 연노랑, 나머지 18개 흰색. (로그만 믿지 않고 실제 셀 색을 읽었다.)

⚠️ **조건부서식 규칙이 2개로 중복됐다** — `lotte.py` 가 실행마다 `addConditionalFormatRule` 을
무조건 추가한다(코드 주석에 이미 "중복 추가되어도 동작 동일, 정기 정리 권장"). 겉보기·동작은 동일.
같은 날 탭을 재실행할 때마다 늘어난다. **고치지 않았다**(사용자 지시 범위 밖) — 필요하면 추가 전 동일 규칙 제거.

## 14. 8/6 현대몰 설화수 주문 추적 (사용자 지시 8)

**구매대장에 8/6 설화수가 0건**이었다(8/6 현대Hmall 기록은 전부 식품). 대장은 best-effort 라
**19계정 전부 로그인해 주문/배송 내역을 직접 조회**했다.

- 주문내역 경로: **`https://www.hmall.com/mo/mpa/selectOrdDlvCrst`** (마이페이지 → JS 클릭으로 확보.
  `/mo/my/orderList` 류는 전부 404). 상세: `/mo/mpa/selectOrdPTCPup?ordNo=…`
- ⚠️ `connect_over_cdp` 가 **탭 0개면** `Browser context management is not supported` 로 죽는다
  → `chrome_launcher.resolve_cdp_port()` 를 먼저 호출해야 한다(`_ensure_tab`).

**결과: 19계정 중 #1 tkdkky2002 하나뿐** (로그인 실패·조회 에러 0건).

| 항목 | 값 |
|---|---|
| 계정 | **#1 tkdkky2002** (주문자 김건엽) |
| 주문번호 | **20260806072690** |
| 주문일 | 2026.08.06 |
| 결제 | **557,888원 · 현대카드 일시불** |
| 상태 | 배송완료 (08/07 도착) |

**구성 = 조합 14** (`g자음생2종 ×2 + h자음생크림리치 ×1`)
- 자음생 2종 세트 ×2 — 162,950원 / 자음생크림 리치 50ml 기획세트 ×1 — 231,988원
- 주문금액 720,000 → 할인 162,112(깜짝 67,500 + 쿠폰 65,250 + 카드즉시 29,362) → **결제 557,888원**
- H.Point 550P 적립 예상

**★ 15:18 이라는 시각은 현대몰에서 확인 불가** — 주문목록·주문상세 어디에도 시:분 표기가 없다.
날짜(`2026.08.06`)와 `[결제] 2026. 08. 06` 까지만 나온다. 15:18 출처는 카드 문자 등 외부 기록일 것.

**미해결 2건**:
- 이 주문이 **구매대장에 없다**(대장 누락 확정). 기록 여부는 사용자 판단 대기.
- **8/6 설화수 채널은 롯데(N=7)였는데 현대몰 설화수가 결제됐다.** 8/6 워크로그에도 "롯데 설화수
  조합24 6건"만 있고 현대몰 설화수 언급이 없다. **누가/왜 담았는지 미규명.**
- 참고: #1 은 8/7 에도 설화수 자음생2종 ×2(172,125원) 주문이 있으나 **주문취소** 상태.

## 15. 다음 할 일

- [ ] **갤러리아 `c` 재고 2 vs 플랜 21** — 담기 전 결정 필요(조합 제외 or 재고 확보)
- [ ] step2 `[바로구매 실패]` 3건(1·22·33, 전부 7단) 원인 규명 — 시트 즉시할인가·실비 빈칸
- [ ] 페이백 판독 0건이 이벤트 종료인지 버그인지 확인
- [ ] `1-1` 이너플로라 30정 URL(`2201017629`) 재확인 — 3일 연속 ERR
- [ ] 8/6 현대몰 설화수(20260806072690) 대장 기록 여부 결정
- [ ] (선택) `lotte.py` 조건부서식 중복 추가 정리
