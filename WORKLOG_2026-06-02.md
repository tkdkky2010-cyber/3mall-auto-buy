# WORKLOG 2026-06-02 — 롯데홈쇼핑 비롯데(삼성/PAYCO) 첫 실결제 + 구매사은 적립금 매처 수정

폰: SM-G9960 무선 adb. 카드앱/PAYCO 인프라 = `hmall_hyundai_buy` 재사용. ⚠️실돈 결제.

## 실결제 결과 (조합 = 설화수)
- **#8 조영은(jye40323)** — 주문 `2026-06-02-F40609`. 조합1(d본윤2종×2+g자음생2종×2). **삼성카드 7% 청구할인**(즉시 488,661 / 청구 ≈454,450). 뷰티포인트 ❌놓침(ARS 통화 중 주문완료 화면 이탈) / 적립금 미진행.
- **#9 김유설(wlstmdlsfk)** — 주문 `2026-06-02-F40709`. 커스텀조합 **e2+h1**(탄력3종×2+자음생크림리치×1). 삼성 7%(즉시 518,421 / 청구 ≈482,130). **뷰티포인트 ✅완료 + 구매사은 적립금 ✅완료**.

## ★ 발견 1: 롯데홈쇼핑 삼성/PAYCO 결제 경로 (첫 라이브)
- 당일 할인카드는 매일 바뀜. **6/2 = 삼성 7%, KB 5%**(롯데 무할인). `select_card_lotte`가 롯데 하드코딩이라 막힘 → 라이브 우회.
- 경로: 다른결제수단 → **신용카드** → '카드 선택' 드롭다운 → **삼성카드** → 일시불 → 동의 → 결제하기
  → **Samsung Card SDK 모달**(monimo/오픈앱카드/다른결제/**간편결제**) → 간편결제 → **PAYCO** → PAYCO앱(`com.nhnent.payapp`) 결제확인(가맹점 LOTTE Homeshopping, SAMSUNG CARD) → 결제하기 → **결제비밀번호 4x3셔플 = `payco_pin6` 137601**(hmall과 동일, 2엔진 실패→4엔진 승급).
- ⚠️ **현금영수증은 카드 변경과 무관하게 유지**(#8 false-alarm), **동의(필수) 체크박스는 OCR상 앞 박스글리프("1"/"7")가 사라지면 체크됨** — `agree_required` true 반환해도 실제 미체크일 수 있어 결제하기 시 "동의 후 구매 가능" 팝업으로 막힘 → 박스(텍스트 좌측끝) 정확 탭 필요.

## ★ 발견 2: ARS 본인인증 발생 — 빈도 **미확정** (1회성 vs 매번)
- 삼성/PAYCO 롯데결제는 PIN 후 **ARS 전화인증**(010-...) 요구. 사용자가 전화 받아 인증해야 완료.
- ⚠️ **단정 금지(2026-06-02 정정)**: "계정마다 매번"이라고 적었으나 **근거 부족**. #8·#9는 **서로 다른 계정의 첫 결제**라 둘 다 "그 계정 최초 1회"에 해당 → **"계정당 1회"인지 "매번"인지 구별할 데이터가 없음.** 계정당 1회라면 최초 수동인증 후 **완전 자동화 가능**.
- **검증 방법**: 같은 계정으로 2번째 롯데 비롯데 결제를 돌려 ARS가 또 뜨는지 확인해야 결론 가능. 그 전엔 단정 X.
- ⚠️ ARS 통화가 롯데앱을 백그라운드로 밀어 **주문완료 화면 이탈 → 뷰티포인트(now-or-never) 소실 위험**(#8 소실). #9는 ARS 완료 직후 주문완료 폴링으로 **뷰티 빠르게 잡아 성공**.

## ★ 발견 3: 구매사은 적립금 매처 수정 (사용자 지적 — 하드코딩/오필터)
- 옛 `claim_lotte_reward`: `'최대'+'적립'`에 `'구매시'/'적립금' 제외` + 주석에 **"20%" 하드코딩**. → 오필터로 "최대 567P/600원 적립"(소액, %없음) 오탐 → 실패.
- **상품상세엔 "최대 N% 적립"이 3종**: ①상단 프로모배너 "'광세일' 구매시 최대 N% 적립금"(섹션 밖, 탭하면 향수 등 엉뚱) ②'구매/리뷰 적립혜택' "최대 NNNP/N원 적립"(%없음) ③**'구매사은·혜택' 섹션 카드 "최대 N% 적립"** ← 정답.
- **수정**(라이브+단위검증): **"구매사은" 헤더 cy 아래로 스코프** + 정규식 `최대\s*\d+\s*%\s*적립`(N=10/15/20… 매일 변동, **숫자 하드코딩 금지**) + ignore 제외 + 최고% 선택. → ①②자동 배제. 탭 → 광세일 행사페이지(행사안내) → 스크롤 → '혜택 신청하기'.
- `lotte_ignore_keywords.txt`에 **"선물" 추가**(+fallback). 기존 키워드(페이백/L.CLUB/이리오십쇼/가정의달/선물하기/무료가입/창립/게이트페이지) 유지.

## 신규/변경 파일
- `phone_auto/lotte_homeshopping_buy.py`: `claim_lotte_reward` 섹션-스코프 매처(±%·헤더 아래·최고%), 틀린 주석 교체, **`select_card_lotte`에 하드코딩 경고 docstring**(로직 변경 X).
- `lotte_ignore_keywords.txt`: "선물" 추가.
- `buy/_load_lotte_custom.py` (신규): 번호조합(COMBOS) 아닌 임의 조합(`"e2+h1"`) cart-only 로더. sulwhasoo 함수 재사용.

## ⚠️ 하드코딩 금지 (사용자 강조 — 오늘 사고의 근본원인)
- 오늘 막힌 근본원인 = `select_card_lotte` **롯데카드 하드코딩**. 당일카드는 매일 바뀌므로 **반드시 배너에서 자동감지**. 확실하지 않으면 하드코딩 금지.

## 다음 TODO
1. **#8 적립금** 미신청(idempotent·신청기간 내 가능, 단 한글검색=최근검색어 한계).
2. **롯데 당일카드 자동감지 + 카드선택 + PAYCO 디스패처 코드화** — 삼성만 검증, KB/기타 미검증. 하드코딩 없이 배너 감지로. (검증 충분해질 때까지 보류)
3. G 한글검색 해결(ADBKeyboard/클립보드) — 현재 최근검색어 의존(재실행 시 검색기록 바뀌면 SKIP).
4. ARS 통화 자동화 가능성 검토(또는 사용자 응답 전제 워크플로).

---

# 월초 리셋(6월) + Step 1 (2026-06-02 — rate-check 파이프라인)

6/1까지 폰결제 작업으로 못 돌린 월초 리셋을 6/2를 사실상 월초로 보고 실행. 재고관리 시트(`1aDJ35…`) [초기화가이드] 6단계 전부 + Step 1 끝까지 완주. **모든 변경은 Google Sheets(소스코드 변경 없음).**

## 한 일 (월초 리셋 6단계, 전부 검증)
- **백업**: 로컬 `rate-check/_backup_inventory_2026-06-02.json`(수식+값 전탭, 6MB) + 개인계정 Drive 복사본. (⚠️서비스계정 `gc.copy`는 Drive 쿼터0으로 실패 → 로컬덤프+MCP copy_file로 우회)
- **절차1** 재고현황 D6:D52/D55:D91(현재재고) → F열(최초재고). 5월말 재고가 6월 최초재고로 확정. **D=F 20/20 일치**.
- **절차2** 재고현황 5월 31열 삭제 → 6/1~8/16(77일), G4 "6월 출고량". ⚠️새 날짜헤더는 **DATE serial(46220~46250)+숫자포맷**(텍스트로 쓰면 MATCH 깨짐 — 1차로 텍스트 썼다 교정, PASTE_FORMAT로 포맷 입힘).
- **절차3** 출고 A2:D 비움(E~H 수식/헤더 보존). 절차1+2+3 완료 후 D=F_new 정상복구 확인.
- **절차4** galleria 6월 구성이 활성 `n`과 **완전 동일** → n행 무접미사 relabel(312행, 1~20), 옛 버전 전삭제. (C상품명은 SUMIFS 무관)
- **절차5** MAP A2:A21=1~20, 이후 삭제(B열 SUMIFS 수식 보존). MAP 조합1 = d:2,g:2,s01:2,s06:6… 6월 구성과 일치 확인.
- **절차6** IN 라벨 1~20 + 날짜열 6/1~8/16(serial 연속, 포맷 정상). IN 입고데이터 전부 5월(6월+ 0)이라 5월삭제로 손실없음(이미 최초재고 반영).
- **검증**: `inventory.py` dry-run → 활성버전 `''`(무접미사 월초) 자동감지 + "전체 일치, 변경 없음".

## Step 1 (공급률 시트 '6.2' 탭)
- galleria 20조합 GWP 6세트=87,600, 공급률 0.52~0.68.
- hmall 카카오페이 롯데 7%, 0.507~0.554 (A53:M78).
- lotte 삼성 7%+플러스쿠폰 10/14%+적립 30,000, 0.457~0.515 (A81:M107).
- **cart_plan: 채널=롯데 N=7** (조합 2/4/5/9/10/15/20), 평균 0.475, 총 2,440,212원 (O1:U14).

## 현재 상태 (verified)
- ✅ 월초 리셋 6단계 + Step 1 전부 완료·검증. 재고 b~h가 리셋후 현재재고와 일치 → cart_plan까지 정합.
- 메모리: `project_monthstart_reset_execution`(실행 gotcha — serial+포맷, relabel 안전법).

## 다음 TODO
1. **Step 2** (Hmall 10% 적립 체크 `step2.sh`) — 사용자 호출 대기.
2. 그 후 Step 3(자연어 plan) → Step 4(cart) → Step 5(결제).
3. (참고) 롯데 적립 tier에서 b만 빈리스트 — b는 20조합 미사용이라 무관.

---

# 오후 작업 (2026-06-02) — 샘플단가 시트 동적화 + Hmall 수식 스크리너 + 23조합 제안

## ★ 샘플단가 하드코딩 → 재고현황 시트 동적로드 (근본수정)
- 발견: `_common.py:SAMPLE_TABLE` 하드코딩 단가가 **재고현황 시트(E열)와 18건 불일치** (예: s09 자음생캡슐세럼5ml 코드 3,000 vs 시트 2,000 / s25 여윤팩 2,000 vs 2,200). 시트가 SoT인데 코드가 drift.
- 수정: `_common.py`에 `sample_prices_from_sheet()`(재고현황 A55:E91 s코드→단가, 1회 캐시) + `lookup_sample`이 **시트 단가 우선**(코드 폴백). 이름→s코드 매칭만 SAMPLE_TABLE 사용. → 앞으로 시트만 고치면 자동 반영.
- 영향: GWP 6세트 87,600→**88,200**, 추증 상향 → 3사 공급률 소폭 하락.

## ★ Step 1 전체 재계산 (새 단가) — 새 '6.2' 탭
- 사용자가 원본 '6.2' 삭제 + 'Copy of 6.2'(백업) 생성 → 전체 재스크랩으로 새 '6.2' 재생성.
- ⚠️ 1차 시도 Hmall에서 patchright session-closed 크래시(반복 CDP attach 여파) → HmallChrome pkill+재기동 후 재실행 완주.
- 결과: galleria(GWP 88,200)·Hmall(20조합 클린, **카카오페이 롯데 2% 페이백 자동**=오전 stem수정 덕)·롯데·cart_plan(lotte N=7, 2,400,857, 평균 0.4672). 'Copy of 6.2'=옛단가 백업 보존.

## ★ Hmall 수식 스크리너 (`_hmall_formula.py`)
- 20조합 실측 preview로 상품별 유효단가 최소제곱 역산 → 113조합(2·3종류)+f5/h3 수식 랭킹. 계정 **#1 tkdkky2002** 기준(쿠폰 계정별 상이).
- 모델 확정(사용자): preview=Σunit·q(카드% 반영) → 결제=preview−**주문할인 20,000**(오늘 월초 1주문/계정) → 실비=×(1−페이백). **쿠폰=정가합산(additive)**: b×4=690,023=916,000×0.81×0.93(=10%+9%) 검증, 순차곱(0.819) 아님.
- 한계: ±0.5% 비선형(고수량 f) → 스크리닝용. b는 20조합 미등장 → `--b4 690023` 주입. 시트 '6.2' A108:G225 기록.
- `--pick`(순위→COMBOS) / `--write`(시트기록) 옵션 추가.

## ★ 23조합 제안 (`_gen_combos_profit.py` + 수동선택) — 채택 보류
- 사용자가 '6.2' Hmall공급률 스크리너에서 22개 수동선택(순위 1,2,6,8,9,10,13,19,23,27,31,34,35,36,39,40,41,46,48,50,58,60) + h×3 = **23조합** 확정.
- ⚠️ **채택 보류(b)**: `_common.py` COMBOS 미교체. 채택 시 20→23 레이아웃 마이그레이션 필요(Hmall 53~81/롯데 84~113/차트 J1:M24/스크리너 115행).
- `_gen_combos_profit.py` = 수익×회전(Σ PROFIT×VELOCITY×q) 생성기(별도, 미채택).

## 커밋 범위 (focused)
- 포함: `_common.py`(동적단가), `_hmall_formula.py`(--pick), `_gen_combos_profit.py`(신규), 본 워크로그.
- ⚠️ 제외(미커밋 유지): `cart/today.json`(일일상태), `phone_auto/lotte_homeshopping_buy.py`(폰 in-flight), `buy/_debug_*`·`fix_carts.py`·`_cart_state.py`·`_ensure_login.py`·`*.bak.json`(스크래치), `rate-check/_backup_inventory_2026-06-02.json`(6MB 백업).

## 다음 TODO (오후 기준)
1. 23조합 채택 여부 결정 → 채택 시 레이아웃 마이그레이션 + Step 1 재실행.
2. b 단가 본측정(다음 Hmall 측정에 b 든 조합 1개 포함 → b4 주입 불필요).
3. 운영 `hmall.py`에 주문할인(2만원) 자동감지 추가(현재 스크리너만 반영).

---

# 롯데홈쇼핑 폰결제 코드화 (삼성/PAYCO/ARS) + 로그인 수정 + ★삼성 결제방식 변경 (2026-06-02 오후)

폰: SM-G9960 무선 adb. ⚠️실돈. 모듈 = `phone_auto/lotte_homeshopping_buy.py`.

## 계정 10 실결제 완료
- **#10 김진화(kim455307)** 주문 `2026-06-02-F71650`. 조합4(f2+g2). 삼성카드 **542,056원**(삼성 7% 청구할인 후 ≈504,110). 삼성/PAYCO 전 과정 라이브 관찰 → **ARS 본인인증 발생**(전화요청→사용자 통화 인증). **뷰티포인트 ✅ + 구매사은 적립금(최대 20%) ✅**.

## 코드화 (당일카드 하드코딩 제거 + 삼성 경로)
- **`detect_card_lotte()`** 신규: 주문서 '청구할인' 배너 `(카드)카드(신용카드/L.PAY) N% 할인` 정규식 → **최고% 자동선택**(하드코딩 제거). 양방향 스크롤 스캔. #10 검증(삼성7/국민5→삼성).
- **`select_card_lotte(day=None)`** 리팩터: 롯데 하드코딩 제거 → 감지 카드 퀵버튼/드롭다운 선택.
- **`pay_lotte_payco(card)`** 신규: 삼성 SDK모달 간편결제→PAYCO박스→결제확인→`payco_pin6` 137601→ARS→주문완료. (★아래 결제방식 변경으로 대체 예정.)
- **`handle_ars_call()`** 신규: ARS 자동응답(받기→대기→키패드→'1' 반복). ⚠️통화화면 앵커 미검증.
- **`dismiss_card_register()`** 신규: F 카드등록 화면 dismiss (삼성=no-op).
- **`claim_lotte_reward(goods_no)`** 리팩터: ★**상품번호(숫자) 검색으로 adb 한글입력 한계 우회**(검색창에 goods_no 입력→상품페이지 자동 오픈). #10 검증. 없으면 최근검색어 폴백.
- **`set_cash_receipt()`** 수정: 키패드 등장 보장 + "입력해주세요" 팝업 처리(#10 사업자번호 미입력 버그 수정).
- **로그인 수정**(사용자 지적): 폼 등장/마이→"계속하기" **슬라이드 정착 대기 1초** + 매 시도 fresh OCR + **PW칸 클리어 가드**(`_clear_field`). 기존엔 정착 전 탭→ID가 PW칸에 입력되는 버그.
- `adb_input.py`: `DEBUG_TAP` env 탭 좌표 로깅.

## ★★ 삼성카드 결제방식 변경 — PAYCO/ARS 완전 회피 (사용자 지정, **미구현 TODO**)
PAYCO는 금액 크면 ARS 인증을 무조건 요구 → 전화 타이밍 의존이라 자동화 신뢰성 깨짐. 현대·롯데 공통. 대신 **일반결제(카드번호 직접)**로 ARS 없이 결제:
1. 삼성카드 결제 팝업 → ❌"간편결제"(PAYCO) → **"다른결제"(왼쪽하단) → "일반결제(카드번호)"**
2. **"카드번호 - 없이 입력"** → 셔플번호판(screencap 가능) → `377989766245897` (★숫자 사이 1초 간격)
3. **"CVC번호 뒤 3자리"** → `536` (1초, 셔플) → **"다음"**
4. **"숫자 6자리 입력"** → 결제비번 `137601` (1초, 셔플) → **"다음"**
5. **"금융인증서" 아래 "모니모 앱"** (⚠️"공동인증서" 아래 모니모 아님 — 둘 있음) → **"김건엽님의 금융인증서"** → `137601`(셔플) → 구매 완료
- 고정값: 카드 `377989766245897` / CVC `536` / 결제비번 `137601` / 금융인증서 `137601` (김건엽 명의 1장 전계정 공용). 셔플 4종 = payco_pin6식 4엔진 OCR, 1초 페이싱.
- **TODO: 다른 아이디로 실험→작동확인→코드화+하드코딩. 현대몰 `hmall pay_samsung`도 동일 수정**(롯데가 hmall 삼성 흐름 재사용하므로 hmall쪽을 정본으로). 메모리 `feedback_samsung_general_payment`.

## 미완/주의
- 계정 11(diinamic): 쿠폰 모달 "적용된 할인혜택이 초기화됩니다" 팝업 미처리로 멈춤(디버깅 보류).
- 무선 adb 간헐 끊김(화면 꺼지면 + 끊기면 connect 포트 변경) → `svc power stayon true` + screen_off_timeout↑로 완화.

## 커밋 범위 (focused — 폰 작업만)
- 포함: `phone_auto/lotte_homeshopping_buy.py`, `phone_auto/adb_input.py`, 본 워크로그.
- 제외: rate-check `_common.py`/`_gen_combos_profit.py`(다른 터미널), `cart/today.json`(일일상태).

---

# 저녁 (2026-06-02) — #11 삼성 일반결제 첫 라이브 풀완주 + 쿠폰/카드선택 버그 수정 (★다른 컴퓨터 인계용 상세)

폰: SM-G9960 무선 adb. ⚠️실돈. 계정 #11 diinamic. **주문 `2026-0602-F88316` 결제 완료(534,584원) + 구매사은 적립금 30,000원 신청 ✅.** 뷰티포인트=이 계정에 뷰티포인트 회원 없어 적립 불가(정상 skip).

## ★ 커밋됨 (commit 0671570, main) — git pull로 다른 컴퓨터 동기화됨
1. **플러스/할인쿠폰 비활성 필터** (`_coupon_enabled` + `set_plus_coupons`/`set_discount_coupons`)
2. **`select_card_lotte` 신규 그리드 루트** (아래 상세)
3. **`buy_one` 카드→현금영수증 재정렬**
4. **`set_cash_receipt` 사업자 라벨 스크롤 fix**

## ★ 발견1: 플러스쿠폰 "적용된 할인혜택이 초기화됩니다" 팝업 — 진짜 원인 (추적기 실측)
- **하지 말 것**: 같은 14% 쿠폰이 **활성1+비활성1(회색)** 있을 때 OCR 텍스트가 동일 → 옛 코드가 위쪽 비활성을 탭 → 적용 안 됨 → 그 제품 미적용 → **`선택완료` 누를 때 팝업**.
- ⚠️ **트리거는 `선택완료`** (쿠폰 옵션 탭 아님). "미적용 제품 있는 채 선택완료" 가 조건. (회색 버튼 탭 자체는 팝업 안 뜸 — 추적기 TAP#로 확정.)
- **수정**: 옵션 텍스트밴드 **픽셀밝기**로 활성/비활성 판별(활성≈20 / 회색≈154, 임계 120). 비활성 제외 후 활성 중 최고%(같은%면 최상단). `_coupon_enabled(gimg, cy)`.
- 검증: #11 플러스 3제품 전부 적용 `[14,14,10]`, 팝업 0회.

## ★ 발견2: 롯데 결제수단 UI = 그리드로 변경 → 카드 선택 신규 루트 (모든 카드 공통, 카드명만 다름)
- **루트**: 주문서 카드영역 → **"다른 결제수단"**(라디오) 탭 → 결제수단 그리드 펼침 → **"신용카드"**(그리드 버튼) 탭 → **"카드 선택" 드롭다운**(행 우측 chevron x≈987) 탭 → 카드목록 팝업 → **당일카드** 탭.
- 카드목록(세로): 롯데카드/신한카드/비씨카드/현대카드/삼성카드/KB국민카드/우리카드/하나카드/NH농협카드. `CARD_GRID_NAME`으로 매핑(삼성→삼성카드, KB→KB국민카드…).
- **당일카드 자동감지**: `detect_card_lotte()` = 주문서 '청구할인 예상가' 배너 `<카드>카드(신용카드/L.PAY) N% 할인` 최고%. 6/2=삼성7%/국민5%.
- ⚠️ **버그 fix(핵심)**: "카드 선택"을 **exact 매칭** 필수 — contains면 안내문 "…비씨**카드 선택** 시 결제…"의 부분문자열을 오매칭해 chevron이 엉뚱한 데(안내문) 탭됨 → 팝업 안 열림. + '다른 결제수단' 못 찾을 때 **양방향 스크롤**.
- ⚠️ **카드 선택이 현금영수증(지출증빙)을 리셋** → `buy_one`에서 cash는 반드시 card 뒤. set_cash_receipt는 지출증빙 탭 후 사업자 라벨이 화면 밖이면 스크롤로 노출.

## ★★ 발견3: 새 삼성카드 결제방식 = "일반결제(카드번호 직접)" — PAYCO/ARS 완전 회피 (라이브 풀완주, ❗미코드화 TODO)
오늘 **수동 단계관찰로 end-to-end 성공**. 전부 OCR 앵커 기반(좌표 하드코딩 X, 키패드는 위치 가변):
1. 주문서 하단 **"NNN원 결제하기"**(cy>2000) → **삼성카드 SDK 모달** 등장(Samsung Card 헤더).
2. SDK 모달 옵션: 삼성카드앱 / monimo pay / 오픈앱카드 / **다른결제(일반/SMS결제)** / **간편결제(PAYCO,삼성페이)**. → ❌간편결제(PAYCO=ARS 위험) ⭕**"다른결제"** 탭(왼쪽 박스).
3. **"다른 결제"** 화면 → **"일반 결제"(카드번호로 결제)** 탭 (❌SMS 결제).
4. **"일반 결제"** 화면: 상단 탭 **인증서**(기본 선택=ARS 아님) / ARS인증. 가맹점 LOTTE Homeshopping + 금액. 필드 2개:
   - **"카드번호 - 없이 입력"** 탭 → 셔플키패드 → **카드번호 15자리** (`card_no`, 15자리 다 누르면 자동닫힘). 마스킹 `3779-89****-**897` 확인.
   - **"CVC번호 뒤 3자리"** 탭 → 셔플키패드 → **CVC 3자리** (`cvc`).
   - **"다음"**(카드+CVC 입력되면 파랑 활성).
5. **"일반결제 비밀번호"** 화면(카드 박스 아래 "롯데"=롯데홈쇼핑 가맹점 표시, 정상): **"숫자 6자리 입력"** 탭 → 셔플키패드 → **결제비번 6자리** (`pin6`) → **"다음"**.
6. **금융인증서/공동인증서 선택** 화면: 각각 [삼성카드 앱 | 모니모 앱]. → **"금융인증서" 헤더 아래 "모니모 앱"** 탭. ⚠️**공동인증서의 모니모 아님**(둘 있음). "모니모 앱 설치하기"(상단)도 아님.
7. **모니모 앱** 진입 → "김건엽님의 금융인증서비스" → **인증서 카드(김건엽 금융인증서, 국민은행) 탭** → "비밀번호를 입력해주세요" + 셔플키패드(4x3) → **인증서 비번 6자리** (`cert_pw6`, =137601).
8. **"금융인증서 인증에 성공하였습니다." → "OK"** 탭 → 롯데앱 복귀 → **주문완료(주문번호)** 폴링.
- 고정값 → **`secrets/card_secrets.json['삼성']`**: `card_no`/`cvc`/`pin6`/`cert_pw6` (김건엽 명의 1장 전계정 공용). 값 자체는 6/2 오후 WORKLOG 섹션 + 메모리 `feedback_samsung_general_payment` 참조.

## ★ 셔플 키패드 처리법 (코드화 시 핵심)
- 키패드는 **로드시 1회만 셔플**(재셔플 X, "재배열"=수동버튼). **위치 가변**(카드 cy~1145 / CVC~813 / 결제비번~640 / 인증서비번~1573-2091). 카드/CVC/결제비번=2행×5(1-5/6-0+gap), 인증서비번=4행×3.
- **1차 = 단일엔진 `_ocr_texts` 단일문자 숫자**(멀티문자 금액 자동제외) → 0~9 다 잡히면 OK (카드/CVC/결제비번 검증됨).
- **2차 = 4엔진 `ocr_keypad.vote_digits(shot, flip_h=False)`** — ⚠️**인증서 비번 키패드는 단일엔진이 3/7/8 누락** → 반드시 4엔진 폴백.
- 매 키 사이 **1초**(카드/CVC) / 0.7~0.8초(비번) 페이싱.

## ❗ 미완 TODO (다른 컴퓨터에서 "삼성 일반결제 코드화 이어서" 하면 여기부터)
**`phone_auto/lotte_homeshopping_buy.py`에 작성:**
1. `_card_secrets()` (secrets/card_secrets.json 로드) + `_tap_shuffle(value, delay)` (위 1차→2차 폴백) 헬퍼.
2. **`pay_lotte_samsung_general()`** — 위 발견3의 1~8단계 + 주문완료 폴링. `pay_lotte_payco` 뒤에 삽입.
3. `buy_one` 삼성 분기: `pay_lotte_payco(use_card)` → `pay_lotte_samsung_general()` 교체 (PAYCO 경로 폐기).
4. ⚠️ **작성만 했고 라이브 미검증** — 다음 삼성 구매 때 end-to-end 재검증 필수(이 주문은 이미 결제됨).

## ⚠️⚠️ 다른 컴퓨터 인계 주의 (git pull로 안 따라오는 것)
- **`secrets/card_secrets.json` = gitignored → git에 안 올라감.** 다른 컴퓨터엔 삼성 일반결제 값(card_no/cvc/cert_pw6)이 **없음** → 그 파일 **수동 복사** 필요(또는 메모리값으로 재작성).
- **자동메모리(`~/.claude/...`) 도 동기화 안 됨** (이 repo 밖). 위 발견들은 본 워크로그에 적었으니 워크로그가 SoT.
- 이 대화 자체도 다른 컴퓨터엔 없음. **이 워크로그 섹션 + 커밋된 코드 주석**이 인계 자료 전부.
- 폰: SM-G9960 무선 adb 재연결 필요(페어링/connect). `_resolve_serial()`이 연결기기 자동선택.

---

# 2026-06-02→03 야간 — 삼성 일반결제 **라이브 검증 완료**(#12) + 동의/키패드 버그 2건 수정

폰: SM-G9960 무선 adb. ⚠️실돈. 모듈 `phone_auto/lotte_homeshopping_buy.py`.

## ★ #12 leenamsu0318 — 삼성 일반결제 첫 라이브 풀완주 ✅
- 주문 `2026-06-02-G05038` (642,638원, 삼성 7% 청구할인). **뷰티포인트 ✅(6,757P) + 구매사은 적립금(최대20%) ✅.**
- `pay_lotte_samsung_general()` 1~8단계 전부 라이브 검증: SDK모달→**다른결제**(❌간편결제/PAYCO)→**일반결제**(❌SMS)→카드번호15+CVC3→다음→비번6→**금융인증서 아래 모니모**(❌공동인증서/❌설치하기)→김건엽 금융인증서→인증서비번6→OK→주문완료. **PAYCO/ARS 완전 회피 확인.**
- 모든 화면 앵커가 코드와 일치(다른결제·일반결제·카드필드'없이입력'·CVC·숫자6자리·금융인증서최근접모니모·김건엽카드). 카드앵커 step2 로직(`금융인증서 헤더 아래 최근접 모니모`)이 공동인증서 모니모 정확 회피.

## ★ 버그수정1: 필수동의 체크박스 — 좌표 빗나감 → **픽셀 검증+재시도** (`agree_required`/`_agree_box` 신규)
- **진짜 원인**: 옛 `텍스트cx-210` 탭이 박스(행 좌측 빈사각형 x≈67)를 빗나가 **미체크** → '주문 내용 확인 후 동의하셔야 구매 가능' 팝업으로 결제 막힘. ← 이게 'samsung_modal 미도달'로 보였던 것의 실제 원인(모달이 팝업에 가려짐).
- WebView라 **uiautomator 미노출**(노드15/텍스트0) → element-handle 불가. **픽셀 방식**: 박스 좌우 테두리(어두운 수직선) 검출 → 중심x, 내부 어두운픽셀로 체크여부 판정(실측 미체크 dark=0 / 체크 dark=92). 체크 확인될 때까지 최대5회 탭(이미 체크면 탭 안 함=토글오프 방지).
- `buy_one`: `agree_required()` 실패 시 결제 전 **abort(AGREE_FAIL)** — 미체크로 결제 시도해 막히는 낭비 방지.

## ★ 버그수정2: 셔플 키패드 — stray 오염 → **2엔진→4엔진 + ROI 제한** (`_tap_shuffle`)
- 단일엔진: 인증서 키패드서 일부 숫자 누락(가변, #12는 1·2 / #13은 2·4·5·7). 4엔진(roi無): **상태바'100'→0@(975,46)·라벨→8@(453)·카드마스킹 숫자**를 stray 오매핑 → cert_pw6에 '0' 있으면 상태바를 탭해 비번 틀림.
- **수정(사용자 합의=2엔진 먼저, 안되면 4엔진)**: `vote_digits`로 **2엔진(vision+gcv) voting → 실패 시 4엔진**, 둘 다 **`roi_y_frac=(0.45,0.98)` 키패드 하단 제한**으로 stray 배제(카드마스킹 0.42·금액 0.32·상태바 상단 전부 배제, 키패드 0.48~0.87만). value 숫자만 issubset 되면 채택. voting이라 단일엔진 오독(8↔0) 교차검증됨. ★4엔진부터 하면 gcv 네트워크로 느림 → 2엔진 우선.
- #12 카드/CVC/pin은 2엔진, cert도 2엔진 roi로 0~9 정매핑 확인(별도 실측).

## ⚠️ #13 Lee0128 — 미완 (폰 보안잠금)
- 로그인→카트→쿠폰→카드(삼성 자동감지)→지출증빙→**동의 체크 확인됨(fix 작동)**→삼성모달→다른결제→일반결제→카드/CVC→비번→인증서까지 **자동 통과**. cert 키패드 1차 매핑 실패 → 2엔진 roi로 코드 수정.
- 재주행 중 **모니모 cert 세션 타임아웃**(내 편집 지연) → 롯데 복귀. 재주행 시 **모니모 포그라운드 잔류 → LOGOUT_FAIL** → 모니모 force-stop. 직후 **사용자가 화면 off → 보안잠금(Bouncer, screencap 차단)**으로 adb 진행 불가. **#13 미결제. 폰 수동 잠금해제 후 재개 필요**(svc power stayon true + screen_off_timeout 30분 설정해 재발 완화).
- ⚠️ **6/3 당일 롯데카드 = 국민(KB)** (삼성 아님). 롯데 **KB 결제경로 미검증**(`buy_one`은 롯데=LOCA/삼성=일반결제만 분기, 그 외 UNVERIFIED_CARD). #13/#14를 6/3에 KB로 진행하려면 KB 롯데경로 신규 관찰·코드화 필요.

## ★ 현대몰(hmall) 삼성결제 = 아직 **PAYCO 구방식** (포팅 TODO)
- `phone_auto/hmall_hyundai_buy.py:pay_samsung()` (L691) = **PAYCO 경유**(원결제→간편결제 PAYCO→PIN137601→'확인'). 오늘 롯데에 적용한 **일반결제(카드번호 직접)와 다름**.
- 사용자 요청: 현대몰도 삼성 일반결제 방식으로 통일 필요 → `pay_lotte_samsung_general` 로직 재사용해 `pay_samsung` 포팅(미착수, 라이브 미검증 예정).

## ★ 국민(KB) 페이백 계수 0.99 — 코드 정합 확인
- `rate-check/_common.py:CARD_PAYBACK["KB국민카드"]=0.01`(페이백 **비율** 1%) → `hmall.py:185` `구매가격=preview×(1-payback)=×(1-0.01)=`**×0.99** 계수. **코드상 KB=0.99 계수 정합**(_common은 비율 0.01로 저장, 가이드 md는 ×0.99 표기 — 같은 값). rate-check 코드는 6/3 미변경(git M 없음) → 0.99 수정했다면 Google Sheet쪽.
