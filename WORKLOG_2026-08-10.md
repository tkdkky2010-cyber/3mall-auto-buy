# WORKLOG 2026-08-10 — step1/step2 **둘 다 차단** / 원인 = 출구 IP 가 터키(이스탄불)

> ## ⛔ 2026-08-18 무효화 — **해외 IP 조건 소멸. 이 문서의 IP/VPN 대목은 이제 안 맞는다.**
>
> 작업 환경이 **윈도우 PC(한국 KT, 출구 IP `KR/Seoul`)** 로 옮겨졌다. 실측 확인함.
> 아래 §6·§11·§14 의 터키 출구 IP · VPN 확장 · **해외 IP SMS 본인확인**은 **이 환경에 해당 없다.**
> - `login()` 은 ID/PW 자동 로그인으로 그대로 통과한다 → **SMS 핸드세이크 불필요.**
> - VPN 확장(Planet VPN / VeePN) **설치할 필요 없다.**
> - §8·§15 의 "VPN 해제 후 재실행" · "SMS 핸드세이크 후 진행" TODO 는 **폐기**(조건 소멸).
> - ⚠️ 단 **§16 의 교훈 3·4 는 유효**하다 — "확장 프록시는 `page.request.get()` 을 우회한다",
>   "같은 로그인 실패라도 원인이 다르다(느림 vs 차단) → 화면 본문을 읽어라". 진단 원칙이라 IP 와 무관.
> - 상세: `WORKLOG_2026-08-18.md`

## 1. 사용자 지시

1. "여기서 작업할때 워크트리에 체크표시하고안하고 뭐가차이나는거야?" → 설명만(코드 변경 없음).
   이 레포는 실행 필수 파일 상당수가 gitignore(`hmall_config.json`·`secrets/`·`profiles/`·`buy/.env` 등)라
   **워크트리에선 로그인부터 안 된다** → 항상 OFF 권장. 현재도 OFF(`git worktree list` 1건, `.git`).
2. "현재 워크트리 켜져있음?" → 꺼져 있음 확인.
3. "폴더전체읽고 스텝1,2 실행해 주의사항잘읽어"

## 2. 실행 전 — `git pull` 로 커밋 3개 이어받음 (중요)

로컬 `fc3e0ba`, origin `6e1ef02` — **3커밋 뒤처져 있었다.**
안 당겼으면 **8/7 에 고친 색칠·미러·조건부서식 코드를 stale 로 돌릴 뻔했다.**

`970aa03`(조합번호 색칠 원복) · `d9035e5`(미러 색 지우기 + 조건부서식 중복) · `6e1ef02`(워크로그 §17).
`READ_FIRST.md` 가 +50 줄 늘어 **처음 읽은 내용이 이미 구버전**이었다 — 다시 읽었다.

> 교훈: 세션 시작 시 폴더를 읽더라도 **`git fetch` 로 원격 상태를 먼저 본다.**
> 다른 PC 세션이 흔한 프로젝트라 "읽었다"와 "최신을 읽었다"가 다르다.

## 3. CDP 9222 — 탭 0개 (자동메모리 `hmall-9222-zero-page-cdp` 그대로 재현)

`/json/version` 은 정상인데 `/json/list` 가 `[]`. `PUT /json/new` 로 탭 생성 후 3개 확보.
(전 세션 잔여 탭: 롯데 주문서 · hmall 주문 페이지)

## 4. step1 실패 — 갤러리아 1/5 에서 `ERR_CONNECTION_RESET`

```
[b] → https://www.galleria.co.kr/goods/initDetailGoods.action?...goods_no=2502913432
selenium.common.exceptions.WebDriverException: net::ERR_CONNECTION_RESET
❌ 갤러리아 실패 (rc=1)
```

**step1 은 부분 실행이 불가능하다** — `hmall.py:377` 이 `'8.8' 탭 없음 → 먼저 galleria.py 실행 필요`
로 죽고, `lotte.py:275` 도 `load_galleria_composition_from_sheet` 로 추증/GWP 를 읽는다.
갤러리아가 막히면 3사 전부 못 돈다.

## 5. step2 실패 — Hmall 로그인 `[FATAL]`, 그런데 **`tee` 가 rc 를 삼켰다**

```
[INFO] 포트 9222 — Hmall 로그인 안 됨, 다음 후보
  [LOGIN ERR] tkdkky2002: Locator.fill: Timeout 30000ms exceeded.
    waiting for get_by_role("textbox", name="Hmall / H.Point 아이디")
[FATAL] 로그인 실패
```

- **셀렉터 문제가 아니었다.** 그 탭의 실제 DOM 을 읽으니 `input` 0개 / `TITLE: ERROR: The request
  could not be satisfied` / 본문 **`403 ERROR — Request blocked … CloudFront`**.
  로그인 폼이 아예 안 내려온 것이다.
- `cart/show.py` 표는 **8/7 자 stale**(`날짜: 2026-08-07`)이었다. 오늘 값이 아니다.
- 미러도 `원본 탭 '8.08 식품' 읽기 실패` — 만들어진 적이 없으니 정상 반응.

### ★ `bash stepN.sh | tee` 는 rc 를 못 본다

`step2.sh` 는 마지막에 `exit $CHECK_RC` 로 실패를 돌려주는데, `bash step2.sh 2>&1 | tee -a log`
로 돌리면 **파이프라인 rc = `tee` 의 rc = 0**. 실행 알림은 "exit code 0" 으로 왔고 실제론 실패였다.
→ **rc 를 믿지 말고 출력의 `[FATAL]`·`❌` 를 직접 본다**(또는 `set -o pipefail` / `PIPESTATUS`).
step1 도 같은 이유로 exit 0 으로 보고됐다.

## 6. ★★ 진짜 원인 — 이 맥의 출구 IP 가 **터키(이스탄불)** 다

두 실패의 공통 원인. 브라우저(샌드박스 밖, 실제 자동화가 쓰는 그 Chrome)에게 직접 물었다:

```
[Chrome] https://ipinfo.io/json → {"ip":"88.239.128.223",
  "hostname":"88.239.128.223.dynamic.ttnet.com.tr", "city":"Istanbul",
  "country":"TR", "org":"AS9121 Turk Telekomunikasyon"}
```

Bash(curl)·Chrome 양쪽 동일. 시스템 프록시 설정은 없다(`scutil --proxy` 비어 있음) → **VPN/우회 경로가
머신 전체에 걸려 있다.**

### 도달성 실측 (Chrome `request.get` — 자동화와 같은 경로)

| 사이트 | 결과 |
|---|---|
| `www.hmall.com` | **403** (CloudFront `Request blocked`) — `/`·`/mo/main`·`/mo/cob/loginForm` 전부 |
| `www.galleria.co.kr` | **ECONNRESET** — `www`·`m`·`dept`·apex 전부. TCP 는 붙고 **TLS 에서 리셋** |
| `www.lotteimall.com` | 200 |
| `www.naver.com` | 200 |

- 갤러리아 HTTP/80 은 자기 앞단 squid 가 `502 / X-Squid-Error: ERR_READ_ERROR 104` 를 준다.
- DNS 는 정상 — 로컬·8.8.8.8·1.1.1.1 모두 `121.168.9.99` 동일(하이재킹 아님).
- **외부망 교차검증**: Anthropic 인프라(WebFetch)에서도 갤러리아 ECONNRESET / Hmall 403.
  즉 우리 회선만의 문제로 단정할 순 없으나, **어제(8/7) 같은 코드로 둘 다 정상 완주**했고
  바뀐 변수는 출구 IP 하나다.
- 롯데·네이버가 200 인 건 몰마다 차단 정책이 달라서다(전면 지오블록이 아니라 WAF/평판 기반).

**→ 조치는 사용자 몫**: VPN 끄거나 한국 출구로 바꾼 뒤 재실행. 네트워크/시스템 설정은 건드리지 않았다.

## 7. 피해 없음 (확인함)

- 공급률 시트에 **`8.8` 탭 미생성 / `8.08 식품` 탭 미생성** — 부분 기록·오염 0.
- `cart/today.json` 은 **8/7 자 그대로**(오늘 값으로 덮어써지지 않음).
- 롤백할 것 없음. 코드 변경 0.

## 8. 다음 할 일

- [x] ~~**VPN 해제 후 재실행**~~ — **폐기(2026-08-18)**: 윈도우 PC 는 한국 IP 라 VPN 자체가 없다.
- [ ] 재실행 전 CDP 탭 0개면 `PUT /json/new` 프리플라이트
- [ ] `stepN.sh | tee` rc 삼킴 — `set -o pipefail` 추가 검토(오늘은 코드 안 건드림)
- [ ] 8/7 이월: 갤러리아 `c` 재고 2 vs 플랜 21 / step2 `[바로구매 실패]` 3건(1·22·33) 원인 /
      페이백 판독 0건 / `1-1` 이너플로라30정 URL `2201017629` (4일 연속 ERR) /
      8/6 현대몰 설화수 `20260806072690` 대장 기록 여부

━━━━━━━━━━━━━━━━━━━━ 2차 세션 ━━━━━━━━━━━━━━━━━━━━

# 2차 세션 — VPN 확장으로 Hmall 우회 + step1 완주(26/27) / **핸드세이크 모드**

## 9. 사용자 지시 (원문)

4. "vpn 껐어 다시 실행해" / 5. "다시 vpn켰어 근데 지금 하나 누르면 로딩 시간이 엄청 오래걸려서
   천천히 진행해볼수있나? 오늘만? 핸드세이크넣어서?" / 6. "hmall 되는데??" /
7. "저 cft만 막힌거같은데 너가 우측 브라우저에서 진행해봐도돼" / 8. "오늘만 안되는거니까 코드전체수정하지말고" /
9. "아 그 브라우저에는 확장프로그램으로 VPN썼거든 그러니까 가능" / 10. "일단 그럼 갤러리아 롯데 진행해
   그리고 CFT에 구글익스텐션넣어줄게" / 11. "VPN추가완료 현대몰 가능" / 12. "로그인했어 다시 실행해"

## 10. ★★ 시스템 시계가 2일 틀려 있었다 — 오늘은 8/8 이 아니라 **8/10**

세션 시작 `date` = `2026-08-08 15:07 KST`. 그런데 갤러리아가 탭 `'8.10'` 을 만들길래 확인하니
`2026-08-10 11:11 +03`. **NTP 대조 결과 지금 시계가 정확**(`time.apple.com` 오차 0.09s) →
**아까 시계가 2일 뒤처져 있었고** VPN 토글 중 재동기화되며 교정됐다. 타임존도 KST → **+03(이스탄불)**.

- 그래서 1차 세션에서 만든 워크로그 파일명이 틀렸다 → `WORKLOG_2026-08-08.md` → **`WORKLOG_2026-08-10.md` 로 `git mv`**.
- ⚠️ **TZ 가 +03 이면 이스탄불 18:00 = KST 자정.** 탭 이름(`today_tab_name()`)이 KST 기준과 어긋나기
  시작하므로 하루 작업은 이스탄불 18:00 전에 끝내야 한다.
- 1차 세션에서 `dbadd1b` 커밋 시각이 `2026-08-08 15:37 +0900` 으로 박힌 것도 이 때문(잘못된 시계).

> 교훈: **`date` 출력도 관측이다.** 스크립트가 만든 탭 이름과 어긋나면 시계부터 의심한다.

## 11. ★★★ 1차 세션 진단은 절반만 맞았다 — 진짜 원인은 **해외 IP 본인확인**

1차 세션 결론은 "출구 IP 가 터키라 갤러리아 RESET / Hmall 403". 절반은 맞았다.

| 관측 | 1차 결론 | 실제 |
|---|---|---|
| 갤러리아 RESET | 터키 IP 차단 | ✅ 맞음 (VPN 확장 붙이니 해소) |
| Hmall 403 | 터키 IP 차단 | ✅ 맞음 (CloudFront WAF, `robots.txt` 조차 403 = 엣지단) |
| Hmall **로그인** 실패 | (2차 초반) "느린 회선, 고정 대기 부족" | ❌ **틀림** → §14 |

**사용자가 준 결정적 정보**: 메인 Chrome 만 되는 이유 = **확장 VPN**(`Planet VPN` /
`VeePN`, 둘 다 `proxy` 권한). CFT·인앱 브라우저·외부망은 확장이 없어 전부 터키 출구였다.
→ 사용자가 CFT 에 확장 설치 → **Hmall 403 즉시 해소**(`title=현대홈쇼핑 - 현대Hmall`).

⚠️ **주의**: Playwright `page.request.get()` 은 확장 프록시를 **우회**한다 —
확장을 켠 뒤에도 IP 조회는 계속 터키(46.234.15.10)로 나왔다. **페이지 탐색만 확장을 탄다.**
IP 조회 결과로 "확장이 안 먹었다" 고 오판하지 말 것.

## 12. 핸드세이크 모드 — step1.sh 대신 5개 스크립트를 하나씩

사용자 지시 5(천천히·핸드세이크·오늘만) + 8(코드 전체 수정 금지)에 따라
`step1.sh` 일괄 대신 **개별 실행 + 사이마다 사람이 확인**.

### 임시 러너 `scratchpad/slow_run.py` (레포 무수정, 커밋 안 함)

| 문제 | 우회 |
|---|---|
| selenium 클라이언트 타임아웃 120s 초과 (`lotte.py` 첫 `driver.get`) | `socket.setdefaulttimeout(600)` — selenium 4.36 `ClientConfig.timeout` 이 `socket.getdefaulttimeout()` 을 쓴다 |
| `buy/run.py add_to_cart` 의 하드코딩 `wait_for_timeout(1500)` 부족 | `Page.wait_for_timeout` 을 **×3 배율**로 몽키패치 (`SLOW_WAIT_MULT`) |

⚠️ `RemoteConnection.set_timeout()` 은 selenium 4.36 에서 **못 쓴다**(`_client_config` 가 None → AttributeError).

## 13. step1 결과 (탭 `8.10`)

| # | 단계 | 결과 |
|---|---|---|
| ① | 갤러리아 | ✅ 27조합 A1:K62. **REDIRECTED 0건**, 신규 0개, GWP 지문일치(재판독 불필요) |
| ② | 재고관리 | ✅ 조합 24~27 차이 → **새 버전 `d` 생성**(`1d 8/10`…`27d 8/10`) |
| ③ | Hmall | ⚠️ **26/27** — A65:M97 + L1:L28. 조합 26 만 ERR |
| ④ | 롯데 | ✅ 27조합 A100:M133 + M1:M28 + 조건부서식 |
| ⑤ | 카트플랜 | ✅ O1:V14, **channel=lotte N=7** |
| ⑥ | 미러 | ✅ `당일 설화수` 133행 — **원본 63칸 = 미러 63칸, 차집합 0 실측검증** |

### 당일 조건

| 몰 | 카드 | 쿠폰/적립 | 공급률 |
|---|---|---|---|
| 갤러리아 | — | 기본 10% + 쿠폰 16~18% | 0.4843~0.5336 |
| Hmall | **토스페이 삼성 7%** (페이백 1%) | — | 0.4835~0.5341 |
| 롯데 | **KB국민 5% 청구할인** (페이백 1%) | 쿠폰 12~13%, 적립 15만↑ 15,000원 | 0.4776~0.5263 |

추증(당일): b 20,700 / c 10,900 / d 15,500 / e 19,500 / f 16,500 / g 26,800 / h 28,400 / **n 15,500**.
**`d` 와 `n` 이 15,500 으로 같지만 오염 아님** — `n` 의 사은품 구성이 오늘 바뀌어 `d` 와 동일해졌고,
`inventory.py` 가 독립적으로 같은 변화를 조합 24~27 차이로 잡아냈다(교차검증).

### 카트플랜 (channel=lotte, N=7, 총 2,533,316원, 평균 0.5019)

조합 `3, 8, 12, 22, 24, 25, 27` (계정 1~7 순). 색칠 `[3,5,12,13,14,23,24,25,27]` — 8/7 고정세트 정상.
**재고 전부 충분**(c 2/1, e 36/12, f 106/4, g 22/3, h 23/1, n 99/6) — 8/7 의 `c` 2 vs 21 같은 문제 없음.

## 14. ★★★ Hmall 로그인 = **해외 IP 본인확인(SMS)** — 자동화 불가

조합 26 재시도 중 세션이 끊겨 재로그인이 돌았고, 로그인 페이지 본문에서 발견:

```
최근 로그인 | 휴대폰 인증 | 닫기 |
해외 IP로 로그인 시 고객님의 개인정보 보호를 위해 본인 확인이 필요합니다. | 인증번호 발송 | 확인
```

- `login()` 이 아이디·비번을 넣고 눌러도 **알럿 없이 로그인 페이지에 남는다** → `[LOGIN UNKNOWN]`.
  1차 진단(느린 회선 탓 고정 대기 부족)은 **틀렸다.** 대기를 아무리 늘려도 안 뚫린다.
- **SMS 인증번호는 코드가 넣을 수 없다** → 오늘은 **사용자가 CFT 창에서 직접 로그인**하는 핸드세이크로 처리.
- 한 번 로그인하면 `hmall.py`/`check10.py` 는 세션을 재사용해 로그인 단계를 건너뛴다.
  단 **세션이 중간에 끊기면 그 지점부터 다시 막힌다**(실제로 27조합 측정 중 끊겼다).
- 자동메모리 `hmall-session-drop-preflight` 의 연장선 — 해외 IP 조건이 붙으면 자가복구 재로그인이 무력.

### 담기 실패는 별개로 **타이밍이 맞았다** (구분해서 기록)

`[ERR] 장바구니 버튼 못 찾음` 27/27 → 실측으로 원인 확정:
- 셀렉터 `button.btn-cart` 는 **현재 DOM 과 일치**(visible, `btn-linered` 아님, 조건통과 1개).
- `구매하기` 클릭 후 `btn-cart` 등장까지 **≈3초**인데 코드 대기는 **1.5초** → `candidates.length===0`.
- ×3 배율 적용 후 **0/27 → 23/27**. 남은 4건 중 7·20·22 는 단일조합 재시도로 성공(단일행 targeted 갱신).

## 15. 남은 것

- [ ] **조합 26** (`f윤90 1 + g자생2 2 + n탄력EX75 1`) — 담기 실패 → 체크아웃 미도달 → 로그인 차단. 미측정
      ※ 2026-08-18: '로그인 차단'은 해외 IP 본인확인이 원인이었다 → **한국 IP 에선 재현 안 될 것.** 담기 타이밍(≈3초) 문제만 남는다.
- [ ] **step2 (check10)** — 미실행. ~~SMS 핸드세이크 후 진행~~ → **2026-08-18: 한국 IP 라 SMS 조건 없음. 그냥 실행하면 된다.**
- [ ] step2 후 `mirror_daily.py food` (미러는 best-effort 라 rc 로 판단 금지)
- [ ] 8/7 이월: `1-1` 이너플로라30정 URL `2201017629` (4일 연속 ERR) / 페이백 판독 0건 /
      8/6 현대몰 설화수 `20260806072690` 대장 기록 여부

## 16. 오늘 배운 것 (재발방지)

1. **`bash stepN.sh | tee` 는 rc 를 삼킨다** — 실패가 exit 0 으로 보고된다. `${PIPESTATUS[0]}` 를 쓰거나 출력의 `❌`·`[FATAL]` 을 직접 본다.
2. **`date` 도 관측이다** — 스크립트가 만든 탭 이름과 어긋나면 시계를 의심한다(오늘 2일 차이).
3. **`page.request.get()` 은 브라우저 확장 프록시를 우회한다** — 확장 VPN 효과를 IP 조회로 판정하지 말 것. 페이지 탐색으로 판정.
4. **같은 "로그인 실패"라도 원인이 다르다** — 느림(대기 부족) vs 차단(해외 IP 본인확인). 화면 본문을 읽어야 갈린다.
5. **검증 스크립트가 틀려서 "0" 이 나올 수 있다** — 미러 색 대조에서 시트 ID 를 안 써 `0칸` 오판. 정상값(63=63)과 대조해 자기검증할 것.
