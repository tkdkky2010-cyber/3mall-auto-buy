# WORKLOG 2026-08-04 — 롯데 8~14 결제 완주 + 현대 카트필 + cart_items 조용한누락 수정

## 1. 적립 구간 조합 계산 (사용자 요청, 코드 변경 없음)

전 구간 적립률 10% 정액 구간제 → **초과분은 적립 0** 이므로 "구간에 가장 가깝게" 가 곧 최적.

- 데이즈온 파라다이스 188,480 / 알파 45,524 → 100만 구간 최적 = **알파 22개 1,001,528원**(+1,528).
  박스비율 27:8(=구매수량 3:1) 강제 시 **6:2 = 1,221,928원**(실효 8.18%), 비율 풀면 5:2 = 1,033,448원(9.68%).
- 유산균 A 56,571(6박스) / B 27,626(3박스) / C 이너플로라 111,242 → 50만 구간 **A1+B8+C2 = 500,063원**(초과 63원).
  수량 균형 요구 시 **A2+B2+C3 = 502,120원**(수량차 1). 완전동일 3:3:3 은 586,317원으로 8.53% 까지 하락.
- 센텔리안 #36 104,625 / #37 61,018 / #38 133,920 → **#37×6+#38×1 = 500,028원(9.999%)** 이 최고.
  #36+#38 만: #38×3 = 401,760(9.956%). #36+#37 만: #37×5 = 305,090(9.833%) — #37 은 5개일 때만 구간에 맞는다.

> 10만·20만 구간은 초과분이 10% 를 넘어 실효 9.05% 까지 떨어진다. **50만 구간이 항상 유리.**

## 2. 현대몰 카트필 (계정 1·2·3·4·6)

| 계정 | 담은 것 | 카트 실측 |
|---|---|---|
| 1 tkdkky2002 | #37×2 + #38×1 | ✅ 2건 |
| 2 1plu1mall1 | #36×1 | ✅ 1건 |
| 3 kgi5907@daum.net | #37×2 + #38×1 | ✅ 2건 |
| 4 1plus1mall0 | #36×1 | ✅ 1건 |
| 6 1plus1mall4 | #34×2 + #17×1 | ✅ 2건 |

★ **계정 5 는 플랜에 없으므로 `1-4` 와 `6` 을 나눠 실행**했다. `run.py` 는 플랜이 비어도 로그인 후
`clear_cart` 를 돌리므로 `1-6` 으로 돌렸으면 **5번 카트를 비웠을 것**이다.

### ★ #1·#2 헛담기 — `[OK]` 는 담김의 증거가 아니다

1차 실행에서 5계정 전부 `[OK]` 가 찍혔는데, 나중에 카트를 실측하니 **#1·#2 는 빈 카트**였다
("장바구니에 담긴 상품이 없습니다"). #3·#4·#6 은 정상. 재실행하니 두 계정 다 정상 반영.

- 원인: `buy/run.py:416` `add_to_cart` 가 **장바구니 버튼 클릭 성공만으로 True 를 반환**한다.
  담긴 건수를 확인하지 않는다. (롯데 `lotte_add_combo` 는 카트 N건 검증이 있음 — 현대만 없다.)
- 왜 실패했는지는 **미규명**. 같은 실행·같은 상품(#3 도 #37/#38)인데 계정별로 갈렸다.
- → 다음 할 일에 `add_to_cart` 담기 검증 추가.

## 3. 롯데 8~14 조합12 담기 → 폰 결제 완주

담기: `buy/sulwhasoo.py lotte <idx> 12` × 7계정, **전부 카트 2건 검증 통과**. 비번변경 캠페인 7계정 모두 정상 통과.

결제: `python3 -m phone_auto.lotte_homeshopping_buy 8 9 10 11 12 13 14`
조합12 = **e 탄력3종×2 + h 자음생크림리치×1**, 롯데카드 7% 청구할인, 계정당 **552,544원**.

| # | 계정 | 주문번호 | 뷰티 | 적립 |
|--:|---|---|:--:|:--:|
| 8 | jye40323@naver.com | 2026-08-04-J37187 | ✓ | ✓ |
| 9 | wlstmdlsfk@naver.com | 2026-08-04-J36436 | ✓ | ✓ |
| 10 | kim455307@naver.com | 2026-08-04-J36610 | ✓ | ✓ |
| 11 | diinamic@naver.com | 2026-08-04-J36730 | **?** | ✓ |
| 12 | leenamsu0318@naver.com | 2026-08-04-J37004 | ✓ | ✓ |
| 13 | Lee0128 | 2026-08-04-J37051 | ✓ | ✓ |
| 14 | johwajeong2762@naver.com | 2026-08-04-J37401 | ✓ | ✓ |

- **#8 은 1차에서 `PAY_FAIL@loca_app`** — LOCA 앱(`com.lcacApp`) 에 0.7초 만에 진입했으나
  `sleep 2500ms` 후 `tap_dump_text '결제하기'` 가 8초 안에 못 찾음. **주문 미생성/돈 안 나감.**
  실행 중 LOCA 화면을 dump 로 떠보니 `결제하기` 버튼은 **정상 존재**(이용금액 552,544원, LOCA Professional 2224 선택됨)
  → 버튼 부재가 아니라 타이밍. 나머지 6계정이 같은 경로로 통과 → **일회성 판정, 코드 미수정**(사용자 지시).
  재실행 1회로 성공(J37187).
- **#11 뷰티포인트만 미확인** — "적립신청 완료문구 미확인(미적립 가능)". 주문완료 화면 only 라 재진입 불가.
  해당 계정 뷰티포인트 내역 직접 확인 필요.
- 요약의 `적립✓` 는 클릭 성공이 아니라 **"신청이 완료"/"신청완료" 문구를 실제로 읽었을 때만** 찍힌다
  (`lotte_homeshopping_buy.py:1185`, `completed` 또는 `already`). 즉 이 표는 관측 기반.

## 4. `rate-check/hmall.py cart_items()` — 상품명 화이트리스트가 조용히 누락 (수정)

계정6 카트에 2건이 담겼는데 `cart_items()` 가 **1건만 반환**. 하마터면 "담기 실패" 로 오판할 뻔했다.

- 누락된 것 = `[우수고객전용] 데이즈온 원데이 알파CD … 8박스/알파cd`
  → 이름이 `알파cd` 로 끝나 필터 `설화수|기획세트|\d+ml|\d+개$` 의 어디에도 안 걸림.
- 이 함수는 **담기 검증 + `_cart_is_empty`(clear_cart 검증)** 에 쓰이므로 누락 = 조용한 오독.

**DOM 실측** (계정6 카트): 카트 섹션 `div.shipping-listwrap` → 행 `div.pdwrap.pdlist.ml` **2개 = 삭제 버튼 2개**.
상품명 = 행 innerText 첫 줄.

**수정**: 이름 정규식 폐기 → **행(`div.pdwrap`) 단위 카운트**. 카트 섹션 스코프는 유지
(하단 '최근 본 상품' 캐러셀 혼입 방지 — 2026-08-02 사고).

**라이브 검증** (현대몰 실제 카트 5계정): 6번 2건 / 3번 2건 / 4번 1건 / 1번 2건 / 2번 1건 — 전부 행 수·삭제버튼 수와 일치.
빈 카트(#1 재담기 전)에서 `[]` 반환도 확인 — '최근 본 상품' 이 섞이지 않았다.

## 5. 어제(8/3) 롯데 1~7 구매 확인 — 조회 경로 신규 확보

구매대장(`secrets/purchase_ledger.json`) 에 **8/2·8/3 기록이 0건**(대장은 best-effort). 8/2~8/4 워크로그도 없어
로컬 근거가 전혀 없었다 → **PC 웹 주문내역을 직접 조회**해서 확인.

**조회 경로 (신규 확보, 읽기 전용)**
```
로그인 → https://www.lotteimall.com/mypage/searchMyLotteMain.lotte   (마이롯데, fn_goMyLotte())
  · 적립금 잔액/쿠폰/L.POINT 가 라벨 다음 줄
  · fn_goSearchOrderStepByMyLotte()  → 주문내역 (searchOrderList.lotte)
  · fn_goSearchSaveAmtList()         → 적립금 내역 (searchSaveAmtList.lotte)
  · fn_goMyEventList()               → 이벤트 응모내역 (event/prizewinner.lotte)
```
⚠️ `/mypage/main.lotte`·`/order/orderList.lotte` 는 **404**(추측 URL 금지). 링크는 전부 `fn_go*` onclick.

**결과**

| # | 계정 | 8/3 주문 | 상품 |
|--:|---|---|---|
| 1 | tkdkky2002 | **없음** | 최근 주문 07/15 |
| 2 | kms3945 | 2026-08-03-I99942 | 탄력케어 세트 2개 (8/2 I11226 윤조90 2개도 있음) |
| 3 | kgi5907 | 2026-08-03-J00522 | 윤조90 기획세트 2개 (8/2 I21621 도 있음) |
| 4 | owyura | 2026-08-03-J00742 | 탄력케어 세트 2개 (8/2 I12110 도 있음) |
| 5 | yr5326 | 2026-08-03-J00908 | 탄력케어 세트 2개 (8/2 I22042 도 있음) |
| 6 | ybkim8225@gmail.com | 2026-08-03-J01648 | 탄력케어 세트 2개 |
| 7 | lee6040314@naver.com | 2026-08-03-J02224 | 탄력케어 세트 2개 |

전부 도착예정 08/07, 배송 진행 중. **#1 미구매는 8/1 워크로그 §13/§14 의 "비번 거부" 미해결 항목이 원인.**

### 광세일 적립 신청 여부 = **확인 못 함**

- **적립금 내역** — 지급 완료분만 나온다(6/1~4 구매 → 7/2 부여 식으로 수주 지연). 어제 신청분은 아직 안 뜬다.
- **이벤트 응모내역 '구매/참여'** — 광세일은 여기 안 남는다(최근이 7/20 으랏차차 식품대전).
- 결국 **폰 앱 광세일 행사페이지의 `혜택 신청완료` 표시**가 유일한 확인 수단이다.
  `claim_lotte_reward(goods_no=...)` 는 미신청이면 **바로 신청까지 해버리므로**, 구매 상품과 다른 코드로 호출하면
  #14 사고(자음생크림리치 샀는데 자음생2종에 적립)를 반복한다. 위 표로 계정별 구매 상품은 확보됨.

## 6. #1 롯데 로그인 — 8/1 "비번 거부" 해소 확인

`lotte_login` True, 마이롯데에서 **김건엽 고객님 / 적립금 20,000원** 확인. 내일 구매 대상에 넣어도 된다.
(사용자가 `lotte.json` 비번을 갱신하기로 함.)

## 7. 코드 전체 훑기

25개 모듈 14,555줄 **구문 검사 전부 통과**, TODO/FIXME 잔여 없음. 미해결 3건:

| # | 내용 | 위치 |
|---|---|---|
| 1 | **`n` 을 롯데·갤러리아에 담을 수 없다** — `LOTTE_PRODUCTS`/`GALLERIA_PRODUCTS` 가 `b~h` 7개뿐인데 `COMBOS #24~27` 은 `n` 을 쓴다. 담기 시 `[ERR] sku 'n' 상품 없음` 즉시 실패 | `buy/sulwhasoo.py` |
| 2 | **갤러리아 리디렉트 abort 가드 없음** — `redirected` 플래그만 달고 값을 그대로 쓴다 (8/1 오염사고 구조 그대로) | `rate-check/galleria.py:140,511` |
| 3 | **`add_to_cart` 담기 미검증** — 클릭 성공 = `[OK]`. 오늘 #1·#2 헛담기의 원인 | `buy/run.py:416` |

## 8. READ_FIRST.md — 세션 종료 시 워크로그 필수 규칙 추가

사용자 지시. 시키지 않아도 세션 끝에 `WORKLOG_YYYY-MM-DD.md` 를 남기고 커밋까지 한다.
같은 날 파일이 있으면 이어붙인다. **8/2·8/3 이 안 남아 오늘 계정별로 다시 뒤진 것이 계기.**

## 9. 다음 할 일

- [ ] **내일 롯데 구매: #15·#16·#17·#18·#19·#20·#1** (사용자 지정). #1 은 비번 갱신 후 포함
- [ ] **현대몰 1·2·3·4·6 결제** — 카트 담긴 채 대기 중
- [ ] **광세일 적립 신청 확인** (롯데 #2~#7) — 계정별 구매상품 goods_no 로 폰 광세일 페이지 진입.
      읽기 전용 확인 후 미신청분만 신청할 것
- [ ] **#11 뷰티포인트 실적립 여부** 직접 확인 (주문완료 only 라 자동 재시도 불가)
- [ ] `buy/sulwhasoo.py LOTTE_PRODUCTS`/`GALLERIA_PRODUCTS` 에 **`n` 추가** (8/1 부터 이월)
- [ ] `buy/run.py add_to_cart` 에 **담기 검증** 추가 (`cart_items()` 가 이제 믿을 수 있으므로 바로 쓸 수 있음)
- [ ] `galleria.py` 리디렉트 **abort 가드** (8/1 부터 이월)
- [ ] 8/1 이월: **#15·#16 결제 재시도** (당시 `이용한도 초과`) — 내일 15·16 구매에 포함되는지 확인 필요

---
---

# ━━━ 2차 세션 — 윈도우 이식 (step1/step2 크로스플랫폼화) ━━━

목표: **같은 코드가 맥에서도 윈도우에서도 충돌 없이 돌 것.** 플랫폼은 `sys.platform` 으로 자동 판단 —
사용자가 지정하거나 분기를 고를 일이 없다.

## 10. ★ 먼저 확인한 것 — step1/step2 는 폰과 무관하다

```
step1: galleria(selenium) → inventory → hmall(playwright) → lotte(selenium) → cart_plan
step2: check10(playwright) → show
```

`rate-check/*.py` · `cart/*.py` 전체에 **`adb`/`phone_auto` 의존이 0건**. 즉 폰 자동화의 Apple Vision OCR
문제(아래 §13)는 step1/2 와 완전히 분리돼 있고, 이번 작업에 OCR 이식은 필요 없었다.

## 11. 윈도우에서 **즉시 죽는** 충돌 2건 (수정 완료)

### ① `signal.SIGALRM` — 윈도우에 존재하지 않음 (`rate-check/hmall.py`)

`_pick_cdp_backend` 가 patchright 행(무한대기) 방지용 12초 알람에 `signal.SIGALRM`/`signal.alarm()` 을 썼다.
둘 다 **유닉스 전용** → 윈도우에선 `AttributeError` 로 step1 3/5단계에서 죽는다.

**수정**: 워커 스레드 + `join(12)` 로 교체. 행이면 join 이 12초 뒤 리턴하고 데몬 스레드는 프로세스를 안 잡는다.
sync playwright 는 스레드별 인스턴스면 안전 — 생성·사용·종료가 전부 워커 안에서 끝난다.

**검증(맥)**: 살아있는 9222 → `patchright` 0.4s / 죽은 9999 → `ECONNREFUSED` 감지 후 `playwright` 폴백 0.1s.
교체 전과 의미 동일.

### ② `open()` 인코딩 미지정 — 모듈 로드 시점에 터짐

윈도우 파이썬 기본 인코딩은 **cp949**. UTF-8 한글 JSON 을 못 읽는다. 문제는 이게 **모듈 최상단**이라는 것:

```python
rate-check/lotte.py:43   IDS = json.load(open(... sulwhasoo-ids.json))   # import 즉시 UnicodeDecodeError
rate-check/hmall.py:63   IDS = json.load(open(... sulwhasoo-ids.json))
rate-check/hmall.py:298  cfg = json.load(open(ROOT / "hmall_config.json"))
```

encoding 미지정이 총 15곳. **15곳을 각각 패치하는 대신 `PYTHONUTF8=1` 을 자식 프로세스에 주입**했다
(step1.py/step2.py 의 `CHILD_ENV`). 회귀 위험이 없고 한 곳만 본다.

**검증**: `PYTHONUTF8=1` 자식 → `locale.getpreferredencoding(False)` = `utf-8` 확인.

## 12. 조용히 저하되던 것 + 진입점 (수정 완료)

### ③ chromedriver 버전매칭이 윈도우에서 무력화 (`rate-check/_common.py`)

```python
arch = "mac-arm64" if platform.machine() == "arm64" else "mac-x64"   # 윈도우도 mac-x64 로 감
cands = glob(base / f"{major}.*" / "chromedriver")                    # 윈도우는 chromedriver.exe
```

죽지는 않지만 캐시 미스 → `None` → Selenium Manager 위임 → **이 함수가 막으려던 "매 실행 첫 attach
버전 mismatch 실패"가 윈도우에서 그대로 재현**된다. → `darwin`/`win`/`linux` 3분기 + `.exe` 처리.

**검증(맥)**: `mac-arm64/148.0.7778.97/chromedriver` 매칭 — 수정 전과 동일.

### ④ `.sh` 진입점 → `step1.py` / `step2.py` 신설

`bash step1.sh` 는 윈도우에서 안 돈다. `launch-hmall-chrome.sh` 는 `osascript`·`pgrep`·`pkill`·`curl` 까지 써서
이식이 아니라 재작성이 필요했다.

- **`step1.py`** — 5단계 순차 실행. 갤러리아 `rc=2`(GWP 판독 PENDING) 의미 보존, 나머지는 실패 시 즉시 중단.
- **`step2.py`** — check10 → show. check10 종료코드를 그대로 반환(show 는 조회 전용이라 판정 제외).
- 기존 `.sh` 는 **지우지 않고 남겨둠**(맥 기존 사용 무영향).
- 어디서든 `python3 step1.py` / `python step1.py` 하나로 통일.

### ⑤ `chrome_launcher._launch` 윈도우 분기

맥 = 기존 `.sh` 그대로. 윈도우 = **CFT 를 직접 Popen** (`_launch_windows`).
`CFT_BIN` 환경변수 1순위, 없으면 `~/ChromeForTesting/chrome/win*/chrome-win*/chrome.exe` 최신 버전.
포트 응답까지 최대 timeout 초 폴링. 맥 launcher 와 동일 플래그(포트/프로필/팝업차단해제).

**검증(맥)**: `resolve_cdp_port(9222)` → 9222 재사용, LAUNCHER 존재 확인 — 기존 경로 그대로.

### ⑥ `buy/requirements.txt` 보강

**`patchright` + `python-dotenv` 2개뿐이었다.** 실제로는 selenium(갤러리아·롯데) · gspread(시트) ·
playwright(폴백) · requests · Pillow 가 필요해서, 그대로 설치하면 다른 컴퓨터에서 ImportError 로 죽는다.
step1/2 용과 폰 자동화용을 섹션으로 분리하고, **pyobjc 는 "윈도우에서 설치 금지"** 로 주석 처리(맥 전용).

## 13. 윈도우에서 **아직 못 돌리는 것** — 폰 결제

폰 자동화 전체가 Apple Vision 위에 있다. `flow_runner._ocr_texts`(`import Vision, Quartz, Foundation`)가
유일한 텍스트 OCR 진입점이고 **폴백 엔진이 없다**. `ocr_tap`/`wait_text`/`screen_has` 가 전부 이걸 쓴다.

- 키패드는 이미 뚫려 있다 — `ocr_keypad.vote_digits` 가 `vision → +easyocr → +claude 승격` 사다리,
  `_ocr_claude` 는 **파일 핸드셰이크**(request.png/response.json)라 **OS 무관**.
  `PAY_VISION_MODE=1` 구간 인계도 동일(선례: NH 방패 키패드 → 롯데 #9 주문 2026-07-31-G83859).
- 남은 건 **화면 이동 판정**용 `_ocr_texts` 하나. 여기에 `darwin=Vision / 그 외=easyocr` + 실패 시 클로드 승격
  사다리를 붙이면 된다. 승격 판정은 `_ocr_texts` 가 아니라 **`ocr_find`/`wait_text`/`screen_has`** 에서
  해야 한다(그 함수들만 "무슨 텍스트를 찾는지" 안다 — `_tap_shuffle` 의 `need = set(value)` 와 같은 구조).
- easyocr 한국어 모델(`korean_g2.pth`)은 캐시에 이미 있고 윈도우에선 자동 다운로드된다.

⚠️ 실돈 경로라 **엔진을 바꾸면 롯데 A~G 전 스텝 + 카드앱 flow 라이브 재검증이 필요**하다.
첫 실행은 무인 금지 — 한 계정씩 관찰.

## 14. 윈도우 준비물

**gitignore 라 커밋에 없는 것** — 수동 복사 필수:
`hmall_config.json` · `lotte.json` · `galleria.json` · `credentials.json` · `beautypoint.json` ·
`gen-lang-*.json` · `lotte_address_map.json` · `hsmaster/config/sulwhasoo-ids.json`

> 특히 `sulwhasoo-ids.json` 은 8/1 §8 경고 그대로 — 없으면 `PRODUCTS` 엔 `n` 이 있는데 `load_ids()` 엔
> 없어서 galleria/lotte 가 `KeyError` 로 죽는다.

**깨끗한 것 (확인 완료)**: 유닉스 전용 표준모듈(`fcntl`/`termios`/`pwd`/`os.fork`) 0건, `shell=True` 0건,
경로는 `Path` 일관 사용, `Path.home()` 윈도우 정상, 시트 인증키는 리포 상대경로.

## 15. 2차 세션 다음 할 일

- [ ] **윈도우 첫 실행 검증** — `python step1.py` / `python step2.py`. CFT 설치 경로 확인 후
      필요하면 `CFT_BIN` 환경변수 지정
- [ ] 윈도우에 gitignore 파일 8종 복사 (§14)
- [ ] `flow_runner._ocr_texts` 엔진 사다리 (§13) — 폰 결제를 윈도우로 옮길 때
- [ ] `/tmp` 하드코딩 28곳 → `tempfile.gettempdir()` (phone_auto 전용, 폰 이식 시 필요)
