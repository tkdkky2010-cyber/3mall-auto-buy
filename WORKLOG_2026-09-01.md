# WORKLOG 2026-09-01 — 담기 16계정 (현대 10 + 롯데 6), 결제 없음

## 0. 사용자 지시 (문장 그대로)

> "롯데몰 15-20번 아이디로 설화수 12번조합 담아주고 현대몰 1-5번 아이디로 식품 2,3,22,25번 제품 2개씩
>  장바구니에 11-15번 아이디로 식품 9번 선택1 오리지널 아메리카노 로 2개씩 담아줘"

## 1. 결과 — 담기 16/16 (전부 **재로그인 후 DOM 실측 검수**)

| 몰 | 계정 | 담은 것 | 카트 표시금액(쿠폰 전) |
|---|---|---|--:|
| 현대 | #1~#5 | 2번 `2151046312` ×2 / 3번 `2225431602` ×2 / 22번 `2244934734` ×2 / 25번 `2243971283` ×2 | 634,400 |
| 현대 | #11~#15 | 9번 `2246603712` [선택 1] 오리지널 아메리카노 ×2 | 77,600 |
| 롯데 | #15~#20 | 조합12 = e 탄력3종 `2923406968` ×2 + h 자음생크림리치 `2719761746` ×1 | 630,000 |

- 실행: `python buy/run.py 1-5` / `python buy/run.py 11-15` / `python buy/sulwhasoo.py lotte {15..20} 12`
- 현대 비우기 10/10 · 담기 25/25, 롯데 6/6 (비번변경 캠페인은 6계정 모두 `30일간 보이지 않기` 로 닫힘).
- 결제는 안 했다(지시가 담기까지). 롯데는 `LOTTE_CART_ONLY=true` 그대로.

## 2. ★ 롯데 수량 검증이 **6계정 전부 조용히 통과했다** — 근본 수정

담기 로그가 6계정 모두 이 줄을 남겼다:

```
[WARN] 담기 검증 — 카트 2건 확인, 그러나 **수량을 읽지 못했다** (기대 [1, 2])
```

8/31 에 "줄 수만 세면 수량 오류를 못 잡는다"며 넣은 수량 대조가 **판독을 못 해서 항상 WARN 으로
빠져나가고 있었다.** 원인은 DOM 이다 — 롯데 PC 장바구니의 수량은 화면 텍스트가 아니라
**`<input name="ord_qty">` 의 value** 다. 그래서 `innerText` 에서 `^N개$` 를 찾는 정규식은
**어떤 날에도 매칭될 수 없다.** 카트가 정상인 날엔 티가 안 나고, **수량이 틀린 날에도 똑같이
통과했을** 구멍이다(= 8/31 에 막으려던 그 사고가 판독 경로만 바꿔 그대로 남아 있었다).

- 수정: `buy/sulwhasoo.py lotte_add_combo` — 행(`input[name=goods_no]` 보유) 단위로
  `goods_no`·`ord_qty` 를 읽어 대조. 옛 DOM 용 innerText 경로는 **폴백으로만** 남겼다.
- 라이브 검증(#20 재실행): `[cart] 2719761746x1, 2923406968x2` → `[OK] 담기 검증 — 카트 2건, 수량 [1, 2]`.
- 오늘 6계정은 별도 스크립트로 `ord_qty` 를 직접 읽어 **전부 `e×2 + h×1` 일치**를 확인했다.

> 교훈(자동메모리 `observation-failure-not-absence` 와 같은 계열): **"못 읽었다"를 경고로만 두면
> 그 경고는 곧 배경소음이 된다.** 판독 경로가 구조적으로 매칭 불가면 WARN 이 아니라 버그다.

## 3. 현대몰 담기 검수에서 드러난 사실 2개

### 3-1. `[OK] 담기 x2` 로그는 카트 검증이 아니다
`buy/run.py add_to_cart` 의 `[OK]` 는 **버튼 클릭이 됐다**는 뜻이고 카트를 다시 읽지 않는다.
그래서 10계정을 재로그인해 `div.pdwrap` 행의 `p.option-info` 를 직접 읽어 검수했다.
- 견과 2종은 행 텍스트에 옵션명(`1.시그니처오리지널22gx100봉`)과 수량(`2개`)이 **span 2개**로 들어간다.
  `innerText` 앞 8줄만 보면 `TV+` 배지 때문에 수량 줄이 밀려 안 보인다 — 줄 자르기로 오판할 뻔했다.

### 3-2. 정가가 하루 만에 7,620원 내렸다
4품목 ×2 정가 합 = **8/31 642,020 → 9/1 634,400**. 담긴 상품·수량은 동일하다(몰 판매가 변동).
→ 오늘 결제하면 8/31 의 535,837원과 **금액이 다르다.** 주문서에서 실측할 것.

### 3-3. `buy/products.json` 이름과 실제 상품명이 어긋나 있다
- `"2"`(=`2151046312`) 파일상 "하루견과 초록색 100봉" / 실제 **`순수견과100% 하루견과 넛츠시그니처 23gx100봉`**
- `"3"`(=`2225431602`) 파일상 "하루견과 갈색 100봉" / 실제 **`ONLY견과 시그니처오리지널 22gx100봉`**

slitmCd 는 8/31 결제분과 동일하므로 **담긴 물건은 맞다.** 다만 워크로그·구매대장의 "초록/갈색"
표기가 실제 상품명과 대응이 안 된다. 이름을 지금 바꾸면 과거 기록과 어긋나므로 **건드리지 않았다** —
사용자 확인 후 정리할 것.

## 4. 다음에 할 일 / 주의

- **결제 시 `MAX_PAY` 는 조합별로 다시 잡을 것.** 롯데 조합12 카트 = 630,000원(쿠폰 전, 정가 700,000).
  조합25 의 `MAX_PAY=560000` 은 그대로 쓰기 전에 조합12 주문서 실측이 필요하다.
- **현대몰 캠페인 제약과의 충돌**: 8/31 확정안은 "현대몰 전체 하루 5계정"인데 오늘 담기는 #1~5 + #11~15 = 10계정이다.
  담기는 무해하지만 **결제까지 10계정을 하면 제약을 깬다.** 오늘 결제 대상은 사용자 지시를 받고 정할 것.
- 이디야(9번)도 같은 `잘사는 추석 10%적립 (~09/23)` 대상이다(카트 행에 배지 확인).

---

# ━━━ 2차 세션 (윈도우) — 전 계정 결제 16/16 ━━━

## 0. 사용자 지시 (문장 그대로)

1. "무선 디버깅 172.30.1.87:38039 연결해줘"
2. "전체 결제해줘"
3. "니 지금 딴데보고있는데?"
4. "가장마지막결제 한도초과였음 . 2/2/2/2/개 사는거였음. 오늘 결제 얼마해야돼?"
5. "김건우 아이디 현대카드 결제못했음 현대카드 결제 남은 금액 알려줘"
6. "3번은 왜 하나카드로했음? 그리고 현대카드 최종결제 총 금액 알려줘"
7. **"왜 하나카드로 결제한거야 7% , 5% 너무다르잖아. 이런 실수하지않도록 해"**
8. "그리고 다른결제도해야되잖아 이디야도 사야되는데 최종결제그액알려줘 다시"
9. "130만원 뚫어놨음 남은결제 다 시작해"
10. "끝나면 워크로그 쓰고 커밋해줘"

## 1. 결과 — 16/16 결제 완료

### 현대몰 식품 (2/3/22/25 각 2개) — 정가 634,400 → 쿠폰 후 543,200

| # | 계정 | 카드 | 결제(카드청구) | 주문번호 |
|---|---|---|--:|---|
| 1 | tkdkky2002 | 현대 7% | 504,656 | 20260901071631 |
| 2 | 1plu1mall1 | 현대 7% | 505,046 | 20260901072364 |
| 3 | kgi5907@daum.net | **하나 5%** | 515,024 | 20260901073018 |
| 4 | 1plus1mall0 | 현대 7% | 505,176 | 20260901075901 |
| 5 | 1plus1mall1 | 현대 7% | 505,176 | 20260901076835 |

### 현대몰 이디야 오리지널 아메리카노 ×2 — 정가 77,600 → 쿠폰 후 65,960

| # | 계정 | 카드 | 결제 | 주문번호 |
|---|---|---|--:|---|
| 11 | kimgaeun04 | 현대 7% | 61,343 | 20260901077131 |
| 12 | Jinhwa4553 | 현대 7% | 61,343 | 20260901077700 |
| 13 | gcd0327 | 현대 7% | 61,343 | **미확보**(§4) |
| 14 | johwajeong | 현대 7% | 61,343 | 20260901079559 |
| 15 | jyj041220 | 현대 7% | 61,343 | 20260901080182 |

### 롯데 조합12 (e 탄력3종 ×2 + h 자음생크림리치 ×1) — 전 계정 롯데카드 7%

| # | 계정 | 주문서 버튼금액 | 주문번호 | 뷰티 | 구매사은 |
|---|---|--:|---|---|---|
| 15 | joyoungjin1220 | 549,411 | 2026-09-01-F62759 | ✅ | ✅ |
| 16 | kimmk2008 | 534,399 | 2026-09-01-F63052 | ✅ | ✅ |
| 17 | miu1838 | 534,426 | 2026-09-01-F63424 | ✅ | ✅ |
| 18 | nami8225 | 538,576 | 2026-09-01-F63848 | ✅ | ✅ |
| 19 | ybkim9960 | 555,175 | 2026-09-01-F64060 | ❌ **미확인** | ✅ (사후 회수) |
| 20 | yeonsuk0428 | 534,427 | 2026-09-01-F64180 | ✅ | ✅ |

- 전 계정 할인쿠폰 2/2 + 플러스쿠폰 2장(13%·10%) + L.POINT 적용. `MAX_PAY=560000` 전 계정 통과.
- ⚠️ **롯데 금액은 주문서 '결제하기' 버튼 값 = 카드 청구할인 반영 전**이다. 구매대장에 찍히는
  `506182원` 은 전 계정 동일한 **rate 시트 기준값**이지 실청구액이 아니다(#15 549,411 / #16 534,399
  인데 대장은 둘 다 506,182 인 것이 증거). 실청구는 카드 명세로 확인할 것.
- #19 뷰티포인트는 완료문구 미확인 → **주문완료 화면 only 라 복구 불가**. 구매사은은
  `claim_lotte_reward(goods_no="2923406968")` 로 사후 회수 성공 (`최대 15% 적립 / completed:True`).

## 2. ★ 현대카드 CC61 한도초과 — #4·#5 1차 거절

`KCP 결제 승인 요청 실패하였습니다. (CC61 : 거래금액 한도초과)`

- 오늘 현대카드 승인 누적 #1+#2 = **1,009,702원** 시점에 505,176원이 거절됐다.
  → 남은 한도 < 505,176. 한도 150만이면 잔여 490,298원으로 딱 앞뒤가 맞는다(추정).
- 사용자가 **130만원 한도를 확보**한 뒤 재실행 → #4·#5 즉시 승인.
- **실패 지점의 이름이 원인을 가리지 않은 좋은 사례** — fail_audit 가 CC61 코드까지 그대로 떠서
  추측 없이 원인이 확정됐다.

## 3. ★★ 사용자 지적으로 고친 것 — 카드 선택이 사실상 '운'이었다

> **"왜 하나카드로 결제한거야 7% , 5% 너무다르잖아. 이런 실수하지않도록 해"**

#3 이 **하나 5%** 로 결제돼 현대 7% 대비 **10,843원**을 더 냈다. 원인 두 겹.

1. `detect_card()` 가 **금액 최저**로 골랐는데 그 금액 판독이 흔들린다.
   실측 로그: `후보 2개 — 하나 504,656 / 현대 504,656` — **같은 값**(5%·7%면 같을 수 없다).
   동점이면 어느 카드가 뽑히는지가 무작위였다.
2. 주문서 카드할인 캐러셀이 **슬라이드를 1개만 렌더링할 때가 있다.** #3 은 후보가 하나뿐이라
   비교 자체가 없었다(그 계정만 `[detect_card] 후보 N개` 줄이 없다).

**수정** (`phone_auto/hmall_hyundai_buy.detect_card`)
- 선택 기준 = **할인율 `N%` 최고** 우선, 같으면 금액 최저. `%` 토큰은 짧아 OCR 이 안정적이다.
- 화면 밖 사실을 아는 **독립 출처** 추가 — `todays_card_ranking()` 이 `cart/today.json` 의
  `payment.card_slides` 에서 오늘 카드별 %를 읽는다(오늘 `{'현대': 7, '하나': 5}`).
  주문서에서 잡힌 카드보다 높은 %가 오늘 존재하면 **그 카드를 반환** → `select_card` 가 그리드로
  강제 선택하고, 기존 금액일치 검증이 그대로 걸려 잘못 잡히면 거기서 막힌다.
- 재실행 로그로 효과 확인: `후보 2개 — 하나 505,176(?%) / 현대 505,176(7%) → **현대 (7%)** 선택`
  (금액은 여전히 똑같이 잘못 읽히는데 %가 갈라준다.)
- 자동메모리 `pick-highest-discount-card` 신설.

## 4. 윈도우 이식 버그 4건 — 전부 "조용히 죽는" 형태였다

| # | 증상 | 진짜 원인 | 근거 |
|---|---|---|---|
| 1 | `AFTER_PAY_IDENTITY_FAIL` (#1·#2, 본인인증 100% 실패) | `_kbd_is_korean()` 이 **자모를 OCR** 로 찾는데 윈도우 OCR 이 자모를 **모양 닮은 글자로 바꿔 읽는다** — `ㅂ→BB` `ㄱ→기` `ㅅ→人` `ㅁ→口` `ㄴ→L` `ㅇ→0` `ㄹ→근`. 한글 키보드인데 매칭 0건 | 한글 키보드 스샷을 `_ocr_texts` 로 직접 돌려 확인. 스페이스바 라벨(`한국어`/`English (US)`/`中文`)은 **깨끗하게 읽힌다** → 그걸로 판정하도록 교체, 폰에서 3가지 상태 실측 검증 |
| 2 | `buy.py reward` 가 **ModuleNotFoundError: fcntl** | 적립 동시실행 락이 **POSIX 전용** | 윈도우엔 fcntl 없음 → `msvcrt.locking` 분기 + 10분 대기 루프(`signal.alarm` 도 POSIX 전용이라 제거) |
| 3 | 락 고친 뒤 `TypeError: argument of type 'NoneType'` | `subprocess.run(text=True)` 가 **cp949** 로 디코딩 → 자식이 찍는 한글에서 UnicodeDecodeError → **리더스레드가 죽고 stdout=None** | 예외 메시지 실측. `encoding="utf-8", errors="replace"` + `PYTHONIOENCODING` 명시 |
| 4 | 적립·주문조회가 **180초 타임아웃** | **PC 포트 체인(9222~9224)과 폰 WebView forward(9223)가 겹친다.** PC CFT 가 죽자 `resolve_cdp_port` 가 9223 을 '살아있는 CFT'로 보고 재사용 — 실제론 폰이었다 | `curl 127.0.0.1:9223/json/version` → `{"Android-Package": "com.hmallapp", "User-Agent": "... Android 15; SM-G9960 ..."}`. `adb forward --list` 로 확정 |

### 4-1. `_alive()` 는 "응답이 있다"가 아니라 "**PC 크롬이다**"를 확인해야 한다
`chrome_launcher._alive()` 가 `/json/version` 본문을 파싱해 `Android-Package` 또는 UA 의 `Android`
가 있으면 **False + 경고**. 안 그러면 PC 자동화(담기·적립·주문조회)가 조용히 폰을 조종한다.
→ 폰 forward 포트(`hmall_webview.LOCAL_PORT=9223`)를 체인 밖으로 빼는 것도 검토할 것(미적용).

### 4-2. 현대 #13 주문번호 미확보
`DONE(beauty_fail:no orderComplete target)` — 결제는 됐는데 주문번호를 못 긁었다.
**주문 성사 근거 3가지**: ① PC 카트 0건(결제 전 1건, 주문되면 비워짐) ② H.Point 적립 신청 성공
③ 주문완료 마커 감지. 주문번호만 대장에 `-` 로 남았다 → 앱 주문내역에서 채울 것.
⚠️ 주문내역 URL(`/mo/myp/ordList` 등)은 **전부 404**, 마이페이지는 JS 링크라 PC 자동조회 경로가 없다.

## 5. 수정 목록

| 파일 | 수정 |
|---|---|
| `phone_auto/hmall_hyundai_buy.py` | `_kbd_is_korean` 스페이스바 라벨 판정 + dump 병합 / 한글전환 실패 시 스샷 저장 / `todays_card_ranking()` 신설 / `detect_card` **할인율 우선 + today.json 대조 강제선택** |
| `buy.py` | 적립 락 `fcntl`→플랫폼 양립(`msvcrt`) / 적립 서브프로세스 `encoding="utf-8"` |
| `chrome_launcher.py` | `_alive()` 가 **폰 WebView 를 CFT 로 오인하지 않도록** `/json/version` 검사 |
| `buy/sulwhasoo.py` | (1차 세션) 롯데 카트 수량 검증 `ord_qty` 판독 |
| `cart/today_carts.json` | 결제 결과 16건 기록(주문번호·금액·카드) |

## 6. 다음에 주의할 것

- **롯데 대장 금액 ≠ 실청구액.** 대장은 rate 시트 기준값이라 계정이 달라도 같은 값이 찍힌다.
  실측이 필요하면 주문서 버튼 금액(로그) 또는 카드 명세를 봐야 한다.
- **이디야(#11~15)는 계정당 61,343원이라 '잘사는 추석' 10만원 티어에 못 미친다.** 이 계정들이
  캠페인 첫 구매면 적립 0원이다 — 티어를 채우려면 계정당 10만원 이상이 필요하다.
- 현대카드는 오늘 **누적 약 250만원**(식품 4건 + 이디야 5건)을 썼다. 내일 재개 전 한도 확인.
- #19 뷰티포인트 미적립 가능 — 사람이 확인. (뷰티는 회수 수단이 저장소에 없다.)
