# ⚠️ 작업 시작 전 필독 (AI/Claude 규칙)

**어떤 작업이든 시작하기 전에, 먼저 이 폴더 전체를 읽어 전체 흐름을 파악한 뒤 작업을 시작한다.**
흐름을 모른 채 코드를 건드리거나 새로 만들면, 이미 결정·구현된 것을 모르고 헤매거나 중복/오류를 만든다.

## 왜 (실제 사고 사례)
- 2026-06-01: 어제 `phone_auto/hmall_hyundai_buy.py`에 **7개 카드사 전부 SDK + 당일카드 자동감지(`detect_card`/`CARD_ALIASES`) + `select_card` 라우팅**을 만들어 놨는데,
  그걸 안 읽고 롯데홈쇼핑을 "롯데카드 전용"으로만 보고 비롯데 당일카드 앞에서 헤맴. → **전체를 안 읽어 흐름을 몰랐던 것**이 원인.
- 2026-06-06: 전날 커밋된 주문서 포인트/카드선택 **700px 정본**(`flow_runner.py`)을 안 보고 설화수 경로에 OCR 방식을 중복 구현 →
  설화수 16건 **포인트 미사용** 결제 + #17 카드선택 오진. → 같은 원인.

## ★★ 사용자가 정한 상수를 "stale 해 보인다"고 자동계산으로 바꾸지 말 것

**사용자가 지정한 값이 낡아 보이면 바꾸지 말고 묻는다.** 자동으로 "더 똑똑하게" 만들면
사용자가 의도한 의미가 사라지고, 조건이 바뀌는 날 조용히 터진다.

### 실제 사고 — 조합번호 색칠 (2026-08-02 → 2026-08-07 발현)

**조합번호 셀 색칠의 원래 의미 = "주로 사는 조합번호"(사용자가 지정하는 고정 세트).
그날 카트플랜 결과가 아니다.**

| 날짜 | 커밋 | 무슨 일 |
|---|---|---|
| 07-29 | — | 사용자 지시: "주로 사는 조합번호를 색칠" |
| 08-01 | `8019f52` | 구현 — `HIGHLIGHT_COMBOS = {3, 5, 12, 13, 14}` 고정 |
| **08-02** | `67f4fa2` | **클로드가 지시 없이** "하드코딩이 stale" 이라 판단 → **오늘 플랜 조합**으로 변경 |
| 08-05 | `cbf8342` | 사용자 지시 "14번도 색칠" → `HIGHLIGHT_COMBOS_ALWAYS = {14}` |

08-02 커밋 메시지에 **사용자 지시 언급이 없다** — 클로드 단독 판단이었다.
8/6 은 채널=롯데(N=7)라 7개만 칠해져 티가 안 났고, **8/7 채널이 갤러리아(N=36)로 바뀌자
tier_a 조합 25개가 전부 칠해져** 사용자가 지적했다. 조건이 바뀌는 날 터지는 전형.

- 조합번호는 **안 밀렸다** — 07-29 시점 COMBOS 1~23 과 현재 1~23 이 동일(24~27 만 추가).
- `CHANNEL_DEFAULT_N = {galleria: 36, hmall: 36, lotte: 7}` — 채널이 갤러리아/Hmall 이면
  N=36 이라 라운드로빈이 가용 조합을 거의 전부 쓴다. **플랜 기준 색칠은 구조적으로 "전부 칠하기"가 된다.**

### 색 두 가지를 혼동하지 말 것

| 색 | 위치 | 규칙 | 코드 |
|---|---|---|---|
| **연노랑** `(1.0, 0.9, 0.6)` | 3사 섹션 A열 + 차트 J열 조합번호 | **주로 사는 조합번호**(사용자 지정 고정) | `rate-check/cart_plan.py` |
| **연두** `(0.72, 0.92, 0.72)` | 차트 K2:M{1+N} | 행별 **최저** 공급률 셀 = 3사 중 베스트 | `rate-check/lotte.py` 조건부서식 |

**공급률은 낮을수록 좋다** (`순 ÷ 소비자가`). 연두 조건부서식이 `MIN` 을 쓰는 이유다.

### ★ 출력물은 **두 군데**다 — 양쪽 다 실측 검증할 것

step1/step2 의 결과물은 **공급률 시트(`M.D` / `M.DD 식품` 탭)** 와 **재고관리 시트
(`당일 설화수` / `당일 식품` 탭, `mirror_daily.py`)** 두 곳에 남는다. 원본만 고치고 미러를
안 보면 사용자는 **깨진 쪽을 본다**(2026-08-07 실사고 — 원본은 9개로 고쳤는데 미러엔 25개가 그대로).

### ★ "칠하기만 하고 지우지 않는다" — 같은 버그가 두 파일에 있었다

`repeatCell` 배경색 쓰기도, `mirror_daily._paint` 도 **색칠만 하고 이전 색을 지우지 않았다.**
`dst.clear()` 는 **값만** 지우고 서식은 남긴다. 대상이 줄어든 날 예전 색이 그대로 남는다.
→ `cart_plan.CLEAR_BG` / `mirror_daily.WHITE_BG`(시트 전체 초기화 후 복사) 로 양쪽 다 수정.

**교훈 — 버그 하나를 고치면 같은 모양을 폴더 전체에서 찾는다.** 이번엔 `cart_plan` 만 고치고
`mirror_daily` 를 안 찾아서 사용자가 지적했다. 로그 숫자가 변하면(`127칸 → 63칸`) **줄어든 쪽이
목적지에서 어떻게 되는지**를 반드시 묻는다. 성공 메시지는 검증이 아니다.

## 시작 전 반드시 읽을 것 (순서)
1. **이 파일** (READ_FIRST.md)
2. `GOAL.md` — 프로젝트 north star(매일 아침 1회 호출 → 4단계 파이프라인)
3. `CHROME_SETUP.md` — Chrome for Testing/Profile 6/CDP 포트 규칙
4. **최신 `WORKLOG_YYYY-MM-DD.md`** (날짜 큰 것부터) — 가장 최근 결정·검증·미완·교훈
5. `LOTTE_HOMESHOPPING_STEPMAP.md` — 롯데홈쇼핑 폰결제 정본 스텝맵 (A~G)
6. `rate-check/RULES.md` — rate-check 절대룰(캐시금지/하드코딩금지 등)
7. `.claude/agents/project-manager.md` — 일일 5단계 오케스트레이션
8. **자동메모리** `/Users/jasonkim/.claude/projects/.../memory/MEMORY.md` + 관련 파일들
9. **관련 소스 정독** — 작업 영역 코드. 특히:
   - `phone_auto/hmall_hyundai_buy.py` — **멀티카드 결제 정본**: `detect_card`/`CARD_ALIASES`/`select_card`/`select_card_discount`/`_pick_card_from_grid`/`pay_hyundai|lotte|kb|hana|bc|samsung|nh`/`buy_one` 디스패처. `CARDS_SUPPORTED`=현대·롯데·KB·하나·BC·삼성·NH (7개 전부 라이브검증).
     ★**현대카드는 무조건 `pay_hyundai_general`(일반 결제)** — 앱카드 결제 금지(사용자 지시 2026-08-19).
     앱카드는 누적금액 임계 초과 시 '현대카드 인증이 필요합니다' 모달로 자동화가 막힌다. 계정을 골라
     적용하던 `GENERAL_PAY_IDS` 는 삭제됐다 — 임계가 계정별 누적이라 **어제 되던 계정이 오늘 막힌다.**
   - `phone_auto/lotte_homeshopping_buy.py` — 롯데홈쇼핑 폰 구매(A~G). 카드앱 구간은 위 SDK/카드 JSON 재사용.
   - `phone_auto/coords/apps/*.json` — 카드앱 flow(lotte_card/kb_kbpay/hana_card/bc_paybook_isp 등). 카드앱 PIN 구간은 **몰 무관 재사용**.

## 핵심 아키텍처 (흐름 요약 — 외워둘 것)
- **결제 = [몰-side: 결제하기 + 카드수단 선택] + [카드앱: PIN flow]**. 카드앱 구간은 어느 몰에서 호출하든 동일 → `*.json` flow / hmall `pay_X`의 카드앱 부분 재사용.
- **현대몰 주문서 포인트/카드선택 정본 = `flow_runner.py` `hmall_use_all_points` + `hmall_select_card_discount`** (식품·설화수 공통, 새로 만들지 말 것):
  맨위→700px → ① H.Point '전액사용' 탭(<100P 비활성/버튼없음=skip) → ② 캐러셀 '즉시할인' 행 금액 == 하단 '결제하기' 금액 **일치=선택됨(탭 금지)** / 불일치=탭→재확인 / 계속 불일치=**결제 차단**.
  당일카드 아닌 강제선택만 그리드(`_pick_card_from_grid`: 결제수단변경→신용카드 선택→페이/Pay→카드 탭). **'미등록' 개념 없음** — 모든 카드 항상 선택 가능.
- **삼성·NH = 항상 에이전트 비전 핸드세이크**(플래그 없음, 로컬 OCR 자동입력은 코드에서 삭제됨).
  카드번호 화면에서 `manual=True` 로 정지 → `phone_auto/samsung_enter` · `nh_enter` 로 이어받고,
  **마무리 `finish`(현대몰) / `finish_lotte`(롯데) 를 반드시** 친다(대장·적립이 거기 있다).
  현대몰 삼성은 **인증서 단계 없음**(pin6 → '결제' → 주문완료). 비번화면 발급사가 '롯데'로 떠도 정상 —
  실제 승인은 삼성이다(2026-08-07 라이브검증, 주문 20260807004446).
- **토글 UI 원칙**: 현대몰 체크박스/카드할인 행은 토글 — **상태 확인 없이 무조건 탭 금지** (카트 전체선택=체크박스 픽셀색 R-G>60, 카드할인=금액일치로 판정).
- **당일 할인카드**는 매일 바뀜 → `detect_card()`로 주문서 '카드할인' 토큰 감지(`CARD_ALIASES`). 하드코딩 금지.
- **롯데홈쇼핑 비롯데 카드**: 롯데-side 카드수단 선택은 신규 관찰 필요하나, 카드앱 flow는 재사용. hmall `buy_one` 디스패처 패턴(detect_card→select_card→pay_X)을 그대로 이식.
- 뷰티포인트 적립 = 주문완료 화면 only(now-or-never), reward(구매사은 적립금)는 신청기간 내 재진입 가능. **뷰티가 reward보다 먼저+성공해야 함**.

## ★★ 결제 후 키패드 = **무조건 에이전트 핸드셰이크** + 실패는 **resume**으로 이어붙인다

**사용자 지시 2026-08-19 (문장 그대로):**
> "해당 ocr 로 실패하면 처음부터 다시시작이아니라 그 자리에서 핸드세이크로 해결하는 방법 고안해내서
> 앞으로 무조건 이런식으로하는거, 다른카드사 쓸때도, 다른 홈쇼핑에서도 이렇게 해줘야돼
> 카드비번 pin 부분 말고도 다른부분에서도 resume기능써. 무조건 이거 해야 시간 절약됨"
> "홈쇼핑 창 결제하기 이후 모든 카드번호 및 pin 번호 입력하는 부분의 ocr 은 핸드세이크로 바꾸는 거…
> nh카드 그렇게 하고 있잖아"
> "현대카드 무조건 앞으로 핸드세이크로 처리해 / 현대몰 설화수 현대몰 식품 롯데몰 설화수 모두"

### ★★ 핸드셰이크는 **멈추는 지점이 아니다 — 에이전트가 끝까지 완주한다** (2026-08-30 라이브검증)

> 사용자 지시 (문장 그대로, 2026-08-30):
> **"핸드세이크로 nh랑 롯데랑 다른카드사들 이용해도 결제 끝까지 완료할 수 있음. 아무런 문제없음."**
> **"핸드셰이크에서 멈추지말고 끝까지 결제진행해"**

`*_HANDOFF` 상태를 "사람을 기다리는 정지"로 읽지 말 것. **그 자리에서 에이전트가 이어서 완주한다.**
2026-08-30 롯데 #1(NH농협) 전 구간 라이브 완주 — 주문 `2026-08-30-D57689` / 541,800원.

```
box1                          # 1칸 = 비보안 IME → 스크립트가 adb input text 로 직접 입력
box2 "<1행6열>" "<2행6열>"     # 이하 전부 셔플 보안키패드
box3 … / box4 … / cvc …       # ★칸을 옮길 때마다 재셔플 → 칸마다 스크린샷 새로 뜬다
confirm                       # 카드정보 제출
pinfield → pin6 … → confirm   # 결제비번 6자리 → 최종 제출 → 주문완료
finish_lotte <idx> combo=N card=NH   # 주문번호·구매대장·뷰티·구매사은 (현대몰은 finish)
```

- **에이전트가 넘기는 건 "키패드 배열"뿐이다** — `"4,-,8,9,5,7"` (6열, `-` = 보안 아이콘 칸).
  값(카드번호/CVC/PIN)은 **스크립트가 `secrets/card_secrets.json` 에서 직접 읽는다.**
  카드번호는 에이전트를 거치지 않는다 (`phone_auto/nh_enter.py` 상단 주석).
- 각 칸은 `[verify] ✓ N칸 4자리` 로 자체검증된다 — 그 로그 없이 다음 칸으로 넘어가지 말 것.
- ⚠️ `pinfield` 는 플레이스홀더 `'숫자 6자리'` 텍스트 좌표를 탭해서 **라벨을 눌러 키패드가 안 뜬다.**
  `[pinfield] 키패드 X` 가 나오면 **입력칸 안쪽**을 직접 탭한다(실측 (660,941)).
- ⏱️ 결제하기 누른 뒤 **5분** 안에 PIN 까지 끝내야 한다 — 판독 왕복이 6회라 지체 금지.

### 그래서 결제 실행은 항상 이 두 개를 켠다

```
CLAUDE_OCR_ONLY=1        # 키패드 판독 = 에이전트 비전만 (로컬 사다리 skip). timeout 자동 240s
```
- `vote_digits`(= `input_pin` 의 단일 판독 관문)에서 `engines=("claude",)` 로 고정 →
  **현대·롯데·KB·하나·삼성·NH 전 카드 / 현대몰 식품·설화수 · 롯데몰 설화수 전부 한 번에 적용.**
- 에이전트는 `phone_auto/_tmp/claude_ocr/request.png` 를 읽고 `response.json`
  (`{"id":..,"digits":[{"d":"7","x":221,"y":1181},…]}`) 을 쓴다. 좌표는 **전체 스크린샷 기준**.
- ⚠️ 실행을 백그라운드로 돌릴 땐 **request.json 감시를 같이 걸어라.** 안 그러면 알아채기 전에
  타임아웃한다(실측: 기본 45s 는 무조건 놓친다).
- ⚠️ **무인/크론 실행에선 켜지 말 것** — 응답할 사람이 없어 키패드마다 240s 를 태운다.

### 왜 (실측 근거, 2026-08-19 #3 카드비번)
로컬 사다리는 실패해도 **어차피 클로드로 승격**되면서 그 앞 시간을 통째로 버린다.
`vision+easyocr` 는 `0,1,2,3,4,5,6,8,9` 를 잡고 **'7' 만 못 잡았다** — 9/10 은 need 에 걸리면 실패다.
그렇게 **3회 재시도 = 120초**를 태운 뒤 핸드셰이크로 넘어가 10/10 성공했다. 처음부터 부르면 그 120초가 없다.

### 실패했을 때: 처음부터 재시작 금지 → `resume`
```
python -m phone_auto.hmall_hyundai_buy resume <계정번호>
```
`detect_screen()` 으로 현재 화면을 판정해 그 지점부터 완주(`CARD_PW`/`PIN_SCREEN`/`ORDER_SHEET`/…).
주문서 재개도 멱등(포인트 이미사용=skip, 카드 금액일치=skip)이라 이중적용이 없다.
- ⚠️ **카드비번 4자리가 이미 입력된 화면에서는 `resume` 을 바로 쓰지 말 것** — `detect_screen` 이
  `CARD_PW` 로 보고 `enter_card_password` 를 다시 불러 **8자리가 된다.** 남은 동작('확인' 제출)만
  하고 나서 `resume` 으로 넘긴다.
- `resume` 도 이제 `_record_after_done` 으로 **구매대장·H.Point 적립·매니페스트 paid** 를 남긴다
  (2026-08-19 신설 — 종전엔 buy_one 에만 있어서 resume 완주분이 조용히 0건이었다).

## ★★ 윈도우 OCR 은 맥보다 약하다 — **판독은 OCR + dump 를 겹쳐 쓴다**

`Windows.Media.Ocr` 은 맥 Apple Vision 이 읽던 것을 놓친다. 2026-08-19 윈도우 첫 폰결제에서
**한 곳도 아니고 다섯 곳**이 이 때문에 터졌다. 한쪽만 믿는 판독은 언제든 조용히 실패한다.

| 대상 | OCR | uiautomator dump |
|---|---|---|
| PIN/카드비번 `확인` (검은 버튼 + 흰 글씨) | ❌ | ✅ `(540,1312)` |
| 토스트 `현대 5% 즉시할인이 적용되었어요.` | ❌ | ✅ `y=2085` |
| `안전한 결제를 위해 추가 인증` 팝업(딤 화면) | ❌ | ✅ |
| 주문서 텍스트 일부 | `口토사OI그`·`丁 0 曰` 로 깨짐 | 깨끗 |
| **로그인 chooser**(WebView) | ✅ | ❌ **node 16개·text 0개** |

- 탭은 `ocr_or_dump_tap()`, 화면 읽기는 `_ocr_texts(cap()) + _dump_texts()` **병합**.
- **dump 가 비는 화면도 있다**(위 표 마지막 줄) → 그래서 OCR 을 버리는 게 아니라 **겹친다.**
- 키패드는 dump 에 안 잡히는 영역 → 그건 위의 **핸드셰이크**가 담당.

## ★★ 롯데 장바구니 담기 — 비밀번호 변경 캠페인 (사용자 4회 지적, 2회 재발)

**로그인 직후 뜨는 비번변경 캠페인을 `30일간 보이지 않기` 로 닫고, 그 다음 본 창에서 담는다.**
안 닫고 담으면 **미로그인 컨텍스트라 실제로 담기지 않는다** (로그만 성공처럼 찍힘).

- 캠페인은 **별도 window** — `.../member/login/forward.popup_pwd_campaign_av.lotte`.
  넘겨받은 page 하나만 검사하면 못 찾고 **로그인 성공으로 오판**한다. → `context.pages` **전부** 스캔.
- 버튼에 **텍스트가 없다**. `<img alt="30일간 보이지 않기">` + 부모 `<a onclick="fn_changeNext()">`.
  `innerText`/`value` 로만 찾으면 절대 못 잡는다. → **`img[alt]` 매칭 필수.**
- 닫은 뒤 **창이 실제로 사라졌는지 검증**. 남아 있으면 `context.pages[-1]` 이 그 창을 잡아
  담기가 헛돌고(카트 0건), 창이 닫히는 순간 `Target page ... has been closed` 로 죽는다.
- ⚠️ **`지금 변경하기` 절대 클릭 금지** — 비밀번호가 실제로 바뀐다.
- 정본 = `buy/sulwhasoo.py` `_lotte_campaign_pages` / `_lotte_dismiss_pw_campaign` / `_lotte_pw_campaign_present`.
- 담기 후 **`카트 N건` 검증 로그 확인** 없이 "담기완료" 라고 보고하지 말 것.
- 피해 이력: 2026-07-21 1차 재발 / 2026-08-01 #2 카트 0건·#3 page-closed.

## ★ 장바구니는 하루 지나면 무용지물 — 전날 카트는 지워도 된다

**날이 바뀌면 전날 담아둔 카트는 쓸모가 없다**(그날 쿠폰·적립·카드할인 기준이 바뀜).
`buy/run.py` 가 담기 전에 돌리는 `clear_cart` 로 **전날 카트를 지워도 문제없고, 확인받을 필요도 없다.**
워크로그에 "카트 담긴 채 결제 대기"가 남아 있어도 그건 그날 안 쓰면 버리는 값이다. (사용자 지시 2026-08-05)

> 단 **같은 날 안에서** 결제 대기 중인 카트를 덮어쓸 때는 평소대로 알린다.

## ★ 워크로그 = 프롬프트 7개마다 + 세션 종료 시 (자동, 커밋·푸시까지)

**사용자 프롬프트 7개를 받을 때마다, 그리고 세션이 끝날 때 `WORKLOG_YYYY-MM-DD.md` 를 남긴다.**
시키지 않아도 자동으로. **워크로그만 쓰고 끝내지 말고 `git commit` + `git push` 까지 한 동작이다.**
같은 날 파일이 이미 있으면 새로 만들지 말고 **이어붙인다**(`━━━ N차 세션 ━━━` 구분선).

> 왜 7개마다인가: 세션 끝에만 쓰면 중간에 받은 지시가 유실된다. 실제로 "아이클타임 지워라"·
> "출력 순서 바꿔라" 지시가 기록되지 않아 2026-08-05 까지 반영이 안 된 채 남아 있었다.
> **사용자 지시는 코드에 반영하든 못 하든 그날 워크로그에 문장 그대로 남긴다.**

- 남길 것: **실행 결과(주문번호·건수·금액) / 발견한 사실 / 고친 것과 그 이유 / 실패와 원인 / 다음 할 일**
- 실패는 반드시 원인까지 — "실패했다"만 쓰면 다음 세션이 같은 걸 또 판다.
- 관측으로 확인한 것과 로그만 보고 추정한 것을 **구분해서** 쓸 것.
- 작업이 끝나면 **커밋까지** 한다(워크로그 + 그날 코드 변경).

> 왜: 8/2·8/3 세션이 워크로그를 안 남겨, 8/4 에 "어제 산 게 제대로 됐나" 를 확인하려고
> 주문내역·적립금·이벤트응모내역을 계정별로 다시 뒤져야 했다. 구매대장은 best-effort 라 8/2·8/3 이 0건이었다.

## 규칙
- 작업 지시 받으면 → 위 목록 읽기 → "흐름 이렇게 이해했다" 짧게 확인 → 작업. **읽기 전 코드 수정 금지.**
- 실결제(돈)·되돌리기 어려운 행동은 PIN 직전 금액 확인. 단 "N번~M번" 범위 지시는 중간 멈춤 없이 완주(자동메모리 lotte-range-run-no-pause).
- 추측 패치 금지 — 틀린 원인부터 파악(systematic-debugging). 실제 화면/코드 확인 후 수정.
- **페이지에서 뭘 읽을 땐 반드시 해당 영역으로 스코프를 좁힌다.** `document.querySelectorAll` 로
  전체를 긁으면 하단 **'최근 본 상품'·추천 캐러셀**이 섞여 들어온다 (2026-08-02: 현대몰 장바구니를
  그렇게 읽어 카트에 없는 상품을 '담겨있다'고 오독 → 사용자에게 잘못 보고). 장바구니는
  `rate-check/hmall.py cart_items()` 사용 — 카트 섹션(`div.shipping-listwrap`) 안에서만 읽는다.
- **관측이 도메인 상식과 모순되면 관측부터 의심한다.** "27조합 측정했는데 왜 무관한 상품이 있나"
  같은 모순은 그 자체가 내 읽기가 틀렸다는 신호다. 모순을 안은 채 보고하지 말 것.
- **★하단에 고정 버튼이 있는 화면에서 "그 텍스트는 없다"고 단정 금지** (2026-08-30 실사고).
  롯데 주문서는 `…원 결제하기` 가 하단에 **고정**돼 있어, 그 뒤로 밀린 행은 OCR 목록에서 통째로
  사라진다. 나는 잘린 스크린샷만 보고 `다른 결제수단` 이 "화면에 없다"고 결론내고, 그 전제로
  우회 코드까지 새로 썼다. 사용자가 "가장 아래쪽 주문 내역 확인 동의 위에 있는데 못찾은 거임"
  이라고 지적해 틀린 게 드러났다(OCR 은 (230,1610)에 **멀쩡히 읽고 있었다**).
  → **스크롤해서 고정버튼 위로 올린 뒤 다시 본다**(`_scroll_to(..., max_cy=PAY_BTN_TOP_CY)`).
- **★특수 케이스를 새로 파기 전에 기존 호출 규약을 제대로 썼는지부터 본다.** 위 사고에서 나는
  `WIN_ONLY_FIX` 분기를 하나 더 만들었는데, 실제 원인은 분기 부족이 아니라 **`max_cy` 인자 누락**
  이었다. 플랫폼 분기는 늘리지 말고 이미 있는 처방을 쓴다.

## ★ 롯데 결제 후 구매사은 적립은 **항상 최대로 신청한다**

> 사용자 지시 2026-08-30 (문장 그대로):
> "롯데건은 결제 끝나면 cft에서 항상 최대 x원 ,최대x % 포인트 적립신청하는걸로 해"

`claim_lotte_reward` 가 주문완료 뒤 구매사은 화면에서 신청한다. 화면에
`적립금 N원 적립가능! / 최대 혜택을 달성했어요 / 혜택 신청완료` 가 뜨는지 **확인하고 넘어간다.**
(2026-08-30 #1: 541,800원 구매 → 150,000원 기준 달성 → **15,000원 최대** 신청완료.)
구매사은은 신청기간 내 재진입이 되므로 놓쳐도 복구 가능하지만, **뷰티포인트는 주문완료 화면 only**
(now-or-never)라 순서를 바꾸지 말 것.
