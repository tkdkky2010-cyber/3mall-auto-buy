# WORKLOG 2026-05-31 — 현대 결제 버그수정 + 멀티카드 통합 계획

정본: `phone_auto/hmall_hyundai_buy.py` (compile OK). 무선 adb (IP 매번 바뀜 → `_resolve_serial` 자동).

## 오늘 결과 (실결제 검증)
- **#5 / #7 / #8 / #9** = 완전 자동 완주 (결제 + 뷰티포인트 적립).
  - #7·#8·#9 는 **첫 결제라 본인인증 name+birth 폼**(생년월일) 경로까지 통과 ✓ (한글키보드 globe + 이름/생년월일/성별, dump검증 1회 재시도 후 성공).
  - #8·#9 는 **현대카드 미등록** 계정 → **신용카드 선택 → 그리드 → 현대카드(즉시할인 5% 배지)** 자동 선택으로 완주.
- **#6 ⚠️ 구매금지(INACTIVE) 계정인데 실수로 결제됨 → 사용자가 주문취소(전액환불)**. 다음부터 #6 절대 결제 X.

## 오늘 고친 버그 (전부 hmall_hyundai_buy.py)
1. **전체선택이 주문에 1개만 반영** → `cdp_select_all`: basktList WebView가 **2개 모두 visible**(stale 3/3 + real 1/3), 첫 target만 클릭하던 버그. → **모든 basktList target 클릭 + native 화면 OCR의 (n/n) 검증**(CDP 아닌 화면이 ground truth).
2. **카드할인 false-positive** → `select_card_discount`: ① OCR이 '현대 5% 즉시할인 323,190원'을 덩어리/별도로 들쭉날쭉 → **cx 무관 금액패턴**으로. ② 하단 'NNN원 결제하기' 버튼 오탭 → **'결제하기' 들어간 행 제외**. ③ 검증을 '현대카드' 글자(배너 오탐) → **'적용' 토스트 / 결제수단 placeholder 사라짐**으로.
3. **카드 미등록 계정 처리** → `_pick_hyundai_from_grid`: 결제수단 '신용카드 선택'이면 탭 → 카드 선택 그리드 → '현대카드' 탭. (#7=등록 의심이었으나 실제 #8·#9 미등록 → 그리드로 해결, 등록 불필요)
4. **pay_hyundai**: 결제하기 후 '카드종류 선택/신용카드 선택'(카드 미등록) 뜨면 **"현대카드 미등록" 명확 실패** (false-positive 제거).
5. **resume 마커 순서**: PIN 입력 후 최종 확인화면이 헤더에 'PIN번호 결제' 유지 → **PAY_CONFIRM('결제합니다')을 PAY_METHOD('PIN번호 결제')보다 먼저** 판정 (안 그러면 무한루프, #6 실측).
- 교훈: **"ok 보고"는 CDP/내부값 아니라 native 화면 OCR로 검증.** WebView 중복 target 상존.

## 카드할인 / 카드선택 메커니즘 (실측 확정)
- 주문서 '카드할인' 섹션에서 **당일 할인카드(오른쪽 금액 적힌 행) 탭 → 결제수단 자동 그 카드 + 즉시할인 적용**.
- ⚠️ **'현대카드 Ed2 7% 청구할인'처럼 `>`만 있고 금액 없는 건 이벤트 안내 → 탭 금지.** 금액 카드 2개면 **왼쪽(첫번째)**.
- **카드 미등록 계정**: 결제수단 '신용카드 선택' → 탭 → '카드 선택' 그리드(현대카드=즉시할인5% 배지 등) → 그 카드 탭. **등록/카드번호입력 불필요.**

## 오늘 추가한 롯데 골격 (멀티카드 시작점)
- `LOTTE_FLOW` 상수, `pay_lotte()` (검증된 `lotte_card.json` flow[11:] FlowRunner 재사용 — 단 아래 #2 함정 미해결), CLI `lotte`.
- **롯데 흐름**(lotte_card.json, 5/29 검증, 주문 20260529075522): 원결제하기 → 로카페이(앱카드) 결제하기 → 롯데앱(com.lcacApp) 자동이동 → 로카페이 결제요청 결제하기 → **간편번호6(137601, 셔플이나 content-desc dump → source=dump)** → 확인 → hmall 복귀 자동 주문완료. (마지막 결제완료 step은 ignore_fail)

---

# 다음 할 일 — 멀티카드 통합 (계획 검토 완료, 코드 전)

## 4-레이어 구조
```
A 공통전(카드무관): 콜드런치(retry)→광고닫기→로그인→cart_state→goto_cart→cdp_select_all→구매하기→주문서
B 카드선택: detect_card()(당일 캐러셀) → select_card(card)(캐러셀 금액행 or 결제수단변경→그리드→<card>카드)
C 카드SDK(결제하기 포함, 카드마다 다름): CARD_HANDLERS[card]()
D 공통후(카드무관): 주문완료 확인 → close_home_popup(주문완료 광고) → beauty_reauth(뷰티포인트)
```

## 카드별 SDK
| 카드 | 상태 | 재사용 자산 |
|---|---|---|
| 현대 | ✅ 완성(오늘 견고화) | OCR 상태머신(PIN방식→PIN6→본인인증분기→카드비번) |
| 롯데 | 골격만 | lotte_card.json **롯데앱 네이티브 부분(간편번호 dump)** 재사용 + hmall-쪽 OCR 글루 |
| 하나 | 재사용 | hmall_pay.run_hana + hana_pin6(셔플) + hana_card.json |
| KB | 재사용 | hmall_pay.run_kb + kb_kbpay.json |
| 삼성/NH/BC | 신규(자산) | samsung_monimo/bc_paybook_isp.json + 각 키패드 preset |
| 키패드 OCR | 공유 | ocr_keypad presets (vision+gcv 2엔진, 실패시 4엔진) |

## 계획 검토에서 찾은 문제 8개 (반영해야 함)
1. **(최대위험) pay_hyundai 분해 = 현대 회귀 위험** → 구매하기·카드할인을 밖으로 뺄 때 **단계마다 현대 회귀검증**.
2. **dump 함정**: lotte flow는 hmall화면을 tap_dump_text로 누르는데 **hmall WebView는 dump 안 잡힐 수 있음** → 롯데도 **hmall-side=OCR / 카드앱 네이티브=flow재사용**으로 쪼개야. ("flow[11:] 통째 재사용"은 위험 — **먼저 주문페이지에서 tap_dump_text 먹히는지 30초 테스트**로 작업량 확정).
3. **detect_card는 계정별 아니라 당일별** (캐러셀=그날 프로모션, 모든 계정 동일) → **계정→카드 매핑 파일 불필요**.
4. handle_after_pay(본인인증)는 공통D 아니라 **현대 SDK(C)**. D=주문완료+뷰티만.
5. **결제하기 버튼은 공통 아님** → 각 카드 SDK(C) 첫 스텝(현대=OCR, 롯데=OCR).
6. **resume는 현대 전용**(detect_screen 마커 현대화면) → 비현대 SDK 실패 시 전체 재시작(후순위).
7. 롯데 카드선택: 일반 select_card(B) vs 검증된 lotte flow(step5~9) → **일반 B 쓰되 롯데로 실검증**.
8. 키패드 엔진 카드별 상이(현대2엔진/재사용4엔진/롯데dump) — 작동은 함. beauty_reauth가 모든 카드 동일 orderComplete 가정 — 비현대 미검증.

## 작업 순서
1. **현대 안전 분해 + 회귀검증** (A/B/C/D 레이어로 리팩터, 현대 안 깨지게).
2. **롯데**: dump 테스트로 작업량 확정 → 검증SDK 재사용 + 글루. 오늘 같은 날(현대 할인) 수동 카드선택 실결제로 검증.
3. 하나/KB: 검증흐름 재사용 통합 — 각 카드 가능일 검증.
4. 삼성/NH/BC: 신규 — 각 카드 결제일.
- ⚠️ **검증 제약**: 각 카드는 실결제로만 검증 (그 카드 할인일 또는 수동선택). 옛 코드(hmall_pay/flow_runner/coords/ocr_keypad)는 **자산 창고로 유지, 안 버림.**

## 핵심 파일
- 정본: `phone_auto/hmall_hyundai_buy.py` (현대 완성 + 롯데 골격 + resume + 멀티타겟 전체선택 + 카드할인/그리드)
- 자산: `phone_auto/coords/apps/{lotte_card,hana_card,kb_kbpay,samsung_monimo,bc_paybook_isp}.json`, `phone_auto/hmall_pay.py`, `phone_auto/flow_runner.py`, `phone_auto/ocr_keypad.py`
- 로그: `logs/2026-05-31-hyundai-*.log`

---

# 멀티카드 통합 — 구현 완료 (2026-05-31 저녁) — 현대+롯데 자동 end-to-end 검증

## 결과
- **현대(캐러셀) + 롯데(그리드+롯데앱 SDK) 둘 다 자동 완주 검증** (#4 둘 다 DONE+뷰티). 디스패처 작동.
- `buy_one(idx, card=None)` 디스패처: **card 미지정=당일카드 자동감지 / 지정=강제**. CLI `... 롯데 4` = 롯데 강제.

## 4-레이어 (구현됨)
```
A 공통전: 콜드런치→광고닫기→로그인→cart_state→goto_cart→cdp_select_all→구매하기→주문서   [카드무관]
B detect_card(당일 캐러셀 카드사) → select_card(card, day)
C 카드SDK: 현대=pay_hyundai(결제하기→PIN→본인인증→카드비번) / 롯데=pay_lotte(OCR+롯데앱 dump재사용)
D 공통후: 주문완료 → close_home_popup → beauty_reauth(뷰티포인트)   [카드무관]
```

## ★ 카드선택 규칙 (다음에 롯데 할인날 오면 — 코드수정 없이 그냥 됨)
- **롯데 할인날(카드할인 캐러셀에 롯데 뜸)**: `python3 -m phone_auto.hmall_hyundai_buy 4` (card 미지정)
  → `detect_card`가 캐러셀에서 **롯데** 감지 → `select_card(롯데, 롯데)` → `day==card` → **캐러셀 선택(할인 적용)** → pay_lotte.
  → **그날은 그리드 안 씀, 캐러셀 자동.** select_card가 알아서 분기. **그냥 돌리면 됨.**
- **오늘처럼 롯데 비할인날 테스트**: `... 롯데 4` 강제 → `day(현대)≠card(롯데)` → **결제수단변경+그리드 스크롤**로 롯데 선택(할인 없음).
- ⚠️ 캐러셀에 있는 당일카드를 굳이 그리드로 우회 금지(오류↑) — select_card가 자동 회피.

## ⚠️⚠️ 오늘 실수/함정 (재발방지 — 다음 카드 추가/롯데 할인날 반드시 확인)
1. **전체선택 (n/m) 오탐**: 화면 첫 "(n/m)"이 **추천 캐러셀**일 수 있음(#4: 카트1개인데 캐러셀'(1/7)' 읽음) → 반드시 **"일반상품 (n/m)" 행에서만** 판정(`_cart_count`). + **CDP basktList는 분절**(stale/1개짜리 WebView 동시 visible)이라 신뢰X → **native '일반상품' 헤더 체크박스 직접 탭**.
2. **detect_card 토큰 분리**: OCR이 '현대 5% 즉시할인'과 '255,968원'을 **별도 item**으로 쪼갬 → 금액행만 보면 카드명 못 찾음 → **카드할인 영역 전체에서 카드사 토큰 검색**(위=첫번째 당일카드).
3. **detect_card 스크롤 필수**: 구매하기 직후 주문서 상단=상품정보, **카드할인은 아래** → 스크롤하며 찾기.
4. **`_pick_card_from_grid` 스크롤 2곳**: ① 결제수단변경/신용카드선택 opener가 화면 아래 → 스크롤 ② 카드 그리드 길어서 타겟 카드 아래 → 그리드 스크롤.
5. **hmall=WebView→dump 불가**(uiautomator 0 text node 실측) → hmall-side 전부 **OCR**. **카드앱 네이티브(롯데 com.lcacApp)=dump 가능** → 간편번호 셔플도 content-desc dump로 정확. **롯데 flow 통째 재사용 X — hmall-side(원결제하기·로카페이결제하기)는 OCR로 쪼개고, 롯데앱부분(flow[14:])만 FlowRunner**.
6. **카드 미등록 계정**: 결제수단 '신용카드 선택' placeholder → 그리드에서 카드 선택(**등록/카드번호입력 불필요**). 결제하기 후 '카드종류 선택' 뜨면 명확히 실패 보고.
7. **계정/카드 실수**: card 미지정으로 돌리면 **당일카드(오늘=현대)로 감** → 특정 카드 테스트는 **반드시 `카드명 idx`로 강제**. (오늘 #4를 `4`로 돌려 현대로 잘못 진행한 실수 → `롯데 4`로 정정.)
8. **#6 = INACTIVE 구매금지.** 비당일 카드 테스트는 실결제(할인없음)라 신중.

## 다음 카드 추가 패턴 (하나/KB/삼성/NH/BC — 각 카드 결제일에)
1. `CARDS_SUPPORTED`에 카드 추가 + `CARD_GRID_NAME`에 그리드 표기명 추가.
2. `pay_<card>()` 작성: **hmall-side(원결제하기·카드결제방식)=OCR** + **카드앱 네이티브=dump/flow재사용**(기존 coords/keypad/hmall_pay 로직 활용).
3. `buy_one` 디스패처에 `elif use_card == "<카드>"` 추가.
4. 그 카드 결제일(또는 `카드명 idx` 강제)로 실검증. ⚠️ 위 함정 1~8 확인.

## ✅ KB국민카드 SDK 완성 (라이브검증 2026-05-31, #1 주문 20260531079448)
- **루트**: 원결제하기(OCR) → **'KB Pay 결제' 노란박스(OCR, 정확매칭)** → KB앱(`com.kbcard.cxh.appcard`) 자동이동 → **결제하기(tap_dump_text)** → [FLAG_SECURE 비번화면] **간편번호6(137601, content-desc dump, 자동제출)** → hmall 복귀 주문완료. `pay_kb()` + `_wait_app(pkg)` 코드화.
- **핵심 발견**:
  - **KB앱 = FLAG_SECURE**(화면캡처 검정) **but uiautomator dump O** → 버튼/키패드 전부 content-desc로 정확(롯데와 같은 dump 패턴). screencap/OCR 대신 dump 써야 함.
  - **간편번호 6자리 = 자동제출** — draft의 '입력완료' 탭 **불필요**(실측: 입력완료 dump 실패해도 결제 완료됨). `input_pin source=dump`만으로 완주.
  - KB앱 결제하기/키패드 **좌표 하드코딩 대신 dump_text/input_pin dump**로 견고화(draft 미검증 좌표 폐기).
- **⚠️ #1 뷰티포인트 놓침 (타이밍 교훈)**: 수동으로 한 단계씩 몰며 dump 확인하느라 지체 → **주문완료 페이지가 곧 home(`initApp`)으로 자동이동**, orderComplete WebView가 `vis=hidden` 됨 → `beauty_reauth` "no orderComplete target" 실패. **디스패처 플로우(pay_kb→즉시 close_home_popup+beauty)에선 잡힘**(현대/롯데처럼). → **교훈: pay_kb는 지체 없이 완주, 주문완료 직후 즉시 뷰티 처리해야 함.** #1 적립 1건은 손실(테스트 계정).

## ★ 카드할인 캐러셀 별칭/예상표 (오결제 방지 — CARD_ALIASES)
- **실측**: 현대 할인날 캐러셀 = `"현대 5% 즉시할인"` + `"<금액>원"`(OCR 별 item). **그리드명(현대카드) 아니라 짧은 토큰(현대)** 으로 뜸. 금액은 카트합계마다 달라지는 무의미값 → 매칭은 토큰으로만(금액은 '선택가능 할인행 존재' 판정에만).
  - ※ 과거 디버깅 때 본 `323,190원`은 **상품 1개만 잘못 선택된 카트** 금액이라 예시로 무의미.
- **⚠️ 안전 이슈**: 별칭 누락 시 `detect_card=None` → `use_card`가 **'현대'로 폴백** → 삼성 할인날에 현대 오결제 위험. → `CARD_ALIASES`로 변형표기 망라해 detect 견고화.
- **예상 캐러셀 표기 → 토큰(별칭) → 동작** (첫 실제 할인날 첫 런에서 검증):

  | 카드 | 예상 표기 | 별칭 | 결과 |
  |---|---|---|---|
  | 현대 | `현대 N% 즉시할인` ✅실측 | 현대 | pay_hyundai |
  | 롯데 | `롯데 N% 즉시할인` | 롯데/롯데카드 | pay_lotte |
  | KB | `KB국민 N%`/`KB N%`/`국민 N%` | KB국민·KB·국민·케이비 | **pay_kb** |
  | 하나 | `하나 N%` | 하나 | UNSUPPORTED(SDK 미구현) |
  | 삼성 | `삼성 N%` | 삼성 | UNSUPPORTED |
  | NH | `NH농협 N%`/`농협 N%` | NH농협·NH·농협 | UNSUPPORTED |
  | BC | `BC N%`/`비씨 N%` | BC·비씨·페이북 | UNSUPPORTED |
  | 신한/우리 | (그날 뜨면) | 신한·우리 | UNSUPPORTED |

- SDK 없는 카드는 detect는 되되 `buy_one`이 `UNSUPPORTED_CARD:<카드>`로 **안전 정지**(현대 오폴백 방지). SDK 추가 시 `CARDS_SUPPORTED`에 넣으면 즉시 활성.
- **미검증 1개**: 캐러셀 **KB행 탭 → 결제수단 KB 세팅** 여부(현대는 검증, 메커니즘 동일이라 高확률). detect 텍스트는 별칭으로 거의 안전.

## TODO (요청, 미완)
- **카드공통 resume**: stage-detector(카트/주문서/카드SDK화면/주문완료 감지 → 그 단계부터 이어가기). 현재 resume는 현대 화면 마커만 — 롯데앱/로카페이/KB앱 마커 + 카드SDK 재개 분기 추가 필요.
- **KB 디스패처 풀검증**: `KB <idx>`로 pay_kb + 즉시 beauty 한 번에 완주(다른 계정/카트 필요). 캐러셀 KB행 세팅 검증은 실제 KB 할인날.
