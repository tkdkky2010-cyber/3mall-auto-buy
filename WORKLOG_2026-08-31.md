# WORKLOG 2026-08-31 — 롯데 조합25 #8~14 결제 7/7 (윈도우, 하나카드)

## 1. 결과

| # | 계정 | 주문번호 | 결제금액 | 뷰티 | 구매사은 |
|---|---|---|---|---|---|
| 8 | jye40323 | 2026-08-31-D96691 | 538,994 | ✅ | ✅ |
| 9 | wlstmdlsfk | 2026-08-31-D97182 | 539,076 | ✅ | ✅ |
| 10 | kim455307 | 2026-08-31-D96032 | 539,376 | ✅ | ✅ |
| 11 | diinamic | 2026-08-31-D97922 | 539,076 | ⚠️ **확인 불가** | ✅ (사후 회수, 최대 15%) |
| 12 | leenamsu0318 | 2026-08-31-D98438 | 539,074 | ✅ | ✅ |
| 13 | Lee0128 | 2026-08-31-D99630 | 539,074 | ✅ | ✅ |
| 14 | johwajeong2762 | 2026-08-31-D99870 | 539,074 | ✅ | ✅ |

- 카드 = **하나** 전 계정. 청구할인 배너에 `비씨 5%` / `하나 5%` 가 나란히 떠 손해 없음.
- 가드 `MAX_PAY=560000` 전 계정 통과.
- ⚠️ #11 뷰티포인트는 주문완료 화면 only(now-or-never)라 **자동 회수 불가**. 저장소에
  확인 도구도 없다(`beautypoint.json` 은 자격증명만, 이를 쓰는 코드 없음) → 사람이 확인할 것.

## 2. ★★ 오늘의 결론 — 실패의 공통 뿌리는 "관측 실패를 사실로 단정"

윈도우로 넘어온 뒤 실패가 몰린 이유는 카드사도, 몰도 아니었다. **화면을 못 읽었을 때
'없다'고 단정하고 `ok:True` 로 넘긴 코드**가 곳곳에 있었고, 맥은 Apple Vision OCR 이
화면을 직접 읽어 그 경로를 안 타서 드러나지 않았을 뿐이다.

같은 형태로 5건이 나왔다.

| 증상(로그에 찍힌 이름) | 실제 원인 | 어떻게 확인했나 |
|---|---|---|
| `CARD_FAIL:'다른 결제수단' 미도달` ×3 | 그 행이 cy≈2141 로 밀려 **하단 고정 결제하기 버튼에 덮임** | OCR 은 (230,1610)에서 멀쩡히 읽음 → 판독이 아니라 위치 |
| 카트 `e` 1개 (금액 436,500) | `+` 4개 중 앞 2개가 숨김 → `.first` 가 숨긴 걸 집어 타임아웃 → `except: break` 로 조용히 통과 | `order_count` 실측, 수정 후 538,994 |
| 지출증빙만 켜지고 사업자번호 공란 | 라디오(cx 624)가 아니라 **라벨 글자(cx 733)** 를 탭 | 탭 후 OCR 에 `㉧ 소득공제` 그대로 |
| `PAY_FAIL@hana_modal` | ㉠ 현금영수증 발급방식 팝업이 결제를 막음 ㉡ 화면 중간 `결제하기` 오탭(@540,1968) | `_screen_debug` 로 팝업 직접 확인 |
| `LOGOUT_FAIL` ×3 | 폰이 **카드앱에 갇힘** (하나앱 결제화면 / 현대카드 앱) | `mCurrentFocus` 로 확인 |

### 2-1. 이름이 원인을 가린다

`PAY_FAIL@hana_modal` 은 하나 경로를 의심하게 만들었지만 실제 원인은 현금영수증과
버튼 오탭이었다. `LOGOUT_FAIL` 은 롯데 로그인을 의심하게 만들었지만 실제로는 앞선 계정이
남긴 카드앱이었다. **실패 지점의 이름 = 원인이 아니다.** 그래서 검수를 넣었다(§3).

### 2-2. 한 계정의 실패가 뒤 계정을 무너뜨린다

#8 이 하나앱에서 죽자 폰이 `com.hanaskcard.paycla/OnlinePaymentActivity` 에 갇혔고,
#9·#11 이 롯데 화면에 닿지도 못하고 연달아 `LOGOUT_FAIL`. `reset_lotte_app` 이 롯데앱만
force-stop 했기 때문이다. 연쇄를 끊는 게 개별 실패를 고치는 것보다 이득이 크다.

## 3. ★ 신설 — 계정 실패 검수 (`phone_auto/fail_audit.py`)

> 사용자 지시 (문장 그대로):
> "윈도우결제는 지금 모든 카드 다 최초이니까 앞으로 윈도우에서 결제할 때는
>  롯데몰, 현대몰 가릴거없이 모두 계정실패 왜 실패했는지 검수하는 과정 넣어놔"

실패 즉시(다음 계정 콜드런치가 화면을 날리기 전에) 남긴다 — 포그라운드 액티비티 /
살아있는 카드앱 / 화면 텍스트(OCR+dump, cy순) / 스크린샷.
출력 `phone_auto/_tmp/fail_audit/<날짜>/audit.jsonl` + 로그 블록. **읽기 전용**(탭·종료 안 함).
롯데·현대 양쪽 계정 루프에 연결. `DONE`/`STOP_BEFORE_PAY`/`*_HANDOFF` 는 검수 대상 아님.

바로 값을 했다 — #13 `CART_FAIL` 의 원인이 **실제 현대카드 결제 알림 배너**(뚜레쥬르 8,400원)가
카트 아이콘(1002,148)을 덮은 것이라고 첫 실패에서 특정됐다. 종전이면 또 추측했을 자리다.

⚠️ 오탐 하나 — '살아있는 카드앱'에 `com.samsung.android.spay`(상시 시스템 서비스)가 잡혀
원인처럼 표시됐다. **포그라운드가 카드앱일 때만** 연쇄 원인으로 표시하도록 고쳤다.

## 4. ★ 롯데 적립 "cft에서" 는 **사이트가 막아서 불가능**

> 사용자 지시 2026-08-30: "롯데건은 결제 끝나면 cft에서 항상 최대 x원 ... 적립신청하는걸로 해"

CFT 경로(`buy/lotte_reward.py`)를 만들어 실제로 돌렸다. 상품상세 → 구매사은 카드 →
광세일 행사페이지까지 정상 도달하지만, **'혜택 신청하기' 클릭 시 alert 로 거부된다**:

```
"'롯데홈쇼핑 앱'에서 응모 가능 합니다."
```

행사안내 문구도 "롯데홈쇼핑 모바일 앱(APP)으로 구매 후 신청 시" 다. → **적립은 폰 경로.**
모듈은 남겨둔다(앱 전용이 아닌 이벤트엔 그대로 동작, 앱 전용이면 `app_only: True` 로 이유를 밝히고 멈춤).

⚠️ 이 모듈 첫 판이 **눌렀다는 이유로 `ok:True`** 를 돌려줬다 — 오늘 하루 걷어낸 바로 그 안티패턴을
새로 만든 것이라 즉시 고쳤다(`ok = completed`). 같은 실수는 새 코드에서도 난다.

## 5. 수정 목록

| 파일 | 수정 |
|---|---|
| `lotte_homeshopping_buy.py` | 카드 섹션 `max_cy` / 지출증빙 **라디오** 탭 + `사업자` 등장 검증 / `set_cash_receipt` 가 **포인트 사용량**으로 필수 여부 판정 / `entered:False` → `CASH_RECEIPT_FAIL` 로 결제 전 중단 / 금액 판독 실패 시 `AMOUNT_UNREADABLE` 중단 / 하나·삼성도 `_tap_pay_button` 사용 / `reset_lotte_app` 이 카드앱 정리 + **롯데앱 포그라운드 검증 3회** / 카트 진입 전 `statusbar collapse` / 하나 모달 미도달 시 `_screen_debug` |
| `buy/sulwhasoo.py` | 카트 수량 = `order_count`(보이는 행만) 로 **검증**, 값이 실제로 오르는 '+' 만 사용 |
| `phone_auto/hmall_hyundai_buy.py` | 실패 검수 연결 |
| `fail_audit.py`, `buy/lotte_reward.py` | 신설 |
| `READ_FIRST.md` | 적립 CFT 불가 실측 기록 |

## 6. 다음에 주의할 것

- **`max_cy` 는 만능이 아니다.** 현금영수증 탐색에 `max_cy=1100` 을 걸었다가 섹션을 못 찾아
  '섹션 없음(L.POINT 0)' 으로 오판 → 결제가 팝업에 막혔다(#9·#10). 상한은 **탭 대상**에만 건다.
- **필수 여부는 화면이 아니라 이미 아는 사실로 판정한다.** 현금영수증 필수 여부는
  `포인트 used` 로 갈린다 — 같은 로그에 `used:1` 과 `L.POINT 0` 이 나란히 찍혔던 게 증거다.
- 하나앱은 연속 호출 시 '앱을 다시 실행해주세요' 로 막는다. `force-stop` 만으론 안 풀리고
  **계정 간 2분 간격**이 실효가 있었다(8/28 기록과 동일).

## 7. 세션 후반 — #11 적립 회수 / CFT 검증 / step2 구매계획

### 7-1. #11 구매사은 적립 사후 회수 성공
주문완료 화면을 놓쳐도 **`goods_no` 검색 경로로 회수된다**(실측):
`claim_lotte_reward(goods_no="2923406968")` → `{'card': '최대 15% 적립', 'completed': True}`.
로그인만 그 계정으로 맞춰주면 된다. 반면 **뷰티포인트는 회수 불가**(주문완료 화면 only)이고
`beautypoint.json` 은 자격증명만 있을 뿐 이를 쓰는 코드가 없어 **자동 확인 수단도 없다**.

### 7-2. 적립 CFT 경로는 사이트가 막는다 (§4 참조)
`buy/lotte_reward.py` 로 실제 확인. alert `"'롯데홈쇼핑 앱'에서 응모 가능 합니다."`.
READ_FIRST 에 기록했다. 다음에 같은 길을 가지 말 것.

### 7-3. step2(현대몰 식품) 9/23 구매계획 — 계산 결과만 기록

대상 제품(전부 `P202608247695` 잘사는 추석 10%적립, **9.23 02:00 마감**)과 단가(`kakao_price`):

| id | 제품 | 단가 |
|---|---|---:|
| 2 | 하루견과 초록 100봉 | 76,864 |
| 3 | 하루견과 갈색 100봉 | 75,668 |
| 22 | 올바른건강식품 와이 9박 | 63,022 |
| 25 | 유기농 석류젤리 9박 | 52,364 |

**핵심 제약 = 아이디당 품목별 하루 2개.** 이러면 병목이 금액이 아니라 **일수**가 된다.
아이디 하나의 하루 최대 = 4품목 × 2개 = 8개 = **535,836원** → 300만원에 최소 6일.

| 구성 | 아이디당 | 필요일수 | 병목 |
|---|---:|---:|---|
| 균등 11/11/12/11 | 3,010,120원 / 45개 | 6일 | 와이 12개 |
| 4배 17/17/4/4 (2·3 을 22·25 의 4.25배) | 3,054,588원 / 42개 | **9일** | 견과 17개 |

**하루 5아이디 × 22일(9/1~9/22) = 110 아이디·일** → 4배안은 아이디당 9일이라
**최대 12아이디**만 300만원 완주 가능. 20아이디는 이 조건에서 불가능하다.

**아이디 1개의 9일 패턴** (품목별 하루 2개 준수 — 실행 시 이 표대로)

| 일차 | 구성 | 수량 | 금액 |
|---|---|---:|---:|
| 1일차 | 초록2 + 갈색2 + 와이2 + 젤리2 | 8개 | 535,836 |
| 2일차 | 초록2 + 갈색2 + 와이2 + 젤리2 | 8개 | 535,836 |
| 3일차 | 초록2 + 갈색2 | 4개 | 305,064 |
| 4일차 | 초록2 + 갈색2 | 4개 | 305,064 |
| 5일차 | 초록2 + 갈색2 | 4개 | 305,064 |
| 6일차 | 초록2 + 갈색2 | 4개 | 305,064 |
| 7일차 | 초록2 + 갈색2 | 4개 | 305,064 |
| 8일차 | 초록2 + 갈색2 | 4개 | 305,064 |
| 9일차 | 초록1 + 갈색1 | 2개 | 152,532 |
| **계** | 초록17 · 갈색17 · 와이4 · 젤리4 | **42개** | **3,054,588** |

와이·젤리는 4개뿐이라 **앞 이틀에 몰아 끝내고**, 나머지 7일은 견과(2·3)만 채운다.
견과 17개 ÷ 하루 2개 = 9일이 곧 아이디당 소요일수다.

확정안 = **10아이디 / 5개씩 2조 / 조당 9일 연속** (1조 #1~#5 9/1~9/9, 2조 #6~#10 9/10~9/18,
버퍼 9/19~9/22). 총 30,545,880원 / 420개 / 적립 300만P(9.82%).
제품별 총 필요량: 초록 170개 · 갈색 170개 · 와이 40개 · 젤리 40개.
12아이디(3,665만원)가 산술적 최대지만 **버퍼 0일**이라 권하지 않는다 — 티어를 놓치면
200만 티어로 떨어져 계정당 10만P를 잃는다.

⚠️ 미확인 전제 2개 (실행 전 반드시 확인):
- **300만 티어가 계정 누적인지** — 주문 단위면 2개씩 사는 주문이 전부 10만 티어에 걸려
  계정당 24만P 로 떨어진다(계획 전제는 '기간 내 계정 누적').
- **계정당 결제 한도** — 아이디 하나가 300만원을 쓴다. 카드·계정 한도에 걸리면 일정이 무너진다.
→ 9/1 첫날은 5아이디 전부가 아니라 **1~2아이디로 먼저 돌려** 한도·재고·적립 반영을 확인할 것.

---

# ━━━ 2차 세션 (맥, 야간) — 현대몰 식품 9/23 캠페인 착수 ━━━

## 0. 사용자 지시 (문장 그대로)

1. "8.31 - 9.23 까지 300만원 구매하고 2,3,22,25 구매하는거"
2. "현대몰 전체를 통해서 하루에 5계정 구매가능 / 상품 품목별로 2개씩 구매가능 /
   아이디별로 4개 상품 구매 모두 가능 / 현대몰 식품 조합이야 워크로그 살펴봐 제대로 오늘워크로그"
3. "견과류 4: 나머지1 비율로 구매해서 300만이상"
4. "B안으로 할건데 그럼 계정별로 17 17 4 4 개씩 사는거야?"
5. "2/2/2/2 개사니까 5% 카드할인받고 535837 원 결제되는데?"
6. "괜찮아 나머지 마지막날들을써가지고 내가조치할거야 일단 290만원이상을 채우는게 목표야 / 4-5 계정 구매진행해"
7. "다시해봐"

> ⚠️ 내가 처음에 `2,3,22,25` 를 **설화수 조합번호**로 읽었다. 실제는 **식품 상품번호**다.
> 사용자가 "현대몰 식품 조합이야" 로 정정했다. 조합/상품 번호 체계가 둘 다 존재하므로
> 다음에도 **어느 체계인지 먼저 확인**할 것.

## 1. 확정된 캠페인 (§7-3 계획을 사용자 제약으로 재계산)

이벤트 `P202608247695` **잘사는 추석 10%적립**, 마감 **9.23 02:00**.
티어 100만→10만P / **300만→30만P** / 1,000만→100만P.

**제약**: 현대몰 전체 하루 5계정 / 아이디당 품목별 하루 2개 / 4품목 전부 가능
→ 계정 하루 최대 8개 = **535,837원**.

**확정 = B안** (견과 : 나머지 ≈ 4:1) — 계정당 **초록17 · 갈색17 · 와이4 · 젤리4 = 42개 = 3,054,590원**, **9일**.

| 일차 | 구성 | 금액 |
|---|---|--:|
| 1~2 | 초록2 갈색2 와이2 젤리2 | 535,837 |
| 3~8 | 초록2 갈색2 | 305,064 |
| 9 | 초록1 갈색1 | 152,532 |

- 계정 배정: **#1~#10 = 300만 완주** / **#11~#15 = 9/23까지 가능한 만큼**(사용자 지시, 미달 허용).
- 슬롯: 8/31~9/22 = 23일 × 5 = **115**. 11계정이면 99슬롯(버퍼 16), 12계정이면 108(버퍼 7).
- 목표선은 사용자가 **290만원 이상**으로 완화했다("나머지 마지막날들을 써서 내가 조치").

### 금액 검증 (주문서 실측, #5 카트)

```
정가 합            642,020
쿠폰 -77,982   →   564,038      ← '총 결제금액'
KB국민 5% 즉시할인 →  535,837      ← '즉시할인' 행
```
- **카트 화면은 쿠폰 전 금액**(정가×0.95 = 609,919)을 보여준다. 카트만 보고 판단하면 안 된다.
- 8/30 워크로그가 남긴 `170,810`(초록 2개)도 **카트 판독값**이지 결제액이 아니다.
- 사용자가 지적한 **535,837** 이 맞다. 내가 단가를 2개가격÷2 로 쪼개며 0.5원씩 버려 1원 어긋났다.
  → **라인 금액(kakao_price)을 그대로 더한다.** 단가 환산 금지.

## 2. 실행 결과 — 담기 5/5, **결제 5/5** (주문내역 실측)

| # | 계정 | 주문번호 | 결제금액 | 카드청구 | H.Point | 결제 주체 | 적립 |
|--:|---|---|--:|--:|--:|---|---|
| 1 | tkdkky2002 | 20260831119419 | 535,837 | 535,837 | 0 | 사용자 | ✅ 이미신청 |
| 2 | 1plu1mall1 | 20260831119625 | 535,859 | 535,859 | 0 | 사용자 | ✅ 이미신청 |
| 3 | kgi5907@daum.net | 20260831119719 | 535,837 | 535,837 | 0 | 사용자 | ✅ 이미신청 |
| 4 | 1plus1mall0 | 20260831123467 | 537,255 | 508,895 | 28,360P | 코드 | ✅ 신규신청 |
| 5 | 1plus1mall1 | 20260831123992 | 537,742 | 499,632 | 38,110P | 코드 | ✅ 신규신청 |
| | **합계** | | **2,682,530** | **2,616,060** | **66,470P** | | |

- 전 계정 **4품목 × 2개**, **KB국민카드 일시불**, 주문금액 642,020 / 쿠폰할인 77,982 동일.
- 담기 = `buy/run.py 1-5` (비우기 5/5, 담기 20/20).
- 구매대장 20행(5계정 × 4품목) 기록 완료. #1~#3 은 사용자 결제분이라 **주문내역 실측 후 사후 기록**했다.

### ★ 포인트를 쓰면 **총액이 오른다** (실측으로 처음 확인)

카드 즉시할인 5% 가 **카드 청구분에만** 붙는다. 포인트로 낸 만큼 할인 대상이 줄어든다.

| # | 카드 즉시할인 | 포인트 | 결제금액 |
|--:|--:|--:|--:|
| 1 (포인트 X) | 28,201 | 0 | 535,837 |
| 4 (포인트 O) | 26,783 | 28,360P | **537,255** |

→ 28,360P 를 써서 현금 28,360원을 아꼈지만 **즉시할인 1,418원을 잃었다**. 순이득이긴 하나,
   **티어가 '카드청구' 기준이면 포인트가 누적을 깎는다**(아래 §5).

### ★ 폰 로그의 금액은 '카드 청구액'이다

`[#4] 결제 예정 금액: 508,895원` 은 **카드 청구액**이고 주문의 결제금액은 **537,255원**이다.
둘을 같은 것으로 보고 보고하면 틀린다. 주문내역 `결제/취소내역` 이 정본:
`신용카드 508,895원 + H.Point 28,360P = 537,255`.

### 9일차 완주 시 계정별 전망

| # | 결제금액 기준 | 카드청구 기준 |
|--:|--:|--:|
| 1·3 | 3,054,590 | 3,054,590 |
| 2 | 3,054,612 | 3,054,612 |
| 4 | 3,056,008 | 3,027,648 |
| 5 | 3,056,495 | 3,018,385 |

어느 기준이든 **300만 돌파**. 사용자 목표선 290만원은 여유 있게 넘는다.

## 3. ★ 근본원인 — 콜드런치 직후 **플레이스토어가 포그라운드를 뺏는다**

#4·#5 가 1차 시도에서 연속으로 `EXC:webview_devtools 소켓 없음` 으로 죽었다.
앱/계정 문제가 아니라 **대기 부족**이었다. 실측 폴링:

```
10s  socket=0  focus=null
20s  socket=0  focus=com.hmallapp/initActivityAliasDefault
30s  socket=0  focus=com.android.vending/AssetBrowserActivity   ← 플레이스토어
70s  socket=0  focus=com.android.vending/AssetBrowserActivity
80s  socket=1  focus=com.hmallapp/main.MainActivity             ← 정상
```

앱 버전 7.0.8 / 최종 업데이트 5/18 → **업데이트 안내가 플레이스토어를 띄운다.**
`hmall_webview.reset_to_main` 의 소켓 대기 상한이 **12초**라 그 앞에서 포기했다.

**수정** (`phone_auto/hmall_webview.py`):
- 상한 **12s → 120s**.
- 대기 중 `topResumedActivity` 가 `com.android.vending` 이면 **back** 으로 빠져나오고,
  hmall 이 아니면 monkey 로 다시 앞으로 보낸다.
→ 재실행에서 로그인 chooser 통과, **#4 결제 77초 완주**.

**교훈**: "소켓 없음"은 판독 실패가 아니라 **다른 앱이 앞에 있다**는 뜻일 수 있다.
고정 타임아웃은 남의 앱이 끼어드는 날 터진다.

## 4. ★ 검수 배선 버그 — `name '_texts' is not defined`

어제 신설한 `fail_audit` 호출부를 롯데에서 현대로 **그대로 복사**해서, 현대 모듈에 없는
`_texts` 를 넘기고 있었다. 실패할 때마다 검수가 `NameError` 로 죽어 **증거를 못 남겼다**
(오늘 #4·#5 첫 실패가 정확히 그랬다 — 원인 특정을 손으로 해야 했다).

**수정**: 현대의 정본 관용구(`_ocr_texts(cap()) + _dump_texts()`)를 lambda 로 넘긴다.
> 검수 코드가 조용히 죽으면 **검수가 없는 것보다 나쁘다** — 있다고 믿게 만든다.

## 5. 미확인 / 주의

### ★ 포인트 정책 확정 (사용자 지시, 문장 그대로)
> "포인트 다 써야해 안넘으면 제품한개더사면돼"

**전액사용 유지**, 모자라면 9일차에 1개 추가. 포인트를 끄자는 내 제안은 기각됐다.
포인트 전액사용을 유지해도 **5계정 전부 카드청구 기준 300만 돌파**한다(가장 빠듯한 #5 가 +18,385).
9일차는 초록1·갈색1 뿐이라 상한까지 383,780원을 더 얹을 여유가 있다.
⚠️ 이벤트 적립 30만P 가 캠페인 도중 지급되면 전액사용이 그걸 다 써 카드청구 누적이 급감한다 —
9일차 직전에 누적 확인 후 부족분을 추가할 것.

- [ ] **300만 티어의 기준액이 '결제금액'인지 '카드청구'인지 미확인.** #4·#5 처럼 포인트를 쓰면
      둘이 최대 38,110원 벌어진다. 카드청구 기준이면 #5 가 3,018,385 로 마진 18,385원까지 좁아진다.
      → 포인트가 많은 계정은 **캠페인 중 포인트 사용을 끄거나** 마지막 날 1개를 더 얹어 보전할 것.
- [ ] **티어가 계정 누적인지 주문 단위인지도 여전히 미확인.** 주문 단위면 하루 53만 주문이
      전부 50만 티어(5만P)에 걸려 계정당 45만P 로 떨어진다. #1~#5 적립 반영을 며칠 뒤 확인.
- [ ] 구매대장 `amount` 는 **즉시할인가 합(535,837)** 이라 실제 결제금액·카드청구와 다르다.
      티어 추적을 대장 합산으로 하면 틀린다 → `cart/today_carts.json` 의
      `paid_amount`/`card_charged`/`points_used` 를 정본으로 쓸 것(이번 세션에 신설).
- [ ] `#4` 주문번호 판독 실패 — 주문내역 실측으로 사후 보완했다(20260831123467).
      반복되면 8/30 롯데의 `confirm_order_via_cdp` 류 폴백을 현대에도 붙일 것.

### 주문내역 조회 경로 (이번에 확인 — 다음에 헤매지 말 것)

```
/mo/mpf/selectMyPageMain   ← 마이페이지 (홈의 '마이페이지' 링크는 JS 드로어라 이동 안 됨)
  → a '주문/배송 상세' 클릭 → /mo/mpa/selectOrdPTCPup?ordNo=<주문번호>
```
- `/mo/mpa/ordList` 류는 **전부 404**다(12개 후보 전멸). 추측하지 말고 마이페이지에서 타고 들어간다.
- PC 홈은 `#modal-root` 팝업이 클릭을 가로챈다 → `닫기|오늘 그만` 먼저 누를 것.
- 금액 정본은 `결제/취소내역` 블록: `신용카드 N원 + H.Point NP` 가 `결제금액` 과 합이 맞는다.

## 6. 변경 파일

| 파일 | 변경 |
|---|---|
| `phone_auto/hmall_webview.py` | 소켓 대기 12s→120s + 플레이스토어 back 이탈 (§3) |
| `phone_auto/hmall_hyundai_buy.py` | fail_audit `texts_fn` NameError 수정 (§4) |
| `buy/cart_plan.json` | 식품 2·3·22·25 × qty2 × 계정1~5 (이전 설화수 플랜은 `.bak_20260831_sulwha`) |
| `cart/today_carts.json` | 8/31자 재생성 (이전 8/30자는 `.bak_20260830`), #4 paid |
| `WORKLOG_2026-08-31.md` | 2차 세션 (이 절) |

## 7. 다음 세션 (9/1)

1. step1/step2 재실행 → `today.json` 갱신 (날짜 바뀌면 카트도 재담기).
2. 담기 = **#1·#2·#3·#5 + 신규 1계정** 으로 5슬롯을 채우면 오늘 잃은 슬롯을 만회한다.
   (#4 는 1일차를 끝냈으므로 9/1 은 2일차 = 4품목 2개씩.)
3. 결제는 KB 경로가 dump 기반 자동입력이라 핸드셰이크 없이 계정당 ~80초.
