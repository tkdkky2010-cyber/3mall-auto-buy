# WORKLOG 2026-08-01 — Step1/2 실행 + 갤러리아 리디렉트 오염 사고 + 본품 n 추가

## 1. Step 1 / Step 2 실행 완료

시트 탭 `8.1`, N=23 조합.

| 몰 | 공급률 범위 | 최저 조합 |
|---|---|---|
| 갤러리아 | 0.4983 ~ 0.5727 | 1 |
| Hmall | 0.4839 ~ 0.5385 | 2 |
| 롯데 | 0.4792 ~ 0.5275 | 2 |

- **GWP (7.31–8.31) 8품목** = 1세트 19,400원 / 70만↑ 4세트 77,600원
  (진생솝·순행클렌징오일50·자음생캡슐세럼5·자정앰플세럼7·여윤팩35·상백선크림10·자음생크림리치5·자음생마스크)
  → 7.28 세트 대비 **윤조에센스8ml·자정수/유액25ml 빠지고 진생솝·캡슐세럼5·크림리치5 들어옴**.
- 추증: b 20,700 / c 10,300 / d 15,400 / e 19,400 / f 16,400 / g 25,300 / h 28,300
- 롯데: 쿠폰 b12·c14·d14·e14·f12·g14·h12 / 롯데카드 5% 청구할인 + 2% 페이백 / 적립 150,000↑ → 10,000원
- Hmall: 당일카드 KB국민 5% (페이백 1%)
- **cart_plan: lotte N=7** → 조합 3·4·8·12·13·22·23, 총 2,624,057원, 평균 공급률 0.5086
- **Step 2**: 10%적립 9건 (#10·12·13·14·20·22·31·33·39, 전부 7단) / 적립없음 14 / ERR 7건은 **판매중단** 판정 (스크래퍼 실패 아님).

## 2. ★ 갤러리아 리디렉트 = 조용한 데이터 오염 (오늘 최대 사고)

첫 Step1 실행에서 **f/g/h 가 b/c/d 페이지로 리디렉트**됐는데, `galleria.py` 가 abort 하지 않고
**그 엉뚱한 페이지 값을 원래 코드 자리에 그대로 시트에 썼다.**

- 로그: `[g] 리디렉트 감지 (…goods_no=2502913432)` ← 2502913432 는 **b** 의 goods_no
- 결과: 추증 f=20,700(=b) / g=10,300(=c) / h=15,400(=d) — **중복값 우연으로만 발견**
- 에러도 exit code 도 안 남 → 그냥 보면 성공처럼 보임. hmall·lotte 는 추증을 시트에서 읽으므로 **3사 전부 오염**.

**조치**: Step1 중단 → galleria 단독 재실행 1회로 정상 스크랩(f 16,400 / g 25,300 / h 28,300, 전부 distinct)
→ inventory 이후 단계만 이어서 실행.

**근본 원인 + 수정**: 짧은 URL(`?goods_no=`)에 `sale_shop_divi_cd` 등 파라미터가 없어서 리디렉트됨.
사용자가 **전체 파라미터 포함 링크**를 제공 → `sulwhasoo-ids.json` 에 `galleria_url` 필드 신설,
`galleria.py scrape_product(url=...)` 가 있으면 그걸 사용(없으면 기존 짧은 URL 폴백).

> ⚠️ **남은 한계 (미수정)**: 리디렉트를 감지하고도 abort 하지 않는 구조 자체는 그대로다.
> fallback 검색 모드가 엉뚱한 페이지를 반환해도 `redirected=True` 플래그만 달고 값을 쓴다.
> **매 실행 후 b~h 추증 금액이 서로 겹치는지 확인할 것.** (자동메모리 `galleria-redirect-silent-corruption`)

## 3. 진생솝 s30 — 처/쳐 표기 불일치

사용자가 재고현황에서 구 s39(시그니쳐 진생 페이셜 솝)를 **s30 으로 통합**하고 C열 이름을
`시그니쳐 진생 페이셜 솝 25g`(**쳐**)로 변경. 그런데 갤러리아 페이지·GWP 이미지는 `시그니처`(**처**) 표기라
매칭 실패 → 신규(단가미정) 오판.

- `_common.py SAMPLE_TABLE` 에 **처 표기 alias** 추가 → s30
- `sample_prices_from_sheet` docstring: **C열만 읽는다**(B열 중문에 병기된 구명칭은 매칭 안 됨) 경고 추가
- 가이드 md s30 명칭 + s39 통합 이력 기록
- 검증: `시그니처…` / `시그니쳐…` / `진생솝 25g` / `진생솝25g` 4종 전부 → `s30 @1,500`

## 4. 월초 리셋 — 사용자가 완료

MAP 활성 버전 `''`(접미사 없음) + 전 코드 D==F + G(8월 출고량)=0 확인 → 8월 리셋 완료 상태.
(오염된 galleria 로 만들어졌던 임시 버전 `'b'` 도 리셋으로 사라짐.)
`_common.py INVENTORY_PRICE_RANGE` 도 `A55:E91` → `A55:E105` 로 넓혀져 s33~s38 이 정상 로드됨(37개 s코드).

## 5. ★ 본품 `n` 추가 — 탄력크림EX 75ml (단품)

**코드는 `i` 가 아니라 `n`** — 재고현황에 이미 있던 `n 탄력크림 75ml` 과 같은 SKU.
(`i` 는 재고현황에서 클렌징오일 200ml 가 쓰는 중 → 건드리지 않음.)

- 소비자가 **135,000원**, 갤러리아 `2502913443` / 현대 `2228732430` / 롯데 `2923426629`
- 검증: 갤러리아·현대 페이지 제목 `설화수 [공통]탄력크림EX 75ml` 확인. 스크랩 정상
  (`--only n --skip-sheet` → basic 10%, coupon 14%, 추가증정 6개 15,400원, 신규 0개, 리디렉트 없음)
- 이 상품은 **단품/기획세트가 종종 바뀜** → 바뀔 때마다 사용자가 새 링크 제공, `sulwhasoo-ids.json` 의 `n` 갱신

**반영 위치**: `_common.py`(PRODUCTS·SHORT_NAME) / `sulwhasoo-ids.json` / `hsmaster products.ts` / 가이드 md.

> ⚠️ **`PRODUCT_VELOCITY` · `PRODUCT_PROFIT` · `NAVER_SETTLED` 의 `n` 값은 미정** — 사용자 수치 대기.
> `cart_plan.combo_score` 가 이 dict 들을 직접 인덱싱하므로 **n 을 COMBOS 에 넣기 전에 반드시 채울 것**(없으면 KeyError).
> **조합(COMBOS) 재설계는 보류** — 사용자가 다시 생각하기로.

## 6. 하드코딩 `"bcdefgh"` 리터럴 일괄 제거

본품이 8개가 되면서 7개 가정 코드가 전부 깨지므로 `PRODUCT_CODES` 파생으로 교체.

| 파일 | 내용 |
|---|---|
| `_common.py` | 추증가치 행 파싱 / 샘플 행 파싱 — 리터럴 + `A6:H50` 고정범위 → `col_letter(1+len(PRODUCT_CODES))` + `GALLERIA_DATA_END_ROW` 파생 |
| `lotte.py` | 스크랩 루프 / 쿠폰 요약 / `'bcdefgh'[i]` + `if i < 7` → `PRODUCT_CODES` |
| `cart_plan.py` | `product_totals`, 시트 하단 본품 수량 그리드 |
| `inventory.py` | `load_composition_from_sheet` products dict |

> `A6:H50` 은 이미 두 겹으로 틀려 있었다 — 열(H=7상품)도, 행(50 ≠ 현재 `GALLERIA_DATA_END_ROW`=55)도.

## 7. `cart_plan.read_stocks` 재고 오매핑 수정

`D6:D12` 를 위치로 b~h 에 매핑했는데 실제 행은 **e~h 가 5~8행 / b~d 가 27~29행** →
7개 전부 다른 상품(k·l·n·a30 등) 재고를 읽고 있었다. n 추가로 8개가 되면서 그대로 두면 더 어긋나므로
**A열 코드 라벨 조회로 교체**(RULES §1-4).

검증 — 매칭된 각 행의 재고현황 상품명이 `PRODUCTS[code]` 와 전부 일치:
`e 탄력3종 / f 윤조에센스90 / g 자음생2종 / h 자음생크림리치 / n 탄력크림 75ml / b 윤조3종 / c 자음2종 / d 본윤2종`

> 작업 중 b/c/d 행이 27~29 → 33~35 로 이동하는 것을 실제로 관측했다. **위치 매핑이 왜 안 되는지의 산 증거.**
> 재고는 `참고용 — 분배엔 미반영` 이라 오늘 플랜/시트 결과에는 영향 없음.

## 8. ⚠️ 다른 컴터 이관 시 주의

`hsmaster/config/sulwhasoo-ids.json` 은 **gitignore 대상** → 오늘 추가한 `n` 항목과 `galleria_url` 8개가
**커밋에 안 들어간다.** 다른 컴터에서는 이 파일을 직접 갱신해야 한다.
안 하면 `PRODUCTS` 에는 n 이 있는데 `load_ids()` 에는 없어서 **KeyError 로 galleria/lotte 가 죽는다.**

---
---

# ━━━ 저녁 세션 (Mac mini) ━━━

## 10. e/f/g/h/n 조합 3사 공급률 전수 측정 → COMBOS #24~27 확정

소비자가 **70만~79만** (그 아래는 GWP 4세트 77,600원을 못 받아 급락) 조건으로 e·f·g·h·n 조합 **34개 전수**.

- 갤러리아·롯데 = 수식 계산. **검증**: 오늘 시트에 이미 있는 9개 조합(#1·3·4·5·12·13·14·22·23)을 재계산해 **소수점 4자리까지 전부 일치**.
- Hmall = **34조합 실측**(장바구니→체크아웃 캐러셀 가격, KB국민 5%). 실패 0건. 기존 8개 조합 재측정값도 오늘 시트와 완전 일치.
- n 데이터: 갤러리아 재스크랩(basic 10%/coupon 14%/추증 15,400), **롯데 쿠폰 14% 신규 확인**.

**결과 — 34조합 전부 롯데가 최저.** 상위 4개가 전부 **g+n 조합**:

| 조합 | 소비자가 | 롯데 | Hmall | 갤러리아 |
|---|--:|--:|--:|--:|
| g×2 + n×2 | 720,000 | **0.4859** | 0.4902 | 0.5361 |
| f×1 + g×2 + n×1 | 725,000 | 0.4893 | 0.4969 | 0.5363 |
| e×1 + g×1 + n×2 | 710,000 | 0.4909 | 0.4954 | 0.5413 |
| e×2 + n×2 | 700,000 | 0.4960 | 0.5007 | 0.5467 |

> **n 은 g 의 약점을 상쇄한다.** g 는 추증 25,300원으로 가장 무거워 단독은 불리(g2+h1 0.4956)한데, 추증이 가벼운(15,400) + 롯데 쿠폰 14%(f·h 는 12%)인 n 을 붙이면 최상위가 된다. n 없이 e/f/g/h 만으로는 롯데 0.4928 이 한계.
> 갤러리아만 순위가 다른데(전 상품 쿠폰 14% 균일이라 n 의 쿠폰 우위 소멸) 34조합 모두 3사 중 최악이라 실전 영향 없음.

**사용자 선택 → `COMBOS` #24~27 추가** (총 27개): `g2+n2` / `e2+n2` / `f1+g2+n1` / `e1+g1+n2`.
레이아웃 자동 재계산 확인: Hmall 58~90 / Lotte 93~126 / 차트 J1:M28.

## 11. `n` 3개 dict 확정 + lotte.py 하드코딩 제거

| 항목 | 값 | 근거 |
|---|--:|---|
| `PRODUCT_VELOCITY["n"]` | 20 | 사용자 지정 (e 와 동률 최고) |
| `NAVER_SETTLED["n"]` | 81,137 | 85,800 × 0.934 + 1,000 (3천원 배송) |
| `PRODUCT_PROFIT["n"]` | 14,987 | 정산 81,137 − 매입 66,150(135,000 × **공급률 49%**) |

> 수익금은 정산금만으로 안 나온다 — **매입원가(=소비자가 × 그날 공급률)** 가 있어야 한다.
> 기존 코드들의 함의 원가율이 0.436~0.509 로 제각각인 이유도 각자 다른 날 기준이기 때문.
> 공급률 1%p 당 n 수익 1,350원 변동. 오늘 실측 구간(0.4859~0.4960) 중앙이 49%.

`lotte.py:44` 의 `PRICES = {'b':…,'h':…}` 하드코딩 → `PRODUCTS` 파생으로 교체. **안 고쳤으면 #24~27 에서 `KeyError: 'n'`.** (8/01 낮 "bcdefgh 리터럴 제거"에서 빠졌던 곳.)

## 12. ★★ 롯데 카트담기 — 비밀번호 변경 캠페인 (사용자 4회 지적, 2회 재발)

**증상**: `_lotte_cart_fill.py 3 1 5` 에서 #2 `담기 검증 실패 — 카트 0건`, #3 `Target page ... has been closed`. 둘 다 **`[OK] lotte 로그인` 을 찍고도** 실패.

**실측 진단**(추측 패치 금지 원칙대로 화면부터 확인) — 원인이 **2개**였다:

```
page[0]  .../member/login/forward.popup_pwd_campaign_av.lotte   ← 캠페인 (별도 창!)
page[2]  .../main/viewMain.lotte                                 ← 진짜 로그인된 본 창

팝업 창 내부:  <a onclick="self.close()">        <img alt="새창 닫기">
               <a onclick="fn_changeNext()">     <img alt="30일간 보이지 않기">  ← 눌러야 할 것
```

1. **캠페인이 별도 window** → `_lotte_dismiss_pw_campaign` / `_lotte_pw_campaign_present` 가 넘겨받은 page 하나만 보므로 **둘 다 장님** → `present=False` → **로그인 성공 오판**.
2. **버튼에 텍스트가 없다.** 라벨이 `<img alt=…>` 에만 있어 `innerText||value` 매칭이 절대 못 잡는다. (2026-07-21 1차 수정이 무효였던 이유.)
3. 창이 안 닫힌 채 남으면 `context.pages[-1]` 이 그 창을 잡아 담기가 미로그인 컨텍스트에서 헛돌고(카트 0건), 창이 닫히는 순간 `page has been closed`.

**수정**: `_lotte_campaign_pages()` 신설 — **context 의 모든 창 스캔** + **`img[alt]` 매칭** + 클릭 후 **`close()` 검증**. `main()` 의 페이지 선택도 `LOTTE_PW_CAMPAIGN_URL` 창 제외.
**검증**: `[popup] 비번변경 캠페인 '30일간 보이지 않기' 클릭 (img-alt)` → 창 3개→2개 → **#2·#3 재실행 둘 다 카트 2건 담기 성공**.

**재발 방지 3중**: ① 코드 ② `READ_FIRST.md` 전용 섹션 + 자동메모리 `lotte-pw-campaign-popup` ③ **`.claude/settings.json` PreToolUse 훅** — 롯데 카트 명령 실행 전 하네스가 규칙을 강제 주입(발동 확인 완료). ①②는 안 읽으면 그만이지만 ③은 의지와 무관하게 뜬다.

## 13. 오늘 카트담기 / 결제 결과

**카트담기 조합3(e×2+f×2)**: #2·#3·#4·#5·#15·#16 ✅ / **#1 tkdkky2002 = 비번 거부**(자동화로 못 고침, 사용자 점검 필요).

**롯데 포인트 조회 (#15~19)**:

| # | 계정 | 적립금 | L.POINT |
|--:|---|--:|--:|
| 15 | joyoungjin1220 | **20,000원** | 2,784P |
| 16 | kimmk2008 | **20,000원** | 2,784P |
| 17 | miu1838 | 0 | 2,874P |
| 18 | nami8225 | 0 | 2,794P |
| 19 | ybkim9960 | 0 | 559P |

> 조회 URL = `https://www.lotteimall.com/mypage/searchMyLotteMain.lotte` (라벨 다음 줄이 값, L.POINT 는 '조회' 클릭 필요). #15·#16 이 동일값이라 #16 재로그인 재조회로 교차확인함.

**폰 결제 #15·#16 → 둘 다 `PAY_FAIL@return`**. 당일카드 롯데 5% 자동선택까지 정상, 결제 요청에서 실패.
폰 다이얼로그 실확인: **`오류사유:[이용한도 초과]`** — 자동메모리 `card_limit_order_complete_timeout` 예측 적중.
**돈 안 나감 / 중복 없음 / 장바구니 유지** → 한도 리셋 후 재시도 또는 다른 카드.

## 14. 다음 할 일

- [ ] **#15·#16 결제 재시도** (롯데카드 한도 리셋 후, 또는 다른 카드로). 카트는 담긴 채 유지됨
- [ ] **#1 tkdkky2002 롯데 비번 확인** (`lotte.json`) — 담기 자체가 안 됨
- [ ] `galleria.py` 리디렉트 시 abort 가드 (스크랩한 상품명 ↔ 요청 코드 대조)
- [ ] `buy/sulwhasoo.py` `LOTTE_PRODUCTS` 에 **n 없음** — #24~27 담으려면 추가 필요 (lotte.py `PRICES` 와 같은 종류의 잔여 하드코딩)
- [ ] 다른 컴터 `sulwhasoo-ids.json` 갱신 (gitignore라 커밋 안 됨 — 이 컴터는 오늘 n + galleria_url 8개 반영 완료)
