# WORKLOG 2026-07-05 (Hmall 로그인 차단 근본해결 — CfT→실 Chrome)

## 요약 (한 줄)
Step 2 로그인 실패의 진짜 원인 = **CfT(Chrome for Testing) 바이너리가 Hmall 안티봇에 감지돼 ID/PW 로그인 차단**. 양쪽 런처(9222·9223)를 **실제 Google Chrome 바이너리**로 전환해 근본 해결(실측 검증 완료). 보험으로 `HMALL_USE_CFT=1` 폴백 토글 추가. 오늘 식품 10% 대상 0개(이벤트 종료).

## ★핵심 사건: CfT 로그인 차단 (실측, 추측 아님)
- **증상**: Step 2(`cart/check10.py`) 로그인 클릭 후 `loginForm`에 머무름 + 알럿 "**로그인에 실패하였습니다. 다른 로그인 수단을 이용바랍니다.**" (비번 불일치 아님 — 카카오/네이버/휴대폰 유도).
- **범위**: 계정#1(tkdkky2002)·#2·시크릿모드·포트 무관, **CfT면 전부 차단**.
- **결정적 실측**: 동일 계정 tkdkky2002 을 **실제 Chrome(9224 전용프로필)** 으로 하면 → URL `md/dpl/index` 리다이렉트 + `로그아웃`=True = **즉시 로그인 성공**. 9222·9223 에서도 실 Chrome이면 성공 재현.
- **왜 여태 9222 식품결제는 됐나**: CfT라서가 아니라 **Profile 6 세션이 유지돼 생짜 ID/PW 로그인을 안 했기 때문**(login()이 "로그아웃" 감지 시 즉시 통과). check10 프로필은 세션 풀려 생짜 로그인 → CfT 차단 노출.
- 7-03 워크로그의 12·13·14 적립 실패("로그인에 실패…다른") 도 동일 현상.

## 변경 (⚠️ 커밋 안 함, uncommitted)
1. **`launch-check10-chrome.sh`** (9223): CfT → 실 Chrome `/Applications/Google Chrome.app/...` + `~/Check10RealChrome`. `HMALL_USE_CFT=1` 폴백 분기.
2. **`hsmaster/scripts/launch-hmall-chrome.sh`** (9222): CfT → 실 Chrome + `~/HmallChrome/Profile 6`. `HMALL_USE_CFT=1` 폴백 분기.
3. **`step2.sh`**: 9223 종료를 `pkill -f "remote-debugging-port=9223"`(포트기준)로 — 프로필명 변경(Check10Chrome→Check10RealChrome) 대응.
- (앞선 7-03분 미커밋 잔존: `buy/run.py`, `buy/food_buy.py`, `buy/cart_plan.json`)

## ▣ 보험 (안 되면 예전 방식)
- **`HMALL_USE_CFT=1`** env → 두 런처가 즉시 예전 CfT 방식으로 복귀(코드/ git 불필요). 예: `HMALL_USE_CFT=1 bash step2.sh`.
- 원본 런처 git tracked → `git checkout -- <파일>` 복구 가능.
- check10은 프로필 분리(RealChrome vs Check10Chrome)라 두 방식 서로 간섭 없음.

## ⚠️ Keychain Safe Storage 주의
macOS Chrome 쿠키는 Keychain "Safe Storage" 키로 암호화 → **CfT와 실 Chrome은 키체인 항목이 달라** 바이너리 전환 시 기존 프로필 세션을 못 읽음(로그아웃처럼 보임). **문제 아님** — 실 Chrome은 ID/PW 재로그인이 되므로 자동 복구.

## 검증 (end-to-end)
- 9223 실 Chrome: check10 `[OK] 로그인` → Step 2 완주, 시트 탭 **'7.05 식품' 생성**, today.json 날짜 7-05.
- 9222 실 Chrome: 계정#1 ID/PW 로그인 성공(`md/dpl/index` 리다이렉트) — 결제 경로 로그인 정상.
- step2.sh 9223 종료 정상.

## Step 2 결과 (7-05)
- **10% 적립 상품 0개**. 7-03의 "건강은 金이다 최대 10%적립"(그날 10개) 이벤트 **종료**. → **오늘 식품 구매 대상 없음.**

## 12·13·14 주문 취소 정리 (사용자: 적립 미반영으로 취소, 재구매 안 함)
- 아이디: 12=Jinhwa4553, 13=gcd0327, 14=johwajeong (전부 상품33 GRN 붓기캔디, 결제는 됐었으나 취소).
- **`secrets/purchase_ledger.json`**: 7-03 붓기캔디 6행 제거 (57→51). amount 전부 null이라 금액합계 영향 없음.
- **`buy/_food_done.json`**: `[12,33][13,33][14,33]` 제거 (26→23).
- 백업: 두 파일 `.bak_20260705_cancel121314`. 복구하려면 백업 되돌리기.
- 12·13·14 H.Point 적립 재확인 TODO는 취소로 **무의미해짐**(폐기).

## 남은 것 / 다음
- 7-03 미결제 **16(28)·17(28)·18(25)·19(25)** — 오늘 10% 0개라 재개 보류(그 상품들 오늘은 10% 아님). 재개하려면 `python3 buy/food_buy.py 16-19`.
- (선택) 커밋 정리: 런처 3종 + 7-03 미커밋 3종 + 취소 정리.
- 9222 실 Chrome 로그인된 채 떠있음(테스트 흔적) — 필요시 정리.
- 메모리 `chrome_setup` 갱신 완료(CfT 차단→실 Chrome, 폴백, Keychain).
