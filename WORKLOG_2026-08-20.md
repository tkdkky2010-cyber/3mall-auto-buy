# WORKLOG 2026-08-20 (목) — step1/step2 + 담기 플랜 2회 수정

## 지시 (사용자 문장 그대로)

> 커밋푸시해줘 / 스텝1,2 실행해
> 멈춰
> 이걸로 수정해서 담아줘  (← 카트플랜 시트 캡처 2장)

## 1. Step 1 — GWP 재판독 발생, 채널 galleria

첫 실행이 `rc=2 GWP_PENDING` 으로 정지. **이미지를 직접 보고 판독**해
`rate-check/_tmp/gwp_2026-08-20.json` 작성 후 재실행 → 완주.

### ★ GWP 구성이 실제로 바뀌었다 (복사했으면 틀렸다)

| | 8/19 (7.31 - 8.31) | **8/20 (8.19 - 8.31)** |
|---|---|---|
| 빠짐 | 자정앰플세럼 7ml, 여윤팩 35ml | — |
| 추가 | — | **순행클렌징폼 50ml, 윤조에센스 8ml** |
| 공통 6종 | 진생솝25g·순행클렌징오일50ml·자음생캡슐세럼5ml·상백선크림10ml·자음생크림리치5ml·자음생마스크1매 | 동일 |

기간·구성 모두 원본 확대(`_gwp_zoom.png`, `_gwp_period.png`)로 재확인 후 기입.
안내문의 「절대 이전 날짜/구성 복사 금지 — 이미지가 바뀌어서 재판독 요청된 것임」이 정확했다.

### 결과

| 단계 | 결과 |
|---|---|
| 갤러리아 | ✅ 27조합, REDIRECTED 0 |
| 재고관리 | ✅ **새 버전 `h`** (`1h 8/20`…`27h 8/20`) |
| Hmall | ✅ 27/27 |
| 롯데 | ⚠️ `d`·`f` 일시품절 (아래) |
| 카트플랜 | ✅ `O1:V32`, **channel=galleria N=36**, 13,263,263원 / 평균 0.5062 |
| 미러 | ✅ `8.20` → 재고관리 `당일 설화수` (63칸, 133행) |

### ★ 롯데 `_sold_out()` 첫 라이브 발동 (8/19 워크로그의 "라이브 미검증" 해소)
```
[WARN] d 후보 1개 전부 일시품절 — 마지막 후보로 측정 진행(값 신뢰 주의)
[WARN] f 후보 1개 전부 일시품절 — 마지막 후보로 측정 진행(값 신뢰 주의)
```
**후보가 1개뿐이라 폴백할 대상이 없어 품절 상품 가격으로 그대로 측정했다.** 감지는 됐지만
대안이 없는 게 문제 → `d`·`f` 상품번호 후보 보강이 필요(`hsmaster/config/sulwhasoo-ids.json`).

### Gemini 방어가 실제로 작동했다
```
[INFO] CDP 기본 핸들이 콘텐츠 탭이 아니어서 전환 ('https://gemini.google.com/glic?hl=ko' → ...)
```
8/19 에 넣은 `use_content_tab` 이 갤러리아·롯데 양쪽에서 발동. 안 고쳤으면 오늘 판독이 통째로 오염됐다.

## 2. Step 2 — 10% 13종

`8.20 식품!A1:R21` 신규 탭 + 재고관리 `당일 식품` 미러 21행.
**10%적립 13 / 적립없음 4 / 에러 2.**

- 오늘까지(8.20 23:59) = `1`,`1-1`,`11`,`17`,`22`,`25`,`33`,`34`,`35` (건강식품 브랜드 특별전 `P202608145877`)
- 8.30 까지 = `2`,`3`,`8`,`9` (릴레이푸드 `P202608074038`, 5단 tier)
- 이월 미해결 그대로: `#8 이디야 디카페인` 금액 전부 `—` / `#28`·`#39` `ERR 판매중단`
- 미러 로그 `배경색 초기화 후 0칸 복사` = 식품 탭엔 조합번호 색칠이 없어 정상(값은 21행 복사됨).
  설화수 미러(63칸)와 숫자가 달라 보이는 게 정상이라는 점을 기록해 둔다.

## 3. 담기 — 플랜이 2번 바뀌었다

### 1차 (중단됨)
`22×2, 34×2 → 계정 1-10` / `2×2 → 계정 1-3` 로 담기 시작 → 사용자 "멈춰" 로 중단.
⚠️ PowerShell `Tee-Object` 버퍼 때문에 **어디까지 담겼는지 로그가 남지 않았다**(task output = `[killed]` 뿐).

### 2차 (확정)
| 상품 | 수량 | 계정 |
|---|---|---|
| 22 올바른건강식품 와이 9박 | 2 | **11~19** |
| 34 데이즈온 파라다이스 버닝 9박스 | 2 | **11~19** |
| 2 하루견과 초록색 100봉 | 2 | 1~3 |

**`buy/run.py 1-19` 로 전 계정을 돌렸다** — 1차 중단 잔여물이 어디 남았는지 모르는 상태였고,
`run.py` 는 플랜에 없는 계정의 카트를 **비우기** 때문에 이렇게 해야 플랜대로 수렴한다.

결과: **19/19 계정, 담기 21/21, 비우기 19/19, 실패 0.**
`#4~10` 은 `□ 0/0` (플랜에 없어 비우기만) = 의도된 동작.

## 4. 오늘 배운 것

1. **중단은 기록을 남기지 않는다.** `Tee-Object` 버퍼 탓에 1차 담기가 어디까지 갔는지 못 봤다.
   → 상태를 모를 땐 **플랜 전체 범위를 다시 돌려 수렴시키는 게 안전하다**(부분 확인보다 확실하고,
   로그인 비용도 어차피 비슷하다).
2. **"바뀌었을 것"이 아니라 "바뀌었다"를 눈으로 확인해야 한다.** GWP 는 실제로 2종이 교체됐다.
   이미지 확대 판독을 안 했으면 사은품 가치가 틀려 공급률이 조용히 어긋났다.

## 5. 다음 할 일

- [ ] 롯데 `d`·`f` 상품번호 **후보 보강** — 1개뿐이라 품절 시 폴백이 없다.
- [ ] `cart/today_carts.json` 은 아직 8/19 자 — 결제 시 오늘자 재생성 필요(안 하면 preflight 차단).
- [ ] `#8 이디야 디카페인` 금액 미판독 (8/19 이월).
- [ ] 11~19 카트(22·34)는 **미결제 상태로 남았다** — 22·34 행사가 8.20 23:59 종료라 다음날 확인 필요.
