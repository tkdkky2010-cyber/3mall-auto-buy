# WORKLOG 2026-06-01 — BC·NH 카드 검증 완료(7카드 전부) + 토스페이 경로 탐색 + adb 경로 자동화

정본: `phone_auto/hmall_hyundai_buy.py`. 무선 adb (mDNS `adb-RFCNC0WRL9Z...`, SM-G9960). 모든 결제는 계정 #3.

## 오늘 결과 (실결제 검증)
- **BC 비씨카드(페이북)** ✅ 라이브검증 — 주문 `20260601059538`, 490,000원(청구 370,440) 비씨카드 일시불 + 뷰티 적립완료. (어제 CC61 한도초과 거절 → 사용자가 한도 풀어줌 → 통과). 그리드 강제선택(`BC 3`).
- **NH 농협카드** ✅ 라이브검증 (미착수→설계+구현+검증 한방에) — 주문 `20260601062093`, 198,000원(청구 128,380) NH농협카드 일시불 + 뷰티 적립완료. 그리드 강제선택(`NH 3`).
- **→ 7개 카드 SDK 전부 라이브검증 완료**: 현대·롯데·KB·하나·삼성·BC·NH.
- 참고: 오늘 당일 할인카드 = **삼성**. (BC/NH는 비당일이라 그리드 강제선택으로 검증)

## NH = PAYCO 우회 (핵심)
- 농협 결제앱(NH앱)은 무선/USB 디버깅 전부 차단 → NH앱 직결 불가. **PAYCO 우회**(NH카드 PAYCO 사전등록).
- 경로: 원결제하기(OCR) → **농협카드 팝업 우측상단 '다른 결제'(OCR)** → **우측하단 'PAYCO'(OCR)** → [삼성과 동일] PAYCO 결제확인 → 결제하기 → PIN(payco_pin6, 137601) → '확인'.
- 농협 팝업·PAYCO 화면 = **screencap OCR 정상**(FLAG_SECURE 아님, 차단은 NH앱에만 해당). 사용자가 경로 정확히 짚어줘서 `pay_samsung` PAYCO 꼬리 재사용 + 진입부만 신규 → **첫 시도 성공**.
- ⚠️ NH PIN: 2엔진(vision,gcv)이 셔플키패드를 고정 1-9로 오독→실패, **4엔진 승급으로 10/10 성공**. payco_pin6는 4엔진 필요할 수 있음.
- 코드: `pay_nh()` 신규 + `CARDS_SUPPORTED`에 NH + `buy_one` 디스패처 NH 분기.

## adb 경로 자동화 (양 컴퓨터 호환 — 중요)
- 첫 BC 런이 즉시 크래시: `phone_auto/hmall_webview.py`의 adb 기본경로 `/Users/jasonkim/platform-tools/adb`가 **이 맥미니엔 없음**(homebrew `/opt/homebrew/bin/adb`).
- 두 컴퓨터 동일 사용자명 `jasonkim`(맥미니 `Jasons-Mac-mini` / 맥북에어 `jasonkims-MacBook-Air`, iCloud 동기화). adb 위치만 다름(맥북에어=`~/platform-tools`).
- **수정**: `ADB = os.environ.get("ADB_BIN") or shutil.which("adb") or "<기존 폴백>"`. → 양쪽에서 `ADB_BIN` 없이도 자동 검출. (검증: 맥미니서 `/opt/homebrew/bin/adb` 자동 resolve)

## 캐러셀(자동감지+할인) 경로 검증 — 사용자 지적으로 발견
- SDK(앱/PAYCO/PIN)는 7개 다 됐지만, **`select_card`의 캐러셀 분기(당일 할인카드 자동감지+할인 적용)는 현대만 검증**됐었음. 나머지는 다 그리드 강제선택으로만 검증.
- `select_card`: `day==card`면 `select_card_discount`(캐러셀), 아니면 `_pick_card_from_grid`(그리드).
- ⚠️ 위험: `detect_card`의 `CARD_ALIASES`가 당일카드 인식 실패 시 → **현대로 폴백 → 오결제 위험**. 자동감지 경로가 실사용 경로라 검증 필요.
- **오늘 삼성 캐러셀 검증 (결제 0원)**: `_verify_samsung_carousel.py` 신규 — detect_card=삼성 자동감지 ✅ → 캐러셀 선택 `{ok, amt:'121,961원', applied:True}` 할인적용 ✅ → 원결제하기 → 삼성 PAYCO 팝업 도달 → **PIN 전 종료(결제 X)**.
- → 캐러셀 검증 현황: **현대·삼성 ✅ / 롯데·KB·하나·BC·NH ❌(각 카드 할인날에만 검증 가능)**.

## 토스페이 경로 탐색 (실결제 X) — 다음 토스 할인날 대비
- 상세 = **`TOSS_PAY_NOTES.md`**(레포 루트) + 자동메모리 `toss-pay-flow`. 탐색도구 = `phone_auto/_toss.py`.
- 토스페이 = 간편결제 채널이 카드 감쌈. 카카오페이는 타 폰 사용중이라 제외.
- 경로: 주문서→결제수단변경→'페이/Pay'탭→'토스페이'→원결제하기→'다음'→토스앱(`viva.republica.toss`).
- 토스앱 결제화면 = screencap OK(OCR), 카드선택('결제수단 변경·설정'). **PIN화면 = FLAG_SECURE(screencap 검정) BUT uiautomator dump 됨 → `input_pin source=dump`, 6자리 셔플, ⚠️숫자좌표 하드코딩 금지(매번 dump 라이브), PIN=137601** (KB/롯데/NH앱 동일 패턴).
- 오늘은 비할인날이라 단독경로만 관찰(토스앱서 카드 직접선택). 할인날엔 캐러셀이 토스+카드 자동선택.
- **pay_toss 미구현** → 토스 할인날 라이브 작성+검증(NH 패턴).

## 신규/변경 파일
- `phone_auto/hmall_hyundai_buy.py`: `pay_nh()` 신규, BC/NH docstring 검증표기, CARDS_SUPPORTED에 NH + 토스 포인터 주석, 디스패처 NH 분기.
- `phone_auto/hmall_webview.py`: adb 경로 `shutil.which` 자동검출.
- `phone_auto/_verify_samsung_carousel.py` (신규, 캐러셀 무결제 검증), `phone_auto/_toss.py` (신규, 토스 탐색도구).
- `TOSS_PAY_NOTES.md` (신규), `logs/2026-06-01-bc-1.log`/`-nh-1.log`/`-samsung-carousel-verify.log`.

## TODO (다음)
- **토스페이 pay_toss**: 토스 할인날 라이브 작성+검증 (TOSS_PAY_NOTES.md 참고).
- **나머지 카드 캐러셀 검증**: 롯데·KB·하나·BC·NH — 각 카드 할인날에 `... idx`(카드 미지정) 자동감지로 검증.
- **KB 디스패처 풀검증**: `KB <idx>` pay_kb + 즉시 beauty 한방 완주.
- **카드공통 resume**: 비현대 SDK 화면 마커 추가.
