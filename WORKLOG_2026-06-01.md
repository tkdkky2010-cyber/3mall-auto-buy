# WORKLOG 2026-06-01 — BC·NH 카드 검증 완료(7카드 전부) + 토스페이 경로 탐색 + adb 경로 자동화

정본: `phone_auto/hmall_hyundai_buy.py`. 무선 adb (mDNS `adb-RFCNC0WRL9Z...`, SM-G9960). 모든 결제는 계정 #3.

## 오늘 결과 (실결제 검증)
- **BC 비씨카드(페이북)** ✅ 라이브검증 — 주문 `20260601059538`, 490,000원(청구 370,440) 비씨카드 일시불 + 뷰티 적립완료. (어제 CC61 한도초과 거절 → 사용자가 한도 풀어줌 → 통과). 그리드 강제선택(`BC 3`).
- **NH 농협카드** ✅ 라이브검증 (미착수→설계+구현+검증 한방에) — 주문 `20260601062093`, 198,000원(청구 128,380) NH농협카드 일시불 + 뷰티 적립완료. 그리드 강제선택(`NH 3`).
- **→ 7개 카드 SDK 전부 라이브검증 완료**: 현대·롯데·KB·하나·삼성·BC·NH.
- 참고: 오늘 당일 할인카드 = **삼성**. (BC/NH는 비당일이라 그리드 강제선택으로 검증)

## NH = PAYCO 우회 (핵심)
- 농협 결제앱(NH앱)은 무선/USB 디버깅 전부 차단 → NH앱 직결 불가. **PAYCO 우회**(NH카드 PAYCO 사전등록).
- 경로: 원결제하기(OCR) → **농협카드 팝업 우측상단 '다른 결제'(OCR)** → **우측하단 'PAYCO'(OCR)** → [삼성과 동일] PAYCO 결제확인 → 결제하기 → PIN(payco_pin6, 137601) → '확인'.
- 농협 팝업·PAYCO 화면 = **screencap OCR 정상**(FLAG_SECURE 아님, 차단은 NH앱에만 해당). 사용자가 경로 정확히 짚어줘서 `pay_samsung` PAYCO 꼬리 재사용 + 진입부만 신규 → **첫 시도 성공**.
- ⚠️ NH PIN: 2엔진(vision,gcv)이 셔플키패드를 고정 1-9로 오독→실패, **4엔진 승급으로 10/10 성공**. payco_pin6는 4엔진 필요할 수 있음.
- 코드: `pay_nh()` 신규 + `CARDS_SUPPORTED`에 NH + `buy_one` 디스패처 NH 분기.

## adb 경로 자동화 (양 컴퓨터 호환 — 중요)
- 첫 BC 런이 즉시 크래시: `phone_auto/hmall_webview.py`의 adb 기본경로 `/Users/jasonkim/platform-tools/adb`가 **이 맥미니엔 없음**(homebrew `/opt/homebrew/bin/adb`).
- 두 컴퓨터 동일 사용자명 `jasonkim`(맥미니 `Jasons-Mac-mini` / 맥북에어 `jasonkims-MacBook-Air`, iCloud 동기화). adb 위치만 다름(맥북에어=`~/platform-tools`).
- **수정**: `ADB = os.environ.get("ADB_BIN") or shutil.which("adb") or "<기존 폴백>"`. → 양쪽에서 `ADB_BIN` 없이도 자동 검출. (검증: 맥미니서 `/opt/homebrew/bin/adb` 자동 resolve)

## 캐러셀(자동감지+할인) 경로 검증 — 사용자 지적으로 발견
- SDK(앱/PAYCO/PIN)는 7개 다 됐지만, **`select_card`의 캐러셀 분기(당일 할인카드 자동감지+할인 적용)는 현대만 검증**됐었음. 나머지는 다 그리드 강제선택으로만 검증.
- `select_card`: `day==card`면 `select_card_discount`(캐러셀), 아니면 `_pick_card_from_grid`(그리드).
- ⚠️ 위험: `detect_card`의 `CARD_ALIASES`가 당일카드 인식 실패 시 → **현대로 폴백 → 오결제 위험**. 자동감지 경로가 실사용 경로라 검증 필요.
- **오늘 삼성 캐러셀 검증 (결제 0원)**: `_verify_samsung_carousel.py` 신규 — detect_card=삼성 자동감지 ✅ → 캐러셀 선택 `{ok, amt:'121,961원', applied:True}` 할인적용 ✅ → 원결제하기 → 삼성 PAYCO 팝업 도달 → **PIN 전 종료(결제 X)**.
- → 캐러셀 검증 현황: **현대·삼성 ✅ / 롯데·KB·하나·BC·NH ❌(각 카드 할인날에만 검증 가능)**.

## 토스페이 경로 탐색 (실결제 X) — 다음 토스 할인날 대비
- 상세 = **`TOSS_PAY_NOTES.md`**(레포 루트) + 자동메모리 `toss-pay-flow`. 탐색도구 = `phone_auto/_toss.py`.
- 토스페이 = 간편결제 채널이 카드 감쌈. 카카오페이는 타 폰 사용중이라 제외.
- 경로: 주문서→결제수단변경→'페이/Pay'탭→'토스페이'→원결제하기→'다음'→토스앱(`viva.republica.toss`).
- 토스앱 결제화면 = screencap OK(OCR), 카드선택('결제수단 변경·설정'). **PIN화면 = FLAG_SECURE(screencap 검정) BUT uiautomator dump 됨 → `input_pin source=dump`, 6자리 셔플, ⚠️숫자좌표 하드코딩 금지(매번 dump 라이브), PIN=137601** (KB/롯데/NH앱 동일 패턴).
- 오늘은 비할인날이라 단독경로만 관찰(토스앱서 카드 직접선택). 할인날엔 캐러셀이 토스+카드 자동선택.
- **pay_toss 미구현** → 토스 할인날 라이브 작성+검증(NH 패턴).

## 신규/변경 파일
- `phone_auto/hmall_hyundai_buy.py`: `pay_nh()` 신규, BC/NH docstring 검증표기, CARDS_SUPPORTED에 NH + 토스 포인터 주석, 디스패처 NH 분기.
- `phone_auto/hmall_webview.py`: adb 경로 `shutil.which` 자동검출.
- `phone_auto/_verify_samsung_carousel.py` (신규, 캐러셀 무결제 검증), `phone_auto/_toss.py` (신규, 토스 탐색도구).
- `TOSS_PAY_NOTES.md` (신규), `logs/2026-06-01-bc-1.log`/`-nh-1.log`/`-samsung-carousel-verify.log`.

## TODO (다음)
- **토스페이 pay_toss**: 토스 할인날 라이브 작성+검증 (TOSS_PAY_NOTES.md 참고).
- **나머지 카드 캐러셀 검증**: 롯데·KB·하나·BC·NH — 각 카드 할인날에 `... idx`(카드 미지정) 자동감지로 검증.
- **KB 디스패처 풀검증**: `KB <idx>` pay_kb + 즉시 beauty 한방 완주.
- **카드공통 resume**: 비현대 SDK 화면 마커 추가.

---

# 오후 작업 (2026-06-01)

## 삼성 캐러셀(자동감지+할인) 무결제 검증
- 지적: SDK는 7카드 다 됐지만 `select_card` **캐러셀(당일 할인카드 자동감지+할인)** 경로는 현대만 검증됐었음. 나머지는 그리드 강제선택만.
- 오늘 당일카드=삼성 → `_verify_samsung_carousel.py`로 결제 0원 검증: detect_card=삼성 ✓ → 캐러셀 선택 `{ok, amt 121,961원, applied:True}` ✓ → 원결제하기 → 삼성 PAYCO 팝업 도달 → PIN 전 종료.
- 현황: 캐러셀 검증 = 현대·삼성 ✅ / 롯데·KB·하나·BC·NH ❌(각 할인날 필요).

## step1 완주 + hmall connect 버그 픽스 (근본원인 해결)
- step1 Hmall 단계가 **15분 무한대기**. 근본원인: `rate-check/hmall.py`가 patchright `connect_over_cdp`를 timeout/fallback 없이 호출 → Chrome 148에서 patchright connect 간헐 행. (run.py엔 fallback 있었으나 hmall.py 누락)
- 픽스: `_pick_cdp_backend()` — 12s 프로브로 patchright 확인 → 행/실패 시 plain playwright fallback + connect `timeout=20000`. 무한대기 불가.
- 9222 stale(0 targets) → pkill HmallChrome + launch-hmall-chrome.sh 재실행. adb 경로도 ADB_BIN→shutil.which.
- step1 완주: 갤러리아(GWP 87,600=자정수+자정유액 합본, 이중계산0) → 재고(20조합 n) → Hmall(20조합) → 롯데(롯데카드 7%청구할인) → 카트플랜 O열.
- 카트플랜 조정: `--max-product f=9,h=4`로 f 12→9, e 3→5 (사용자 요청, E위주).

## 설화수 계정간 sleep=0 (요청: 전체 파일)
- 설화수 3사몰은 계정 간 대기 없음(7분은 Hmall 식품 buy/run.py 전용). 3파일 수정:
  - `buy/hmall_sulwhasoo_first_cart.py` ACCOUNT_DELAY_SEC 20→0
  - `phone_auto/hmall_combo_checkout.py` PAY_DELAY 420→0, SKIP_DELAY 8→0
  - `_tmp_lotte_run.sh` WAIT_SEC 420→0
- `buy/run.py` ACCOUNT_DELAY_SEC=420 유지(식품).

## 롯데 카트 #1-7 담기 (cart_plan대로)
- `buy/sulwhasoo.py lotte <idx> <combo>` cart-only. #1~7 = 조합 4,5,9,9,10,15,20. 전원 성공.
- ⚠️ iCloud dataless 함정: `credentials.json`이 cloud-only(evict)라 읽기 5분 hang → `brctl download`로 config 전부 materialize. (재발 가능)
- zsh word-split 버그(`set -- $pair` 미분할) → `read`로 수정.

## 롯데홈쇼핑 폰결제 풀 캡처 + 스텝맵 (재구축 준비)
- getevent(`/dev/input/event8`, 0~4095→1080x2400) + 프레임 1.2s 루프로 김건엽(계정3) 구매 3회 + 계정19 비번메시지 시연 캡처. 자산: `logs/2026-06-01-lotte-getevent.log`(26k줄) + `phone_auto/_tmp/_lf/*.png`(2095장).
- **`LOTTE_HOMESHOPPING_STEPMAP.md` 작성** (신 정본, 5/27 런북 대체):
  - 아키텍처: 롯데 프론트(로그인~카드선택~결제하기) 빌드 + 카드앱구간 `pay_lotte` 재사용 + post 적립.
  - 확정 앵커: 비번메시지 dismiss="**30일간 보이지 않기**"(검정 "지금 변경하기" 회피), 현금영수증 지출증빙 사업자번호 **507-18-15504**, 주소 203호, 할인쿠폰 10%×3, 플러스쿠폰 조건부 최고%, 당일카드(오늘 롯데카드), 다음에도사용="네 사용할게요".
  - **G(적립금신청)=웹 크롬으로 분리** 결정(폰 OCR보다 견고, sulwhasoo.py 인프라 재사용, step1 이벤트 매칭). 구매는 앱 필수/신청은 웹 OK.
- 미완: `pay_lotte_homeshopping` 코딩(다음 세션, 스텝맵+프레임 기준). C 섹션 정확순서·F 카드등록 정밀추출 남음.

## 롯데 #4 폰 실결제 — Claude 자율 주행 성공 (저녁)
- `phone_auto/_toss.py`에 **tapt(OCR텍스트탭)/type(특수문자 안전 타이핑)** 추가 → 이걸로 롯데홈쇼핑 #4(owyura=김유라) 전 과정 자율 주행: 로그아웃→로그인→장바구니 전체선택→주소203호→할인쿠폰10%×2→플러스쿠폰14%/10%→적립금+L.POINT 전액→현금영수증 지출증빙 507-18-15504→롯데카드→동의→결제하기→로카페이(LOCA앱)→간편번호 137601(dump 셔플)→**결제완료 (주문번호 2026-06-01-F02625, 526,615원)**.
- ★ 발견: **쿠폰 적용이 적립금/L.POINT 리셋** → 순서 = 쿠폰 먼저, 포인트 마지막. / 비번메시지 실제버튼="30일간 보이지 않기"(검정 "지금 변경하기" 회피). / LOCA = com.lcacApp PayRequestActivity→SimpleNumberAuthActivity(간편번호 dump 137601 자동제출).
- ⚠️ **뷰티포인트 적립 = 코딩 난제**: 주문완료 화면 "정보제공 동의 안내" 박스가 내부 스크롤 div인데 adb swipe가 페이지를 스크롤해버려 "동의함" 도달 실패 → 사용자가 #4 수동 완료. 다음 세션 #5로 nested-scroll 풀어 코드화.
- 상세 라이브검증 = `LOTTE_HOMESHOPPING_STEPMAP.md` 하단 "2026-06-01 라이브 주행 검증" 섹션.

## 다음 세션 TODO (롯데)
1. #4 웹에서 20% 적립신청 (G, 웹 Chrome 흐름 매핑)
2. 라이브 흐름 → `pay_lotte_homeshopping` 코드화
3. #5(yr5326) 코드로 실행 (실결제)
4. 뷰티포인트 동의박스 nested-scroll #5로 풀어 코드화 → 완성
