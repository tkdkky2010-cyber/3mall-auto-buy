# 롯데홈쇼핑 폰앱 결제 — 재구축 스텝맵 (2026-06-01 getevent+프레임 캡처 기반)

> 5/27 `LOTTE_PAYMENT_RUNBOOK.md`는 낡음(카메라 PIN 등). 이 문서가 신 정본.
> 캡처 자산: `logs/2026-06-01-lotte-getevent.log`(26k줄, 터치 좌표) + `phone_auto/_tmp/_lf/*.png`(2095장).
> 구간: 1차구매 김건엽=계정3(완결) / 2차(한도초과 실패) / 계정19(비번메시지 시연) / **계정3 최종구매=프레임1946~2095(대표·플러스쿠폰 포함)**.
> 좌표 변환: ev(0~4095) → screen `x*1080/4095, y*2400/4095`. 터치디바이스 `/dev/input/event8`.

## 아키텍처 (핸드오프)
- **🔨 롯데 프론트(공통, 모든 계정 동일 — 계정만 파라미터)**: 앱초기화+로그인 → 장바구니 전체선택 → 결제설정(적립금/L.POINT/현금영수증/주소/쿠폰) → **카드사 선택** → 결제하기
- **♻️ 핸드오프 → 현대몰 `pay_lotte` 재사용**: 결제하기 후 LOCA(com.lcacApp)앱 진입 → 간편번호 137601 → 결제완료. **카드앱 구간 동일.**
- **🔨 롯데 post(고유)**: 뷰티포인트 적립 + 카드등록 + 적립금신청
- 핵심 작업 = ①공통 프론트(특히 카드 *선택*) ②post 적립. 가운데 카드앱은 pay_lotte 드롭인.

## A. 앱초기화 + 로그인
1. 홈 제스처 → 멀티태스킹 → 전체 앱 종료 → 롯데앱 실행(L 스플래시/LUXURY 광고)
2. 우측하단 "마이" → 우측상단 톱니바퀴(설정) → 로그아웃 → "마이" → **"아이디/비밀번호로 계속하기"**
3. 아이디 입력 → 비밀번호 입력(영문+숫자+특수 8~15자) → 로그인
4. **[조건부] 로그인 직후 스샷 2회로 팝업 확인 (OCR-detect → dismiss)**:
   - **비밀번호 변경 캠페인** (프레임1903 확정): 좌측 흰버튼 **"30일간 보이지 않기"** 탭 (≈289,872). ⚠️ 우측 검정 **"지금 변경하기"**(≈787,873) 절대 금지.
   - **리뷰 작성 안내**: **"나중에 할게요"** 탭 (이번 캡처들엔 안 뜸 — OCR 앵커로만)
   - **광고 팝업**(공세일/20% 등): 계정별 "오늘 하루 보지 않기" 누른 상태면 안 뜸 → 뜨면 닫기/X. (post에도 등장)
   - 둘 다/모두 없으면 바로 B.

## B. 장바구니
1. 우측상단 **장바구니(쇼핑백, 숫자 有 — 이미 PC로 담아둠)**
2. 좌측상단 **"일반" 왼쪽 체크박스** = 전체선택 (헤더 "일반 (n/n)" 로 검증, substring 매칭)
3. **주문하기** (1회 탭, "결제하기" 등장으로 전환검증 — 2회 탭 금지)

## C. 결제설정 (섹션 순서는 결제화면 위→아래 고정 / 코딩 시 프레임1946~로 핀)
1. **적립금 "모두 사용"** / **L.POINT "모두 사용"** (항상 전액)
2. **[조건부] 현금영수증** (L.POINT 사용 시 활성): "지출증빙" 클릭 → 사업자번호 3칸 **507 / 18 / 15504**. L.POINT 0이라 비활성이면 skip.
3. **주소 변경**: "변경 >"(우측정렬, "배송방법"/"픽업" 제외 최대 cx) → 주소목록 **"203호" 검색/포함** 주소 선택 (화곡동890/203호)
4. **할인쿠폰**: 섹션 → "변경 >"(안내문 "버튼" 제외) → dropdown 3개 각 **10%** 선택 → 선택완료
5. **[조건부] 플러스쿠폰**: 활성화된(받은) 제품만, **최고 할인율** 선택(모달 안닫히면 다음% 시도). 받은 게 없으면 패스.
6. **동의(필수)** 체크
7. **당일 할인카드 선택**: 다른결제수단 → 신용카드 → **당일카드**(step1 rate시트 K2:M로 확인, 2026-06-01=**롯데카드**). silent default 금지.
8. **결제하기**

## D. 카드결제 (← `pay_lotte` 재사용 / 핸드오프)
1. 🆕 **"지금 사용하신 카드를 다음에도 사용할까요?" → "네, 사용할게요"** (핸드오프 직전/직후 위치는 프레임으로 확정)
2. LOCA 앱(DIGI LOCA / LOCA Professional) → 결제하기 → **간편번호 137601**(LOCA 키패드) → 결제완료 → "확인"
3. → 이 구간 = `hmall_hyundai_buy.pay_lotte` 와 동일(로카페이 dump). 재사용.

## E. 뷰티포인트 (★롯데는 본인 아이디로만 적립)
1. 구매완료 후 뷰티포인트 적립 창 → **스크롤 내려 "동의함"** → **"적립신청"** → **"확인"**

## F. 🆕 카드등록 (정밀 매핑 TODO)
- 결제 후 카드등록 안내 화면 등장(처음 본 화면). 프레임1946~ 구간에서 정확한 화면/버튼 추출 필요.

## G. 적립금 신청 — ★웹 크롬으로 분리 (폰 아님, 결정 2026-06-01)
- **전제**: 구매는 앱으로(완료된 계정만 가능). 단 **적립신청은 해당 아이디로 웹 크롬 로그인해서 가능** → 폰 흐름에서 분리.
- **왜 웹**: ① sulwhasoo.py CDP Chrome 인프라 재사용 ② 웹 DOM이 폰 OCR보다 견고 ③ 폰 pay 흐름이 A~F로 단순해짐 ④ 1~7번 일괄 배치 가능.
- **흐름(폰 기준 참고, 웹도 동일 위치)**: 결제완료+뷰티 후 → "주문 고객님을 위한 특별혜택" 진입 → "고객님, 사은품 놓치지마세요♥" → 여러 이벤트 中 **step1이 찾은 당일 적립금 이벤트**("구매사은 혜택"의 "최대 ~% 적립")와 매칭 → 그 이벤트 눌러 적립신청. (광고팝업 공세일/20% 닫기 동반)
- **step1 연동**: rate-check가 당일 적립 이벤트를 이미 식별 → 이벤트명/식별자 저장 → 적립신청 시 매칭.
- **구현**: 별도 함수(예: `sulwhasoo.py lotte_reward_claim <idx>` 또는 신규 스크립트). 웹 플로우는 Chrome으로 따로 매핑(폰보다 쉬움).
- ⚠️ **휴대폰 앱으로 구매한 계정만 적립신청 가능** (구매=앱 필수, 신청=웹 OK).

## 코딩 메모
- 전부 **OCR 앵커 기반**(좌표 하드코딩 X). 단 키패드/체크박스 등 절대좌표 필요한 건 프레임 실측값 사용.
- 팝업(비번/리뷰/광고) = `dismiss_if_present` 패턴(OCR text 찾으면 탭).
- 카드앱 PIN 구간은 pay_lotte 재사용 → 카메라 불필요.
- **미완 정밀추출**: C의 섹션 정확 순서/좌표, F 카드등록, G 적립금신청 — 보존된 프레임1946~2095 + getevent로 코딩 시 화면별 확정.
- 검증: 다음 롯데 결제일 라이브 (계정1~7).

---

## ★ 2026-06-01 라이브 주행 검증 (계정4 owyura=김유라, 실결제 완료 — 주문번호 2026-06-01-F02625, 526,615원)
Claude가 `phone_auto/_toss.py`(tapt=OCR텍스트탭, type=안전타이핑 추가)로 **전 과정 자율 주행 성공** (뷰티포인트 동의 단계만 사용자 수동). 검증된 사실:
- **로그아웃→로그인**: 설정(우상단 톱니) → "로그아웃" → 마이 → "아이디/비밀번호로 계속하기" → ID칸 탭+input text, PW칸 탭+input text(특수문자 `!@`는 subprocess argv로 OK) → 로그인.
- **★ 결제설정 순서 확정**: **할인쿠폰 → 플러스쿠폰 → 적립금/L.POINT(전액사용)**. ⚠️**쿠폰 적용이 적립금/L.POINT를 리셋함** → 포인트는 반드시 쿠폰 다 끝낸 뒤 마지막에 "전액사용".
- 할인쿠폰: 섹션 탭(라디오) → "변경 >" → 상품별 dropdown → 모달서 "즉석쿠폰 N원 10% 할인" 탭 → 선택완료.
- 플러스쿠폰: 동일 패턴 → 모달서 최고%(예: 14%) 탭. 상품마다 %다름.
- 적립금/L.POINT = "전액사용" 버튼 탭.
- **현금영수증**: "지출증빙" 라디오 탭 → 빈 입력칸 3개(테두리만, OCR 안잡힘 → 화면 크롭으로 좌표 확인) 탭+input "507"/"18"/"15504". 입력 시 화면 자동스크롤 주의.
- **카드**: "최근 사용한 결제수단"에 롯데카드/KB/삼성 퀵버튼 → 롯데카드 탭(선택시 테두리 강조). "롯데카드 7% 할인" 배너로 당일카드 확인.
- 동의(필수) 체크박스(텍스트 좌측) 탭.
- **결제하기 → 카드결제 화면 "로카페이(앱카드) 추천" → 결제하기** → LOCA앱 `com.lcacApp/...PayRequestActivity`("로카페이 결제 요청", 금액 확인) → 결제하기 → `...SimpleNumberAuthActivity`(간편번호) → **dump 셔플 키패드 137601 자동제출** → "결제 요청되었습니다 → 확인" → 롯데 주문완료(주문번호).
  - ("다음에도 사용할까요?" 팝업은 이번엔 안 뜸 — 안 뜨면 바로 로카페이 결제하기.)
- **E 뷰티포인트 (★코딩 난제)**: 주문완료 화면 → "적립신청" 누르면 "정보제공 동의를 하셔야" 안내 → "정보제공 동의 안내" 박스가 **내부 스크롤 div**인데 **adb input swipe가 박스 대신 페이지를 스크롤**해버림(동의함 도달 실패). → **5번 계정으로 nested-scroll 방법 풀어서 코드화 필요** (작은 contained swipe / 다른 제스처 시도).

## 다음 세션 TODO (사용자 지시)
1. **#4 웹에서 20% 적립신청** (G, 웹 Chrome — 흐름 매핑 필요)
2. **위 라이브 흐름 → `pay_lotte_homeshopping` 코드화**
3. **#5(yr5326) 그 코드로 실행** (실결제)
4. **뷰티포인트 동의박스 nested-scroll을 #5로 풀어서 코드화 → 완성**
