# WORKLOG 2026-05-29 — 폰 결제 자동화 path-1 표준화

## 핵심 결정

1. **정본 경로 = path 1: `buy/run.py` → `phone_auto/flow_runner.py` + `coords/apps/*.json`(flow_payment).**
   - `hmall_pay.py`(어젯밤 6종을 실제 구동한 in-app 드라이버)는 deprecated 예정. 단, 작동 로직은 레퍼런스로 포팅.
2. **하이브리드 운영 모델 확정**: 코드(flow_runner)가 빠른 반복 실행, 깨지면 내가(에이전트) 투입해 진단·수정·코드 반영. 무인(4am)은 실패 시 상태저장+멈춤→나중에 수정.
3. **카드 플로우는 책상에서 못 만든다 — 폰 라이브 캡처 필수** (추측 코드화 = 깨진 JSON 재발). 각 카드 = test 결제+취소 1회 세션.

## 오늘 한 일

### ✅ BC카드(페이북) — flow_runner 무인 자동결제 end-to-end 검증 완료
- `coords/apps/bc_paybook_isp.json` 를 **hmall-driven 18스텝으로 전면 재작성** (기존 QR/PC결제코드/카메라 모델 폐기).
  - 흐름: 장바구니→구매하기→원결제하기→KCP "다음"→페이북 **카드결제 선택(+verify_selected 가드)**→결제하기→**결제비번 6자리(ADB 4-engine vote, nf_key_serial, 카메라 X)** → 자동제출.
- `python3 -m phone_auto.flow_runner bc_paybook_isp flow_payment` 로 **실결제 성공(주문 20260529058176, ₩63,665) → 사용자 취소.**

### ✅ flow_runner.py 버그 4건 수정 (py_compile OK, BC로 라이브 검증)
- `input_pin` **use_camera step-override 복원** 추가 (저장만 하고 복원 안 하던 상태누수 fix).
- `input_pin` **ADB(screencap) 모드도 4-engine `vote_digits` 1회 매핑 후 연속탭** — 단일엔진 `_ocr_digits`가 '0' 누락하던 문제 제거. 매 키 재OCR 안 함(셔플 1회뿐).
- **`verify_selected` 액션 신설** — 페이북머니 하드 가드 (text=selected & forbid_text not selected).
- **`tap_dump_text` `exact` 옵션** 추가 — 버튼 라벨 정확매칭(안내문구 substring 오탭 방지).

### ✅ 현대카드(현대몰) — OCR 4엔진 input_pin 으로 END-TO-END 코드화 + 라이브 검증 완료 (5/29 13:04)
- `coords/apps/hyundai_card.json` flow_payment = hmall-driven 26스텝. PIN/카드비번을 **하드코딩 좌표 → `input_pin`(OCR 4엔진 vote_digits) 으로 전환**(사용자: "셔플아님이지만 OCR로 좌표 확정"). `ocr_keypad.py`에 **`hyundai_hmall_pin6`(roi 0.33~0.58)·`hyundai_hmall_pw4`(roi 0.35~0.61)** preset 신설(고정 3x4, ADB screencap 10/10 실측).
  - 흐름: 장바구니→구매하기→주문결제(현대카드)→'{금액}원 결제하기'→'PIN번호 결제'→(10초)→PIN dot 탭→**PIN 137601(OCR)**→**PIN '확인'(540,1320)**→'결제하기'(WebView dump)→'안전한 결제' 팝업 '확인'→'카드 비밀번호 4자리' 란 탭→**카드비번 1376(OCR)**→**본인인증 '확인'(540,1312)**→주문제출.
  - **라이브 결과**: 21스텝 무인 완주, PIN/카드비번 OCR 둘 다 **10/10**, 모든 '확인'·팝업 통과 → 주문제출 도달. 알파CD **일일 구매수량 한도 초과**로 최종 주문만 미완(= 코드 무관, 수량은 사용자가 컨트롤). **돈 안 나감.**
  - **핵심 교훈(재발방지)**: 현대 **PIN/결제 키패드·'확인' 버튼은 네이티브** → `uiautomator dump`가 뒤 WebView를 잡아 `tap_dump_text` 실패. **키패드 숫자=ADB screencap OCR, '확인'버튼=OCR 실측 고정좌표.** PIN 6자리는 **자동제출 아님 → '확인' 필수**(이전 세션이 이걸 빠뜨려 "두 번 일" 발생). 세션만료(이전 수동 16분)는 무인 완주(~70초)로 해소.
  - **알려진 이슈(현대 무관)**: `_ocr_tesseract`가 numpy array 입력 시 `'utf-8' 0x89` 에러로 죽음(pytesseract↔py3.13) → 현재 4엔진 중 tesseract 빠진 3엔진(easyocr+vision+gcv)으로 10/10. 다른 셔플 카드 작업 시 fix 검토.

### 기타
- `buy/run.py` `cardCd01`(BC) → use_camera **False**(ADB). 카메라 불필요 반영.
- `buy/cart_plan.json`(2026-05-29 kakao cart-only) **제거** → `buy/_cart_plan_2026-05-29_kakao.bak.json` 백업. (사용자: "오늘 거 아닌 듯" → 실제 today 플랜 별도 수급 필요)
- 디버그 전수조사 완료 (아래 "남은 작업"에 반영).

## 발견한 gotcha (재발방지)

- **tap_dump_text는 substring** → 버튼은 `"exact": true` 필수 (예: "다음"이 안내문구 `[다음]버튼을...`(540,1159) 먼저 잡음).
- **주문·결제 페이지엔 `close_popup_if_present` 금지** — dump close-button 오탐으로 (539,1428) 탭→상품페이지 튕김.
- **hmall 콜드스타트 = 런치 광고팝업**("오늘 그만 보기" notShownToday / "닫기" txtClose) → 플로우 진입 전 닫아야. 현재 플로우는 "클린 메인(팝업 X)" 전제.
- **현대 PIN/카드비번 키패드 = 고정(셔플 X)·이미지(dump에 숫자 없음)·ADB screencap 읽힘** → OCR 1회로 좌표 잡아 고정 tap. PIN은 화면 중앙(y~890-1244), 카드비번은 y~946-1300.
- **세션 타임아웃**: 단계마다 캡처/분석하며 끌면 hmall 결제세션 만료 → 코드화 끝나면 캡처 없이 빠르게 완주해야.

## 남은 작업 (우선순위)

1. **현대 빠른 재실행 검증** — 위 flow_payment를 캡처 없이 한 번에 돌려 본인인증(1376)→주문완료까지 확인. (PIN까지는 검증됨)
2. **나머지 카드 라이브 코드화** (각 test결제+취소): **하나·롯데·삼성**. KB/NH는 좌표 미완이라 더 손봐야.
   - `hana_card.json`·`lotte_card.json` = **JSON 문법 깨짐(HEAD부터)** + 틀린 standalone 모델 → 재작성.
   - `samsung_monimo.json` = standalone/카메라 모델 → hmall-driven 재검토.
3. **flow_runner #6**: 카메라 pre-vote BUTTON grid가 삼성 3-col 하드코딩(~825행) → 4-col(kb_pin6 등) preset 기반 동적화 (카메라 카드 작업 시).
4. **DRY_PAYMENT** 안전장치가 인앱 모델에서 유효한지 검증.
5. **hmall_pay.py** deprecated 처리.
6. **오늘 실제 cart plan** 수급 → 카트 담기/결제 (cart_plan.json 재생성).

## 실행 메모
- 폰 ADB: `adb pair 172.30.1.32:<port> <code>` + `adb connect 172.30.1.32:33001` / 작업 시 `ANDROID_SERIAL=172.30.1.32:33001`.
- 카드 PIN: 6자리 공통 `137601`, 현대 카드비밀번호 4자리 `secrets/card_secrets.json` 현대.card_pw4.
- flow 실행: `ANDROID_SERIAL=... python3 -m phone_auto.flow_runner <card> flow_payment` (use_camera 기본 False).
