# WORKLOG 2026-08-30 — 롯데 조합25 #1 결제 (윈도우)

> 이 파일은 8/30 야간 세션(롯데 #1)에서 새로 쓴 것이다. 같은 날 앞선 워크로그는
> 커밋 `19734d9` 에서 삭제됐다.

## 1. 결과

| # | 계정 | 주문번호 | 결제금액 | 카드 | 뷰티 | 구매사은 |
|---|---|---|---|---|---|---|
| 1 | tkdkky2002 | **2026-08-30-D57689** | **541,800원** | NH농협 (5% 청구할인 → 514,710 예상) | 사용자 수동 적립 | ✅ 15,000원 **최대** 신청완료 |

- 혜택: 할인쿠폰 2/2 (70,000) + 플러스쿠폰 14%·14% 2장 (88,200) + 포인트 0
- 도착예정 9/4(금). 구매대장 기록 완료.
- ⚠️ 구매대장에 적힌 금액은 **509,563원**(rate 시트값 = 카드 청구할인 반영 후)이다.
  주문서 버튼 실제 금액은 **541,800원**. 둘은 기준이 달라 어긋나는 게 정상이다.

## 2. ★★ 오늘의 결론 — 핸드셰이크로 **끝까지 결제된다** (사용자 지시)

> 사용자 문장 그대로 (2026-08-30):
> **"핸드세이크로 nh랑 롯데랑 다른카드사들 이용해도 결제 끝까지 완료할 수 있음. 아무런 문제없음."**
> **"핸드셰이크에서 멈추지말고 끝까지 결제진행해"**

종전 코드/문서는 NH·삼성을 만나면 `*_HANDOFF` 로 **멈추고 사람을 기다리는** 것처럼 적혀 있었다.
오늘 실제로 끝까지 갔다 — 멈출 이유가 없다. 에이전트가 그 자리에서 이어서 완주한다.

### 실제로 돈 것 (롯데 #1, 전 구간 라이브)

```
box1            # 1칸 = 비보안 IME → 스크립트가 adb input text 로 직접 입력
box2 "<1행6열>" "<2행6열>"   # 이하 전부 셔플 보안키패드 → 칸마다 재셔플 → 매번 새로 판독
box3 ...
box4 ...
cvc  ...
confirm         # 카드정보 제출
pinfield        # 결제비번 칸 포커스 (키패드 소환)
pin6 ...
confirm         # 최종 제출 → 주문완료
finish_lotte 1 combo=25 card=NH   # 주문번호·구매대장·뷰티·구매사은
```

- **에이전트가 넘기는 것은 "배열"뿐이다.** 1행 6열 / 2행 6열을 `"4,-,8,9,5,7"` 형태로 준다
  (`-` = 보안 아이콘 칸). 값(카드번호/CVC/PIN)은 **스크립트가 `secrets/card_secrets.json` 에서
  직접 읽는다** — 에이전트를 거치지 않는다(`phone_auto/nh_enter.py` 상단 주석).
- 칸을 옮길 때마다 **재셔플**되므로 칸마다 스크린샷을 새로 떠야 한다. 실측 6회 왕복.
- 카드번호 4칸 전부 `[verify] ✓ N칸 4자리` 로 자체검증됐다.

### ⚠️ `pinfield` 가 키패드를 못 띄운다 (오늘 유일한 인계 구간 실패)

`pinfield` 는 OCR 로 `'숫자'/'6자리'` 텍스트를 찾아 그 좌표를 탭하는데, 그건 **플레이스홀더 텍스트**라
탭이 **라벨 쪽(312,941)** 으로 들어가 `키패드 X` 가 됐다. 입력칸 **안쪽(660,941)** 을 직접 탭하니 떴다.
→ 다음에도 `[pinfield] 키패드 X` 가 나오면 그냥 입력칸 안쪽을 탭할 것.

## 3. ★ 근본버그 — 카드 단계가 `결제하기` 버튼 뒤에 깔린 행을 못 찾았다

`CARD_FAIL:'다른 결제수단'→'신용카드' 그리드 미도달(4회 재시도)` 로 **3회 연속** 죽었다.

### 원인 (실측)

- `select_card_lotte` 의 스크롤 폴백이 **방향 고정**이었다 — `attempt<2` 면 아래로, 그 뒤엔 위로.
  쿠폰 구역에서 결제수단까지 900px×2 로는 못 닿는데 3·4번째에 방향을 뒤집어
  **주문서 꼭대기로 되돌아간다.** (사용자 지적: "왜 굳이 또 스크롤을 끝까지 올림?")
- 그리고 진짜 원인은 **판독이 아니라 위치**였다. 윈도우 OCR 은 `다른 결제수단` 을
  **(230,1610) 에 멀쩡히 읽는다**(실측). 폴백이 멈춘 자리에선 그 행이 cy≈2141 —
  화면 하단에 **고정된 `541,800원 결제하기` 버튼 뒤에 깔려** OCR 목록에서 사라진 것이다.

### 수정

`PAY_BTN_TOP_CY = 1950` 신설 + 폴백을 `_scroll_to("다른 결제수단", max_cy=PAY_BTN_TOP_CY)` 로 교체.
쿠폰 단계가 이미 쓰던 `max_cy` 처방과 같다. **플랫폼 분기 없음** — `_scroll_to` 가 맥=legacy /
윈도우=신규로 내부에서 갈린다. → 수정 후 `당일카드 감지/선택 = NH (5%) via 다른결제수단>신용카드>카드선택` 통과.

## 4. ★ 내가 한 실수 — "화면 하나 보고 없다고 단정"

**잘린 스크린샷 아래를 확인하지 않고 `다른 결제수단` 이 화면에 없다고 결론냈다.**
그 전제로 '최근 사용한 결제수단에 이미 선택됨을 인식하는' 우회 헬퍼(`_recent_method_already`)와
`WIN_ONLY_FIX` 분기까지 새로 썼다. 사용자가 **"화면 가장 아래쪽 주문 내역 확인 동의 위에 보면
다른 결제수단 있는데 너가 못찾은거임"** 이라고 지적해 틀린 게 드러났고, 그 코드는 전부 되돌렸다.

- READ_FIRST 의 **"관측이 도메인 상식과 모순되면 관측부터 의심한다"** 에 정확히 걸리는 사례다.
- 교훈: **하단 고정 버튼이 있는 화면에서 "없다"는 판정은 금지.** 스크롤해서 버튼 위로 올린 뒤 다시 본다.
- 두 번째 교훈: 특수 케이스(`WIN_ONLY_FIX` 분기)를 새로 파기 전에 **기존 호출 규약(`max_cy`)을
  제대로 쓰고 있는지부터** 본다. 오늘 원인은 분기 부족이 아니라 인자 누락이었다.

## 5. `resume` 이 CART 에서 장바구니를 **다시 찾아 들어간다** (미수정, 우회함)

`resume` 이 `CART` 로 판정해놓고 `goto_cart_select_all()` 이 **장바구니를 떠나
`마이`(755,2225) → 카트 아이콘(1002,148)** 으로 재진입한다. 그 왕복이 2회 연속 깨졌다:
`wait_text('주문하기') ✗TIMEOUT` → `CART_FAIL:장바구니 미도달`.

- 좌표는 맞다(이 화면 카트 아이콘 실측 = (1003,149)). 페이지 로딩 중 탭이 흐른 것으로 보인다.
- **우회**: 카트에서 사람이/에이전트가 `주문하기` 를 직접 1회 탭해 주문서를 만든 뒤
  `resume` 을 붙이면 그 구간을 안 탄다. 오늘 그렇게 뚫었다.
- [ ] **다음에 고칠 것**: `detect_screen()==CART` 면 재진입을 건너뛰고 **그 자리에서** 전체선택→주문하기.

## 6. 결제 가드 — `MAX_PAY` 는 조합마다 다시 잡는다 (사용자 지적)

> 사용자: "MAX_PAY=560000 유지 이거는 다른 조합 구매할 때는 56만원 넘어갈 수도 있는데?"

맞다. 조합25 기준 세 값이 이렇게 갈린다:

| 상태 | 금액 |
|---|---|
| 정상 (할인쿠폰+플러스쿠폰) | 541,800 |
| 할인쿠폰만 (플러스쿠폰 누락) | 630,000 |
| 쿠폰 0장 | 700,000 |

→ `MAX_PAY=560000`. 조합이 바뀌면 세 값이 통째로 바뀌므로 **그대로 재사용 금지.**
⚠️ rate 시트 금액으로 자동계산하면 안 된다(시트=청구할인 **후**, 버튼=**전**).

- [ ] **자동 보정 후보**: 장바구니 버튼이 `주문하기 (2건 630,000원)` 으로 **플러스쿠폰 누락 시 금액**을
  그대로 보여준다. 조합 무관하게 `MAX_PAY = 카트 버튼 금액` 으로 잡으면 "플러스쿠폰 0장이면 차단"이
  자동으로 성립한다. 사용자 확인 후 반영할 것.

## 7. 사용자 지시 (문장 그대로 보존)

- **"원격제어 하지마"** — 이번 세션 전 구간 ADB CLI 로만 진행했다.
- **"핸드세이크로 nh랑 롯데랑 다른카드사들 이용해도 결제 끝까지 완료할 수 있음. 아무런 문제없음."**
- **"뷰티포인트는 내가 적립완료했음 롯데건은 결제 끝나면 cft에서 항상 최대 x원, 최대x % 포인트 적립신청하는걸로 해"**
  → 오늘 `claim_lotte_reward` 가 이미 **최대(15,000원 / 150,000원 기준 달성)** 로 신청완료됐다.
    화면 문구 `적립금 15,000 원 적립가능! / 최대 혜택을 달성했어요 / 혜택 신청완료`.
- **"포인트는 남아있으면 입력하는 란이 있고 없으면 포인트 자체가 없기 때문에 없는거야"**
  → `포인트 {'used': 0}` 은 **정상**이다. 버그로 보고 파지 말 것.
  (`현금영수증 skip: 섹션 없음(L.POINT 0)` 도 같은 이유로 정상.)

## 8. 변경 파일

| 파일 | 변경 |
|---|---|
| `phone_auto/lotte_homeshopping_buy.py` | `PAY_BTN_TOP_CY` 신설 / `select_card_lotte` 스크롤 폴백 → `_scroll_to(max_cy=...)` |
| `READ_FIRST.md` | 핸드셰이크 = 끝까지 완주 / 하단 고정버튼 판정 금지 / 롯데 적립 최대신청 |
| `cart/today_carts.json` | 롯데 #1 paid + 주문번호·금액·카드 |
| `WORKLOG_2026-08-30.md` | 신설 (이 파일) |

## 9. 남은 것

- [ ] 롯데 #2~7 미결제 (카트 담긴 상태). 같은 절차로 진행 가능.
- [ ] `resume` CART 재진입 제거 (5절)
- [ ] `MAX_PAY` 카트금액 자동보정 (6절)
- [ ] `pinfield` 입력칸 탭 좌표 보정 (2절)
