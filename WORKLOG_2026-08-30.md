# WORKLOG 2026-08-30 (일) — 현대몰 식품 13~19 롯데카드 결제 7/7

## 지시 (사용자 문장 그대로)

> 13-19 현대몰 장바구니담아놨어 구매진행해
> 119314 172.30.1.99:42679 페어링코드이고 포트번호 34457 임
> 했음 그리고오늘 구매할때 포인트 다써줘
> 왤케 느려? 맥에서하는거랑 속도가달라 왜?

## 0. 오늘 산 것

| | 값 |
|---|---|
| 몰 | 현대Hmall (식품) |
| 상품 | `2` 하루견과 초록색 100봉 = [우수고객전용] 순수견과100% 넛츠시그니처 23gx100봉 **×2** |
| 계정 | 13~19 (gcd0327 / johwajeong / jyj041220 / miu1838 / skykow / ybkim9960 / yeonsuk0428) |
| 담기 | 이 세션 아님 — 사용자가 미리 담아둠 |
| 당일카드 | **롯데 5% 즉시할인** (캐러셀 후보 1개, `detect_card` 자동감지) |
| 적립행사 | 릴레이푸드 최대 10%적립 `P202608074038` (~8.30 23:59, 5단 tier / 15만원↑ 15,000P) |
| 포인트 | **전액사용**(사용자 지시). #13 만 잔액 0 |
| 결과 | **7/7 결제 완료**, 적립 7/7 확인 |

### 결제 결과

| # | 아이디 | 주문번호 | 결제금액 | 카드청구(포인트 차감 후) | 판정 |
|---|---|---|---|---|---|
| 13 | gcd0327 | 20260830062103 | 144,229 | 144,229 | 자동 |
| 14 | johwajeong | 20260830062763 | 145,406 | 121,876 | **오판→수동복구** |
| 15 | jyj041220 | 20260830062954 | 145,536 | 119,396 | **오판→수동복구** |
| 16 | miu1838 | 20260830063209 | — | 120,298 | 자동 |
| 17 | skykow | 20260830063780 | 144,831 | 132,791 | **오판→수동복구** |
| 18 | ybkim9960 | 20260830063921 | — | 130,188 | 자동 |
| 19 | yeonsuk0428 | 20260830064381 | 145,498 | 120,118 | **오판→수동복구** |

합계 카드청구 **888,896원**. 주문서 할인 구성(실측 #15): 주문금액 179,800 − 쿠폰할인 17,980
− 카드 즉시할인 6,284 − 결제금액할인쿠폰 10,000 = 결제금액 145,536 → 포인트 차감 → 카드청구.

## 1. ★★ LOCA 앱 등록정보 만료 — 결제가 통째로 막혔다

첫 실행 #13 이 `tap_dump_text '결제하기' 미발견 (timeout 8s)` 로 죽었다. **이름이 원인을 가렸다.**
LOCA 앱을 직접 띄워 dump 를 읽으니 결제화면이 아니라 모달이었다:

```
앱 재설정 안내 / 앱에 등록된 정보가 만료되어, 안전한 이용을 위해 다시 인증이 필요합니다.
로카페이, 간편번호, 지문·얼굴인식 등 사용하시던 정보도 함께 등록해 주세요. [취소] [초기화]
```

- 8/25 KB Pay(USB 디버깅 차단이 'KB앱 미진입'으로 나타남)와 **완전히 같은 함정**.
- 재등록은 본인인증/비밀번호라 자동화 대상이 아니다 → 사용자가 직접 처리 후 재개, 그 뒤 전 계정 정상.
- → `_diagnose_loca()` 신설. 롯데앱 flow 실패 시 화면을 읽어
  `LOCA_APP_RESET_REQUIRED` / `LOCA_APP_UPDATE_REQUIRED` / `LOCA_LOGIN_REQUIRED` 로 **이름 붙여** 죽고,
  못 알아본 화면은 텍스트를 그대로 로그에 남긴다.

## 2. ★★ 현대몰에 돈 가드가 없었다 (롯데에만 있었음)

8/25 사용자 지시("현대몰에서도 kb카드 쓰는날있을거임, 그럼 그 때도 이지랄 안나게")를 이제 이행:

- `read_pay_amount()` — 하단 `N원 결제하기` 를 **OCR + dump 병합**으로 읽음(쪼개진 OCR 폴백 포함)
- `money_guard(idx, res)` — `MAX_PAY` 초과 / **금액 판독 실패** / `STOP_BEFORE_PAY=1` 이면 결제 안 함
- `buy_one` 과 `resume` **양쪽**에 배치 (한쪽에만 있는 가드는 곧 구멍)

실사용: `MAX_PAY=150000`. 즉시할인이 안 걸리면 151,820원이라 자동 차단된다.
실행 전 `STOP_BEFORE_PAY=1` 로 #13 을 실돈 없이 예행 — 금액·카드·가드를 먼저 검증했다.

## 3. ★★★ `wait_order_complete` 가 결제 성공을 실패로 오판 — 4계정

**결제는 됐는데 대장·적립·paid 가 통째로 안 남았다.** (#14·#15·#17·#19)

- 1차 원인: 이 함수가 **OCR 단독**이었다. 롯데 파일엔 8/25 에 dump 폴백을 넣어놓고(`5e7895f`)
  **현대몰 파일엔 같은 모양을 안 고쳤다** — READ_FIRST 「버그 하나 고치면 같은 모양을 폴더 전체에서 찾는다」 위반.
  → `_read()` 로 `_ocr_texts + _dump_texts` 병합. 고친 뒤 #16·#18 은 주문번호까지 판독 성공.
- 2차 원인(미해결): 병합 후에도 #17·#19 는 실패. 카드앱 구간 소요가 **#16 39.7s / #18 39s 는 성공,
  #17 43.0s / #19 는 실패** — 주문완료 화면이 20s 창을 벗어나 home 으로 자동이동하는 것으로 보인다.
  **타임아웃을 늘리는 추측 패치는 하지 않았다.** 다음 세션 과제는 §6.
- 복구: 4계정 모두 PC 주문내역에서 주문번호·금액 실측 → 대장 수동기록 + 매니페스트 `paid=True`.
  **결제 실패가 아니라 판독 실패**임을 주문내역으로 확정하고 나서 기록했다(추측 기록 금지).

## 4. ★★ H.Point 적립이 이 PC 에서 통째로 안 걸렸다 — `python3` 스텁

```
[적립] 시도1 미확정(login_ok=False) — 재시도  ×3 → RESULT ok=False | stderr: Python
```

`buy.py` 의 `BROWSER_PY` 기본값이 `"python3"` 인데, 윈도우에서 그건 **Microsoft Store 스텁**
(`.../WindowsApps/python3`)에 잡힌다. 스텁은 아무것도 실행하지 않고 "Python was not found" 만 찍고 끝난다.
→ 적립 서브프로세스가 `login True` 를 못 내고 **계정마다 3회 재시도 후 0건**.

- 8/25 인코딩 사고와 **같은 계열**: 윈도우 포팅에서 "조용히 빈 결과" 가 나오는 자리.
- → `_resolve_browser_py()` — 후보를 **실제로 한 줄 실행해 보고** 고른다. 이름이 있다고 쓰지 않는다.
- 적립 최종: 7계정 전부 `이미신청`(login True 상태에서 이벤트 페이지 `.complete` 판독).
  ⚠️ 단 #13~15 는 이 세션에서 신청 클릭이 성공한 적이 없는데도 완료로 읽혔다 →
  **이 행사는 구매 시 자동응모되는 유형일 가능성**. 다음에 같은 행사면 확인해 볼 것.

## 5. 구매대장 금액 기준이 섞였다 → 근본수정

`record_food` 는 금액을 `cart/today.json` 의 즉시할인가에서 가져오는데 그건 **포인트 사용 전** 값이다.
포인트를 쓰면 실제 청구액과 어긋난다(#16 대장 144,229 vs 실청구 120,298). 자동기록분과 수동기록분이
서로 다른 기준이 됐다.

- → `_record_after_done` 이 **`money_guard` 가 읽은 실측 `pay_amount` 를 우선** 사용(단일상품 주문 한정,
  혼합 카트는 상품별로 못 쪼개니 종전대로).
- 오늘분 2건(#16·#18)은 로컬 JSON + 구글시트 **양쪽** 정정 완료(READ_FIRST 「출력물은 두 군데」).

## 6. 아직 안 끝난 것

- [ ] **`wait_order_complete` 20s 창 문제** — dump 병합으로도 #17·#19 실패. 추측 패치 대신
      **CDP 폴백**(hmall webview 로 마이페이지/주문상세를 열어 주문번호를 읽는다)을 넣는 게 맞다.
      화면 판독에 의존하지 않으니 타이밍과 무관해진다.
- [ ] 뷰티포인트 `재인증 버튼 없음` 은 식품 주문에선 정상(설화수 전용) — 상태문구가 실패처럼 보이니 분리할 것.
- [ ] `cart/today.json` 의 `payment.kakao_price` 는 check10 이 '바로구매 실패'로 못 읽어
      **주문서 실측치(144,229)를 손으로 채웠다**(`kakao_price_source` 에 출처 명시). check10 바로구매 실패 원인 미조사.

## 7. 속도 — "맥보다 왜 느린가" (실측)

| 동작 | 이 PC(윈도우·무선 ADB) |
|---|---|
| `adb screencap` 왕복 | 0.47s (58KB) |
| **`uiautomator dump` 왕복** | **2.5~2.8s** |
| cap + OCR 1회 | 1.4s |
| 같은 화면 판독량 | OCR **7개** vs dump **69개** |

계정당 총소요: 맥 **75.2s**(8/5 KB) → 윈도우 **94~106s**. **무선 ADB 탓이 아니다**(screencap 0.47s).
비용은 `uiautomator dump` 2.5~2.8초이고, 윈도우 OCR 이 약해서(7개 vs 69개) 판독마다 dump 를 겹쳐야 한다.
= **정확도 때문에 지불하는 속도**. 8/25 에 dump 가 죽어 있었을 때 쿠폰 0장·전체선택 0/2 사고가 그 대가였다.
그리고 배치 벽시계의 대부분은 속도가 아니라 **계정 간 7분 간격**(6회=42분)이다.

선택지(사용자 판단): ① `HMALL_ACCOUNT_GAP_SEC` 축소 ② dump 를 항상 겹치지 말고 OCR 실패 시에만 호출
(계정당 20~30초 절약, 대신 OCR 오독을 교정할 기회 상실).

## 8. 오늘 배운 것

1. **증상 이름이 원인을 가린다 — 또.** `결제하기 미발견`=LOCA 등록만료, `ORDER_NOT_COMPLETE`=판독실패(결제는 성공),
   `적립 무응답`=python3 스텁. 셋 다 이름만 보면 엉뚱한 데를 판다.
2. **한 파일에서 고친 버그는 다른 파일에도 있다.** 8/25 에 롯데 `wait_order_complete` 에 dump 폴백을 넣고
   현대몰을 안 봤다. 그 대가가 오늘 4계정 기록 누락이다.
3. **"결제 실패"와 "판독 실패"를 구분하지 못하면 이중결제로 간다.** 오늘은 재시도 전에 주문내역부터 확인했다.
4. **윈도우 포팅의 조용한 실패는 아직 남아 있다.** 인코딩(8/25) 다음은 인터프리터(오늘). 외부 이름에
   의존하는 자리는 전부 "실제로 실행해 보고" 고르는 방식으로 바꿔야 한다.

━━━━━━━━━━━━━━━━━━━━━━ 병합 (다른 PC 8/26·8/28 작업과 합침) ━━━━━━━━━━━━━━━━━━━━━━

푸시하려다 원격이 5커밋 앞서 있는 걸 발견했다 — **다른 PC 가 8/26·8/28 에 같은 파일을 고쳐놨다.**
즉 오늘 작업은 그만큼 **뒤처진 코드 위에서** 한 것이다. 그냥 합치면 한쪽이 지워지므로 겹치는 곳을 손으로 합쳤다.

| 자리 | 내 쪽(8/30) | 원격(8/26·8/28) | 합친 결과 |
|---|---|---|---|
| 결제 직전 가드 | `money_guard` + `MAX_PAY` | `HMALL_STOP_BEFORE_PAY` + `pay_button_amount()` | **`money_guard` 하나로 통합** — MAX_PAY·판독실패·정지(두 env 이름 모두 수용)·15만원 tier 문구 유지 |
| 금액 판독 | `read_pay_amount`(OCR+dump) | `pay_button_amount`(OCR 단독) | **`pay_button_amount` 를 정본으로 두고 dump 병합·쪼개진 OCR 폴백을 이식**, 내 중복 함수는 삭제 |
| 롯데 `_scroll_to` | OCR+dump 병합 + `max_cy` 상한 | **양방향 스크롤**(#2 630,000원 사고 수정) | **둘 다 살림** — 병합 판독 유지 + 못 찾으면 반대방향 재탐색 |
| `cart/today*.json` | 오늘(8/30) 데이터 | 8/28 데이터 | 오늘 것 채택 |

**여기서 알게 된 사용자 지시(내가 모르고 있던 것):**
> 2026-08-24 "마지막 결제하기 버튼 누르기 전에 가격확인 — 15만원 넘는지"
> 적립 tier 기준은 **포인트 차감 전** 금액이라 `HMALL_NO_POINTS=1` 과 같이 쓴다.

오늘은 사용자가 "포인트 다써줘" 라고 지시해 전액사용으로 갔다. 다만 오늘 상품은 **포인트 차감 전
금액이 이미 144,229원**(15만원 미만)이라 15만원 tier 는 애초에 닿지 않았다 — 포인트 사용이
tier 를 떨어뜨린 건 아니다. (쿠폰할인 17,980 + 결제금액할인쿠폰 10,000 이 이미 들어간 결과)

**교훈: 작업 시작 전에 `git fetch` 부터 한다.** 다른 PC 와 같은 레포를 쓰는데 오늘은 5커밋 뒤처진
코드로 결제까지 돌렸다. 원격에 이미 있는 수정을 다시 만들거나(그럴 뻔했다), 원격 수정을 덮어쓸 수 있다.
