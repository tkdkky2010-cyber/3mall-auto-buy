# WORKLOG 2026-07-10 (롯데 #11~14 LOGOUT_FAIL 근본원인 = 폰 절전/잠금 → wake preflight 수정)

## 요약 (한 줄)
어젯밤 #11~14 연속 `LOGOUT_FAIL`의 근본원인 = **폰 화면 절전+보안잠금 → screencap 완전검정(밝기 0.0) → OCR 전맹**.
코드에 화면 상태 확인이 0줄이었음 → `wake_screen()` preflight 추가(양 몰 공용), buy.py python 경로 폴백 수정.

## 사고 타임라인
- 07-09 23:57 **#10 결제 성공** (2026-07-09-C25028, 515,242원, 삼성 7%, 뷰티✓/적립✓, ledger 기록).
- 이후 몇 분 idle 사이 폰 화면 절전 → **기기 보안잠금**(deviceLocked=1, keyguard showing=true).
- 00:0x #11~14 연속 실행 → 전 계정 `LOGOUT_FAIL` ("로그아웃 미발견"). 실제로는 로그아웃 문제가 아니라
  **검은 화면을 OCR**한 것 (증거: 실패 런 마지막 `/tmp/_hd_buy.png` 평균밝기 0.0 / phone_auto 전체에 WAKEUP 코드 0건).
- 5/31 워크로그 "폰 끊김=절전" 교훈이 코드에 미반영이었던 것이 원인의 원인.

## 수정 (커밋 대상)
1. **`phone_auto/hmall_hyundai_buy.py`**: `wake_screen()` 신설 (cap 아래, 정본 공용).
   WAKEUP(keyevent 224) → 키가드면 dismiss-keyguard+스와이프(비보안만 풀림) → 재판정.
   보안잠금은 adb로 못 풂 → `ok:False`. 판정 문자열 SM-G9960 실측(`mWakefulness=Awake`/`showing=true`).
   hmall `buy_one` 시작 시 호출 → 잠기면 `SCREEN_LOCKED` 즉시 반환.
2. **`phone_auto/lotte_homeshopping_buy.py`**: `buy_one` 시작 시 wake_screen → `SCREEN_LOCKED` 즉시 반환.
   main 루프: SCREEN_LOCKED 면 **나머지 계정 중단**(잠긴 폰에 헛돌기 금지).
3. **`buy.py`**: `PHONE_PY` 기본 `/opt/homebrew/bin/python3` 이 시스템에서 사라짐(brew python3 심볼릭 소멸,
   3.13만 남고 pyobjc/Vision 없음) → 존재하는 인터프리터 폴백(`/usr/bin/python3`=Vision+GCV 확인됨).
   이전엔 문서대로 `python3 buy.py 롯데 N` 치면 FileNotFoundError.

## 검증
- 문법 OK + `buy.PHONE_PY=/usr/bin/python3` 해석 확인.
- **라이브**: 잠긴 폰에서 `lotte_homeshopping_buy 11 12` → `[#11] SCREEN_LOCKED … [STOP] 나머지 계정 중단` ✓ (결제 없음).

## 무선 ADB (자동메모리 adb-phone-wireless-connect 상세)
- 사용자 값 순서 = `<페어링코드> <IP:페어링포트> 포트번호 <연결포트>`. pair↔connect 포트 뒤바꾸면 protocol fault.
- 데몬은 `ADB_MDNS_OPENSCREEN=1` 필수 (기본 mDNS는 macOS Bonjour에서 데몬 크래시). `adb mdns services`로 실포트 교차확인.
- 현재 연결: `192.168.0.106:38101` (SM-G9960).

## 현황 / 다음
- 07-09 카트(계정 8~14, combo3) 최종: **#8·#9(수동)·#10 결제 완료 / #11~14 = 포기**(사용자 결정 07-10 — 당일 지나 혜택 소멸).
- ⚠️ **#11~14 장바구니에 combo3 잔존** — 다음 롯데 카트 담기/결제 때 잔여물 확인(중복·구버전 조합 주의).
- 잔존 이슈: 삼성 금융인증서 셔플 OCR 오독(07-09 #9, 재발 시 인증서 단계만 수동) / ledger 롯데 order_no/amount 간헐 null.
- 이번 수정 3건(wake preflight/SCREEN_LOCKED 중단/buy.py PHONE_PY 폴백)은 **미커밋** — 다음 커밋 때 포함.
