# NH 일반결제 — 에이전트(클로드) 비전 키패드 입력 정본 (2026-06-26 라이브 확정)

> **OCR 정본 = 에이전트(클로드) 비전.** nppfs 보안키패드는 로컬 OCR(easyocr/vision/gcv, tesseract 제거)이
> 부정확(셀별로 잘라도 4/10). **클로드가 스샷을 직접 읽으면 10/10.** 그래서 키패드는 클로드가 읽고 고정그리드로 탭한다.
> 무인/스케줄 실행에선 클로드가 루프에 없어 불가 → **대화형(step1 실행) 에서만** 이 방식 사용. 무인은 로컬 3엔진 폴백.

## 키패드 동작 (실측)
- **셀 위치 고정, 숫자 라벨만 셔플.** 방패(🛡) 2칸은 decoy.
- **같은 칸 4자리 입력 동안은 셔플 안 됨** → 칸마다 1회만 읽으면 됨.
- **칸이 바뀌면(box2→3→4, CVC, 비번칸) 재셔플** → 새로 읽는다.
- box1(cardno1)=비보안 IME칸 → 시스템 키보드(고정). `adb shell input text 5424` 로 입력(자동 box2 이동).
- box2~4(cardno2~4), CVC(inputCVC), 6자리 비번 = nppfs 보안키패드(셔플).

## 고정 그리드 (`phone_auto/hmall_hyundai_buy.py`)
- `KP_COLS = (173, 320, 467, 614, 761, 908)` — 6열 중심 x (device px, 1080폭)
- 숫자 2행 y = **키패드 컨테이너 top + (217, 362)**. 컨테이너 top 은 필드마다 다름:
  - 카드칸 키패드 top≈893 → 행 1110/1255
  - CVC 키패드 top≈1005 → 행 1222/1367
  - 6자리 비번 키패드 top≈963 → 행 1180/1325
  - → `_kp_top()` 가 dump 로 top 자동 검출. `_grid_tap(value, layout, top)` 가 탭.

## 절차 (step1/NH 결제 시)
1. **전처리(새 카드창까지 자동)**: `NH_VISION_MODE=1 ANDROID_SERIAL=<serial> /opt/homebrew/bin/python3 -m phone_auto.hmall_hyundai_buy <idx> NH`
   → 로그인→카트→주문서→H.Point→NH선택→결제하기→다른결제→일반결제 후 **카드창에서 정지**.
2. **box1**: `adb -s <serial> shell input text 5424` (시스템 키보드). 1.5s 대기.
3. **box2~4**: 칸마다 [스샷 → 리사이즈(>2000px 면 `sips -Z 1700`) → 클로드가 배열 읽기 → `_grid_tap` 또는 직접 adb tap].
   - 카드번호 5424 1672 1035 9398. box2=1672, box3=1035, box4=9398.
4. **CVC**: 자동 이동됨. 스샷 → 읽기 → CVC=953 탭.
5. **확인**: `adb tap 705 1170` (폼 파란 확인).
6. **6자리 비번**: "숫자 6자리" 칸 탭(dump로 위치) → 키패드 스샷 → 읽기 → 137601 탭.
7. **최종 확인**: `adb tap 705 1170` → "주문 완료" 확인.

## 주의
- **세션 타임아웃 ~5분** — 카드창 열린 뒤 빨리 끝내야 함. 오래되면 "결제가 취소되었습니다"(카드오류 아님). 새 세션에서 빠르게.
- 스샷은 device 1080x2400 → **2000px 초과라 클로드가 못 읽음. 항상 `sips -Z 1700` 리사이즈 후 읽기** (그리드 좌표는 device 기준이라 리사이즈 무관).
- 카드번호 한 자리라도 틀리면 "해당카드가 없습니다 (입력오류 N/3)" → 3회 누적 잠김. 비전으로 한 자리씩 확인하므로 정확.
- 카드 secrets: `secrets/card_secrets.json['NH']` (card_no/cvc/pin6). 코드/커밋에 평문 금지.
