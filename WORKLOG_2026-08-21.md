# WORKLOG 2026-08-21 — 롯데 담기 3s 경계버그 근본수정 / 조합14 폰결제 #9~11 / 현대몰 식품 담기 13계정

## 1. 롯데 #9~15 조합14 담기 — 1차 1/7 실패 → 근본원인 수정 → 7/7

사용자 지시: "9-15번 롯데 아이디에 14번 조합 담아줘" (조합14 = g 자음생2종×2 + h 자음생크림리치×1).

1차 실행: **#9만 성공, #10~15 전부 담기 검증 실패(카트 0~1건)**. 담기 자체는 "성공"처럼 찍히는데
카트가 비는 조용한 실패였다.

### 근본원인 (실측으로 확정)

1. `#saveCart-btn` 클릭 전 **옵션(타입 선택→세트) 클릭이 TimeoutError** 로 죽고 있었는데
   `except: pass` 라 **로그가 없었다**.
2. 옵션 미선택 상태로 장바구니를 누르면 사이트가 **"타입 옵션을 선택해주세요" alert** 를 띄우는데,
   Playwright 기본값이 dialog 자동 dismiss 라 **alert 도 소리 없이 사라졌다.**
3. 옵션 클릭이 왜 타임아웃이냐: **'타입 선택' 버튼이 뷰포트 하단 경계에 딱 걸쳐 있다**
   (rect top=810, viewport height=811 — 1px 만 보임). Playwright 자동 스크롤·안정화 루프가
   여기서 **약 3초**를 소모하는데 코드 타임아웃이 정확히 3000ms → **경계선에서 실패**.
   창 크기·페이지 상태에 따라 되다 안 되다 한 이유(1차 #9 성공, 나머지 실패)가 이것.

### 수정 (buy/sulwhasoo.py)

- 옵션 클릭 전 **명시적 `scrollIntoView({block:'center'})`** + 타임아웃 3000→**6000ms**
  (실측: 스크롤 선행 시 클릭 1.6초에 통과).
- 옵션 실패를 **삼키지 않고 로그 출력**.
- `lotte_add_combo` 에 **dialog 로거** 등록 — alert 메시지가 이제 로그에 남는다
  (`[dialog:alert] …타입 옵션을 선택해주세요`). 이게 없었으면 원인 못 잡았다.
- 쿠폰 레이어(`#layer_down_coupon`) 강제 숨김을 **옵션 선택 앞으로** 이동
  (기존엔 saveCart 직전에만 숨겨 옵션 단계가 무방비 — 이것도 실제 가로챔 사례 재현됨).

수정 후 #10~15 **전부 `카트 2건` 검증 통과**. #9 포함 7/7.

> 교훈: **"클릭 성공 ≠ 담김"인 사이트에서 실패를 삼키는 except/자동 dismiss 는 디버깅을 불가능하게
> 만든다.** 이번에 로그 2종(옵션 실패·dialog)을 남기게 한 것이 원인 확정의 결정타였다.

## 2. 현대몰 식품 담기 — 계정 4~16, 13/13

사용자 플랜(시트 캡처): #2 하루견과 초록색 100봉 ×2 → 4~6 / #25 유기농 석류젤리 9박 ×2 → 7~11 /
#22 올바른건강식품 와이 9박 ×2 → 12~16.

- `buy/cart_plan.json` 8/21 자로 갱신 → `buy/run.py` 4-8 / 9-12 / 13-16 분할 실행(600초 제한).
- **13/13 담기 성공** (로그인 1차 실패 → 쿠키폐기 재시도로 전부 회복: #5·#8·#11).
- ⚠️ **#12(Jinhwa4553) 카트에 삭제 불가 잔여상품 1건** — '[우수고객전용] 스키니랩 시서스 다이어트'
  선택삭제 버튼 없음. **식품 결제 전에 처리 방법 확인 필요** (같이 결제될 위험).

## 3. 롯데 조합14 폰결제 — #9~11 완료, #12~15 사용자 '스탑'으로 미착수

무선 ADB: pair 192.168.0.8:44231(코드 566645) → connect :40567, OPENSCREEN 데몬. 한 번에 연결.

| 계정 | 주문번호 | 금액 | 뷰티포인트 | 비고 |
|---|---|---|---|---|
| #9 wlstmdlsfk | 2026-08-21-J61486 | 520,455 | ✅ 완료 | 8/18 `loca_method` 실패 재발 안 함 |
| #10 kim455307 | 2026-08-21-J61890 | 520,455 | ✅ 완료 | |
| #11 diinamic | 2026-08-21-J62399 | 520,455 | **멤버십 없음(정상 실패)** | 아래 상세 |
| #12~15 | — | — | — | 사용자 '스탑' 지시로 미착수. **날짜 지나면 카트 폐기, step 재실행부터** |

- 카드: 전 계정 당일카드 **롯데 7%** 자동감지, LOCA PIN flow 완전 자동(핸드세이크 불필요 — 삼성·NH만 해당).
- 구매대장: 3건 모두 자동기록(조합명 `g자생2 2 + h자생크림 1`, 520,455원, 주문번호 포함).
- 구매사은 적립금: 3계정 모두 **'최대 N% 적립' 카드 미발견 → SKIP** — 이 상품이 광세일
  구매사은 비대상일 가능성. 신청기간 내 재진입 가능하므로 대상 맞다면 추후 일괄 신청.
- **nap() sleep 축소(배율 0.4) 첫 라이브 검증 통과** — 3계정에서 카드선택 밀림/쿠폰 applied=0/
  주소 팝업 문제 없음.

### #11 이 두 번 발목 잡은 것

1. **1차 CART_FAIL**: 장바구니에서 전체선택이 안 된 상태(일반 0/2 → '주문하기 (0건 0원)').
   **폰에 다른 앱 알림이 떠서** 전체선택 탭이 빗나간 것(사용자 확인). 재시도로 결제 완료.
2. **재시도 중 '버벅거림'(사용자 스탑)**: 결제·대장기록까지 끝난 뒤 뷰티포인트 단계에서
   **"고객님의 정보와 일치하는 뷰티포인트 카드 등록 고객이 없습니다" 팝업**이 떴는데,
   스크립트는 '적립신청 완료' 문구만 폴링하며 대기 → 멈춘 것처럼 보임.
   → **다음 할 일: claim_beauty_point 에 이 팝업 감지 추가** ('카드 등록 고객이 없습니다' →
   BEAUTY_NONE 즉시 확정 + 확인 탭). 멤버십 없는 계정마다 매번 헛폴링하게 된다.

## 4. preflight stale 우회 (롯데 한정, 기록 남김)

`cart/today.json`·`cart/today_carts.json` 이 8/18 자라 결제 preflight 가 하드스톱 →
**`ALLOW_STALE_CART=1` 로 우회**. 근거: 롯데 설화수 결제는 두 파일을 안 쓴다
(대장=record_combo→rate 시트, 적립=화면 기반). rate 시트엔 **8.21 탭이 존재**(step1 오늘 실행됨)
→ 대장 금액은 오늘자 실측. **현대몰 식품 결제 전에는 반드시 step2 재실행 + today_carts.json 갱신**
(오늘 담은 식품: 2×2@4-6 / 25×2@7-11 / 22×2@12-16).

## 5. 사용자 질문 검증 — "오늘 g 10%가 아니고 8%인 거 체크했어?"

확인 결과 **8% 기준으로 맞게 잡혀 있다**:
- 카트 실물: g 450,000→**414,000(8%)**/2개, h 270,000→229,500(15%).
- rate 시트 8.21 탭 롯데 조합14 = 최종구매가 **520,455** / 공급률 0.4865.
- 역산: 8% 상품합 643,500 × 할인쿠폰10% × 플러스쿠폰(~3%) × 롯데카드7% ≈ 520,455 일치.
  10% 기준(634,500)이면 ~513,000 이 나와 시트와 불일치 → 시트는 8% 반영이 맞다.

## 6. 다음 할 일

- [ ] **#12~15 롯데 조합14 결제** — 카트는 담겨 있으나 **날짜 바뀌면 폐기, step1/step2 부터**
- [ ] **claim_beauty_point '카드 등록 고객 없음' 팝업 감지** (#11 헛폴링 재발 방지)
- [ ] **현대몰 #12 카트 잔여상품**(스키니랩 시서스, 삭제불가) 처리 확인 — 식품 결제 전 필수
- [ ] 식품 결제 전 **step2 + today_carts.json 오늘자 갱신**
- [ ] 구매사은(광세일) 대상 여부 확인 — 대상이면 #9~11 일괄 신청 (기간 내 가능)
- [ ] #1 롯데 비밀번호 사용자 확인 (6번째 이월)
- [ ] #8 구매대장 보정 (8/18 건 — 조합24 + 금액, 이월)
