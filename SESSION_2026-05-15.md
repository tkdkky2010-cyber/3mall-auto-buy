# 2026-05-15 세션 흐름

> 다음 세션에서 "어제 뭐했는지" 빠르게 파악용. 코드 변경 + 결정 + 미해결.

---

## 1. 오늘 한 것 (시간순)

### Step 1 재실행 (사용자가 5.15 시트 지운 후)
- `RATE_CHECK_CDP_PORT=9224` Chrome 9224 attach
- [rate-check/galleria.py](rate-check/galleria.py) → b~h 7상품 스크랩 + 11조합 계산 → A1:K45
- [rate-check/_tmp/hmall_all.py](rate-check/_tmp/hmall_all.py) → cart 옵션변경 + 결제 페이지 카드 캐러셀 측정 → A44:J60 + L1:L12
- [rate-check/_tmp/lotte_all.py](rate-check/_tmp/lotte_all.py) → 쿠폰/카드/적립 + 11조합 계산 → A62:M79 + M1:M12 + 조건부 서식

### 롯데 적립금 수동 패치
- `_check_lotte_reward.py` 정규식 lazy match 버그로 **모든 상품 7,500원**으로 잘못 검출됨 (실제 상위 tier는 15,000원)
- 사용자가 시트 직접 지우라고 했음 → `/tmp/lotte_reward_fix.py` 작성해서 시트 G69:J79 + M1:M12 직접 패치 (적립 15,000원 기준 재계산)
- **공급률 변화** (참고용 — 매일 다름):
  - 적립 7,500 → 15,000 으로 +7,500원 차감 → 순구매가 −7,500원 → 공급률 약 0.01 낮아짐

### 전체 검토 + 수정 (사용자 요청 "과거 코드 검토")
**삭제된 캐시 파일** (RULES.md §3 위반)
- `rate-check/_lotte_reward_dump.json`
- `rate-check/_tmp/gwp_2026-05-15.json`

**stale layout 정리** (시트 layout = galleria 1~43 / hmall 44~60 / lotte 62~79)
- [galleria.py:488](rate-check/galleria.py:488) — `A1:Z60` wipe (Hmall 영역 침범 ⚠️) → `A1:I43`
- [galleria.py:315](rate-check/galleria.py:315) — `range(45)` → `range(43)`
- [_tmp/lotte_all.py:232](rate-check/_tmp/lotte_all.py:232) — "행 100~" → "행 62~"
- [hmall.py](rate-check/hmall.py) (미사용 초안) — START 65→44, "active script는 _tmp/hmall_all.py" 명시

**적립금 tier 누락 가시화**
- [_tmp/lotte_all.py:172](rate-check/_tmp/lotte_all.py:172) — 7개 상품 동일 적립값이면 "상위 tier 누락 가능" 자동 경고

**문서 정리**
- [project-manager.md:88-94](.claude/agents/project-manager.md:88) — Hmall 행 65→44, 롯데 적립금 tier 이슈 알려진 이슈 명시
- [Sulwhasoo_Supply_Rate.md](rate-check/Sulwhasoo_Supply_Rate.md) — 하드코딩 예시 4곳 추상화:
  - 쿠폰 예시 13%/15% 제거
  - 갤러리아 계산 예시 (`225,000 × 2 × 0.9 × 0.85 × 0.978 = 336,311`) → 추상 구조
  - 롯데 계산 예시도 동일하게
  - 카드할인 예시 ("5%면 0.95, 7%면 0.93") → 추상
  - Sheets 구조 표 → 현행 layout (62~79) + RULES.md §13 참조

**커밋**
- [5966bff](https://github.com/tkdkky2010-cyber/3mall-auto-buy/commit/5966bff) on `claude/agitated-jones-069b90` (5 files)

### 38조합 확장 검토 (논의만, 미반영)
- 사용자가 [롯데홈쇼핑_조합번호_38개_v2.xlsx] 보냄
- **검증 결과:**
  - 1~11: 현행과 100% 동일 (v1에서 변경됐던 8번도 v2에서 원복)
  - 12: h×3 (810,000) — 특수 케이스, 12번 자리 고정
  - 13~38: 정가 오름차순 (725k~795k)
- **시간 영향:** Galleria/Lotte는 순수 수학이라 무시 가능, **Hmall만 +15-18분** 증가 (조합당 ~35초 cart 옵션변경 + 결제 페이지 진입 + 카드 측정)
- **시트 사이즈 영향:**
  - rate-check 한 탭: ~80행 → ~150행 (2배, 무시 가능)
  - 재고관리 한 버전: ~55행 → ~190행 (3.5배)
  - Google Sheets 천만 셀 한계 멀음
  - 체감 느려짐: 재고관리 SUMIFS 수식 구조에 따라 0.5-2초 정도 가능
- **결론:** 38조합 가도 큰 문제 없음. 단 미구현 — 다음 세션 작업 대기.

---

## 2. 현재 상태 (코드/시트)

### 코드 — 모든 stale 패턴 0건 (RULES.md §11 체크리스트)
- 0.948 / 정가×0.9 math: 0
- cache file read/write: 0 (negative ref 주석만 4곳)
- rank_by_rate / sorted by 공급률: 0
- 행 65 / 행 100 layout 잔존: 0
- sum(reward × qty): 0
- 코드 안 하드코딩 예시값: 0

### main vs worktree sync
- 모두 일치 (`diff -q` 0건)

### 시트 5.15
- 갤러리아 1~43 / Hmall 44~60 / 롯데 62~79
- J1:M12 비교 차트 (조건부 서식: 행별 최저값 연두색)
- 롯데 적립 = 15,000원 (수동 패치)

### git
- worktree 브랜치: `claude/agitated-jones-069b90`
- 마지막 커밋: [5966bff](https://github.com/tkdkky2010-cyber/3mall-auto-buy/commit/5966bff)
- main 브랜치는 다른 in-flight 변경 (buy/, cart/) 있음 — 건드리지 않음

---

## 3. 알려진 이슈 (다음 세션에서도 나타날 수 있음)

### A. 롯데 적립금 tier 누락
- `_check_lotte_reward.py` 정규식 (`re1 = /([\d,]{3,})\s*원\s*이상[\s\S]{0,80}?([\d,]{2,})\s*(?:원|P)\s*적립/g`) 이 lazy match로 **첫 tier만** 잡고 상위 tier 놓치는 케이스 있음
- 오늘 증상: 7개 상품 모두 동일 7,500원 (실제 상위 tier 15,000원 존재)
- 대응:
  - lotte_all.py 가 7개 동일값 검출 시 자동 경고 (RULES.md §7 참고 안내)
  - 사용자가 실제 이벤트 페이지 확인 후 시트 G69:J79 + M2:M12 수동 패치
- **수정 보류** 이유: 실제 페이지 DOM 구조 안 보고 정규식만 고치면 더 망가질 위험 → 다음에 사용자가 페이지 보여주면 fix

### B. Hmall 11조합 → 38조합 미반영
- v2 xlsx 검증 끝 — 가능
- 작업 항목:
  1. `_common.COMBOS` 38개로 확장
  2. 시트 layout 재설계 (galleria 1~?, hmall ?~?, lotte ?~? — 38행 + 헤더/요약 고려)
  3. 비교 차트 J1:M39 (또는 J1:M40)
  4. 조건부 서식 범위 K2:M39 로 확장
  5. inventory.py: 11→38 조합 처리 (현재 `range(1, 12)` 하드코딩 다수)
  6. Hmall cart 조작 — 38회 옵션변경/결제 진입 시 timeout/실패 retry 강화

### C. Lotte/Galleria buy 자동화 PM 통합 안 됨
- `buy/sulwhasoo.py` 에 `galleria_*` / `lotte_*` 코드 있지만 PM workflow에 안 묶임
- OK캐시백 자동 로그인 미구현 (현재 `enter_via_okcashbag` 미로그인 시 raise — 사용자가 카카오 1회 수동 로그인)
- 작업 항목:
  1. `okcashbag.json` 의 id/pw 사용한 자동 로그인 코드 추가
  2. PM .md 에 갤러리아/롯데 Step 4/5 통합 (조건부 OK캐시백 분기 포함)
  3. 작동 검증 (현재 코드는 있지만 실제 결제 검증 안 됨)

---

## 4. 작업 룰 (다음 세션에서도 지킬 것)

핵심만. 자세한 건 [rate-check/RULES.md](rate-check/RULES.md).

1. **사용자가 "확인하라"고 하면 실제 페이지/element 직접 본다.** 출력값/캐시/이전 메시지로 판단 X.
2. **하드코딩 daily 값 (쿠폰%, 적립금, 단가) 코드/문서에 박지 않는다.** "예: N%" 같은 illustrative example도 금지.
3. **결과 캐시 파일 (`*.json`) 절대 만들지 않는다.** Google Sheets 가 SoT.
4. **시트 layout 변경 시 RULES.md §13 + 모든 mall 스크립트의 START 변수 동시 update.**
5. **롯데 적립금은 조합당 1회 (`max(applicable)`).** `sum(reward × qty)` 절대 X.
6. **갤러리아 쿠폰은 `button.down em` 한 곳에서만.** body wide regex 금지.
7. **롯데 쿠폰은 쿠폰받기 팝업 첫 항목.** body 전체 max % 금지.
8. **Chrome 인스턴스 분리** — rate-check 9224, check10 9223, buy/run.py 9222.
9. **워크트리에서 작업 후 main 경로 sync 필수** — 사용자 스크립트는 main 경로 실행.

---

## 5. 다음 세션 시작할 때 빠른 체크

```bash
# 1. 캐시 파일 잔존 확인
ls rate-check/*.json rate-check/_tmp/*.json 2>/dev/null
# 결과: 빈 줄이면 OK, 뭐 있으면 SoT 위반

# 2. main vs worktree sync
for f in rate-check/galleria.py rate-check/_tmp/lotte_all.py rate-check/_tmp/hmall_all.py rate-check/RULES.md rate-check/_common.py .claude/agents/project-manager.md; do
  diff -q "$f" ".claude/worktrees/agitated-jones-069b90/$f" 2>&1 | head
done

# 3. 오늘 시트 탭 존재 확인
# https://docs.google.com/spreadsheets/d/1fxB0UvLRy2iQfonCWn5U5mWnXbzSdn6l4e2XuQluhwo/edit
```

---

## 6. 다음 세션 우선순위 후보 (사용자 결정)

1. **38조합 확장** — v2 xlsx 기준, 시트 layout 재설계 + COMBOS update + inventory.py 일반화
2. **롯데 적립금 정규식 fix** — 실제 페이지 DOM 보면서 lazy match 버그 진짜 수정
3. **Galleria/Lotte buy 자동화 PM 통합** — OK캐시백 자동 로그인 + Step 4/5 분기
4. **rate-check 탭 누적 archive 정책** — 1년 후 365탭 되면 sheet 무거워짐, 월별 분리 등
