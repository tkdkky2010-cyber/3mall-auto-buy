# WORKLOG 2026-08-06 — 8/5 구매기록 감사 + 미확인 항목 종결 + NH 결제경로 점검

※ 8/6 오전 세션(§14 날짜 preflight / §15 IN 시트 변경일 / §16 날짜 리셋 / §17 NH 검증 2건)은
   `WORKLOG_2026-08-05.md` 끝에 붙어 있다. **오늘분은 이 파일이 정본**이고, 앞으로 8/6 기록은 여기에 쓴다.

## 18. 8/5 구매기록 감사 — 워크로그 vs 구매대장 교차대조

사용자 요청: "워크로그 다 잘살펴 어제 구매하는거 기록도 다 나와있나."

### 대조 결과 (시트 `구매대장` 탭 = 정본)

| 구매 | 계정 | 워크로그 | 대장 |
|---|--:|---|---|
| 롯데 설화수 조합24 / 현대카드 | 6 | ✓ 주문번호 전부(§5-3) | ✓ 6행 (금액 공란, `(조합 미지정)`) |
| 현대몰 석류젤리×2 / KB | 9 | ✓ 계정·금액(§8·§12) | ✓ 9행 110,240원 — **주문번호 9건 공란** |
| 현대몰 데이즈온 / NH | 3 | ✓ 주문번호·청구액(§12) | ✓ 6행(계정당 2상품), 주문번호 있음 |
| 현대몰 계정 1·2·3·4 (사용자 직접결제) | 4 | ✗ 값 없음(§8) | **✗ 미기록** |

- 시트 8/5 합계 21행 / 금액합 **1,929,520원** — §17 의 무결성 확인값과 일치.
- **결론: 결제 자체는 워크로그에 전부 남아 있다.** 빠진 건 계정 1~4(사용자 직접결제, 실측값 없음)와
  KB 9건의 주문번호뿐. 나머지는 구매 사실·계정·상품·수량이 대장과 워크로그 양쪽에 있다.

### ★ 로컬 JSON 대장은 신뢰하면 안 된다 (시트가 정본)

`secrets/purchase_ledger.json` 은 **gitignore** 라 PC 간 동기화가 안 된다. 실측 대조:

```
JSON 135건 / 시트 177건 — 8개 날짜 불일치
  7/28·7/30·8/2·8/3 : 로컬 0건 / 시트 5~6건   (다른 PC 세션분이 안 넘어옴)
  8/5              : 로컬 6건 / 시트 21건     (저녁 현대몰 15행이 로컬에 없음)
  7/10·7/13        : 로컬이 1건 많음          (시트 append 실패분)
```

→ **§4-2 의 "8/3 은 구매대장에 0건" 은 이 함정에 걸린 오판이다.** 시트엔 8/3 6건이
주문번호까지 다 있고 8/3 워크로그 §4 표와 일치한다. §4-2 정정 완료.
**앞으로 대장 조회는 시트를 볼 것.**

### 사용자 지시 — 대장에 금액은 불필요

> **"금액까지는 안남겨도됨 수량정도만 해"**

→ 롯데 6건의 금액 공란(`(조합 미지정)` 탓에 rate 시트 조회 실패)은 **보정하지 않는다.**
수량·계정·주문번호가 남아 있으면 충분하다. NH 3건의 대장 금액이 실제 청구액과 다른 것
(즉시할인가 기록 vs NH 5%·H.Point 적용 후 청구)도 같은 이유로 **손대지 않는다.**

### 미확인 3건 — 사용자 지시로 전부 종결

> **"이것도 그냥 다 완료로해"**

| 항목 | 처리 |
|---|---|
| #19 ybkim9960 롯데 뷰티포인트 실적립 | **완료 처리** (추적 종료) |
| 8/4 이월 #11 diinamic 뷰티포인트 | **완료 처리** (추적 종료) |
| 금액 차이(앱 560,982 / 주문서 563,740 vs step1 524,297) | **완료 처리** (원인 규명 안 함) |

8/5 §6 체크리스트 3줄에 `[x]` + 종결 사유 반영했다. **다시 미해결로 되살리지 말 것.**

## 19. NH 결제경로 점검 (사용자 지시: "NH결제 방식은 잘 수정되어있는지확인")

코드를 직접 읽어 확인했다. **8/5 §11·§17 의 수정은 정상 반영돼 있다.**

### ✅ 확인된 것

| 항목 | 근거 |
|---|---|
| NH = 무조건 핸드세이크 정지. 환경변수 게이트 없음 | `hmall_hyundai_buy.py:1555` `step=card_screen_ready` / `manual=True` 무조건 set |
| 옛 로컬 OCR 자동입력 경로 제거 | `pay_nh_general` 에 `_card_no_boxes`+`_tap_secure_each` 없음 (주석에 '되살리지 말 것') |
| 호출부가 `manual` 을 **실패가 아닌 정상 대기**로 처리 | 현대몰 `:1966` / 롯데 `:1369` — 둘 다 `NH_HANDOFF` 로 즉시 return (다음 계정 콜드런치가 결제화면 파괴하는 것 차단) |
| 값은 스크립트가 secrets 에서 직접 읽음, 에이전트는 배열만 | `nh_enter._secrets()` / `tap_digits(value, pos)` — 로그엔 자릿수만 |
| 재탭 = 재셔플 방지 | `nh_enter._tap_box()` 가 `_keypad_up()` 이면 skip |
| 탭 씹힘 칸별 검증 (§17①) | `_nh_field_len()` → `cardno1~4` EditText 길이 → `_verify_box` 불일치 시 rc=1 중단 |
| 주문완료 확인 전엔 대장·적립 기록 안 함 (§17②) | `finish` 의 `completed` 게이트 → 아니면 rc=1, 아무것도 안 씀 |

### ⚠️ 남은 구멍 1개 — **롯데 NH 에는 `finish` 가 없다**

`nh_enter finish` 는 **현대몰 전용**이다:

- `PL.record_food("현대Hmall", …)` 로 몰이 하드코딩
- 카트를 `today_carts.json` 에서 `mall in ("현대","hmall")` 으로만 찾음
- 마무리가 `B.apply_reward_now()` = **H.Point 적립**(현대몰 이벤트)

그런데 롯데도 `:1369` 에서 똑같이 `NH_HANDOFF` 로 일찍 return 한다 → 롯데를 NH 로 결제하면
**대장 미기록 + `claim_beauty_point` 미실행**이 된다. 뷰티포인트는 **주문완료 화면에서만** 되는
now-or-never 단계라(`:1384` 주석) 그 자리에서 놓치면 복구 불가. 롯데 코드 주석 `:1367` 도
"구매대장은 **수동 기록**해야 한다"로 이 상태를 인정하고 있다.

**= 8/5 §13 의 'H.Point 적립 12계정 조용한 누락' 과 완전히 같은 유형이 롯데 쪽에 그대로 남아 있다.**
당장 사고는 아니다 — 롯데 당일카드가 NH 일 때만 발동한다(8/5 롯데는 현대카드였다).

작은 것 2개:
- `cvc`/`pin6` 는 입력 후 **화면 자릿수 검증이 없다**(box1~4 만 `_verify_box`). `_nh_field_len` 이
  `cardno` prefix 만 읽어서 그렇다.
- `nh_enter confirm` 이 `ocr_tap("확인", contains=True)` — 화면에 '확인' 이 여럿이면 오탭 가능.

## 19-2. ★ 위 3건 수정 (사용자 지시)

> **"롯데 NH finish 도 고쳐놔 뷰티포인트적립도중요하지만 건강식품 10% 적립도 중요해"**

### ① `nh_enter finish_lotte` 신설 — 롯데 NH 마무리

`_finish_lotte()` 신설. 롯데 `buy_one` 의 DONE 이후 후처리와 **같은 순서**로 재현한다:

```
주문완료 확인(L._poll_order_complete) → record_combo("롯데홈쇼핑", …, card="NH")
   → dismiss_card_register → claim_beauty_point(idx) → claim_lotte_reward(goods_no)
```

- ⚠️**뷰티 → 적립 순서 고정.** reward 가 홈으로 이동하면 주문완료 화면을 이탈해 뷰티가 소실된다(#6 사례).
- **주문완료 확인 전엔 아무것도 기록하지 않는다**(§17② 와 같은 규칙 — 없는 구매를 장부에 남기는 게 더 나쁨).
- 인자: `finish_lotte <계정> [combo=24] [goods=2923406968]`. combo 는 대장 조합명/금액 조회용,
  goods 는 구매사은 상품번호 검색 override.
- **몰 혼동 가드**: `finish`(현대몰)를 롯데 화면에서 돌리면 딴 몰 장부에 기록되고 H.Point 적립이 돌아간다
  → `_wait_app(L.PKG)` 로 롯데앱이 foreground 면 **거부하고 `finish_lotte` 로 안내**. 반대쪽도 동일.
- 사용자 강조대로 **몰마다 적립 종류가 다르다**: 현대몰=H.Point(건강식품 10% 등) / 롯데=뷰티포인트+구매사은.
  둘 중 하나만 챙기면 나머지는 조용히 날아간다.

### ② CVC·결제비번 자릿수 검증 추가

`_edit_fields()`(EditText 전부 → [(resource-id, 자릿수)]) 로 공용화하고 `_nh_field_len` 을 그 위에 재구성.
`_verify_len(keys, expect, label)` 신설 — cvc/pin6 입력 직후 검증.

★**box1~4 와 판정 규칙이 다르다**: CVC·비번 칸의 resource-id 는 **라이브 실측 전**이라
후보(`_CVC_KEYS`/`_PIN_KEYS`)로 훑는다. 그래서 **0자리는 실패로 보지 않는다** —
'마스킹돼 안 읽힘'과 '탭이 전부 씹힘'을 구분할 수 없기 때문. `0 < got != expect` 일 때만 중단한다.
(결제 중간의 오탐 중단이 미검증보다 위험하다는 판단.)
→ `nh_enter fields` 디버그 명령 신설. **다음 라이브 때 실제 id 를 찍어 후보에 박아 넣을 것.**

### ③ `confirm` 오탭 방지

`'확인'` **완전일치 우선**(`contains=False`, `pick=bottom`), 후보가 2개 이상이면 개수를 로그로 남긴다.
완전일치 0건일 때만 contains 폴백 + 경고(OCR 이 '확 인' 으로 띄어 읽는 경우 대비 — 기존 동작 보존).

### 검증 (폰 미연결 → 오프라인, `record_*` 전부 스텁)

★8/5 §17 에서 모킹 테스트가 `record_food` 를 실제로 호출해 대장을 오염시킨 교훈대로 **기록 함수를 먼저 막고** 돌렸다.

| 케이스 | 결과 |
|---|---|
| 롯데앱 not foreground | rc=1, **호출 0건** |
| foreground OK / 주문완료 아님 | rc=1, **호출 0건** |
| 주문완료 확인됨 | rc=0, 호출 순서 `record_combo → dismiss → beauty → reward` ✓ / 대장 인자 `('롯데홈쇼핑','acct5',24) order_no=… card='NH'` |
| `_verify_len` 3자리/2자리/0자리/id미발견/dump실패 | ✓통과 / ✗중단 / ⚠️진행 / ⚠️진행 / ⚠️진행 |

대장 무결성: entries **135건 유지**(오염 0). **라이브 검증은 다음 NH 결제 때** — 특히 ② 의 실제 id.

## 20. 다음 할 일

- [x] ~~롯데 NH `finish` 경로 신설~~ — 완료(§19-2). **라이브 검증은 다음 NH 결제 때**
- [ ] `nh_enter fields` 로 **CVC·결제비번 실제 resource-id 확정** → `_CVC_KEYS`/`_PIN_KEYS` 에 반영 (§19-2②)
- [ ] 8/5 이월: **#18 nami8225 롯데 비번** 계정 유지/제외 결정
- [ ] 8/5 이월: `1-1` 이너플로라30정 판매중단 추적 / `Sulwhasoo_Supply_Rate.md:188` 옛 n 번호 /
      카드 5장 이상일 때 스크롤 대응 / 윈도우 첫 실행 검증 · gitignore 파일 8종 복사
