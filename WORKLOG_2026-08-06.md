# WORKLOG 2026-08-06 — 8/5 구매기록 감사 + 미확인 항목 종결 + NH 결제경로 점검

※ 8/6 오전 세션(§14 날짜 preflight / §15 IN 시트 변경일 / §16 날짜 리셋 / §17 NH 검증 2건)은
   `WORKLOG_2026-08-05.md` 끝에 붙어 있다. **오늘분은 이 파일이 정본**이고, 앞으로 8/6 기록은 여기에 쓴다.

## 18. 8/5 구매기록 감사 — 워크로그 vs 구매대장 교차대조

사용자 요청: "워크로그 다 잘살펴 어제 구매하는거 기록도 다 나와있나."

### 대조 결과 (시트 `구매대장` 탭 = 정본)

| 구매 | 계정 | 워크로그 | 대장 |
|---|--:|---|---|
| 롯데 설화수 조합24 / 현대카드 | 6 | ✓ 주문번호 전부(§5-3) | ✓ 6행 (금액 공란, `(조합 미지정)`) |
| 현대몰 석류젤리×2 / KB | 9 | ✓ 계정·금액(§8·§12) | ✓ 9행 110,240원 — **주문번호 9건 공란** |
| 현대몰 데이즈온 / NH | 3 | ✓ 주문번호·청구액(§12) | ✓ 6행(계정당 2상품), 주문번호 있음 |
| 현대몰 계정 1·2·3·4 (사용자 직접결제) | 4 | ✗ 값 없음(§8) | **✗ 미기록** |

- 시트 8/5 합계 21행 / 금액합 **1,929,520원** — §17 의 무결성 확인값과 일치.
- **결론: 결제 자체는 워크로그에 전부 남아 있다.** 빠진 건 계정 1~4(사용자 직접결제, 실측값 없음)와
  KB 9건의 주문번호뿐. 나머지는 구매 사실·계정·상품·수량이 대장과 워크로그 양쪽에 있다.

### ★ 로컬 JSON 대장은 신뢰하면 안 된다 (시트가 정본)

`secrets/purchase_ledger.json` 은 **gitignore** 라 PC 간 동기화가 안 된다. 실측 대조:

```
JSON 135건 / 시트 177건 — 8개 날짜 불일치
  7/28·7/30·8/2·8/3 : 로컬 0건 / 시트 5~6건   (다른 PC 세션분이 안 넘어옴)
  8/5              : 로컬 6건 / 시트 21건     (저녁 현대몰 15행이 로컬에 없음)
  7/10·7/13        : 로컬이 1건 많음          (시트 append 실패분)
```

→ **§4-2 의 "8/3 은 구매대장에 0건" 은 이 함정에 걸린 오판이다.** 시트엔 8/3 6건이
주문번호까지 다 있고 8/3 워크로그 §4 표와 일치한다. §4-2 정정 완료.
**앞으로 대장 조회는 시트를 볼 것.**

### 사용자 지시 — 대장에 금액은 불필요

> **"금액까지는 안남겨도됨 수량정도만 해"**

→ 롯데 6건의 금액 공란(`(조합 미지정)` 탓에 rate 시트 조회 실패)은 **보정하지 않는다.**
수량·계정·주문번호가 남아 있으면 충분하다. NH 3건의 대장 금액이 실제 청구액과 다른 것
(즉시할인가 기록 vs NH 5%·H.Point 적용 후 청구)도 같은 이유로 **손대지 않는다.**

### 미확인 3건 — 사용자 지시로 전부 종결

> **"이것도 그냥 다 완료로해"**

| 항목 | 처리 |
|---|---|
| #19 ybkim9960 롯데 뷰티포인트 실적립 | **완료 처리** (추적 종료) |
| 8/4 이월 #11 diinamic 뷰티포인트 | **완료 처리** (추적 종료) |
| 금액 차이(앱 560,982 / 주문서 563,740 vs step1 524,297) | **완료 처리** (원인 규명 안 함) |

8/5 §6 체크리스트 3줄에 `[x]` + 종결 사유 반영했다. **다시 미해결로 되살리지 말 것.**

## 19. NH 결제경로 점검 (사용자 지시: "NH결제 방식은 잘 수정되어있는지확인")

코드를 직접 읽어 확인했다. **8/5 §11·§17 의 수정은 정상 반영돼 있다.**

### ✅ 확인된 것

| 항목 | 근거 |
|---|---|
| NH = 무조건 핸드세이크 정지. 환경변수 게이트 없음 | `hmall_hyundai_buy.py:1555` `step=card_screen_ready` / `manual=True` 무조건 set |
| 옛 로컬 OCR 자동입력 경로 제거 | `pay_nh_general` 에 `_card_no_boxes`+`_tap_secure_each` 없음 (주석에 '되살리지 말 것') |
| 호출부가 `manual` 을 **실패가 아닌 정상 대기**로 처리 | 현대몰 `:1966` / 롯데 `:1369` — 둘 다 `NH_HANDOFF` 로 즉시 return (다음 계정 콜드런치가 결제화면 파괴하는 것 차단) |
| 값은 스크립트가 secrets 에서 직접 읽음, 에이전트는 배열만 | `nh_enter._secrets()` / `tap_digits(value, pos)` — 로그엔 자릿수만 |
| 재탭 = 재셔플 방지 | `nh_enter._tap_box()` 가 `_keypad_up()` 이면 skip |
| 탭 씹힘 칸별 검증 (§17①) | `_nh_field_len()` → `cardno1~4` EditText 길이 → `_verify_box` 불일치 시 rc=1 중단 |
| 주문완료 확인 전엔 대장·적립 기록 안 함 (§17②) | `finish` 의 `completed` 게이트 → 아니면 rc=1, 아무것도 안 씀 |

### ⚠️ 남은 구멍 1개 — **롯데 NH 에는 `finish` 가 없다**

`nh_enter finish` 는 **현대몰 전용**이다:

- `PL.record_food("현대Hmall", …)` 로 몰이 하드코딩
- 카트를 `today_carts.json` 에서 `mall in ("현대","hmall")` 으로만 찾음
- 마무리가 `B.apply_reward_now()` = **H.Point 적립**(현대몰 이벤트)

그런데 롯데도 `:1369` 에서 똑같이 `NH_HANDOFF` 로 일찍 return 한다 → 롯데를 NH 로 결제하면
**대장 미기록 + `claim_beauty_point` 미실행**이 된다. 뷰티포인트는 **주문완료 화면에서만** 되는
now-or-never 단계라(`:1384` 주석) 그 자리에서 놓치면 복구 불가. 롯데 코드 주석 `:1367` 도
"구매대장은 **수동 기록**해야 한다"로 이 상태를 인정하고 있다.

**= 8/5 §13 의 'H.Point 적립 12계정 조용한 누락' 과 완전히 같은 유형이 롯데 쪽에 그대로 남아 있다.**
당장 사고는 아니다 — 롯데 당일카드가 NH 일 때만 발동한다(8/5 롯데는 현대카드였다).

작은 것 2개:
- `cvc`/`pin6` 는 입력 후 **화면 자릿수 검증이 없다**(box1~4 만 `_verify_box`). `_nh_field_len` 이
  `cardno` prefix 만 읽어서 그렇다.
- `nh_enter confirm` 이 `ocr_tap("확인", contains=True)` — 화면에 '확인' 이 여럿이면 오탭 가능.

## 19-2. ★ 위 3건 수정 (사용자 지시)

> **"롯데 NH finish 도 고쳐놔 뷰티포인트적립도중요하지만 건강식품 10% 적립도 중요해"**

### ① `nh_enter finish_lotte` 신설 — 롯데 NH 마무리

`_finish_lotte()` 신설. 롯데 `buy_one` 의 DONE 이후 후처리와 **같은 순서**로 재현한다:

```
주문완료 확인(L._poll_order_complete) → record_combo("롯데홈쇼핑", …, card="NH")
   → dismiss_card_register → claim_beauty_point(idx) → claim_lotte_reward(goods_no)
```

- ⚠️**뷰티 → 적립 순서 고정.** reward 가 홈으로 이동하면 주문완료 화면을 이탈해 뷰티가 소실된다(#6 사례).
- **주문완료 확인 전엔 아무것도 기록하지 않는다**(§17② 와 같은 규칙 — 없는 구매를 장부에 남기는 게 더 나쁨).
- 인자: `finish_lotte <계정> [combo=24] [goods=2923406968]`. combo 는 대장 조합명/금액 조회용,
  goods 는 구매사은 상품번호 검색 override.
- **몰 혼동 가드**: `finish`(현대몰)를 롯데 화면에서 돌리면 딴 몰 장부에 기록되고 H.Point 적립이 돌아간다
  → `_wait_app(L.PKG)` 로 롯데앱이 foreground 면 **거부하고 `finish_lotte` 로 안내**. 반대쪽도 동일.
- 사용자 강조대로 **몰마다 적립 종류가 다르다**: 현대몰=H.Point(건강식품 10% 등) / 롯데=뷰티포인트+구매사은.
  둘 중 하나만 챙기면 나머지는 조용히 날아간다.

### ② CVC·결제비번 자릿수 검증 추가

`_edit_fields()`(EditText 전부 → [(resource-id, 자릿수)]) 로 공용화하고 `_nh_field_len` 을 그 위에 재구성.
`_verify_len(keys, expect, label)` 신설 — cvc/pin6 입력 직후 검증.

★**box1~4 와 판정 규칙이 다르다**: CVC·비번 칸의 resource-id 는 **라이브 실측 전**이라
후보(`_CVC_KEYS`/`_PIN_KEYS`)로 훑는다. 그래서 **0자리는 실패로 보지 않는다** —
'마스킹돼 안 읽힘'과 '탭이 전부 씹힘'을 구분할 수 없기 때문. `0 < got != expect` 일 때만 중단한다.
(결제 중간의 오탐 중단이 미검증보다 위험하다는 판단.)
→ `nh_enter fields` 디버그 명령 신설. **다음 라이브 때 실제 id 를 찍어 후보에 박아 넣을 것.**

### ③ `confirm` 오탭 방지

`'확인'` **완전일치 우선**(`contains=False`, `pick=bottom`), 후보가 2개 이상이면 개수를 로그로 남긴다.
완전일치 0건일 때만 contains 폴백 + 경고(OCR 이 '확 인' 으로 띄어 읽는 경우 대비 — 기존 동작 보존).

### 검증 (폰 미연결 → 오프라인, `record_*` 전부 스텁)

★8/5 §17 에서 모킹 테스트가 `record_food` 를 실제로 호출해 대장을 오염시킨 교훈대로 **기록 함수를 먼저 막고** 돌렸다.

| 케이스 | 결과 |
|---|---|
| 롯데앱 not foreground | rc=1, **호출 0건** |
| foreground OK / 주문완료 아님 | rc=1, **호출 0건** |
| 주문완료 확인됨 | rc=0, 호출 순서 `record_combo → dismiss → beauty → reward` ✓ / 대장 인자 `('롯데홈쇼핑','acct5',24) order_no=… card='NH'` |
| `_verify_len` 3자리/2자리/0자리/id미발견/dump실패 | ✓통과 / ✗중단 / ⚠️진행 / ⚠️진행 / ⚠️진행 |

대장 무결성: entries **135건 유지**(오염 0). **라이브 검증은 다음 NH 결제 때** — 특히 ② 의 실제 id.

## 20. step1 실행 (14:22:37 → 14:37:43, rc=0)

5단계 전부 통과. 갤러리아 GWP 재판독 없이 진행.

- **갤러리아 오염 없음**: `REDIRECTED` 0건 + b~h·n 추가증정가치 전부 상이
  (20,700 / 10,800 / 15,500 / 19,500 / 16,500 / 26,800 / 28,400 / **n 16,400**).
- 재고관리 **전체 일치 → 활성 버전 `c` 유지**(새 버전 안 땄다).
- 단가체크 하드코딩 `SAMPLE_TABLE` 과 **25건 불일치** → 시트값 사용(어제와 동일. 코드 상수가 낡음).
- **오늘 롯데 조건**: 쿠폰 **13%** (f·h 만 10%) / **롯데카드 7% 청구할인**(어제는 현대카드) /
  적립 30만↑ → 30,000원 (store-wide, 전 조합 공통).
- 조합번호 색칠 `[3, 8, 12, 14, 22, 24, 25, 27]` — 14 상시 색칠 정상.

**카트플랜** (channel=lotte, N=7, 총 **2,324,299원**, 평균 공급률 **0.4604**)

| 계정 | 조합 | 공급률 |
|--:|--:|--:|
| 1 | 3 | 0.4653 |
| 2 | 8 | 0.4719 |
| 3 | 12 | 0.4680 |
| 4 | 22 | 0.4808 |
| 5 | 24 | 0.4392 |
| 6 | 25 | 0.4522 |
| 7 | 27 | 0.4456 |

조합 구성은 8/5 와 동일, 평균 공급률만 0.4751 → **0.4604** 로 개선.

⚠️ **`n` 재고 4 인데 플랜 사용량 6** (조합 24·25·27). 재고는 분배에 미반영이라 그대로 들어갔다
— 담을 때 모자랄 수 있다. (8/5 는 재고 7 / 사용 6 이라 통과했었다.)
제품별 총량: `{b:0, c:1, d:0, e:12, f:4, g:3, h:1, n:6}`

## 21. step2 실행 (rc=0) — **10%적립 8 / 적립없음 8 / 에러 3**

계정 #1 tkdkky2002. 시트 **`8.06 식품` 신규 생성**, A1:T21(19개), `cart/today.json` 갱신(date=2026-08-06).

`ten_percent_ids = ['1','11','17','22','25','33','34','35']`

| # | 제품명 | 10% | qty | 즉시할인가 | 실비 | 페이백 |
|---|---|:--:|--:|--:|--:|---|
| 1 | 이너플로라 유산균 60정 | ✓ | 1 | 125,910 | 115,910 | 삼성 100,130 |
| 11 | 라메종드미엘 천연꿀 8병 | ✓ | 1 | 249,280 | 206,902 | — |
| 17 | 데이즈온 원데이 알파 18개 | ✓ | 2 | 91,048 | 70,570 | — |
| 22 | 올바른건강식품 와이 9박 | ✓ | 1 | 66,340 | 61,340 | 삼성 56,079 |
| 25 | 유기농 석류젤리 9박 | ✓ | 1 | 55,120 | 49,608 | 삼성 45,623 |
| 33 | GRN 붓기캔디 | ✓ | 1 | 62,910 | 57,910 | 삼성 52,922 |
| 34 | 데이즈온 파라다이스 버닝 9박스 | ✓ | 1 | 188,480 | 146,438 | — |
| 35 | 데이즈온 프리바이오틱스 트리플 | ✓ | 1 | 69,920 | 53,034 | — |

### 8/5 대비 달라진 것

- **`16` 루솔이 ✓ → ✗ 로 빠졌다** (어제 46,350원으로 10% 대상이었음). 오늘 10%적립 9→8 의 원인.
- **페이백 카드가 KB·비씨 → 삼성** 으로 바뀌었다.
- 가격 인하: 11 라메종드미엘 262,400→**249,280** / 34 파라다이스 198,400→**188,480** /
  35 프리바이오틱스 82,800→**69,920** (즉시할인가).
- 적립없음 8건: 2·3 하루견과, 8·9 이디야, **16 루솔**, 36·37·38 센텔리안.

### ⚠️ `1-1` 이너플로라30정 — **이틀 연속 첫 실행부터 판매중단**

8/5 §2-2 에 "다음에 ✓ 로 바뀌면 링크는 정상이었던 것. 계속 ERR 이면 URL 재확인 필요" 라고 적어둔
그 조건에 걸렸다. **URL(2201017629) 재확인 필요.** 28 스키니랩·39 서정희pick 은 어제와 같은
판매중단이고 사용자 지시대로 목록 유지.

### 결제 전 주의 — `cart/today_carts.json` 은 아직 **8/5 자**

이 파일은 **자동 생성이 없다**(손으로 쓰는 파일). §14 날짜 preflight 가 결제를 `rc=1` 로 막으므로,
오늘 담기를 마친 뒤 오늘자로 다시 쓰지 않으면 결제가 시작되지 않는다. **의도된 차단이다.**

## 22. 다음 할 일

- [x] ~~롯데 NH `finish` 경로 신설~~ — 완료(§19-2). **라이브 검증은 다음 NH 결제 때**
- [ ] `nh_enter fields` 로 **CVC·결제비번 실제 resource-id 확정** → `_CVC_KEYS`/`_PIN_KEYS` 에 반영 (§19-2②)
- [ ] **`1-1` 이너플로라30정 URL 재확인** — 이틀 연속 판매중단(§21)
- [ ] **`n` 재고 4 < 플랜 6** — 담기 전 확인(§20)
- [ ] 담기 후 `cart/today_carts.json` 오늘자로 재작성(안 하면 결제 preflight 가 막는다 — §21)
- [ ] 8/5 이월: **#18 nami8225 롯데 비번** 계정 유지/제외 결정
- [ ] 8/5 이월: `1-1` 이너플로라30정 판매중단 추적 / `Sulwhasoo_Supply_Rate.md:188` 옛 n 번호 /
      카드 5장 이상일 때 스크롤 대응 / 윈도우 첫 실행 검증 · gitignore 파일 8종 복사

## 23. ★★★ 적립 근본수정 — "왜 자꾸 적립을 까먹지?" 의 진짜 원인 2개

사용자 지적: **"적립을 또 까먹네 왜자꾸적립을까먹지? 코드에 없어? 이 부분 다음부터는 실수안하게
코드 다 뒤져서 수정해 현대몰에서 식품구매후 무조건 적립 바로 신청하도록, 롯데도 그렇고"**

먼저 사실관계부터: **적립 호출 자체는 있었고 오늘도 돌았다**(`buy_one` 끝 → `apply_reward_now`).
문제는 **① 절반이 조용히 누락되는 판정 버그**와 **② 했는지 안 했는지 알 수 없는 로그**였다.

### ① ★판정 버그 — prmo 2건 중 1건만 성공해도 `✓`

```python
reward_ok = ("already_done': True" in last_out) or ("'success': True" in last_out)   # 옛 코드
```

**출력 전체를 문자열 검색**한다 → 데이즈온처럼 prmo 가 2개일 때 **하나만 성공해도 True**.
나머지 하나는 실패한 채로 조용히 끝났다. 8/5 §13 에서 "2군데 다 해야 한다"고 데이터는 고쳤지만
**판정은 그대로였다** — 데이터만 고치고 검증을 안 고친 셈.

→ `_parse_hp()` 신설. 서브프로세스 출력을 `HP|prmo|success|already|error` 고정 포맷으로 바꾸고
**prmo 전건이 확인돼야 ok**. 실측 대조:

| 상황 (prmo 2건 중 1건 실패) | 옛 판정 | 신규 판정 |
|---|:--:|:--:|
| `P202608043368` 실패 / `P202607292371` 성공 | **ok=True** (통과) | **ok=False** (미완) |

### ② 로그가 증거를 지웠다 — 2단 절단

| 위치 | 옛 코드 | 결과 |
|---|---|---|
| `buy.py:198` | `{last_out.strip()[-140:]}` | 마지막 140자만 남음 |
| `hmall_hyundai_buy.py` `apply_reward_now` | `tail[-1]` | 그중 **마지막 한 줄만** 출력 |

→ prmo 2건을 돌려도 화면엔 1건만 보인다. 오늘 #2·#3·#4 로그가 정확히 그 모습이었고,
**"안 했다"와 "했는데 안 보인다"를 구분할 수 없었다.** 이게 매번 의심스러웠던 이유.

→ 건별로 `[적립] prmo=P... → 신규신청/이미신청/실패` 한 줄씩 + `RESULT ok=... n/n` 요약줄.
`apply_reward_now` 는 `[적립]` 줄을 **전부** 중계한다.

### ③ rc 가 항상 0 이었다

`buy.py reward` CLI 가 `apply_reward()` 결과를 버리고 `return 0` → 호출측이 실패를 성공으로 봤다.
→ **미완이면 rc=1.**

### ④ 결과를 요약까지 끌고 간다 (현대몰·롯데 공통)

- `buy_one` 이 `res["reward"] = apply_reward_now(...)` 로 결과 보관.
- SUMMARY 줄마다 `적립✓ / 적립⚠️미확정 / 적립⚠️NH미완 / 적립-`.
- 끝에 **`!!!!` 블록**으로 미완 계정 + **재실행 명령까지** 출력(`_reward_warn`).
- 롯데도 동일(`lotte_homeshopping_buy._reward_warn`) — 뷰티는 주문완료 화면 only 라
  복구 불가라는 사실을 같이 찍는다. NH 는 `finish_lotte` 명령 안내.
- `buy.py _pay_cart` 는 `cart["reward_ok"]` 를 즉시 `_save` — 중간에 끊겨도 뭐가 남았는지 남는다.

### 검증 (오프라인 — 실제 신청 호출 없음)

- `_parse_hp` 3케이스 / 옛·신규 판정 대조표(위) / `_reward_tag` 4상태 / `_reward_warn` 현대·롯데 출력 확인.
- **라이브 검증은 다음 결제 때** — 특히 `RESULT ok=True 2/2` 가 실제로 찍히는지.

## 24. 결제 중단 (사용자 지시 "잠시스탑")

`hmall_hyundai_buy 2 3 4 5 7 8` 실행 중 **#5 로그인 직후 중단.**

| 계정 | 결과 |
|--:|---|
| 2·3·4 | **DONE** — 파라다이스×1 / 현대카드 / 188,480원, 대장 기록됨 |
| 5 | **미결제** (앱 콜드런치·로그인 단계에서 중단, 청구 없음) |
| 7·8 | **미실행** (카트는 담긴 채) |

- `beauty_fail:재인증 버튼 없음` 은 정상 — 식품 주문이라 뷰티 대상이 아님.
- ⚠️ **주문번호가 대장에 `-`** (3계정 전부 미판독). 8/5 KB 9건과 같은 증상 — 미해결.
- 담기 실측 검증: 2·3·4·5 파라다이스×1 / 7·8 파라다이스×1+알파CD×2 (카트 DOM 직접 확인).

## 25. 당일 출력물 미러 — 재고관리 시트 고정 탭 (사용자 지시)

> **"매일 스텝1,2 실행하는거 스프레드시트 두군데에 남겨줄 수 있나? 지금 기존에 출력물 남기는 곳은
> 그대로 진행하고 … 날짜별로 계속 새 스프레드시트를 추가하는게 아니라 어제 만들어놓은 탭 위에
> 덮어쓰기 … 다음날 결과도 이전날에 덮어써서줘"**

대상은 **재고관리 시트**(`1aDJ35...` = `INVENTORY_SHEET_ID`, 이미 쓰던 시트라 권한 추가 불필요):

| 원본 (기존 출력, 그대로 유지) | 미러 대상 (덮어쓰기) |
|---|---|
| 공급률 시트 `M.D` 탭 (step1) | 재고관리 **`당일 설화수`** (gid 1888175058) |
| 공급률 시트 `M.DD 식품` 탭 (step2) | 재고관리 **`당일 식품`** (gid 1569385900) |

`rate-check/mirror_daily.py` 신설 + `step1.sh`(6/6 단계) · `step2.sh` 끝에 연결. **best-effort** —
미러가 실패해도 원본은 정상이고 step 자체는 실패로 안 만든다(안내문구만 출력).

### 설계 판단 2개

- **표시값(formattedValue)으로 복사한다.** 수식을 그대로 옮기면 원본 시트의 *다른 탭*을 가리키는
  참조가 대상 시트에서 깨져 **딴 값이 조용히 뜬다.** `value_input_option="RAW"` 로 재해석도 막았다.
- **배경색도 옮긴다.** step1 의 조합번호 색칠은 정보(오늘 플랜 조합 + 상시 14). 값만 옮기면
  어느 조합이 뽑혔는지 안 보인다. 흰색 칸은 건너뛰어 요청 수를 줄인다.
- 원본 탭이 **비어 있으면 덮어쓰지 않는다** — step 실패 후 미러가 돌아 전날 내용까지 날리는 사고 방지.

### 검증 (2026-08-06 실행)

```
[mirror] 배경색 59칸 복사
[mirror] ✓ '8.6'      → 재고관리 '당일 설화수' (133행 덮어씀)
[mirror] ✓ '8.06 식품' → 재고관리 '당일 식품'   (21행 덮어씀)
```

대상 탭 재조회 — 설화수 119행(공급률 리포트 헤더·GWP 구성·3사 조합표), 식품 21행(19개 상품표) 확인.
`googleapiclient` 미설치라 **gspread 만으로** 구현(`fetch_sheet_metadata` + `batch_update`).

## 26. ★ 현대몰 주문번호 판독 신설 — 8/5 KB 9건 · 8/6 8건이 `주문 -` 였던 이유

사용자 지시: **"주문번호 판독 고쳐"**

### 원인 — 읽는 코드가 **아예 없었다**

`buy_one` 은 `order_no = (res.get("pay") or {}).get("order")` 로 대장에 넣는데,
**`pay_hyundai`/`pay_kb`/`pay_bc`/`pay_samsung` 어느 것도 `out["order"]` 를 채우지 않는다**
(`grep '"order"\]'` → 현대몰 파일에 0건). NH 만 `nh_enter finish` 에서 따로 OCR 로 읽었다.
그래서 NH 3건만 주문번호가 남고 **KB 9건(8/5) · 현대 8건(8/6) 은 전부 빈칸**이었다.
"판독 실패"가 아니라 **판독 시도 자체가 없었던 것.**

### 수정

1. **`scan_order_no(txt)` 신설** — hmall 주문번호 = `YYYYMMDD`+6 = **14자리**(`20260805138960` 실측).
   `주문번호 [:：] NNN` 라벨 패턴 → 번호 단독 `\b(20\d{12})\b` → OCR 2덩어리 분리(`20260805 138960`)
   순으로 시도. ★**전체 공백 제거는 금지** — 무관한 숫자들이 붙어 가짜 14자리가 생긴다.
2. **`wait_order_complete` 가 판독을 담당** — 이미 주문완료 화면을 OCR 하고 있으니 제자리다.
   반환에 `order` 추가. 마커가 뜬 뒤에도 번호가 **지연 렌더**될 수 있어 `order_grace=3.0` 만큼만
   추가 폴링한다(롯데 `_poll_order_complete` 가 같은 이유로 재OCR — 06-08/06-22 번호 None 사고).
   ⚠️단 hmall 주문완료는 곧 home 으로 자동이동하고 뒤에 뷰티 재인증이 있어 **길게 기다리면 안 된다.**
3. **기록 시점 3단 폴백** — `pay.order` → `order_complete.order` → **지금 화면 재판독**.
   마지막 폴백은 뷰티/팝업 처리 사이에 번호가 뒤늦게 뜨는 프레임을 잡는다. 셋 다 실패하면
   `⚠️ 주문번호 미판독` 을 남긴다(조용히 빈칸으로 넘어가지 않는다).

### 검증 (오프라인 — 정규식 8케이스)

| 입력 | 결과 |
|---|---|
| `주문번호 20260805138960 완료` | `20260805138960` |
| `주문이 완료되었습니다 20260806123456` | `20260806123456` |
| `주문번호: 20260531079448` | `20260531079448` |
| `주문이 완료 20260806 123456 재인증` (OCR 2덩어리) | `20260806123456` |
| `주문이 완료되었습니다 재인증` | None |
| `배송비 3000 상품금액 188480 결제 279528` | **None** (오탐 없음) |
| `2026-08-06 주문 1건` | **None** |
| `적립 10% 20260806 예정` | **None** |

**라이브 검증은 다음 결제 때.** 오늘 8행은 이미 빈칸으로 기록됐다(소급 안 함 — 화면이 지나갔다).

## 27. 결제 완주 — 6계정 (2·3·4·5·7·8)

| 계정 | 아이디 | 상품 | 카드 | 적립 |
|--:|---|---|---|:--:|
| 2 | 1plu1mall1 | 파라다이스×1 | 현대 | 2/2 |
| 3 | kgi5907@daum.net | 파라다이스×1 | 현대 | 2/2 |
| 4 | 1plus1mall0 | 파라다이스×1 | 현대 | 2/2 |
| 5 | 1plus1mall1 | 파라다이스×1 | 현대 | 2/2 |
| 7 | linguist01 | 파라다이스×1 + 알파CD×2 | 현대 | 2/2 |
| 8 | ybkim8225 | 파라다이스×1 + 알파CD×2 | 현대 | 2/2 |

구매대장 시트 **8행** 기록. `beauty_fail:재인증 버튼 없음` 은 정상(식품 주문 = 뷰티 대상 아님).

### ★ §23 적립 수정의 라이브 첫 검증 — 통과

```
[#8] [적립] prmo=P202608043368 → 이미신청
[#8] [적립] prmo=P202607292371 → 이미신청
[#8] [적립] RESULT ok=True 2/2 계정#8
...
  # 8 ybkim8225        [현대] DONE(...)  적립✓
  [적립] 전 계정 확인 완료 ✓
```

건별 출력 · RESULT 줄 · 요약 태그 · 최종 확인줄 전부 정상. 아침처럼 "마지막 한 줄만" 이 아니다.
`이미신청` 인 이유 = H.Point 적립신청은 **주문 단위가 아니라 계정·이벤트 단위**라
8/5 §13 복구 때 신청해 둔 것이 오늘 주문에도 적용된다.

### 중간에 폰 무선ADB 재연결 필요했음

결제 중단(§24) 후 연결이 끊겼고 **무선 디버깅 포트는 껐다 켜면 매번 바뀐다** → 옛 포트로는
`Connection refused`. 사용자가 새 pair/connect 정보를 줘서 재연결(`172.30.1.16:32889`).
페어링 자체는 유지되지만 **connect 포트는 매번 새로 받아야 한다.**

## 28. ★ 카트 비우기 실패 — 사용자 지적 "기존에 있는거 지우는거 왜 못하니"

10-19 담기 후 카트 실측에서 **판매중단 잔여물**이 나왔다:

| 계정 | 오늘 담은 것 | 남아 있던 것 (8/5 이월, 판매중단) |
|--:|---|---|
| 10 | 석류젤리 ×2 | **서정희pick 올리브오일 ×2** |
| 12 | 석류젤리 ×2 | **스키니랩 시서스 ×1** |

DOM 확인 — 둘 다 진짜 카트 품목(`input[name=backet]` + `shipping-listwrap` 안).

### 원인 ① `clear_cart` 결함 3개 (`buy/run.py`)

1. **'일반상품' 그룹 체크박스 하나만** 눌렀다 → 배송 그룹이 나뉘면 다른 그룹은 선택조차 안 된다(#10).
2. 그 라벨이 없으면 **아무것도 안 하고 return** — `[cart] 일반상품 체크박스 없음` 만 찍고 끝(#12).
3. 삭제 후 **검증이 없어** 남아 있어도 `비우기 완료` 를 찍었다(#10 로그가 그랬다).

→ 개별 체크박스(`name=backet`)를 **전부** 클릭 → 선택삭제 → **0건 확인될 때까지 최대 3회**.
   끝내 남으면 `⚠️⚠️ 잔여 N건 {품목명}` + "이대로 담으면 결제에 섞인다" 경고.
   ★`.checked` 대입 금지(합계 핸들러 미동작) — 8/5 §10 과 동일 규칙.

### 원인 ② ★`cart_items` 가 애초에 덜 셌다 (`rate-check/hmall.py`)

```js
sec = sec.parentElement;
if (sec.querySelectorAll('input[type=checkbox]').length >= 2) break;
```

'일반상품' 라벨의 조상을 타고 오르다 **체크박스 2개 이상인 첫 조상에서 멈춘다.** 배송 그룹이 나뉘면
(그룹선택 1 + 품목 1 = 2) **첫 그룹에서 멈춰 나머지 그룹을 통째로 놓친다.** 실측: #10 카트 2건 → 1건으로 읽힘.

**이게 더 나쁘다** — 이 함수는 `_cart_is_empty`(clear_cart 검증)에도 쓰인다. 즉 **잔여물이 있는데 '비었다'**.
8/2 에 "조합23 잔여물 위에 조합24 담아 1,126,548원으로 측정되고 시트에 기록"된 사고를 막으려고 넣은
검증인데, 정작 이 케이스를 못 잡고 있었다. → **개별 체크박스 기준**으로 재작성(그룹 수 무관).

## 29. 계정 간 7분 간격 — 그 시간에 적립 (사용자 지시)

> **"7분 간격으로 수정해. 결제 완료후 7분, 그리고 그 쉬는 시간 사이에 해당 계정 적립은 바로 하면
> 되잖아 적립완료하고 로그아웃,로그인하면되잖아"**

확인해보니 **폰 결제 경로엔 대기가 아예 없었다**(`main` 루프에 sleep 없음). `ACCOUNT_DELAY_SEC=420` 은
`buy/run.py` 쪽인데 그 파일 결제 코드는 폐기됐고 8/5 에 `CART_ONLY` 기본 true 로 바뀌며 0 이 됐다.
오늘 계정 간격 3~4분은 대기가 아니라 **결제 소요시간**이었다.

→ `_account_gap()` 신설 + `ACCOUNT_GAP_SEC`(기본 420, `HMALL_ACCOUNT_GAP_SEC` 로 override).

- **기산점 = 결제 완료(`res["paid_at"]`)**, 계정 시작이 아니다. 뒤따르는 뷰티·대장·**적립**은
  이 쉬는 시간 안에서 끝나므로 **그만큼 빼고 남은 만큼만** 잔다(사용자 의도 그대로).
- 적립 subprocess 는 매번 쿠키 폐기 후 새 로그인 → 계정 안 섞인다("로그아웃,로그인하면되잖아").
- **결제 안 된 계정은 간격 없음**(paid_at 없음) — 실패에 7분을 쓸 이유가 없다.
- 마지막 계정 뒤에는 대기하지 않는다.

**검증**(`HMALL_ACCOUNT_GAP_SEC=10` 오프라인): 결제실패 → 0s / 이미 12s 경과 → 0s(로그 남김) /
결제 후 3s → **남은 7s 만 대기**.

## 30. 2차 결제 — 2·3·4·5 (파라다이스×2 + 알파CD×1)

| 계정 | 주문번호 | 적립 |
|--:|---|:--:|
| 2 1plu1mall1 | `20260806084573` | 2/2 |
| 3 kgi5907@daum.net | `20260806084936` | 2/2 |
| 4 1plus1mall0 | `20260806085298` | 2/2 |
| 5 1plus1mall1 | `20260806085694` | 2/2 |

### ★§26 주문번호 판독 라이브 첫 검증 — 통과

`[order] 20260806084573` 이 찍히고 대장에도 `(주문 20260806084573)` 로 들어갔다.
아침까지 전건 `주문 -` 이던 것이 해결됐다. **4/4 판독 성공.**

### 적립은 이번만 뒤로 미뤘다 (Chrome 점유 충돌 회피)

10-19 담기가 Chrome 9222 를 쓰는 중이라 `HMALL_NO_REWARD=1` 로 결제만 먼저 했다.
동시 실행하면 담기 도중 로그인이 다른 계정으로 갈려 **엉뚱한 계정 카트에 담길 수 있다.**
→ 담기 종료 후 `buy.py reward 2~5` 실행, **전건 `RESULT ok=True 2/2`.**
§23 의 요약 경고 블록이 4계정을 정확히 잡아줘서 누락 없이 처리됐다(설계대로 동작).

## 31. ★★★ 삼성 = 항상 비전 핸드세이크 (사용자 지시) — 내가 8/5 교훈을 삼성에 적용 안 했다

> **"지금 왜 삼성으로 안했음? 그리고 삼성 왜 실패함?" / "핸드세이크로 제대로 해 좀 제발 그냥 로컬
> OCR하지말고 이전에 잘했는데왜 못하냐" / "오늘 삼성 핸드세이크로 하고 롯데도 삼성뜨면 핸드세이크로 바꿔"**

### 무슨 일이 있었나 — 10-19 결제에서 #10 빼고 9계정 전멸

```
#10  [현대] DONE   주문 20260806087104
#11~#19 [삼성] SAMSUNG_FAIL@card_cvc: '다음' 비활성 — 카드번호/CVC 입력값 오류
```

```
[claude] 로컬 매핑 실패(5/10) → 클로드 승격
[claude-ocr] 판독요청 ... 대기 (timeout 45s)
[WARN] claude 실패: 응답 없음 (45s 타임아웃) — 이 프로세스에선 이후 클로드 skip
```

셔플 키패드를 **로컬 2엔진이 10자리 중 5개(다음 계정 1개, 2개)밖에 매핑 못 했고**, 파일 핸드셰이크로
승격했는데 **내가 스크립트를 백그라운드로 돌려놓고 안 보고 있어서 45초 타임아웃**. 한 번 타임아웃하면
그 프로세스에선 클로드가 꺼져 **나머지 계정까지 전부 같은 자리에서 죽는다.**

**이건 8/5 §11 에 NH 로 똑같이 겪고 적어놓은 함정이다**(*"파일 핸드셰이크는 스크립트가 내 응답을
기다리는데 나는 스크립트를 기다린다 → 무조건 타임아웃"*). NH 는 그때 게이트를 없애고 정지-인계를
기본값으로 만들었는데 **삼성엔 그 수정을 안 했다.** `pay_samsung` docstring 에는 이미
*"PAY_VISION_MODE=1 … 로컬 OCR 이 키패드를 못 잡는 날 이 방식이 4/4 성공"* 이라고 적혀 있었다 —
**정답이 코드에 적혀 있는데 플래그 뒤에 숨어 있었고, 나는 플래그 없이 돌렸다.**

### 왜 #10 만 현대였나 (사용자 질문)

```
#10  현대 탭 @ (730, 1759)
#11  삼성 탭 @ (215, 1753)
```

**cy 가 6px 차이 = 같은 행.** 카드할인 캐러셀은 **2열 그리드**고 삼성(좌)·현대(우)가 나란히 있다
(8/5 §11 `hmall-nh-carousel-2col` 과 같은 구조). 그런데 `detect_card()` 는
`sorted(region, key=cy)` 의 **첫 매칭**을 당일카드로 삼는다 → 2열에서 좌/우 선택이 **OCR 흔들림 6px 로 갈린다.**
계정별 혜택 차이가 아니라 **판독 순서 문제**다.

★돈 문제이기도 하다: `#11 결제 98,998 → 삼성 92,069`(약 7천원 할인) / `#10 95,009 → 현대 97,052`.
**#10 은 더 비싼 카드로 결제했을 가능성**이 있다. (캐러셀 금액의 정확한 의미는 실측 확인 필요 — 미해결)

### 수정 — NH 와 완전히 같은 모양으로

1. **`pay_samsung` 의 `PAY_VISION_MODE` 게이트 제거** → **항상** 카드번호 화면에서 `manual=True` 정지.
   옛 로컬 OCR 자동입력(`_tap_shuffle` 로 카드번호/CVC/비번)은 **삭제**. 되살리지 말 것.
   ★`pay_samsung` 은 **3사 공용 정본**이라(롯데가 `_pay_samsung_shared` 로 위임) **한 곳만 고치면
   현대몰·롯데 둘 다 적용**된다 — 사용자 지시 "롯데도 삼성뜨면 핸드세이크로" 를 코드 한 곳으로 충족.
2. 인증서 구간을 `samsung_cert_step()` / `samsung_cert_done()` 으로 잘라내 러너가 재사용.
   인증서 비번도 셔플이라 거기서도 정지 → `certpw` 로 인계.
3. **호출부**: 현대몰 `buy_one` · 롯데 dispatcher 가 `manual` 을 **실패가 아니라 정상 대기**로 처리하고
   `{카드}_HANDOFF` 로 즉시 return(다음 계정 콜드런치가 살아있는 결제화면을 날리는 것 차단).
4. **`phone_auto/samsung_enter.py` 신설** — 값은 스크립트가 secrets 에서 직접 읽고
   **에이전트는 키패드 좌표만** 넘긴다(`0=x,y … 9=x,y` 10개 전부 — 값 노출 방지).
   `card → cvc → next → pin6 → next → cert → certpw → finish`.
   카드번호는 입력 직후 `card_digits_on_screen()` 로 **자릿수 검증**(8/02 #5: 15자리 중 9자리).
   `next` 는 `next_button_enabled()` 로 **활성 검증 후에만** 탭.
   `finish`/`finish_lotte` 는 몰 단위라 **`nh_enter` 것을 그대로 재사용**(중복 구현 안 함).

### ★놓칠 뻔한 것 — 요약 경고가 삼성 핸드세이크를 안 잡았다

`_reward_warn` 이 `NH_HANDOFF` 만 보고 있어서 **삼성 인계 계정이 `전 계정 확인 완료 ✓` 로 조용히 통과**했다.
→ `_HANDOFF` 를 **카드 무관**으로 잡고 카드별 러너까지 안내하도록 수정(현대몰·롯데 양쪽).
상태 접두어도 `SAMSUNG_` → **카드명 그대로 `삼성_`** 으로 통일(요약이 이 토큰으로 러너를 고른다).

**검증**(오프라인): 좌표 파서 정상/누락/형식오류 3케이스 · 게이트 제거 확인
(`PAY_VISION_MODE` 코드 0건, `_tap_shuffle` 자동입력 0건, `manual=True` 정지 O) ·
요약 태그 `적립⚠️삼성미완` · 경고가 `samsung_enter finish` / `finish_lotte` 를 정확히 안내.
**라이브 검증은 다음 삼성 결제 때.**

## 32. ★★ `detect_card` — 맨 위가 아니라 **가장 싼 카드** (사용자 지시)

> **"가장 할인율 높은 카드를 선택해야지"**

§31 에서 원인만 적고 **고치지 않은 채 넘어간 것**을 수정한다.

### 종전 (버그)

```python
for it in sorted(region, key=lambda x: x["cy"]):   # 화면 위에서부터
    ...
    return key                                      # 첫 매칭 즉시 반환 = **맨 위 카드**
```

캐러셀은 **2열 그리드**라 좌/우 칸의 cy 차이가 6px(OCR 흔들림 수준). 즉 "맨 위"는 사실상 **무작위**다.
8/6 실측: 같은 상품(석류젤리)인데 **#10 은 현대 97,052 / #11 은 삼성 92,069** 로 갈렸다.
#10 은 약 5천원 비싸게 결제됐을 가능성이 있다.

### 수정

- 영역의 **모든 카드 후보를 (금액, 카드) 로 수집** → `min(금액)` 선택.
- 박스 판정(`_row_amt_of`)은 종전 `same_row_amt` 규칙 그대로: 같은 아이템 / 같은 행 ±40px /
  바로 아래 220px·같은 열 350px. **금액 없는 배너 행은 후보에서 제외** → '현대카드 Ed2 7% 청구할인 >'
  오감지 방지(2026-06-05 #17 사고) 유지.
- 후보가 2개 이상이면 **선택 근거를 로그로** 남긴다: `후보 2개 — 삼성 92,069 / 현대 97,052 → 최저 삼성 선택`.

### 검증 (오프라인 4케이스)

| 케이스 | 결과 |
|---|---|
| 2열, 삼성 92,069 vs 현대 97,052 (현대가 위로 읽힘) — **8/6 실제 상황** | **삼성** ✓ |
| 반대로 현대 92,000 이 더 쌈 | **현대** ✓ (위치 무관) |
| 금액이 배너 **위**에 있는 이벤트 배너 + KB국민 77,631 | **KB** ✓ (배너 오감지 없음) |
| 카드할인 섹션에 금액행 없음 | **None** ✓ |

### ⚠️ 따라오는 결과 — 삼성이 최저면 무인 완주가 안 된다

가장 싼 카드가 삼성으로 뽑히면 §31 의 **핸드세이크 경로**를 탄다 = 계정마다 에이전트가 키패드를
4번(카드번호·CVC·비번·인증서비번) 판독해야 한다. 현대/KB 처럼 앱 PIN 경로가 아니면 무인 진행이 안 된다.
**싼 카드를 고르는 것과 무인 완주는 상충한다** — 급하면 `현대` 를 명시 지정해 무인으로 돌리는 선택지가 있다.

## 33. ★★ 핸드세이크면 **루프를 멈춘다** — 안 멈추면 결제화면이 날아간다

§31 을 쓰고 나서야 발견. `buy_one` 이 `{카드}_HANDOFF` 로 return 해도 **main 루프는 다음 계정으로 넘어간다.**
다음 계정은 앱을 **콜드런치**하므로 **카드번호 입력을 기다리던 살아있는 결제화면이 사라진다.**
= 인계 방식 자체가 다계정 실행에서 성립하지 않았다. **NH 도 같은 구멍이었다**(8/5 이후 계속).

→ 현대몰·롯데 **양쪽 루프**에서 `_HANDOFF` 감지 시 `break`. 멈추면서 **다음에 칠 명령을 완성해서 출력**한다:

```
★ #11 삼성 인계 대기 — 여기서 멈춥니다 (나머지 계정 중단)
  1) 화면 판독:  python3 -m phone_auto.samsung_enter shot /tmp/kp.png
  2) 입력 순서:  card → cvc → next → pin6 → next → cert → certpw → finish
  3) 마무리:     python3 -m phone_auto.samsung_enter finish 11 석류
  4) 남은 계정:  python3 -u -m phone_auto.hmall_hyundai_buy 12 13 14 15 16 17 18 19 only=석류
```

남은 계정을 사람이 다시 계산하게 두면 빠뜨린다 → **명령줄을 그대로 준다.** 롯데는 `finish_lotte` + `combo=` 로 안내.

**검증**(오프라인): 삼성/남은계정 있음 · NH/마지막 계정 두 경우 출력 확인.

## 34. 세션 종료 상태 (다른 PC 이어받기용)

### 오늘 결제 결과 — **17건 성공 / 9계정 미결제**

| 대상 | 결과 |
|---|---|
| 2·3·4·5 1차 (파라다이스×1) | ✅ 결제 + 적립 2/2 |
| 5·7·8 1차 (파라다이스×1[+알파CD×2]) | ✅ 결제 + 적립 2/2 |
| 2·3·4·5 2차 (파라다이스×2+알파CD×1) | ✅ 결제(주문번호 4/4 판독) + 적립 2/2 |
| 10 (석류×2) | ✅ 결제 `20260806087104` + 적립 |
| **11~19 (석류×2)** | ❌ **미결제** — 삼성 셔플 키패드 실패(§31). 카트는 담겨 있음 |

⚠️ **날짜가 바뀌면 리셋이다**(자동메모리 `daily-reset-no-carryover`) — 11~19 는 8/6 로 종료.
내일은 step1/step2 재실행부터 시작한다.

### 11~19 를 이어서 하려면 (오늘 안에만 유효)

```
python3 -u -m phone_auto.hmall_hyundai_buy 11 12 13 14 15 16 17 18 19 only=석류
```
→ 최저가 카드가 삼성이면 #11 카드번호 화면에서 **정지**하고 다음 명령을 출력한다(§33).
   `samsung_enter` 로 판독→입력→`finish`, 그 뒤 출력된 명령으로 나머지 계정 진행.

### ⚠️ 다른 PC 로 옮길 때 — gitignore 라 안 따라가는 파일

코드/워크로그는 전부 푸시됐다. **아래는 수동 복사해야 한다**(8/4 §14 와 동일 목록):

| 파일 | 용도 |
|---|---|
| `secrets/` (card_secrets.json, purchase_ledger.json, beauty_point_ledger.json …) | 카드정보·구매대장 로컬본 |
| `hmall_config.json` / `lotte.json` / `galleria.json` | 계정 자격증명 |
| `gen-lang-*.json` | 구글시트 서비스계정 키 |
| `hsmaster/config/sulwhasoo-ids.json` | 설화수 3사 goods_no (8/5 에 n 갱신됨) |
| `buy/cart_plan.json` | 담기 플랜 (`.gitignore:61`) |
| `buy/.env` | PIN·카드명 |

★**구매대장은 시트가 정본**이므로 로컬 JSON 이 안 넘어가도 기록은 안전하다(§18).
★`cart/today.json` · `cart/today_carts.json` 은 커밋돼 있으나 **오늘자**다 —
  날짜 preflight(§14)가 내일 실행을 막으므로 step1/step2 재실행이 정상 절차다.

### 오늘 커밋 (11건, 전부 푸시됨)

`624bb2e` 8/5 구매기록 감사 · `aacb499` 롯데 NH finish · `2978d3f` step1/step2 ·
`586f6f1` 적립 근본수정 · `124227f` 시트 미러 · `8b2acd9` 주문번호 판독 ·
`00c404b` 카트 비우기 + 7분 간격 · `40283eb` 삼성 핸드세이크 · `c0e7262` 최저가 카드 ·
`58df19f` 핸드세이크 루프 정지

## 35. ★ 윈도우 이식 — `_ocr_texts` OS 분기 (사용자 지시 "A 진행")

사용자 질문: **"윈도우에선 핸드세이크 해야되는거아닌가?"** → **핸드세이크만으로는 안 된다.**

### 왜 핸드세이크로 못 때우나 (근거)

핸드세이크가 대신하는 건 **셔플 키패드 숫자 매핑**뿐 = 계정당 3~4회. 그런데 화면 탐색 OCR 은
호출 지점이 **208곳**(hmall 105 / 롯데 87 / flow_runner 16)이고 `wait_text` 는 폴링이라
1회 호출에 최대 수십 폴(오늘 로그 `wait_text('장바구니') ✗TIMEOUT 9.16s / 8폴`).
계정당 수십~수백 번 불린다 → **45초 왕복 핸드세이크로는 물리적으로 불가능.**
→ **키패드=핸드세이크 / 화면탐색=로컬 엔진**. 윈도우엔 Apple Vision 이 없으니 **OS 분기**가 답이다.

### 수정 (`phone_auto/flow_runner.py`)

```
_ocr_texts()  ── import Vision 성공 → _ocr_texts_vision()   [맥, 정본. 손대지 않음]
              └─ ImportError      → _ocr_texts_nonmac()
                                      ├ _ocr_texts_winrt()   Windows.Media.Ocr (Vision 대응물)
                                      └ _ocr_texts_easyocr() 최후수단 (~8s/회, 13배 느림)
```

- 윈도우 1순위를 **Windows.Media.Ocr** 로 둔 이유: OS 내장이라 Vision 과 같은 급의 속도.
  easyocr 는 실측 8s/회라 208 호출 × 8s = 결제 세션(5분)을 못 버틴다 → **상시 사용 금지, 폴백 전용.**
- winrt 실패는 **첫 회만 크게 경고**하고 이후 easyocr 로 고정(`_NONMAC_ENGINE`) — 매 호출 로그 도배 방지.
- easyocr Reader 는 키패드용(영어 전용)과 **별개 인스턴스**(`ko`,`en`)로 둔다.
- ⚠️ **한국어 인식은 OS 언어팩에 달렸다** — 없으면 영어만 잡혀 한글 버튼을 못 찾는다.
  윈도우 준비물: `pip install winsdk` + 한국어 언어팩.

### 검증

| 항목 | 결과 |
|---|---|
| 맥 무회귀 — 분기 경유 vs `_ocr_texts_vision` 직접 | **완전 동일**(items == same → True) |
| 실제 사용 형태 (`scan_order_no`) | `20260806087104` 정상 판독 |
| 비맥 분기 진입(Vision ImportError 시뮬) | ✓ winrt 없음 경고 1회 → easyocr 고정 확인 |
| **윈도우 라이브** | ❌ **미검증**(작성 환경이 맥). 첫 실행 때 반드시 관찰할 것 |

### 윈도우에서 되는 것 / 안 되는 것 (이식 현황)

| 기능 | 윈도우 |
|---|---|
| step1(3사 공급률·재고·카트플랜) / step2(10% 체크) | ✅ Chrome CDP 기반 — 그대로 동작 |
| 카트 담기 · 시트 미러 · 적립신청 · 구매대장 | ✅ 동작 |
| 폰 결제(롯데·현대) | ⚠️ 이번 분기로 **가능해졌으나 미검증** — winsdk + 한국어 언어팩 필요 |
