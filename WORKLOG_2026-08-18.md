# WORKLOG 2026-08-18 — 한국 복귀 후 첫 정상 운영 / 롯데 상품번호 stale 2건 / 폰 결제 sleep 축소

> 8/16~8/19 분을 묶어 기록한다(8/11 이후 워크로그 공백 — 8/16 커밋 `a781b92` 는 코드만 남고 기록이 없었다).

## 1. 환경 — 해외(터키) → 한국 복귀

8/10~8/11 세션의 모든 이상(갤러리아 RESET, Hmall CloudFront 403, 롯데 41초, 캡차 실패)은
**출구 IP 가 터키**였기 때문이었다. 한국 복귀 후 3사 응답이 **2초대**로 정상화됐다.

| 몰 | 해외(8/11) | 국내(8/16~) |
|---|---|---|
| 갤러리아 | 18~44초 / RESET | 2.1초 |
| Hmall | 403 차단 | 2.0초 |
| 롯데 | 41.7초 | 2.5초 |

시계도 한때 **+1일** 틀어져 구글 JWT 가 `invalid_grant` 로 거부됐다(시트 입력 전량 실패).
현재는 구글 서버와 초 단위로 일치. → **`date` 출력도 관측이다.** 스크립트가 만든 탭 이름과
어긋나면 시계부터 의심할 것.

## 2. step1/step2 정상 운영 재개

| 날짜 | step1 | step2 | 채널 |
|---|---|---|---|
| 8/16 | 16분 10초, rc=0 | `8.16 식품` | galleria N=36 |
| 8/17 | 16분 20초, rc=0 | `8.17 식품` | **lotte N=7** |
| 8/18 | 16분 10초, rc=0 | `8.18 식품` (10%적립 **14건**) | **lotte N=7** |

`clear_cart 실패 0` / `add_to_cart 실패 0` — 8/16 커밋(`a781b92`)의 렌더지연 오판 수정이
실전에서 정상 동작 확인됨.

## 3. ★★ 롯데 상품번호가 **두 군데**서 stale 이었다 (8/17)

사용자 지시: "롯데 `g` 를 `2834421446` 로 수정, 앞으로 이걸로".

`hsmaster/config/sulwhasoo-ids.json` 만 고치면 될 줄 알았으나, **`buy/sulwhasoo.py:61` 에
상품번호 테이블이 따로 하드코딩**돼 있었다(주석엔 "월 1회 갱신"). 담기는 그쪽을 읽으므로
config 만 고쳤으면 **구 상품이 그대로 담겼다.**

그 테이블을 확인하다 **`n` 도 어긋난 것을 발견**했다:

| 코드 | 담기가 쓰던 번호 | config(정본) | 실측 |
|---|---|---|---|
| g | 2719761525 (공통)자음생2종 | **2834421446** | (i몰단독)자음생 2종 세트 |
| **n** | 2923418727 | **2923426629** | 구 번호 = **판매중단** 상품 |

→ **rate-check 는 config 번호로 가격을 재고, 담기는 구 번호로 담고 있었다.** `n` 이 들어간
조합(24·25·26·27)은 판매중단 상품을 담으려 한 것 — 8/17 조합24 담기 실패의 유력한 원인.

**수정**: `LOTTE_PRODUCTS` 를 config 에서 읽도록 변경(이름만 코드 유지, 번호는 SoT).
누락 시 `[WARN]` 출력. → 앞으로 config 만 고치면 담기까지 자동 반영된다.

> 같은 유형이 반복된다: `galleria SET_COMBINE_RULES` 의 s07 단가(8/11), 이번 상품번호.
> **"월 1회 갱신" 주석이 달린 사본은 갱신되지 않는다.** 사본을 만들지 말고 SoT 를 읽을 것.

## 4. 8/18 담기 · 결제

**담기**
- 현대몰 #1~5 조합1(`f 윤조에센스90 ×5`) — **5/5**
- 롯데 #8~14 조합24(`g×2 + n×2`) — **7/7**, 전부 `카트 2건` 검증
- 롯데 #1~7 조합14 (8/17) — **6/7** (#1 만 `자격증명 거부`)

**결제 (롯데 폰, 조합24)** — `23:47:40 → 23:59:33`

| 계정 | 결과 |
|---|---|
| #8 jye40323 | ✅ 주문 `2026-08-18-H90059` / 롯데카드 7% / **뷰티포인트 적립 완료** |
| #9 wlstmdlsfk | ✗ `PAY_FAIL@loca_method` — 사용자 확인 결과 **실결제 안 됨** |
| #10 | ✗ **ADB `device not found`** — mDNS 트랜스포트 끊김 |
| #11~14 | 미시도 |

⚠️ #8 구매대장이 **`(조합 미지정) x1 ?원`** 으로 기록됐다 — 조합번호·금액 누락. 보정 필요.

### 결제 전 preflight 에 막혔던 건 (참고)

`cart/today_carts.json` 이 **8/6 자**라 날짜 preflight 가 결제를 막았다. 이 파일은 손으로 쓰는
파일이고 **현대몰 식품**(`record_food`·H.Point prmo)용이라 롯데 설화수 결제와 무관하다
(롯데는 `record_combo` 사용). `ALLOW_STALE_CART=1` 우회 대신 **오늘자·빈 카트로 갱신**했다
— 오늘 식품 담기가 없었다는 게 사실이므로. 구 파일은 `/tmp/today_carts.0806.bak.json`.

## 5. #1 롯데 자격증명 — 5번째 재발

8/1 · 8/3 · 8/11 · 8/17 · 8/18. 8/3 에 DOM 값 + sha1 대조로 **`lotte.json` 의 #1 비밀번호가
서버에서 거부됨**을 확정했다. 여전히 미해결 — **사용자가 실제 비밀번호를 확인해야 한다.**

> 8/18 에 #2·#3 도 같은 증상을 보여 "계정 다수 문제"로 의심했으나, `n` 판매중단 상품 수정 후
> **#2~#7 이 전부 정상 로그인**했다. → **#1 만의 문제가 맞다.**

## 6. ★ 폰 결제가 느린 진짜 이유 (8/19)

사용자 지적: "카드앱 PIN 구간 말고 결제 창에서 이거저거 누를 때 너무 느리다."

가설 검증 — **코드는 오히려 계속 가벼워졌다**(회귀 아님):

| 커밋일 | sleep 합 | OCR 호출 |
|---|--:|--:|
| 6/22 | 178.9s | 96 |
| 7/24 | 185.6s | 99 |
| **8/07 (현재)** | **136.2s** | **69** |

`wait_text` 실측도 정상(폴당 0.9~1.2초, 대개 1~3폴에 발견). **좌표가 틀려 헤매는 정황 없음.**
`✗ ocr_tap '확인' 미발견` 류는 `선택완료 → 적용 → 확인` **폴백 체인**의 정상 no-op 이다.

**진짜 원인 = 로그에 안 찍히는 고정 sleep.** 주문서 구간 로그가 21줄뿐인데 그 사이 함수들이
조용히 잔다: `select_card_lotte` 8.5s / `set_address` 7.6s / 쿠폰 2종 14.2s / `goto_cart_select_all`
6.7s / `set_cash_receipt` 5.3s … **합계 약 48초**(현대몰은 약 23초).

### 조치 — `nap()` 도입 (배율 1개로 조절)

주문서 구간의 `time.sleep(N)` → **`nap(N)`** 치환. 롯데 40곳 / 현대몰 21곳.

```python
_ORDER_SLEEP_SCALE = float(os.environ.get("ORDER_SLEEP_SCALE", "0.4"))
def nap(sec): time.sleep(max(0.2, sec * _ORDER_SLEEP_SCALE))
```

- 배율 0.4 기준 **계정당 롯데 ~30초 / 현대몰 ~13초** 절감
- 실패하면 `ORDER_SLEEP_SCALE=0.7`, 완전 원복은 `=1.0` — **코드 수정 불필요**
- **카드앱 구간(`pay_*`, `flow_runner.run_action`)은 제외** — 오탭 = 카드 잠김
- 대부분 뒤에 `ocr_tap`/`wait_text`/`_scroll_to` 등 **자체 재시도 폴링**이 이어져 중복이었다

⚠️ **라이브 미검증** — 다음 담기/결제 때 확인할 것: ① `select_card_lotte` 카드선택 밀림
② 쿠폰 서브모달을 뜨기 전에 읽어 `applied=0` 되는지 ③ `set_address` 팝업 타이밍.

### OCR 엔진 (사용자 질문)

폰 결제 판독은 **macOS Vision 단일 엔진**이다(easyocr 아님 — 그건 캡차·키패드용 다엔진 투표 쪽).
`VNRequestTextRecognitionLevelAccurate` + `UsesLanguageCorrection=True` + 1080×2400 전량 →
**1회 약 0.7초**. 속도를 더 줄이려면 언어교정 끄기 / 하단 크롭이 후보이나, `Fast` 레벨은
오탭 위험이라 권하지 않는다.

## 7. 안 한 것 · 다음 할 일

- [ ] **#8 구매대장 보정** — 조합24 + 실결제금액 (현재 `(조합 미지정) x1 ?원`)
- [ ] **`nap()` 라이브 검증** — 실패 시 `ORDER_SLEEP_SCALE` 상향
- [ ] **#9 `PAY_FAIL@loca_method` 에 정지-인계 추가** — 삼성·NH 와 같은 패턴 (사용자 제안)
- [ ] **#1 롯데 비밀번호 확인** (5번째 재발)
- [ ] 8/18 카트는 날짜가 바뀌어 **전부 폐기** — 이월 결제 없음

━━━━━━━━━━━━━━━━━━━━ 2차 세션 (윈도우 PC) ━━━━━━━━━━━━━━━━━━━━

# 2차 세션 — 윈도우 PC 신규 셋업 / **윈도우 OCR 라이브 첫 검증**

> 위 1차 세션(맥)과 **다른 기기**의 기록이다. 맥은 8/16~8/19 운영, 이쪽은 윈도우 이식 셋업.
> §1 "해외→한국 복귀"·§6 "OCR 엔진(macOS Vision)" 과 짝이 되는 **윈도우 쪽 절반**이다.

## 요약 (한 줄)
새 윈도우 PC 에 레포 clone + secrets 배치 + 의존성 전부 설치. **`Windows.Media.Ocr` 한글 판독 5/5 실측 성공**
(= `flow_runner.py` 의 "윈도우 경로 라이브 미검증" 경고 해소).

## 8. 사용자 지시 (원문)

1. "git pull" → 빈 폴더라 실패. 2. "https://github.com/tkdkky2010-cyber/3mall-auto-buy 이거야" → clone.
3. "READ_FIRST.md 랑 GOAL.md 읽어줘"
4. "3mall-secrets 폴더 있음 그거 어디다 넣어주면 좋을지랑 여기에 install해야되는거 다 찾아줘.
   pip install winsdk 이것도 해야된다고 하던데 그리고 한국어 패치도 해야된다고 하던데, window vision OCR 도 찾아서 해야된다고 하던데"
5. "메인 ocr 은 뭐야? window ocr 뭐 있다고하던데 너가 직접얘기했음"
6. "윈도우에서 받을 건 두 가지야 — winsdk(또는 winrt 분할 패키지) / **한국어 OCR 언어팩(이게 더 중요)**.
   확인은 `python -m phone_auto.flow_runner ocrcheck`. easyocr 는 회당 8초라 실질적으로 못 써."
7. "3몰 로그인은 왜해"
8. "해외 ip 관련된내용 다 찾아서 수정해 지금부터는 다 한국이니까 고려할필요없어"
9. "원격에 새 커밋이 먼저야 그 위에 필요로한 내용 있으면 여기서 작업한 내용은 그 위에 올려줘"

## 9. 환경

| 항목 | 값 |
|---|---|
| 레포 위치 | `C:\Users\memo\OneDrive\Desktop\3mall-auto-buy` |
| OS | Windows 10 Pro 19045 / 표시언어 ko-KR |
| 출구 IP | `220.118.144.204` — **KR / Seoul / AS4766 Korea Telecom** (§1 한국 복귀와 일치) |

## 10. secrets 배치 — ★한 겹 더 들어가 있었다

사용자가 옮긴 위치가 `3mall-auto-buy/3mall-secrets/` 였다. **그 경로면 전부 못 읽는다**
(코드는 `hmall_config.json` 을 레포 루트에서 찾는다). 루트로 펼치고 빈 폴더 삭제. 충돌 0건.

- `git status` 검증: 21개 중 20개 gitignore 차단. **`.claude/settings.local.json` 만 안 걸려 있었다**
  → 사용자 승인 후 `.gitignore` 에 추가(기기별로 다른 파일이라 커밋하면 다른 PC 설정을 덮어쓴다).
- `profiles/` 는 **현재 파이썬 코드 어디서도 안 쓴다**(hsmaster TS 시절 잔재).
  `secrets/*_session_*.json` 3개도 참조하는 코드 없음. → secrets 묶음에서 빠져 있어도 문제 없다.

## 11. 설치 (전부 완료)

| 항목 | 버전 | 비고 |
|---|---|---|
| Python | **3.12.10** | ★3.13 금지 — `winsdk` wheel 이 **cp312 까지만**. 3.13 이면 소스빌드로 넘어가 실패 |
| pip 13종 | winsdk 1.0.0b10 / patchright 1.62.1 / playwright 1.62.0 / selenium 4.47 / gspread 6.2.1 / requests / dotenv / Pillow 12.3 / numpy 2.5.2 / easyocr 1.7.2 / cv2 5.0.0 / pytesseract 0.3.13 / torch 2.13+cpu | |
| Node.js | v22.21.1 LTS | winget MSI 가 **관리자 권한 요구 → UAC 취소(1602)**. **portable zip** 을 `%LOCALAPPDATA%\Programs\nodejs` 에 풀어 해결 |
| Chrome for Testing | 152.0.7977.42 | `C:\Users\memo\ChromeForTesting\chrome\win64-*\chrome-win64\chrome.exe` |
| adb | 37.0.1 | winget `Google.PlatformTools` |
| 한국어 OCR 언어팩 | **이미 설치돼 있었음** | 별도 설치 불필요 |

- User PATH 에 `nodejs` 추가 + 중복 제거. Python 은 winget 이 이미 등록.
- `winrt-*` 분할 패키지 **불필요** — `winsdk` 가 그대로 설치됨.
- **`hsmaster` 는 `npm install` 불필요** — 파이썬이 `hsmaster/config/sulwhasoo-ids.json` **파일 경로로만** 참조하고
  TS CLI 를 호출하는 곳이 없다. Node 는 CFT 설치(`npx @puppeteer/browsers`) 때문에만 필요했다.

## 12. ★★ 윈도우 OCR — 라이브 검증 완료 (§6 "OCR 엔진" 의 윈도우 쪽 절반)

`flow_runner.py:72` 의 *"⚠️ 윈도우 경로는 라이브 미검증(작성 환경이 맥)"* → **이제 검증됨.**

```
[ocrcheck] OS=Windows 10
  엔진: Windows.Media.Ocr — 설치된 OCR 언어 ['ko']
```

**조용한 실패가 제일 위험**하다는 지적에 따라 실제 한글 판독까지 확인:

```
판독 5개: ['구매하기', '결제하기', '주문완료', '전액사용', '카드할인 12,340원']
```

**5/5 정확. 쉼표 들어간 금액도 정상.** (테스트 이미지 = malgun.ttf 40px)

| OS | 엔진 | 실측 |
|---|---|---|
| macOS | Apple Vision (정본) | ~0.7s/회 (§6) |
| **윈도우** | **Windows.Media.Ocr** | **0.1~0.3s/회** |
| 폴백 | easyocr | ~8s/회 — **상시 사용 금지** |

- 관측된 차이: 예상 출력은 `['en-US','ko-KR']` 인데 실제는 **`['ko']`**. 태그 형식만 다르고 동작은 정상.

## 13. `buy/requirements.txt` 가 코드보다 뒤처져 있었다 → 수정

`winsdk` 가 **requirements 에 없었다.** 코드(`flow_runner.py:99`)는 쓰는데 목록엔 없고,
주석은 *"윈도우에서 폰 결제를 돌리려면 easyocr/클로드 사다리가 먼저 필요하다"* 는 **옛 상태**였다
(`de8292c`·`da0021c` 로 이미 구현됨). 이대로면 **다음 윈도우 PC 에서 또 조용히 easyocr 로 떨어진다.**

→ `winsdk>=1.0.0b10 ; sys_platform == "win32"` 추가(맥에선 마커로 건너뜀).
Python 3.12 고정 이유·`winrt` 분할 패키지 대안·`ocrcheck` 확인법을 주석에 남김.
`pip install --dry-run -r buy/requirements.txt` 로 파싱·해석 검증(exit 0).

## 14. "3몰 로그인 필요" 는 **내가 틀렸던 것** (정정)

세션 중반에 "CFT 띄웠으니 3몰 로그인 하라"고 보고했는데 **틀렸다.** 코드가 ID/PW 자동 로그인한다.

| 몰 | 함수 | 자격증명 |
|---|---|---|
| 현대 | `buy/run.py:201 login()` | `hmall_config.json` (19계정, 계정마다 로그아웃→로그인) |
| 갤러리아 | `buy/sulwhasoo.py:134 galleria_login()` | `galleria.json` |
| 롯데 | `buy/sulwhasoo.py:641 lotte_login()` | `lotte.json` (+ 캡차 자동풀이) |

원인: `CHROME_SETUP.md` 의 "로그인 1회" 를 몰 계정으로 오독. 그건 **CFT 프로필의 Google 계정** 얘기다.

## 15. ★★ 해외 IP 전수조사 — **코드에는 처리 로직이 0개였다**

사용자 지시 8 로 `해외|터키|VPN|본인확인|인증번호|proxy|403|차단` 전수 grep.
8/10 대응은 전부 (a) 사용자 수동 조치(VPN 확장 설치·직접 로그인) + (b) `scratchpad/slow_run.py`
임시 러너(레포 무수정·커밋 안 함) 였다. → **고칠 코드가 없었다.**

### 헷갈리기 쉬운 오탐 — **건드리면 안 되는 것들** (전부 해외 IP 와 무관)

| 위치 | 걸린 이유 | 진짜 원인 |
|---|---|---|
| `hmall_hyundai_buy.py:994` | `본인 확인` / `CertifyGuest` | **토스앱 미로그인** 게스트 인증 |
| `rate-check/lotte.py:55` | 403 | 롯데 **WAF 콜드세션** → 세션 워밍 |
| `cart/check10.py:1347` | 로그인 차단 | Hmall **reCAPTCHA v3** |
| `sulwhasoo.py:524` · `flow_runner.py:1052` | 403 | **GCV BILLING_DISABLED** |
| `hmall_webview.py:7` | ws 403 | **Origin 헤더** |

### 고친 것 — `WORKLOG_2026-08-10.md` 에 무효화 표시 (기록 자체는 안 지움)

과거 워크로그는 **역사라 다시 쓰지 않는다.** 다음 세션이 헛다리 짚지 않게 **추가 표시만** 했다:
최상단 `⛔ 2026-08-18 무효화` 블록 / §8 "VPN 해제 후 재실행" 폐기 /
§15 "SMS 핸드세이크 후 진행" → "한국 IP 라 조건 없음" / 조합 26 차단 원인 주석
(→ 차단은 해외 IP 탓이었고 **담기 타이밍 ≈3초 문제만 남는다**).

⚠️ **§16 교훈 3·4 는 일부러 살렸다** — "확장 프록시는 `page.request.get()` 을 우회한다",
"같은 로그인 실패라도 원인이 다르다(느림 vs 차단) → 화면 본문을 읽어라". **진단 원칙이라 IP 와 무관하다.**

## 16. 검증 (실측)

```
python -m phone_auto.flow_runner ocrcheck   → Windows.Media.Ocr / ko / 판독 5개 전부 정확
python buy.py status                        → 카트 로딩 OK (secrets 읽기 정상)
purchase_ledger._ledger_ws()                → 구매대장 196행 (구글 서비스계정 키 OK)
chrome_launcher.resolve_cdp_port()          → CDP 9222 launch (windows) → 살아있음 True
adb devices                                 → 데몬 기동 OK, 기기 0대(페어링 필요)
```

`resolve_cdp_port()` 의 **윈도우 분기(`_launch_windows`)도 이번이 첫 라이브 실행** — 정상.

## 17. 윈도우 쪽 남은 것

- [ ] 폰 무선디버깅 `adb pair` / `adb connect` — PC 마다 새로 해야 함 (§4 #10 의 `device not found` 와 별개 이슈)
- [ ] CFT 첫 실행 시 `[BLOCKED] 다른 로그인 수단` 뜨는지 관찰
      (7/05 "CfT 바이너리가 Hmall 안티봇에 감지" 이력 → 7/16 단일 CFT 원칙으로 복귀)
- [ ] 윈도우에서 step1/step2 **첫 실행 미검증** (8/04 워크로그의 이월 TODO 그대로)
- [ ] `nap()`(§6)·`LOTTE_PRODUCTS` SoT 참조(§3)는 **맥에서 작성된 미검증 변경** — 윈도우에서 돌릴 때도 같이 관찰

## 18. 오늘 배운 것 (윈도우 이식)

1. **`winsdk` 는 cp312 가 마지막이다** — 윈도우는 Python 3.12 로 고정. 3.13 이면 OCR 경로 전체가 막힌다.
2. **winget MSI 는 UAC 를 탄다** — 비대화형 세션에서 1602 로 죽는다. portable zip + user PATH 가 우회로.
3. **"조용히 실패" 를 잡으려면 엔진 존재 확인으로 끝내면 안 된다** — 실제 한글 이미지를 만들어 판독까지 봐야 한다.
   `ocrcheck` 가 `['ko']` 를 뱉는 것과 `'구매하기'` 를 실제로 읽는 것은 **다른 주장**이다.
4. **문서의 "로그인 1회" 가 어느 로그인인지 확인하고 말하라** — CFT Google 계정을 몰 계정으로 오독해 잘못 보고했다.
5. **"관련 내용 전부 수정" 지시가 와도 먼저 전수조사한다** — 조사 결과 코드 변경 대상이 **0개**였고,
   오탐 5건은 건드렸으면 전부 회귀였다.
