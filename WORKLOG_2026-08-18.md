# WORKLOG 2026-08-18 — 한국 복귀 후 첫 정상 운영 / 롯데 상품번호 stale 2건 / 폰 결제 sleep 축소

> 8/16~8/19 분을 묶어 기록한다(8/11 이후 워크로그 공백 — 8/16 커밋 `a781b92` 는 코드만 남고 기록이 없었다).

## 1. 환경 — 해외(터키) → 한국 복귀

8/10~8/11 세션의 모든 이상(갤러리아 RESET, Hmall CloudFront 403, 롯데 41초, 캡차 실패)은
**출구 IP 가 터키**였기 때문이었다. 한국 복귀 후 3사 응답이 **2초대**로 정상화됐다.

| 몰 | 해외(8/11) | 국내(8/16~) |
|---|---|---|
| 갤러리아 | 18~44초 / RESET | 2.1초 |
| Hmall | 403 차단 | 2.0초 |
| 롯데 | 41.7초 | 2.5초 |

시계도 한때 **+1일** 틀어져 구글 JWT 가 `invalid_grant` 로 거부됐다(시트 입력 전량 실패).
현재는 구글 서버와 초 단위로 일치. → **`date` 출력도 관측이다.** 스크립트가 만든 탭 이름과
어긋나면 시계부터 의심할 것.

## 2. step1/step2 정상 운영 재개

| 날짜 | step1 | step2 | 채널 |
|---|---|---|---|
| 8/16 | 16분 10초, rc=0 | `8.16 식품` | galleria N=36 |
| 8/17 | 16분 20초, rc=0 | `8.17 식품` | **lotte N=7** |
| 8/18 | 16분 10초, rc=0 | `8.18 식품` (10%적립 **14건**) | **lotte N=7** |

`clear_cart 실패 0` / `add_to_cart 실패 0` — 8/16 커밋(`a781b92`)의 렌더지연 오판 수정이
실전에서 정상 동작 확인됨.

## 3. ★★ 롯데 상품번호가 **두 군데**서 stale 이었다 (8/17)

사용자 지시: "롯데 `g` 를 `2834421446` 로 수정, 앞으로 이걸로".

`hsmaster/config/sulwhasoo-ids.json` 만 고치면 될 줄 알았으나, **`buy/sulwhasoo.py:61` 에
상품번호 테이블이 따로 하드코딩**돼 있었다(주석엔 "월 1회 갱신"). 담기는 그쪽을 읽으므로
config 만 고쳤으면 **구 상품이 그대로 담겼다.**

그 테이블을 확인하다 **`n` 도 어긋난 것을 발견**했다:

| 코드 | 담기가 쓰던 번호 | config(정본) | 실측 |
|---|---|---|---|
| g | 2719761525 (공통)자음생2종 | **2834421446** | (i몰단독)자음생 2종 세트 |
| **n** | 2923418727 | **2923426629** | 구 번호 = **판매중단** 상품 |

→ **rate-check 는 config 번호로 가격을 재고, 담기는 구 번호로 담고 있었다.** `n` 이 들어간
조합(24·25·26·27)은 판매중단 상품을 담으려 한 것 — 8/17 조합24 담기 실패의 유력한 원인.

**수정**: `LOTTE_PRODUCTS` 를 config 에서 읽도록 변경(이름만 코드 유지, 번호는 SoT).
누락 시 `[WARN]` 출력. → 앞으로 config 만 고치면 담기까지 자동 반영된다.

> 같은 유형이 반복된다: `galleria SET_COMBINE_RULES` 의 s07 단가(8/11), 이번 상품번호.
> **"월 1회 갱신" 주석이 달린 사본은 갱신되지 않는다.** 사본을 만들지 말고 SoT 를 읽을 것.

## 4. 8/18 담기 · 결제

**담기**
- 현대몰 #1~5 조합1(`f 윤조에센스90 ×5`) — **5/5**
- 롯데 #8~14 조합24(`g×2 + n×2`) — **7/7**, 전부 `카트 2건` 검증
- 롯데 #1~7 조합14 (8/17) — **6/7** (#1 만 `자격증명 거부`)

**결제 (롯데 폰, 조합24)** — `23:47:40 → 23:59:33`

| 계정 | 결과 |
|---|---|
| #8 jye40323 | ✅ 주문 `2026-08-18-H90059` / 롯데카드 7% / **뷰티포인트 적립 완료** |
| #9 wlstmdlsfk | ✗ `PAY_FAIL@loca_method` — 사용자 확인 결과 **실결제 안 됨** |
| #10 | ✗ **ADB `device not found`** — mDNS 트랜스포트 끊김 |
| #11~14 | 미시도 |

⚠️ #8 구매대장이 **`(조합 미지정) x1 ?원`** 으로 기록됐다 — 조합번호·금액 누락. 보정 필요.

### 결제 전 preflight 에 막혔던 건 (참고)

`cart/today_carts.json` 이 **8/6 자**라 날짜 preflight 가 결제를 막았다. 이 파일은 손으로 쓰는
파일이고 **현대몰 식품**(`record_food`·H.Point prmo)용이라 롯데 설화수 결제와 무관하다
(롯데는 `record_combo` 사용). `ALLOW_STALE_CART=1` 우회 대신 **오늘자·빈 카트로 갱신**했다
— 오늘 식품 담기가 없었다는 게 사실이므로. 구 파일은 `/tmp/today_carts.0806.bak.json`.

## 5. #1 롯데 자격증명 — 5번째 재발

8/1 · 8/3 · 8/11 · 8/17 · 8/18. 8/3 에 DOM 값 + sha1 대조로 **`lotte.json` 의 #1 비밀번호가
서버에서 거부됨**을 확정했다. 여전히 미해결 — **사용자가 실제 비밀번호를 확인해야 한다.**

> 8/18 에 #2·#3 도 같은 증상을 보여 "계정 다수 문제"로 의심했으나, `n` 판매중단 상품 수정 후
> **#2~#7 이 전부 정상 로그인**했다. → **#1 만의 문제가 맞다.**

## 6. ★ 폰 결제가 느린 진짜 이유 (8/19)

사용자 지적: "카드앱 PIN 구간 말고 결제 창에서 이거저거 누를 때 너무 느리다."

가설 검증 — **코드는 오히려 계속 가벼워졌다**(회귀 아님):

| 커밋일 | sleep 합 | OCR 호출 |
|---|--:|--:|
| 6/22 | 178.9s | 96 |
| 7/24 | 185.6s | 99 |
| **8/07 (현재)** | **136.2s** | **69** |

`wait_text` 실측도 정상(폴당 0.9~1.2초, 대개 1~3폴에 발견). **좌표가 틀려 헤매는 정황 없음.**
`✗ ocr_tap '확인' 미발견` 류는 `선택완료 → 적용 → 확인` **폴백 체인**의 정상 no-op 이다.

**진짜 원인 = 로그에 안 찍히는 고정 sleep.** 주문서 구간 로그가 21줄뿐인데 그 사이 함수들이
조용히 잔다: `select_card_lotte` 8.5s / `set_address` 7.6s / 쿠폰 2종 14.2s / `goto_cart_select_all`
6.7s / `set_cash_receipt` 5.3s … **합계 약 48초**(현대몰은 약 23초).

### 조치 — `nap()` 도입 (배율 1개로 조절)

주문서 구간의 `time.sleep(N)` → **`nap(N)`** 치환. 롯데 40곳 / 현대몰 21곳.

```python
_ORDER_SLEEP_SCALE = float(os.environ.get("ORDER_SLEEP_SCALE", "0.4"))
def nap(sec): time.sleep(max(0.2, sec * _ORDER_SLEEP_SCALE))
```

- 배율 0.4 기준 **계정당 롯데 ~30초 / 현대몰 ~13초** 절감
- 실패하면 `ORDER_SLEEP_SCALE=0.7`, 완전 원복은 `=1.0` — **코드 수정 불필요**
- **카드앱 구간(`pay_*`, `flow_runner.run_action`)은 제외** — 오탭 = 카드 잠김
- 대부분 뒤에 `ocr_tap`/`wait_text`/`_scroll_to` 등 **자체 재시도 폴링**이 이어져 중복이었다

⚠️ **라이브 미검증** — 다음 담기/결제 때 확인할 것: ① `select_card_lotte` 카드선택 밀림
② 쿠폰 서브모달을 뜨기 전에 읽어 `applied=0` 되는지 ③ `set_address` 팝업 타이밍.

### OCR 엔진 (사용자 질문)

폰 결제 판독은 **macOS Vision 단일 엔진**이다(easyocr 아님 — 그건 캡차·키패드용 다엔진 투표 쪽).
`VNRequestTextRecognitionLevelAccurate` + `UsesLanguageCorrection=True` + 1080×2400 전량 →
**1회 약 0.7초**. 속도를 더 줄이려면 언어교정 끄기 / 하단 크롭이 후보이나, `Fast` 레벨은
오탭 위험이라 권하지 않는다.

## 7. 안 한 것 · 다음 할 일

- [ ] **#8 구매대장 보정** — 조합24 + 실결제금액 (현재 `(조합 미지정) x1 ?원`)
- [ ] **`nap()` 라이브 검증** — 실패 시 `ORDER_SLEEP_SCALE` 상향
- [ ] **#9 `PAY_FAIL@loca_method` 에 정지-인계 추가** — 삼성·NH 와 같은 패턴 (사용자 제안)
- [ ] **#1 롯데 비밀번호 확인** (5번째 재발)
- [ ] 8/18 카트는 날짜가 바뀌어 **전부 폐기** — 이월 결제 없음
