# WORKLOG 2026-09-02 — Step1/Step2 완주 + rate-check 버그 4건

## 0. 사용자 지시 (문장 그대로)

1. "13번 아이디 주문번호 앱에서 찾아서 대장에 넣어줘"
2. "36 37 38 동국제약, 센텔리안 제품은 이제 구매안하기로 했으니 그건 스텝2 할때 안알아봐도돼"
3. "스텝1,2 진행해줘"
4. "앞으로 스텝2에 제품 추가할게 / 상품명 뉴케어 당플랜 프로 호두맛 72팩 <hmall URL> / 쿠팡 링크 <...> / 더 줘야하는거있어?"

## 1. Step 1 결과 (탭 `9.2`)

| 단계 | 결과 |
|---|---|
| 1단계 갤러리아 | 27조합, 공급률 0.4998~0.5350, `A1:K62` |
| 재고 비교 | 4조합(24~27) s27/s28 변경 → **새 버전 `b`** 생성 (`1b 9/2`…`27b 9/2`) |
| 2단계 현대Hmall | 27조합, 전 조합 **토스페이 삼성 7%**, `A65:M97` |
| 3단계 롯데 | 27조합, **KB국민 5% 청구할인**, 적립 0원, `A100:M133` |
| 4단계 cart_plan | **채널 갤러리아 N=36** / 25조합 / 평균 0.5185 / 13,583,810원 |

## 2. Step 2 결과 (탭 `9.02 식품` 신규)

17개 상품(36·37·38 제외 + 40 신규). `cart/today.json` 갱신.
- **40 뉴케어 당플랜 프로 230ml 호두맛 72팩** (신규, `2248814679`) —
  정가 227,700 → 우수가 143,460 → 실비 133,460, **10% 적립 7단 구간 ✓**, 옵션 없음.
- ⚠️ **판매중단 2건**: 28 스키니랩 시서스 / 39 서정희pick 올리브오일. 일시적일 수 있어 목록 유지.

## 3. ★★ 공급률 오측 — "클릭 성공 ≠ 담김" 이 rate-check 에도 있었다

2단계 첫 실행에서 **조합 26 만 공급률 0.3429** (나머지 0.51~0.62). 도메인 상식과 모순 →
관측부터 의심하고 26번만 재측정 → **595,107원 / 0.6001**. 오측이 확정됐다.

원인은 재측정 로그에 드러났다.

```
[STEP] add_to_cart n × 1  [후보 1/2 2228732430]
    [ERR] 장바구니 버튼 못 찾음        ← 재측정에선 실패가 드러남
[STEP] add_to_cart n × 1  [후보 2/2 2228732274]
    [OK] ...
```

첫 실행에선 같은 1순위 후보가 **`[OK]` 를 찍고도 실제로는 안 담겼다.** `add_to_cart` 의 True 는
"버튼을 눌렀다"는 뜻이지 담겼다는 뜻이 아닌데, rate-check 는 그걸 검증 없이 믿고 가격을 쟀다.

**왜 위험한가**: 공급률은 **낮을수록 좋다**. 품목이 빠지면 금액이 낮게 나오고 → 공급률이 낮아지고
→ `cart_plan` 이 그 조합을 **1순위로 고른다**. 조용히 잘못된 조합을 사게 되는 값이다.
(`buy/sulwhasoo.py` 는 같은 이유로 이미 담기 검증을 하고 있었다 — rate-check 만 빠져 있었다.)

→ `rate-check/hmall.py process_combo` 에 **카트 줄 수 검증** 추가. 안 맞으면 경고가 아니라 **중단**.

### 3-1. 2단계 재실행에서 17번이 실패 → 단일 재측정
전체 재실행 시 17번이 `카드할인 캐러셀 없음` 으로 실패(일시적 DOM). `hmall.py 17` 로 재측정해
0.6214 확보. **부분 실행은 해당 행만 쓴다**(`write_sheet(only_idx)` → `A87:M87` + `L18`)는 것을
코드로 확인하고 실행했다 — 전체 범위를 덮어쓰지 않는다.

## 4. 윈도우 cp949 `open()` — 3곳

`rate-check/hmall.py` 가 `sulwhasoo-ids.json` 을 읽다 `UnicodeDecodeError: 'cp949'` 로 죽었다.
**같은 모양을 폴더 전체에서 찾아** 고쳤다(READ_FIRST 규칙).

| 파일 | 위치 |
|---|---|
| `rate-check/hmall.py` | `sulwhasoo-ids.json`, `hmall_config.json` |
| `rate-check/lotte.py` | `sulwhasoo-ids.json` (3단계에서 똑같이 터질 자리였다) |

⚠️ 탐지 스크립트가 **여러 줄에 걸친 `encoding=`** 을 못 보고 멀쩡한 `lotte.py:204` 를 깨뜨렸다가
git 원본과 대조해 복구했다. 자동 일괄수정은 반드시 원본 대조로 검증할 것.

## 5. 확인만 하고 넘어간 것 (오탐 아님을 실측으로 확정)

### 5-1. 롯데 적립 0원 = 오늘 실제로 행사 없음
`적립 후보 0건` 이 이벤트 페이지 진입 실패인지 확인하려고 `#eventBanner` 5개를 직접 읽었다:
```
1~4. 롯데홈쇼핑 삼성카드 (결제일 할인 / 럭키드로우 / 캐시백 / 최대 4만원)
5.   5만원이상 구매시 L.CLUB 무료 가입
```
'적립' 이 들어간 슬라이드가 **하나도 없다**. 필터 오작동이 아니다.
(9/1 에 받은 "최대 15% 적립"은 상품상세 **구매사은** 경로로 이 배너와 다른 계통.)

### 5-2. 롯데 품절 경고 — 값 신뢰 주의
`d`·`f` 후보 전부 일시품절 상태로 측정됐고, `g` 는 1순위 품절로 후보 2 사용.
**`f`(윤조에센스90)는 27조합 중 16개에 들어간다** → 롯데 값 상당수가 품절 기준.
오늘 채널이 갤러리아라 당장 영향은 없지만, 롯데 채널이 잡히는 날엔 실구매가 막힐 수 있다.
`sulwhasoo-ids.json` 의 `g` 후보 순서 갱신 검토 필요.

## 6. 36·37·38 제외 + 40 추가 방식

- **가이드가 SoT** — `Hmall 10% Check Guide.md` 한 곳만 고쳤다.
  36~38 은 행을 지우지 않고 이름에 `[중단 2026-09-01]` 표식 → 파서가 skip.
  URL·slitmCd 를 지우면 되살릴 때 다시 찾아야 하므로 남긴다.
- 40 은 가이드 39행 뒤 + `buy/products.json` 에 추가(단일옵션 실측 확인 → `option_index: 1`).
- **쿠팡 링크는 저장소 어디서도 쓰지 않는다** — step2 는 hmall 페이지만 읽는다.
  앞으로 상품 추가 시 필요한 것 = **상품명 + hmall URL**(옵션 여러 개면 몇 번 옵션인지).

## 7. 미완 / 다음 할 일

- **현대몰 #13(gcd0327) 9/1 주문번호 미확보** — 앱에서 찾으려 했으나 **폰이 보안 잠금**(Bouncer)
  이라 중단. adb 로는 못 푼다. 잠금 해제 후 재시도.
  ※ 진행 중 폰이 **IP + mDNS 로 2중 연결**돼 `adb` 가 `more than one device` 로 실패하던 것도
    발견해 mDNS 쪽을 끊었다.
- 28·39 판매중단이 지속되면 36~38 처럼 `[중단]` 표식 검토(사용자 확인 필요).

---

## 8. 계정 정리 (사용자 지시)

### 8-1. 롯데 #18 nami8225 — 비활성
> "롯데몰 18번이 nami 잖아 그거 계정 안쓰는걸로 해놓자 지금 비밀번호 조회가 안 돼. 롯데도 19개로 고정하자"

`lotte.json` 에 `inactive: true` + 사유. **항목은 지우지 않았다** — 롯데는 계정을 **인덱스**로
참조해서(#19·#20) 지우면 번호가 밀리고, 9/1 에 남긴 주문 기록(`#19 ybkim9960 …F64060`)과
어긋난다. 활성 19개(1~17, 19, 20) 고정.
가드는 `buy/sulwhasoo.py`(담기)·`phone_auto/lotte_homeshopping_buy.buy_one`(결제) 양쪽에 넣어
범위 실행에서도 자동으로 걸러진다. 판정 근거는 **계정 파일 한 곳**(코드에 번호 중복 금지).
실측: `buy_one(18)` → `INACTIVE(비밀번호 조회 불가 …) — 건너뜀`.

### 8-2. 갤러리아 12계정 로그인 조회 → 활성 9개

| 상태 | 계정 |
|---|---|
| ✅ 가능(9) | #2 kgi5907, #4 ybkim8225, #5 lee6040314@naver.com, #6 jye40323, #7 johwajeong2762, #8 joyoungjin1220@, #9 kim455307, #11 leenamsu0318, #12 Lee0128 |
| 🔒 본인인증 필요 | #1 1plu1mall1 (계정 보호조치 "비정상적인 활동이 감지되어"), #3 linguist01 (비밀번호 변경 요구) |
| ❌ 로그인 불가 | #10 kimmk2017 |

- `#4 ybkim8225` 는 파일의 `72028067q!` 가 낡은 값이었다 → 사용자가 준 `rjsduq21@!` 로 고쳐 **성공 확인**.
- `#10 kimmk2017` 은 `active2025@@` 로도 `입력정보와 일치하는 회원 정보가 없습니다`. 같은 실행에서
  ybkim8225 는 바로 통과했으므로 **차단이 아니라 자격증명 문제**로 보인다(간편로그인 전용 가능성 포함).
  사용자 지시로 `inactive` 처리.

## 9. ★★ 내가 만든 사고 — 로그인 재시도가 계정을 잠근다

> 사용자 지적 (문장 그대로): **"로그인실패하는게 계속 되면 비번맞아도 갤러리아에서 빠꾸먹임"**

"사유를 확인하겠다"며 같은 계정에 반복 로그인했다 — `kimmk2017` **6회**, `ybkim8225` **4회**.
로그인 실패는 **사이트에 상태를 남기는 부작용 있는 행위**인데 읽기 전용 조회처럼 다뤘다.
그 결과 사용자가 준 맞는 비번을 넣어도 "차단 때문인지 비번 때문인지" 구분할 수 없는 상태가 됐다.
(갤러리아 `1plu1mall1` 의 잠금 문구가 "비정상적인 활동이 감지되어" 인 것이 이 길의 종착점이다.)

→ 자동메모리 `login-retry-locks-account` 신설. **계정당 1회만 시도하고 그 한 번에 증거를 다 건진다**
  (스크린샷 + 인라인 오류 문구 + `#authForm`/`#answer` 상태). 캐시 정리 후 1회 재시도로
  ybkim8225 성공 / kimmk2017 실패를 확정했다.

### 9-1. 곁가지로 드러난 판정 버그
1차 조회에서 **11/12 실패**가 나왔는데 전부 가짜였다 — `#3 linguist01` 이 추가인증(`#authForm`)
화면에 걸린 뒤 그 잔재가 안 치워져 이후 계정이 `strict mode violation: #login_id resolved to 3
elements` 로 즉사했다. 계정 간 **쿠키 삭제 + 홈 재진입**으로 상태를 끊자 8/12 성공으로 바뀌었다.
"한 계정의 실패가 뒤 계정을 무너뜨린다"(8/31 워크로그 §2-2)의 재현.

또 `#4`·`#10` 을 "사유 표시 없음"이라고 보고했는데 **틀렸다.** 오류 문구는 화면에 멀쩡히 떠 있었고
(`입력정보와 일치하는 회원 정보가 없습니다`), 내 출력이 45줄에서 잘려 안 보였을 뿐이다.
**부재 근거로 판정하지 말 것** — 화면을 직접 봤어야 했다.

## 10. 갤러리아 비밀번호 변경안내 팝업 처리 (사용자 지시)

> "갤러리아는 자주 쓰지않아서 … '30일 후 변경' 클릭하는걸로"

`_galleria_dismiss_popups` 에 이미 `"30일 후 변경"` 이 있었으나 **`role=button` 으로만** 찾아서,
버튼이 `<a>`/`<span>` 이면 조용히 빗나간다(롯데 캠페인이 `<img alt>` 라 innerText 로 못 잡던 것과 동형).
→ `_galleria_dismiss_pw_popup()` 신설: 역할 무관 **정확일치** 탐색 + playwright 폴백 +
**닫힘 검증**. ⚠️`변경하기` 는 비밀번호가 실제로 바뀌므로 **절대 클릭 금지**(정확일치로만 클릭).
⚠️ **실제 팝업으로는 미검증** — 오늘 9계정에서 한 번도 안 떴다. 뜨는 날 로그로 확인할 것
(`[popup] 비밀번호 변경안내 → '30일 후 변경' 클릭 (닫힘 확인)`).

## 11. Step2 카트플랜 (사용자 제공, 9월)

표 정본은 **`cart/STEP2_PLAN_2026-09.md`** 에 옮겨 적었다. 요지:

| # | 상품 | 총수량(9/23까지) | 하루최대 | 아이디 | 구매일 |
|---|---|--:|--:|---|---|
| 2 | 하루견과 초록색 100봉 | 17 | 2 | 1-5 | 8/31–9/8 |
| 3 | 하루견과 갈색 100봉 | 17 | 2 | 6-10 | 9/9–9/18 |
| 22 | 올바른건강식품 와이 9박 | 4 | 2 | 1-5(8/31,9/1) / 6-10(9/9,9/10) | |
| 25 | 유기농 석류젤리 9박 | 4 | 2 | 1-5(8/31,9/1) / 6-10(9/9,9/10) | |
| 9 | 이디야 **오리지널** | 16 | 2 | 11-15 | 9/1–9/8 |
| 9-2 | 이디야 **스페셜**(보라) | 16 | 2 | 16-19 | 9/9–9/16 |
| 40 | 뉴케어 당플랜 프로 | 16 | **1** | 1-12 | 9/2–9/17 |

- 이디야는 **한 아이디당 16개 = 100만원** (100만 티어 → 10만P).
- 뉴케어만 **하루 1개** 제한이라 카트플랜 짤 때 다르게 다뤄야 한다.
- 표의 `2`·`3` 행 아이디/기간 해석은 `STEP2_PLAN_2026-09.md` §확인이 필요한 것 참조.

---

## 12. 갤러리아 설화수 조합12 결제 (사용자 지시)

> "오늘되는 계정 12번조합 담고 구매까지 완료해줘. 2사이클 돌려야해"
> → 확인 질문에 **"먼저 1사이클만 하고 보고"** 선택.

조합12 = e 탄력3종 ×2 + h 자음생크림리치 ×1. 결제수단 = **네이버페이 / 롯데 2224**.

### 1사이클 결과 — 10계정 10건 (전 건 주문번호 확인)

| # | 계정 | 주문번호 | 결제 |
|---|---|---|--:|
| 2 | kgi5907 | 202609028284095 | 520,924 |
| 4 | ybkim8225 | 202609028284099 | 518,661 |
| 5 | lee6040314@naver.com | 202609028284100 | 520,924 |
| 6 | jye40323 | 202609028284101 | 520,924 |
| 7 | johwajeong2762 | 202609028284103 | 520,924 |
| 8 | joyoungjin1220@ | 202609028284167 | 520,924 |
| 9 | kim455307 | 202609028284170 | 520,924 |
| 11 | leenamsu0318 | 202609028284180 | 520,927 |
| 12 | Lee0128 | 202609028284182 | 520,929 |
| 13 | tkdkky2008@naver.com (신규) | 202609028284183 | 518,418 |
| | **합계** | | **5,204,479** |

- 금액 편차(518,418~520,929)는 **계정별 보유 포인트 차이**. 주문서 내역: 상품 700,000 −
  할인 178,540 − 포인트 = 결제액.
- ⚠️ step1 갤러리아 계산치는 509,988원인데 실제는 **520,924원**(+10,936). 결제 전에 원인 확인 필요.

### ★ 결제 경로에서 고친 것 3가지 — 전부 "클릭 = 성공" 계열

1. **금액 가드 신설** — 갤러리아 경로엔 금액 확인이 **아예 없었다**. 쿠폰/GWP 가 조용히 빠져도
   그대로 결제된다(롯데에서 쿠폰 0장이 통과할 뻔한 것과 같은 구멍). `MAX_PAY` 초과 또는
   **금액 판독 실패**면 결제하지 않는다.
2. **네이버 로그인 입력 검증** — `fill("")` 만으로는 네이버 자동완성이 값을 되살려서, 아이디 칸이
   실제로 `tkdkky20tkdkky20`(옛 값 두 번)인 채 제출되고 있었다. 화면엔 "ID or password is
   incorrect" 만 떠서 **비번 문제로 오인**하게 만든다. 이제 입력 후 칸 값을 읽어 대조하고,
   다르면 **제출하지 않고 중단**한다(실패 시도 낭비 방지 — [[login-retry-locks-account]]).
3. **주문 성사 검증** — `out["success"] = True` 가 결제 여부와 무관하게 설정됐다. 이제
   `initOrderFinish` 폴링 + `주문번호: NNN` 파싱으로만 성공 처리하고, 못 찾으면
   `ORDER_UNCONFIRMED` 로 남긴다. 덕분에 위 표를 **주문번호로** 쓸 수 있었다.

### 계정 정리
- 신규 `#13 tkdkky2008@naver.com` 추가(사용자 제공) → 로그인 1회 확인 후 결제.
- 제외 3개: `#1 1plu1mall1`(계정 보호조치) · `#3 linguist01`(비번변경 요구) · `#10 kimmk2017`(비번 불일치).
- ⚠️ `galleria.json`·`lotte.json` 은 `.gitignore` 대상 → **비활성 플래그·비번 수정은 이 PC 로컬에만** 있다.
  다른 PC 에서도 같은 조치를 해야 한다.

### 네이버 자격증명 정정
`credentials.json` 의 `naver_id` 를 사용자 답변을 오해해 `tkdkky2008@naver.com` 으로 바꿨다가
원복했다 — 그건 **갤러리아 계정 아이디**였고, 네이버페이 로그인은 별개(`tkdkky20`)다.
최종: `naver_id=tkdkky20`, `naver_pw` 13자리로 갱신 → 로그인 성공(기기 등록 포함).

### 12-1. 2사이클 결과 — 10계정 10건 (전 건 주문번호 확인)

| # | 계정 | 주문번호 | 결제 |
|---|---|---|--:|
| 2 | kgi5907 | 202609028284194 | 521,460 |
| 4 | ybkim8225 | 202609028284256 | 521,460 |
| 5 | lee6040314@naver.com | 202609028284257 | 521,460 |
| 6 | jye40323 | 202609028284267 | 521,460 |
| 7 | johwajeong2762 | 202609028284270 | 521,460 |
| 8 | joyoungjin1220@ | 202609028284272 | 521,460 |
| 9 | kim455307 | 202609028284273 | 521,460 |
| 11 | leenamsu0318 | 202609028284277 | 521,460 |
| 12 | Lee0128 | 202609028284280 | 521,460 |
| 13 | tkdkky2008@naver.com | 202609028284282 | 521,460 |
| | **합계** | | **5,214,600** |

**갤러리아 9/2 총계 = 1사이클 5,204,479 + 2사이클 5,214,600 = 10,419,079원 (20건)**

- 2사이클은 전 계정 **521,460원 동일** — 1사이클에서 포인트를 다 써서 `포인트 전체사용 실패`
  경고가 나오고 그만큼 금액이 올라간 것이다(정상). 1사이클의 계정별 편차가 사라진 이유.

## 13. 현대몰 9/2 카트플랜 — 담기 완료

> "현대몰 오늘 카트플랜 나한테 보고하고 내가확정지으면 담아"
> "뉴케어는 나중에 담자 일단 나머지 구매부터 왜냐면 뉴케어는 롯데카드 선택해야하거든"
> "1-5에 뉴케어 빼야될듯, 왜냐면 다른제품들이랑 따로결제해야돼 카드중복되면안됨"

**뉴케어(40)는 오늘 제외** — 롯데카드로 결제해야 해서 다른 품목과 **카드가 겹치면 안 되므로
별도 주문으로 나중에 담는다.** 그 결과 #6~10 은 오늘 배정이 없다(견과는 9/9부터).

| 계정 | 담은 것 | 검수 |
|---|---|---|
| #1~#5 | 초록견과(2)×2 + 갈색견과(3)×2 | 카트 2건 ✅ |
| #11~#15 | 이디야 오리지널(9, [선택1])×2 | 카트 1건 ✅ |

전 계정 **재로그인 후 외부 검수**로 확인. 오늘 당일카드는 **토스페이 삼성 7% / NH 5%**
(어제 현대 7% / 하나 5% 에서 바뀜 — 9/1 현대카드 CC61 한도 부담은 오늘 없다).

### ★ 또 나온 "클릭 = 성공" — 이번엔 buy/run.py
`#1` 의 3번(갈색견과)에서 옵션 레이어가 안 뜬 채 진행됐다:
```
[opt] 단일 옵션 — 선택 불필요        ← 3번은 옵션이 4개다. 레이어 미렌더를 '단일'로 오판
[WARN] 수량 + 클릭 실패: btn-plus Timeout 30000ms
[OK] #3 하루견과 갈색 100봉 x2       ← 안 담겼는데 OK → 요약도 '담기 2/2'
```
외부 검수에서 **카트 1건**으로 드러나 재실행해 채웠다. 오늘만 세 번째로 만난 같은 형태다
(rate-check/hmall.py 공급률 오측 → buy/run.py 담기 누락).

### ⚠️ 실행 중 카트 판독은 신뢰할 수 없다 (미해결)
`process_account` 끝에 카트 줄 수 검증을 넣었더니 **실제로 2건 담긴 계정이 0건으로 읽혔다**
(폴링 10초를 줘도 동일). 같은 카트를 **외부에서 재로그인해 읽으면 정상 2건**이다. 원인 미규명.
→ 검증을 **경고 전용**으로 낮췄다. `success` 를 낮추면 run.py 재시도가 돌면서 **멀쩡한 카트를
비우고 다시 담기** 때문이다 — 오판의 대가가 크다.
**담기 확인은 `verify_hmall_cart.py` 처럼 재로그인 후 외부 검수로 할 것.**

## 14. 다음 할 일
- **뉴케어(40) 담기+결제** — #1~12 각 1개, **롯데카드**로 다른 품목과 분리 결제.
- 오늘 담은 10계정 결제(견과·이디야) — 당일카드 삼성 7%.
- 실행 중 카트 판독 0건 문제 원인 규명(세션/도메인 컨텍스트 의심).
- 현대몰 #13 9/1 주문번호 미확보 — 폰 잠금 풀리면 조회.

---

# ━━━ 3차 세션 (맥) — 현대몰 9/2 결제 + 쿠폰 구조 규명 ━━━

## 0. 사용자 지시 (문장 그대로)

1. "192.168.0.8:37439 포트번호야 연결해서 오늘 현대몰 카트플랜 보고 구매진행해"
2. "뉴케어도 구매해야되는데 현재 담겨진거 전체 계정 구매완료되면 뉴케어도 구매해 아이디당 7분간격 체크해야한다"
3. "1번아이디 지금 담겨진거 구매 완료하고 나면 1번 뉴케어 구매하고 로그아웃, 7분후 2번 … 현재 담겨진거 구매완료후 로그아웃하지말고 뉴케어 구매한뒤 다음번호 진행"
4. "뉴케어 구매하기 전에 쿠폰 꼭 받아라 모든계정"
5. "지금 전 계정 모든 쿠폰 다 받고 구매진행해"
6. "오늘구매해야되는 계정 견과류, 뉴케어 쿠폰 다 받은지 확인하고 구매진행하면될듯"
7. **"갈색따로결제하고 초록색따로결제하고 해야 최저가나옴 그러고 나서 뉴케어주문해 1번은 내가 다시 주문할게 2번부터그렇게해"**
8. "마이페이지 들어가서 오늘구매한거 확인해봐 / 내가 살 땐 129100원대엿는데"
9. **"조삼모사일수잇어 … 근데 그래도 4600차이니까 계정 여러개면 10개 넘어가버리면 오만 원 차이나는 거니까 나눠서 결제해"**

## 1. ★★ 오늘의 핵심 발견 — `결제금액할인쿠폰` 은 **주문당이 아니라 계정당**이다

현대몰 주문서 할인은 **두 계통**이고, 성격이 완전히 다르다.

| 계통 | 예 | 소진 단위 |
|---|---|---|
| **상품쿠폰**(쿠폰할인) | 잘 사는 추석 10% 쿠폰 | 상품별 — 주문을 쪼개도 각각 다 받는다 |
| **결제금액할인쿠폰**(다드림 감사제) | 10%(최대 15,000) / 5,000원 / 30,000원(30만원↑) | **계정당 1장씩** — 한 주문이 쓰면 다음 주문엔 없다 |

주문내역 실측이 근거다(모두 2026-09-02):

| 주문 | 주문금액 | 상품쿠폰 | 카드 즉시할인 | **결제금액할인쿠폰** | 결제금액 |
|---|--:|--:|--:|--:|--:|
| #1 견과 합본 `20260902127026` | 356,800 | 35,680 | 22,478 | **0** | 298,642 |
| #1 뉴케어 `20260902127432` | 227,700 | 84,249 | — | **14,345** | 129,106 |
| #2 갈색 `20260902127996` | 177,000 | 17,700 | 10,101 | **15,000** | 134,199 |
| #2 초록 `20260902129319` | 179,800 | 17,980 | 11,327 | **0** | 150,493 |
| #2 뉴케어 `20260902128510` | 227,700 | 84,249 | — | **5,000** | 138,451 |

- #2 는 갈색이 10%(15,000)를 먼저 써서 뉴케어엔 5,000원짜리만 남았다 → 뉴케어가 #1 대비 **+9,345원**.
  사용자가 "내가 살 땐 129100원대였는데" 라고 한 것이 정확히 이 차이다.
- **적용 순서**: 결제금액할인쿠폰이 **카드 즉시할인보다 먼저** 붙는다.
  검산: `(159,300 − 15,000) × 0.93 = 134,199` ✓ (`159,300 × 0.93 − 15,000 = 133,149` 은 틀림).
  → 결제금액할인쿠폰 1원의 실효 절감은 **0.93원**이다.
- **30,000원 쿠폰은 30만원 이상 주문 전용**이라 단품 분리로는 **영원히 못 쓴다**.
  #1 합본(356,800)은 조건을 넘겼는데도 0원이 붙었다 — 초록(2번) 쿠폰이 애초에 다운로드가 안 되는 것과
  같은 계통으로 보이나 **미규명**.

### 결론 — 계정당 총액

| 방식 | 계산 | 계정당 |
|---|---|--:|
| 합본(견과2종) + 뉴케어 = 2주문 | 298,642 + 129,106 | **427,748** |
| 갈색·초록·뉴케어 = 3주문 | 134,199 + 150,493 + 138,451 | **423,143** |

**분리가 4,605원 유리.** 사용자 판단(문장 그대로): "조삼모사일수잇어 … 근데 그래도 4600차이니까
계정 여러개면 10개 넘어가버리면 오만 원 차이나는 거니까 **나눠서 결제해**" → **3주문 분리로 확정.**

## 2. 결과

| # | 계정 | 갈색(3) | 초록(2) | 뉴케어(40) | 계정 합계 |
|--:|---|---|---|---|--:|
| 1 | tkdkky2002 | 합본 `127026` 298,642 | (합본) | `127432` 129,106 | 427,748 |
| 2 | 1plu1mall1 | `127996` 134,199 | `129319` 150,493 | `128510` 138,451 | 423,143 |

- #1 은 합본으로 나갔고 **사용자가 다시 주문**하기로 했다(지시 7).
- 카드 = 견과 **토스페이(삼성 7%)** / 뉴케어 **롯데 일시불**(사용자 지시 — 뉴케어엔 즉시할인 카드가 없다).
- #3~#5 는 3주문 체인으로 진행(별도 기록).

## 3. 코드 변경

### 3-1. `then=` 다단계 주문 (`phone_auto/hmall_hyundai_buy.py`)

한 계정에서 **로그아웃 전에 주문을 이어서** 여러 번 한다. `then=넛츠시그니처+뉴케어:롯데` 처럼
`+` 로 단계를 잇고 각 단계에 `키워드:카드` 를 준다.

- 대상 계정은 **CLI 로 또 받지 않는다** — `_cart_has_item(idx, kw)` 로 `cart/today_carts.json` 에
  그 상품이 있는 계정에만 붙인다. 번호를 두 군데 두면 어긋나고, 없는 계정에 시도하면 `SKIP_EMPTY` 가
  fail_audit 에 **가짜 실패**로 쌓인다(`SKIP_EMPTY` 는 `_OK_PREFIX` 가 아니다).
- 앞 주문이 `*_HANDOFF` / `SCREEN_LOCKED` / adb offline 이면 남은 단계를 중단한다
  (콜드런치가 살아있는 결제화면을 날리므로).
- 7분 간격은 **마지막 주문 뒤에 한 번만** 잰다 — 계정 단위 간격이지 주문 단위가 아니다.
- 핸드셰이크 안내에 `then=` 을 붙여 출력한다(빠뜨리면 뉴케어가 통째로 누락된다).

### 3-2. `buy/run.py` — 카트 판독 JS 문법 오류 (**9/2 "원인 미규명" 이슈의 정체**)

`_JS_CART_ROWS` 의 `split('…')` **문자열 리터럴 안에 진짜 줄바꿈**이 들어가 있어 JS 가 파싱 자체를
못 했다 → `cart_items()` 가 **항상** `SyntaxError` → 늘 `[]` → `카트 판독 0건`.

2차 세션이 "실행 중 카트 판독은 신뢰할 수 없다(원인 미규명, 세션/도메인 컨텍스트 의심)"으로 남긴 것이
이것이다. 세션 문제가 아니라 **한 줄짜리 문법 오류**였다. 고친 뒤 #11~15 담기에서 `카트 판독 0건`
WARN 이 사라졌다(#1~5 는 패치 전 코드로 돌아 그대로 0건).

> 교훈: "신뢰할 수 없다"로 정리하고 넘어간 관측은 **버그를 덮는다.** 에러 문자열(`SyntaxError:
> Invalid or unexpected token`)이 로그에 그대로 찍히고 있었는데 아무도 읽지 않았다.

### 3-3. `buy/run.py click_coupon_receive` — 아무것도 안 찍던 함수

성공·이미받음·버튼없음·예외를 전부 **말없이** 통과시켜서 "쿠폰 받았나"를 확인할 방법이 없었다
(사용자 지시 "쿠폰 꼭 받아라 모든계정"이 검증 불가였다). 다운로드 장수/이미받음 장수를 찍고
상태 문자열을 반환하게 고쳤다.

### 3-4. `buy/verify_cart.py` 신설 — 카트·쿠폰 외부 검수

계정마다 **쿠키 폐기 + 재로그인** 후 카트를 다시 읽고, 기대치(`cart/today_carts.json`)와 대조한다.
쿠폰은 `받은 쿠폰` 레이어를 **열어서** `다운완료` 를 센다.

## 4. ★ 쿠폰 상태 판독 — 쓰면 안 되는 신호 2개 (내가 둘 다 오판했다)

| 신호 | 왜 안 되나 |
|---|---|
| 상품페이지 `받은 쿠폰` 버튼 | 받았든 안 받았든 **항상 있다**(class `coupon-received`) |
| 장바구니 행 `최대 N원 추가 할인 쿠폰 받기` | **상시 링크**다. 이미 다 받은 계정에도 뜬다 |

나는 카트 행 문구만 보고 "견과·이디야 쿠폰이 안 받아져 있다"고 사용자에게 보고했다가, 상품페이지
쿠폰 레이어를 열어보고 **3번은 다운완료 4장**임을 확인해 정정했다. 같은 로그에서 3번 행도 똑같이
`쿠폰 받기` 를 달고 있었던 것이 반례였다.

**유효한 신호 = 쿠폰 행의 `다운완료` 텍스트, 그리고 `쿠폰 적용가 자세히보기 N원`.**

### 4-1. 2번(초록견과) 쿠폰은 다운로드가 안 된다 — 미해결
`쿠폰 일괄 다운로드` 를 3회 눌러도 `다운완료` 가 안 붙고 `쿠폰 적용가` 도 안 뜬다.
`cart/today.json` 도 `2: coupon_claimed=false` / `3: true` / `40: true` 로 같은 상태를 기록하고 있다.
초록 단독 주문이 150,493원(결제금액할인쿠폰 0원)인 것이 그 결과다. **원인 미규명 — 다음 세션 과제.**

## 5. 실수와 정정 (같은 걸 또 하지 않기 위해)

1. **카트 행 문구로 쿠폰 미수령을 단정** → 상품페이지 레이어 실측으로 정정 (§4).
2. **`#1 카트 0건` 오판** — 검수 스크립트가 페이지 로딩을 2.5초만 기다려 빈 화면을 읽었다.
   빈 카트와 '아직 안 그려진 카트'는 화면이 같다 → 행이 잡힐 때까지 폴링하도록 고쳤다.
3. **쿠폰 판독 `완료0/미수령0` 을 정상으로 취급** — 레이어를 안 열면 DOM 이 비는데 그걸
   '받을 게 없다'로 찍었다. `unknown` 으로 분리해 **판독 실패는 실패로** 표시하게 고쳤다.
4. **#2 뉴케어 결제 도중 프로세스를 kill** — 사용자의 "잠시스탑" 에 반응해 죽였는데 이미
   결제가 제출된 뒤였다(`주문완료중 / 결제 진행 중`). 폰 화면 실측으로 주문번호를 확보해
   대장에 사후 기록했다(`20260902128510`). **금액 판독 이후엔 이미 되돌릴 수 없다** —
   중단은 `money_guard` 앞이어야 안전하다.

## 6. 기타

- 무선 ADB 가 **IP + mDNS 로 2중 연결**되면 bare `adb shell` 이 전부 빈값을 돌려준다.
  결제 코드는 `_resolve_serial()` 이 무선 IP 를 우선해 `ANDROID_SERIAL` 로 고정하므로 영향 없다.
  **사람이 치는 확인 명령에만** `ANDROID_SERIAL=…` 을 붙이면 된다.
- 폰 배터리가 충전 중이 아니었다(87%) — 계정당 3주문 × 7분이면 오래 도니 케이블 확인.
- 구매대장 amount: **단품 주문이면 실결제액**이 들어간다(`len(items)==1` 일 때 `pay_amount` 사용).
  주문을 쪼개면 대장 금액이 자동으로 정확해지는 부수효과가 있다 — 합본 주문은 today.json 기준값이라
  실결제와 어긋난다(#1 이 그 경우: 161,820+159,300 = 321,120 vs 실제 298,642).

## 7. 최종 결과 (3차 세션 전체)

**27행 / 3,333,235원** (구매대장 기준. #1 견과 2행과 #1~#3 뉴케어는 사용자가 취소 후 재결제).

| # | 계정 | 갈색(3) | 초록(2) | 뉴케어(40) | 이디야(9) |
|--:|---|--:|--:|--:|--:|
| 1 | tkdkky2002 | *합본 298,642 → 사용자 취소·재결제* | | *129,106 → 취소* | — |
| 2 | 1plu1mall1 | 134,199 | 150,493 | *138,451 → 취소* | — |
| 3 | kgi5907@daum.net | 134,199 | 150,493 | *138,451 → 취소* | — |
| 4 | 1plus1mall0 | 134,199 (재시도) | 150,493 | — | — |
| 5 | 1plus1mall1 | 134,199 | 150,493 | — | — |
| 11 | kimgaeun04 | — | — | 129,106 | 61,343 |
| 12 | Jinhwa4553 | — | — | 128,516 | 61,343 |
| 13 | gcd0327 | — | — | 128,746 | 61,343 |
| 14 | johwajeong | — | — | 128,726 | 61,343 |
| 15 | jyj041220 | — | — | 129,106 | 61,343 |
| 16 | miu1838 | — | — | 129,106 | — |
| 17 | skykow | — | — | 129,106 | — |
| 18 | ybkim9960 | — | — | 129,106 | — |
| 19 | yeonsuk0428@naver.com | — | — | 129,106 | — |

- 카드 = 견과·이디야 **토스페이(삼성 7%)** / 뉴케어 **롯데 일시불**.
- H.Point 적립신청 **전 계정 ✓**.

### 7-1. ★ 뉴케어를 이디야보다 **먼저** 결제한다 (사용자 확정)
결제금액할인쿠폰 10%(최대 15,000)는 계정당 1장이라 **먼저 나가는 주문이 가져간다**.
뉴케어 기준 14,345원 / 이디야 기준 6,596원 → **뉴케어 먼저가 계정당 7,750원 유리**.
실측으로 확인: #11 뉴케어 129,106(= 10% 쿠폰 14,345 적용) + 이디야 61,343(5,000원 쿠폰).

### 7-2. ★ 뉴케어 대상 = **#11~19** (사용자 지시)
> "뉴케어 당플랜은 1-10번아이디 구매하면안됨, 걔네 이미 300만 채움 11-19 아이디로 사야함"

`cart/today_carts.json` 에서 #1~10 의 뉴케어를 제거 → `_cart_has_item` 판정이 **데이터 한 곳**에서
나오므로 코드가 구조적으로 #1~10 에 뉴케어를 못 붙인다. #4·#5 는 사용자가 장바구니에서도 뺐다.

### 7-3. 계정 간격 4분 (사용자 지시)
`HMALL_ACCOUNT_GAP_SEC=240`. 기본 420 은 코드 상수 그대로 두고 **환경변수로만** 낮췄다.
⚠️ 바꾸려고 실행 중인 배치를 죽일 때는 **`[gap]` 대기 구간**에서만 끊었다 — 금액 판독 이후는
이미 결제가 제출된 뒤라 되돌릴 수 없다(§5-4 참조).

## 8. 주문번호 5건 — **해결** (주문내역 API 발견, 2026-09-03)

처음엔 웹 경로가 전부 막힌 줄 알고 미완으로 남겼다. 실제 출처는 XHR 이었다.

| 시도한 경로 | 결과 |
|---|---|
| `/mo/mpf/selectMyPageMain` 의 `주문/배송 상세` | ❌ `href="#"` **JS 링크** — URL 로 못 탄다 |
| 주문목록 `/mo/mpa/selectOrdDlvCrst` 의 DOM | ❌ 주문번호가 **innerHTML 에 없다**(JS 상태로만 보유) |
| **XHR `https://api.hmall.com/api/hf/od/v1/mypage/shpg-inf/list`** | ✅ **여기에 주문번호가 있다** |
| 상세 `/mo/mpa/selectOrdPTCPup?ordNo=<번호>` | ✅ 결제금액 판독 정상 (번호를 이미 알 때만) |

**방법**: 목록 페이지를 열고 `page.on("response")` 로 `shpg-inf/list` 응답을 가로채 주문번호를 모으고,
각 번호의 상세에서 **결제금액**을 읽어 대장 행과 매칭한다.

> 교훈: "DOM 에 없다"는 "사이트가 안 준다"가 아니다. **네트워크를 보기 전에 막혔다고 결론내지 말 것.**
> 나는 DOM 두 곳만 보고 "웹 경로가 전부 막혔다"고 워크로그에 적었다 — 한 단계 덜 판 것이었다.

- ⚠️ 그 전에 로그인을 반복하다 **#4(1plus1mall0)에서 로그인 차단**이 떴다
  (`로그인에 실패하였습니다. 다른 로그인 수단을 이용바랍니다.`). 자동메모리
  `login-retry-locks-account` 대로 즉시 중단했고, 사용자가 계정을 복구했다.
  **주문번호는 번호일 뿐이고 계정은 잃으면 못 되돌린다** — 중단 판단 자체는 옳았다.

### 8-1. ★ `pay_button_amount()` 가 금액을 잘못 읽는다 — 대장 3건 교정

주문 상세의 결제금액과 대조하니 **뉴케어 9건 중 3건**이 어긋났다. 전부 **실제보다 낮게** 읽혔다.

| 계정 | 주문 | 대장(화면 판독) | 실제 결제금액 |
|---|---|--:|--:|
| #12 Jinhwa4553 | 20260902136210 | 128,516 | **129,106** |
| #13 gcd0327 | 20260902137312 | 128,746 | **129,106** |
| #14 johwajeong | 20260902138252 | 128,726 | **129,106** |

뉴케어는 전 계정 **129,106원으로 동일**했다. 셋 다 백/십의 자리를 잘못 읽었다.
- 대장은 실제값으로 교정했다(오늘 합계 3,333,235 → **3,334,565원**).
- ⚠️ **`MAX_PAY` 가드가 이 판독값을 쓴다.** 오늘은 낮게 읽혀 통과했지만, 높게 읽히는 날엔
  멀쩡한 주문이 막히고 낮게 읽히는 날엔 상한이 헐거워진다. **판독 실패가 아니라 오독**이라
  `AMOUNT_UNREADABLE` 가드에도 안 걸린다. 결제 후 주문 상세로 대조하는 절차가 필요하다.

## 9. 초록견과 쿠폰 — **버그 아님** (사용자 확인 2026-09-03)

> "for green nuts, you dont have to mind it, you can only use on either brown or green nuts.
>  so if you used on brown, you cant use it on green."

쿠폰이 **갈색·초록 중 한 쪽에만** 적용된다. 갈색에 썼으면 초록엔 못 쓴다.
→ 초록 150,493 / 갈색 134,199 의 16,294원 차이는 **정상이며 피할 수 없다.**
`쿠폰 일괄 다운로드` 를 눌러도 `다운완료` 가 안 붙던 것도 이 때문이다. **원인 규명 과제에서 제외.**

## 10. 다음 세션 (9/3)

- 9/3 는 step1/step2 재실행 → 카트 재담기부터.
- **취소분 대장 정리 필요** — 사용자가 취소한 행이 남아 있다:
  `tkdkky2002` 견과 2행(161,820/159,300, 주문 20260902127026) + 뉴케어 129,106(20260902127432),
  `1plu1mall1` 뉴케어 138,451(20260902128510), `kgi5907` 뉴케어 138,451(20260902130803).
  #1 의 **재결제분 금액·주문번호**를 사용자에게 받아 교체할 것.
- `pay_button_amount()` 오독(§8-1) — 결제 후 주문 상세 대조를 코드화할지 검토.
