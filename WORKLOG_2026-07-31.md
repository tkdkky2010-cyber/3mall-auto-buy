# WORKLOG 2026-07-31 — 롯데 조합5 담기/결제 + NH 일반결제 비전입력 실전

## 1. 롯데 계정 8~14 조합5 카트담기 ✅
- `python3 buy/_lotte_cart_fill.py 5 8 14` → **7/7 담기완료**.
- 조합5 = **f(윤조에센스90)×2 + g(자음생2종)×2**.
- 로그만 믿지 않고 #8·#11·#14 **직접 로그인해 카트 2품목 확인**(과거 void 사고 대비). 비번변경 캠페인 잔존 없음.

## 2. 결제 결과
| 계정 | 결과 | 비고 |
|---|---|---|
| #8 jye40323 | ✅ 완료(사용자 수동) | 카드번호 자동입력 실패 → 사용자가 직접 입력. 결제+포인트+뷰티포인트 완료 |
| #9 wlstmdlsfk | ✅ **완료(Claude 비전 입력)** | 주문 `2026-07-31-G83859` / 547,319원 / 뷰티 5,701P / 구매사은 15% → **적립금 10,000원** |
| #10 kim455307 | ⏸ 중단 | 카드번호 입력오류 1/3 발생 → 3/3 카드잠김 위험으로 정지 |
| #11~14 | 미진행 | 카트는 담겨 있음 |

## 3. ★ NH농협 일반결제 = 오늘의 핵심 이슈
### 배경
당일카드가 **NH농협**으로 잡히면 `pay_nh_general` 경로(카드번호 16 + CVC 3 + pin6)를 탄다.
NH nppfs 보안키패드는 **방패 아이콘이 숫자 자리에 섞여** 로컬 OCR이 매핑 실패
(`_tap_shuffle` 2엔진→4엔진 × 3ROI 전부 실패 → `PAY_FAIL@card_no`).

### 검증된 규칙 (실측)
1. **전체화면 캡처를 쓴다.** crop 후 offset 을 손으로 환산하면 좌표 오차 → 오입력/넘침.
2. **한 칸(4자리) 입력 중에는 배열 고정**, **칸이 바뀌면 재배열** → 칸마다 새 스크린샷 판독 필수.
   (재배열 후 옛 배열로 탭한 것이 오입력의 직접 원인)
3. 탭 간격 **0.8~1.2초**. 0.5초에선 탭 누락 관측.
4. **세션 만료 ~10분** — 카드16+CVC3 는 2분 내 완료해야 안전. 시행착오로 시간 쓰면 만료(#9 1·2차 실패 원인).
5. 오류 팝업 "입력오류 누적 N/3" — **3회면 카드 잠김**. 불확실하면 재시도 말 것.

### 남은 한계 (미해결)
ADB `input tap` 이 NH 보안키패드에서 **간헐적으로 씹힌다**(4탭→2개 인식, 2탭→1개 인식 관측).
배열을 정확히 읽어도 자릿수가 밀리면 틀린 번호가 되고, 화면엔 ●로만 보여 어느 자리가 틀렸는지 확인 불가.
→ (이후 해소) NH 카드번호·CVC·비번은 **에이전트 비전 핸드세이크 + 코드 입력**이 정본이다.
에이전트는 셔플 키패드에서 **어느 숫자가 어느 좌표인지**만 판독해 주고, 값은 `phone_auto/nh_enter.py` 가
`secrets/card_secrets.json` 에서 읽어 탭한다. **사람이 카드번호를 입력하지 않는다.**

### 코드
- 신규 `phone_auto/nh_vision_input.py` — 검증된 좌표/규칙 모듈화
  (`KEY_COLS`, `ROW_CARD/ROW_CVC/ROW_PIN6`, `build_pos()`, `tap_digits()`; 매핑에 없는 숫자면 탭 안 하고 실패 반환 = 오입력 누적 방지).
- 기존 `pay_nh_general` 에 이미 있던 `NH_VISION_MODE=1` 훅(카드화면 도달 후 정지) 과 함께 쓰는 구조.

## 4. ★ 롯데 구매사은 적립 = 이미 구현돼 있었음 (내 헛수고 기록)
주문완료 화면의 **"사은품 놓치지마세요 → 추가혜택 신청하기"** 목록은 **정답이 아님**(삼성카드 이벤트 등 무관 목록).
정본은 `LOTTE_HOMESHOPPING_STEPMAP.md` **G절**:
> 주문완료 → **구매상품 직접 탭** → 상품상세 → "구매사은·혜택" 섹션 → "최대 N% 적립" → 광세일 → **혜택 신청하기**

`claim_lotte_reward()` 가 이 흐름을 이미 구현 → 호출 한 번으로 완료:
`{'product': '(공통)자음생 2종 세트', 'card': '최대 15% 적립', 'completed': True, 'ok': True}`
**교훈: 새로 만들기 전에 STEPMAP/기존 함수부터 확인할 것.**

## 5. 그 외
- 폰 재페어링: `172.30.1.32:45611` (ADB 데몬이 stale TLS 로 크래시 → `adb kill-server` + `pkill -9 adb` 후 정상).
- 스프레드시트 조합번호 색칠(3,5,12,13,14) 은 `rate-check/cart_plan.py`의 `HIGHLIGHT_COMBOS` 로 Step1 때 자동 적용.

## 6. 다음 할 일
- [ ] #10~14 결제 (카드번호는 수동 입력 권장, 이후 뷰티+구매사은 적립은 `claim_lotte_reward()` 자동)
- [ ] NH 탭 씹힘 근본 대책 검토 (input tap 대신 sendevent, 또는 NH스마트결제(앱카드) 경로 전환)
