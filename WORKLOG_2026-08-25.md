# WORKLOG 2026-08-25 (화) — 롯데 조합25 계정 8~14 + ★윈도우 dump 가 내내 죽어 있었다

## 지시 (사용자 문장 그대로)

> 25번 조합 8-14번 아이디로 구매해줘 USB 유선으로 연결되어있음
> 스텝 1,2 다른대화에서 다 실행했고 담기도 완료되어잇어 그냥바로구매진행해
> 무선으로 연결 222170 172.30.1.6:33171 코드번호이고, 포트번호는 33793이야 이걸로 진행해
> 8다시진행해 한도초과였음 한도 내가 다시 풀었음
> 왜자꾸 문제가 생기지? 똑바로 일처리 안되나? 그리고 resume 기능넣어서 실패한부분부터 다시해 정신똑바로차려
> 오늘 구매하는거 제대로 기록해놔. 다음번에 실수안하게 그리고 롯데몰에서 kb카드쓰는거지만 현대몰에서도
> kb카드 쓰는날있을거임, 그럼 그 때도 이지랄 안나게 동일하게 실수없이 잘 주문되게끔 코드수정잘해줘

## 0. 오늘 산 것

| | 값 |
|---|---|
| 몰 | 롯데홈쇼핑 (설화수 조합 결제는 구매대장 33건 전부 롯데 — 8~14 는 7/21·8/4 에도 같이 돈 배치) |
| 조합 | **25** = `e` 탄력3종(에센셜 탄력케어 세트) ×2 + `n` 탄력크림EX 75ml ×2 |
| 계정 | 8~14 (jye40323 / wlstmdlsfk / kim455307 / diinamic / leenamsu0318 / Lee0128 / johwajeong2762) |
| 담기 | **7/7 완료** (`buy/_lotte_cart_fill.py 25 8 14`, 각 카트 2건 + 쿠폰 다운로드) |
| 당일카드 | **KB국민 7% 청구할인** (현대 5% 도 있으나 KB 가 최고) |
| 정상 결제액 | **530,247원/계정** (청구할인 후 실부담 493,130원 — 주문서 '청구할인 예상가'로 확인) |

## 1. ★★ KB Pay 는 USB 디버깅이 켜져 있으면 실행을 거부한다

사용자가 "USB 유선 연결"이라고 알려준 그 조건이 **정확히 오늘의 첫 벽**이었다.

```
알림: KB Pay 앱을 종료합니다. / USB 디버깅 해제 후 다시 실행해주세요.
logcat: com.kbcard.cxh.appcard/.screen.intro.IntroActivity … isExiting / Splash Screen destroyed
```

- 런처로 직접 띄워도 동일 → **롯데 세션과 무관한 앱 자체 차단**.
- 증상은 `KB앱 미진입`·`KB 결제 모달 미도달` 처럼 **엉뚱한 이름**으로 나타난다. 이름만 보면 flow/좌표를 의심하게 된다.
- **롯데카드(LOCA)·현대카드 앱은 USB 디버깅 상태에서도 정상 실행** — KB 전용 제약이다(둘 다 실측).
- 해결 = **무선 디버깅 전환**. `adb pair 172.30.1.6:33171 <코드>` → `adb connect 172.30.1.6:33793`,
  그 뒤 `adb_enabled=0` / `adb_wifi_enabled=1` 상태에서 KB Pay 가 10초 이상 살아있는 것 확인.

### → 현대몰에도 그대로 온다 (사용자 지시 반영)

카드앱 구간은 몰 무관이라 **현대몰에서 KB 쓰는 날 똑같이 터진다.** 그래서 3사 공용 정본에 프리플라이트를 넣었다:

- `phone_auto/hmall_hyundai_buy.preflight_card_app(card)` — `CARD_APPS_BLOCKED_BY_USB_DEBUG = ("KB",)`,
  `settings get global adb_enabled == 1` 이면 **결제 시작 전에** `KB_APP_BLOCKED` 로 정지 + 해결법 출력.
- 호출: hmall `pay_kb()` / 롯데 `pay_lotte_kb()` **양쪽**. (버그 하나 고치면 같은 모양을 폴더 전체에서 찾는다 — READ_FIRST 규칙)

## 2. ★★★ 이 PC에서 `_dump_texts()` 가 **항상 0개**였다 — 오늘 사고 대부분의 뿌리

`hmall_webview._dump()` 가 `Path(tmp).read_text()` 를 **인코딩 없이** 호출 → 한글 XML 에서 cp949
`UnicodeDecodeError` → `except Exception: return ""` 로 **조용히 삼킴**.

즉 자동메모리 `windows-ocr-needs-dump-merge` 가 지시하는 **"OCR + dump 겹쳐 읽기"가 이 PC에서 한 번도
작동한 적이 없었다.** 항상 OCR 단독이었고, 윈도우 OCR 이 못 읽는 것은 그대로 실패했다.

고친 뒤 실측: **dump 0개 → 78~130개**, 헤더 `일반 (0/2)` 정확히 검출.

### 여기서 파생된 실패들 (증상은 7개, 원인은 하나)

| 증상 | 실제 원인 |
|---|---|
| `CART_FAIL:주문서 미도달` | 카트 헤더를 못 읽어 전체선택이 `일반 (0/2)` 인 채 주문 → '주문하실 상품을 선택해 주세요' |
| `전체선택 실패(헤더=판독불가)` | 위와 같음 (이름을 붙이고 나서야 드러남) |
| **쿠폰 0장으로 결제 직전까지 감** | 쿠폰 UI(`할인쿠폰`·`선택해 주세요`·옵션)가 전부 dump 에만 있음 |
| `할인쿠폰 변경 버튼 미발견` | 앵커를 화면 맨 아래에서 찾고 멈춤 (변경 버튼이 화면 밖) |
| `'신용카드' 미발견` | `다른 결제수단` 라디오 탭이 안 먹었는데 검증 안 함 |

## 3. ★★ 쿠폰 0장 결제 사고 — **하마터면 계정당 15만원**

`set_discount_coupons`/`set_plus_coupons` 가 `applied=0` 인데 **`ok:True` 로 조용히 통과**했다.

```
[#8] 혜택 적용 — 할인쿠폰 {'applied': 0} / 플러스쿠폰 {'applied': 0} / 포인트 {'used': 2}
결제 버튼: 682,167원   ← 시트 기대치(카드할인 전) ≈542,600원
```

수정 후: `할인쿠폰 2장 + 플러스쿠폰 13% 2장 + 포인트 2건` → **530,247원**. 차액 **151,920원/계정**,
7계정이면 약 **106만원**. 로그를 사람이 봐야만 알 수 있는 구조였다 → 그래서 아래 3개를 넣었다.

- `[#N] 혜택 적용 — …` / `[#N] 현금영수증 — …` / `[#N] 결제 예정 금액: …` **숫자로 로그**
- `STOP_BEFORE_PAY=1` — 주문서까지만 만들고 결제 직전 정지(검증용, 실돈 안 나감)
- **`MAX_PAY=<원>` 상한 가드** — 결제 예정 금액이 상한을 넘으면 **결제하지 않고** `AMOUNT_TOO_HIGH` 로 실패.
  오늘 기준 `MAX_PAY=560000` (정상 530,247 / 쿠폰 0장 700,000). **무인 실행의 마지막 안전망.**

## 4. 인코딩 — 윈도우 포팅에서 놓친 자리 전부

| 대상 | 증상 |
|---|---|
| `subprocess(text=True)` **adb 9곳** | cp949 디코딩 에러가 리더스레드에서 터져 `stdout=None` → `_wait_app` 이 `None.splitlines()` 로 크래시 |
| `Path.read_text()` **7곳** | 위 §2 (dump 전멸) |

→ 전부 `encoding="utf-8", errors="replace"` 고정. `_wait_app` 에 `or ""` 가드도 추가.
**교훈: 윈도우 포팅에서 "조용히 빈 문자열"이 나오는 자리는 전부 인코딩부터 의심한다.**

## 5. resume — 실패한 지점부터 이어붙이기 (사용자 지시)

종전 롯데 `resume` 은 **주문완료 화면 뒷처리(대장·뷰티·적립)만** 가능했다. hmall 과 같은 규칙으로 확장:

```
python -m phone_auto.lotte_homeshopping_buy resume 8 combo=25
```

- `detect_screen()` → `ORDER_DONE`(뒷처리만, 결제 재시도 안 함) / `ORDER_SHEET`(주소·쿠폰·포인트·카드·동의 → 결제)
  / `CART`(전체선택 → 주문하기 → 이하 동일) / `OTHER`(**아무것도 안 함** — 이중결제 방지)
- 이를 위해 주문서~완료 구간을 **`_from_order_sheet()` 정본 하나**로 분리 → `buy_one` 과 `resume` 이 같은 코드를 쓴다.
- 실측 검증: 주문서 상태에서 resume → 판정 `ORDER_SHEET`, **금액 530,247원 유지**(쿠폰 초기화 없음 = 멱등),
  화면이 주문서를 벗어나자 `NOT_RESUMABLE` 로 **정확히 거부**.

## 6. 아직 안 끝난 것 (다음 세션이 여기서 시작할 것)

- [ ] **#8 결제 미완** — 남은 두 지점:
      · `현금영수증 {'entered': False, 'err': '사업자 등록번호 라벨 사라짐'}`
      · `AGREE_FAIL:필수동의 체크 실패(픽셀검증)` ← 앞의 현금영수증 실패의 **뒤탈로 보임**
        (앞 단계가 성공했던 실행에서는 동의도 통과했다)
      `ORDER_SLEEP_SCALE=1.0` 으로 되돌려도 동일 → **타이밍 문제가 아니다.**
      두 지점에 화면 계측(`[screen:사업자라벨-미발견]`, `[agree] cy/box_x/checked`)을 심어둔 상태.
- [ ] 9~14 결제 (담기는 살아 있음)
- [ ] `today_carts.json` 이 8/19 자 → 오늘 결제는 `ALLOW_STALE_CART=1` 로 진행 중. 식품 담기 하면 갱신됨.
- [ ] `tap_dump_text` 가 접힌 노드를 탭하던 문제는 0크기 가드로 막았지만, 롯데 webview 는 **화면 밖 노드를
      컨테이너 가장자리 한 점으로 접는다**(실측 `cy=2265` 한 점에 58개) → dump 좌표는 항상 클러스터 필터 필요.

## 7. 오늘 배운 것

1. **"조용히 빈 결과"는 버그의 위장이다.** dump 0개·쿠폰 0장·`entered=False` 가 전부 `ok:True` 로 통과했다.
   0을 반환하는 자리는 **정상 0인지 고장난 0인지**를 구분해 로그로 남겨야 한다.
2. **증상 이름이 원인을 가린다.** `KB앱 미진입`(=USB 디버깅), `주문서 미도달`(=전체선택 0/2),
   `PAY_FAIL@kb_modal`(=사업자번호 팝업). 실패는 **그 자리에서 이름 붙여** 죽어야 다음 사람이 안 헤맨다.
3. **추측 패치를 반복하지 말고 실패 지점 화면을 찍어라.** 화면 계측을 넣자마자 두 건이 한 번에 풀렸다
   (`신용카드` 미발견 = 페이지가 꼭대기 / 라디오가 `최근 사용한 결제수단`에 남아 있음).
4. **돈이 걸린 자동화는 사람이 로그를 보는 것에 의존하면 안 된다.** `MAX_PAY` 처럼 코드가 막아야 한다.

━━━━━━━━━━━━━━━━━━━━━━ 2차 세션 (결제 실행) ━━━━━━━━━━━━━━━━━━━━━━

## 8. 결제 결과 — **7/7 완료** (6건 자동 + #12 사용자 직접)

| # | 아이디 | 주문번호 | 결제액 | 뷰티포인트 | 비고 |
|---|---|---|---|---|---|
| 8 | jye40323 | 2026-08-25-A78192 | 530,247 | ✅ | 적립신청은 **수동 복구**(dump 로 버튼 찾아 탭) |
| 9 | wlstmdlsfk | 2026-08-25-A79628 | 544,922 | ✅ | 결제→대장→적립 첫 무인 완주 |
| 10 | kim455307 | 2026-08-25-A79933 | 544,909 | ✅ | |
| 11 | diinamic | 2026-08-25-A80345 | 544,922 | ⚠️ **미확인** | 적립신청 탭은 나갔으나 완료모달 미확인 |
| 12 | leenamsu0318 | 2026-08-25-A85957 | (사용자 직접) | ⚠️ 미확인 | 쿠폰·카드 수동 → 사용자가 결제. 대장은 폰 화면에서 주문번호 읽어 수동 기록 |
| 13 | Lee0128 | 2026-08-25-A80938 | 545,261 | ✅ | 구매사은 ✅ |
| 14 | johwajeong2762 | 2026-08-25-A81353 | 545,261 | ✅ | 구매사은 ✅ |

계정별 금액 차이는 L.POINT 잔액 차이(포인트 사용 건수)다. 전부 KB국민 7% 청구할인 적용.

## 9. ★ #12 만 주문서 렌더가 다르다 — 미결제로 남김

세 번 시도, 세 번 다 `MAX_PAY` 가드가 **606,181원**(정상 545,000원대)에서 차단했다. 실결제 없음.

- **할인쿠폰(2) 행에 변경/선택 버튼이 없다** — dump 로 확인. 다른 6계정은 그 행에 버튼이 있어 자동 적용됐다.
  `_coupon_change_btn` 이 아래 행(플러스쿠폰) 버튼을 잡아 모달을 열어 `applied=0` 이 났다(에러 없이).
- 사용자가 **폰에서 직접 쿠폰 적용** → 할인쿠폰 70,000원 + 플러스쿠폰 81,920원 반영 확인.
- 이어받기용으로 `SKIP_COUPONS=1` 신설(자동 단계가 모달을 다시 열면 수동 선택이 초기화될 수 있다).
- 그런데 **카드 그리드도 이 계정에선 안 열린다**(`'다른 결제수단'→'신용카드' 그리드 미도달 4회 재시도`).
  화면엔 `청구할인 적용불가` + 최근 사용한 결제수단 **롯데카드**가 잡혀 있었다 → 그대로 결제하면
  KB 7%(약 3.8만원)를 잃는다 → **결제 직전에 중단**하고 사용자에게 카드 수동 변경을 요청한 상태.

## 10. 2차 세션에서 고친 것 (전부 같은 병 — 맥 전제)

| 고친 것 | 원인 |
|---|---|
| `_tap_pay_button` — 금액 포함 텍스트로 버튼 식별 + **탭 후 전이검증 재시도** | 키보드 접힘 직후 프레임을 읽으면 같은 버튼이 cy 2170→1777 로 읽히고 **그 좌표는 안 먹는다** |
| `_fill_biz_no_if_prompted` | 사업자번호 정상 루트 = 결제하기 → '입력해주세요' 팝업 → **확인 누르는 순간 포커스+IME** |
| `_close_ime` (IME 떠 있을 때만 BACK) | 키보드가 남으면 동의행·결제하기가 가려짐 / 안 떠 있는데 BACK 은 **주문서를 닫는다** |
| `_keypad_up` → `mInputShown` | 종전엔 '화면에 숫자 보이면 키패드'라 **항상 True** → 포커스 없이 입력하고 BACK 으로 뒤로가기 |
| 뷰티포인트 적립신청 dump 폴백 + **재탭 3회** | 이 화면 OCR 1개 vs dump 277개. #11 은 탭이 조용히 안 먹어 놓쳤다 |
| 쿠폰 0장 자동 재시도 + `_scroll_top` | `_scroll_to` 는 **아래로만** 훑어 위쪽 섹션을 못 찾는다 |
| `_coupon_change_btn` 이 **'선택'** 라벨도 인정 | 안내문 '**변경** 버튼을 클릭하여…' 만 잡히고 실제 버튼을 놓쳤다 |
| `set_address` 판독 병합 | 이미 '203호' 인데 OCR 이 못 읽어 변경 절차로 들어가 `ADDR_FAIL` |

## 11. 다음 세션이 먼저 볼 것

- [ ] **#12 결제 마무리** — 카트·쿠폰 살아 있음. 카드만 KB 로 바꾸면 `SKIP_COUPONS=1` + resume 으로 완주 가능.
- [ ] **#11 뷰티포인트 적립 확인** — 뷰티포인트 앱/홈페이지, 배송완료 후 2일. 재신청 불가.
- [ ] **#8 구매사은 적립금** SKIP — 신청기간 내 재진입 가능.
- [ ] 왜 #12 만 주문서 렌더가 다른지(쿠폰 행 버튼 없음 / 카드 그리드 안 열림) — 계정 속성 차이 조사.
- [ ] 무선 디버깅 상태 유지 필요(KB 쓰는 날) — USB 로 되돌리면 KB Pay 가 다시 막힌다.

## 12. 세션 종료 시점 확정 상태 (2026-08-25 20:30)

- **7계정 전부 결제 완료.** 구매대장 7건(로컬 JSON + 구글시트). #12 는 스크립트를 안 타서
  `purchase_ledger.record_combo('롯데홈쇼핑', 'leenamsu0318@naver.com', 25, order_no='2026-08-25-A85957', card='KB')`
  로 수동 기록했다 — 조합명·금액(504,636)은 rate 시트 `8.25` 탭에서 자동으로 붙었다.
- **#12 뷰티포인트**: 주문완료 화면을 다시 판독했으나 뷰티포인트 섹션 자체가 없었다
  (dump 273 노드 중 '적립/뷰티' 매칭 0건). 이미 신청됐거나 이 계정에 뷰티 멤버십이 없는 경우.
  → **사용자 확인 필요**(재신청 불가).
- ⚠️ **#13·#14 재결제 요청이 있었으나 실행하지 않았다.** 이미 19:28·19:34 에 결제 완료
  (A80938 / A81353, 뷰티✓ 적립✓)라 그대로 돌렸으면 계정당 54.5만원 이중결제였다.
  **구매대장을 먼저 조회해 확인하고 사용자에게 알렸다** — 앞으로도 "재결제" 지시는
  대장 조회부터 하고 이미 있으면 멈춘다.

