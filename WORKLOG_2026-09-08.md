# 2026-09-08 실행 기록

## 1. 9/7 작업 정리와 중복 경로 진단

- 9/7 롯데 결제 과정에서 사용자가 지적한 카트 수량, 결제 후 뷰티포인트,
  실제 구매상품별 CFT 구매사은, 중복 로그인 문제를 코드·문서 전체에서 다시 확인했다.
- 직접 원인은 필수 기능의 미구현이 아니라 전체 흐름 대신 중간 결제·적립 함수를 별도로 실행하여
  카트 정리·상품별 쿠폰·포인트·현금영수증·카드 비교·사후 적립이 한 거래로 연결되지 않은 것이다.
- 문서와 코드에도 경쟁 경로가 남아 있다. CFT의 구 lotte_checkout 함수는 첫 매칭 카드와
  KB fallback을 쓰지만, 현재 폰 정본 detect_card_lotte 함수는 전체 할인율을 비교한다.
  LOTTE_PAYMENT_RUNBOOK.md와 lotte_homeshopping.json도 현재 Python 흐름보다 낡았다.
- Codex 작업트리와 Desktop 실제 운영 폴더가 별도로 존재하며, 런타임 설정·당일 플랜·로그는
  Desktop 폴더에만 있다. 실제 운영 명령과 기록은 Desktop 저장소를 기준으로 한다.

## 2. 오늘 문서에 확정한 현재 실행 규칙

- 루트 buy.py를 사용자 구매 진입점으로 사용하고 중간 결제 함수를 단독 실행하지 않는다.
- 롯데 CFT는 계정 확인, 기존 카트 완전 삭제, 상품별 쿠폰 수령, 새 상품 담기,
  상품번호·수량 재조회 검증을 담당한다.
- 폰 주문서는 할인쿠폰, 플러스쿠폰, 포인트 전액사용, 최고 카드할인율 선택, 현금영수증,
  필수동의, 결제 순서를 강제한다. 포인트를 사용했는데 지출증빙·사업자번호 재판독이
  안 되면 결제하지 않는다.
- 결제 후 주문번호·주문상세 금액, 뷰티포인트 완료, 실제 구매상품별 CFT 구매사은 결과까지
  남기고 나서 다음 계정으로 넘어간다. CFT가 앱 전용임을 명시한 행사만
  주문상품 일치를 확인한 뒤 폰으로 신청한다.
- READ_FIRST.md와 LOTTE_HOMESHOPPING_STEPMAP.md의 앱 ONLY/CFT 우선 표현을 현재 기준으로
  명확히 했고, LOTTE_PAYMENT_RUNBOOK.md는 현재 실행 금지 보관본으로 표시했다.

## 3. 아직 변경하지 않은 사항

- 중복 함수 삭제, buy.py의 카트 준비 명령 추가, plan_current.json 도입은
  사용자와 최종 구조를 조율한 뒤 별도로 수정한다. 오늘 정리 단계에서 혼자 통합하지 않았다.
- today.json, today_carts.json, cart_plan.json의 정본·생성물 분리 방식도
  사용자 확정 전이므로 기존 형식을 유지했다.

## 4. 1차 커밋·푸시

- 9/7 코드·당일 상태, 구매 필수 규칙 문서, 9/6·9/7·9/8 워크로그를
  커밋 `588f026` (`9/7 실행 기록과 롯데 구매 필수 규칙 정리`)으로 `main`에 푸시했다.

## 5. Step 1 실행 결과 — 설화수 행사율·카트 플랜

- 갤러리아 27개 조합 측정을 완료했다. 행사 탭은 `9.8`, GWP는 70만원 이상
  4세트(가치 79,200원)이며 행사기간은 8/31~9/30이다. 재고 버전 `d`는 기존 27개와
  동일해 새 버전을 만들지 않았다.
- 최초 현대몰 측정에서 장바구니가 비동기로 늦게 그려져 19개 조합이 0줄로 오판됐다.
  `rate-check/hmall.py`의 고정 1.5초 단일 확인을 최대 12초 대기 + 불일치 시 새로고침 후
  8초 재확인으로 최소 수정했다. 끝까지 상품 줄 수가 정확히 일치하지 않으면 중단하는
  fail-closed 기준은 유지했다.
- 실패했던 현대몰 19개 조합을 개별 재측정해 전부 복구했다. 중간에 5·24번은 로그인
  화면 오류, 11번은 장바구니 중복 이동 충돌이 한 번씩 있었으나 성공 처리하지 않고
  마지막에 다시 실행해 실제 상품·수량·가격을 확인했다.
- 현대몰 최적 카드는 전 조합에서 `카카오페이 현대 7%`였다. 현재 페이백 카탈로그에는
  해당 카드명이 없어 현대몰 페이백은 임의 추정하지 않고 0%로 계산했다.
- 롯데 쿠폰율은 b/f 12%, c/d/e/g/h/n 14%, 결제 카드는 국민카드 5%, 페이백은 1%로
  측정됐다. d와 f의 등록 후보는 일시품절 표시가 있어 해당 결과는 주의 대상으로 남겼다.
- 3개 몰의 완성된 값을 다시 계산한 최종 카트 플랜은 `galleria`, 36카트,
  총 결제예상 13,583,810원, 평균 공급률 0.5185다. 제품 총수량은
  b=2, c=21, d=0, e=32, f=35, g=24, h=13, n=14다.
- `9.8` 탭 결과를 재고관리 `당일 설화수`에 133행으로 다시 미러했다.

## 6. Step 2 실행 결과 — 현대몰 식품 10% 적립

- 계정 1번의 로그인된 CFT 세션을 재사용해 등록 상품을 검사했다.
- 검사 결과는 10% 적립 대상 14개, 적립없음 0개, 판매중단 3개다.
- 10% 적립 대상 ID는 `1, 1-1, 2, 3, 8, 9, 11, 16, 17, 22, 25, 33, 34, 35`다.
- 판매중단은 `28 스키니랩 시서스`, `39 서정희pick 올리브오일`,
  `40 뉴케어 당플랜 프로`다.
- `1 이너플로라 60정`, `8 이디야 디카페인`, `34 파라다이스 버닝`은 10% 문구는
  확인했지만 바로구매 진입이 실패해 결제 미리보기 금액을 공란으로 보존했다.
  이전 값이나 추정값으로 채우지 않았다.
- 나머지 대상 상품은 수량·정가·우수가·즉시할인가·실비와 표시 가능한 카드 페이백을
  기록했다. 결과는 `cart/today.json`, 시트 `9.08 식품` A1:V19, 재고관리
  `당일 식품` 19행에 반영했다.

## 7. 범위 확인

- 요청받은 Step 1·2까지만 실행했다. Step 3 플랜 확정 이후의 계정별 카트 담기,
  결제, 결제 후 적립 신청은 실행하지 않았다.
