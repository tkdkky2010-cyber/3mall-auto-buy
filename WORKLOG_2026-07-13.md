# WORKLOG 2026-07-13 (skykow 일반결제 경로 + Hmall 포커스강탈 fix + wake preflight + 옵션2 상품)

## 요약 (한 줄)
1b74b58(07-09) 이후 누적 코드 변경 커밋: **skykow 현대카드 '일반결제' 경로**(앱카드 over-threshold 인증 회피), **Hmall PC 조작 포커스 강탈 근본fix**(new_page→기존 탭 재사용), **폰 절전/잠금 wake preflight**(양 몰), **buy.py 인터프리터 폴백**, **product 34(9번 옵션2 스페셜 아메리카노) 등록**.

## ① skykow 현대카드 '일반 결제' 경로 (phone_auto/hmall_hyundai_buy.py)
- **문제**: skykow 계정은 현대카드 앱카드 등록 계정 — 캐러셀에서 현대카드 선택 시 결제수단이 '앱카드 결제'로 자동선택됨. 앱카드는 **누적금액 임계 초과 시 현대카드 인증**(자동화 불가) 모달이 떠 `PAY_FAIL@order_page:현대 결제방식 화면 미도달` 반복(07-10 #17).
- **fix**: `GENERAL_PAY_IDS = {"skykow"}` (계정 ID 기준 — 인덱스 가변). buy_one 이 현대카드+GENERAL_PAY_IDS 면 `pay_hyundai_general()` 호출:
  카드선택 후 주문서에서 **아래로 스크롤하며 '일반 결제' 탭 OCR 탐색**(결제수단 영역은 카드할인 700px 아래) → 탭 → 결제하기(재렌더 대비 1회 재탭) → 후속 화면 판별(PIN번호 결제/주문완료 등, 모르는 화면이면 OCR 덤프+안전정지).
- **검증**: 07-13 라이브 완주 `✓ #17 결제 완료`. 다른 계정은 이 탭 없음(비등록) → 기존 PIN 경로 유지. **식품·설화수 공통**(단일 진입점).

## ② Hmall PC 조작 포커스 강탈 근본 fix (buy/run.py, cart/check10.py, rate-check/hmall.py, buy.py)
- **원인**: 데스크톱 Chrome(9222/9223) 조작에서 `context.new_page()`(새 탭 생성)가 macOS Chrome 창을 앞으로 끌어올려 사용자 작업창을 뺏음. (bring_to_front/.focus 호출은 없음 — 새 탭이 유일 원인.) 롯데(sulwhasoo)는 이미 기존 탭 재사용이라 안 뺏김.
- **fix**: 6곳 전부 `context.pages[-1] if context.pages else context.new_page()` (기존 탭 재사용) + 페어 `page.close()` 제거:
  - `buy/run.py`: process_account(657) + _read_mypage_paid_count(590), 계정별 close 2곳 제거
  - `cart/check10.py`: _run + _hmall_logged_in + _fresh_page
  - `rate-check/hmall.py`: 메인 page
  - `buy.py`: apply_reward 인라인 스니펫(적립신청 9222)
- → 현대몰 어느 경로(카트담기·check10·공급률·적립)를 돌려도 새 탭 안 열려 Mac 창 안 뺏음. (Chrome 콜드부팅 1회만 예외, 이미 떠있으면 없음.)

## ③ 폰 절전/잠금 wake preflight (phone_auto/hmall_hyundai_buy.py, lotte_homeshopping_buy.py) — 07-10 home
- 화면 절전으로 OFF면 screencap=검정 → OCR 전맹 → 모든 버튼 미발견(롯데 #11~14 4계정 연속 LOGOUT_FAIL 사고). `wake_screen()`: WAKEUP→비보안 키가드 dismiss+스와이프→여전히 잠김=보안잠금(adb 불가)→ok:False. buy_one 첫 단계에서 호출, SCREEN_LOCKED면 즉시 중단(헛돌기 금지). lotte main 은 잠김 감지 시 나머지 계정도 STOP.

## ④ buy.py 인터프리터 폴백 — 07-10 home
- `PHONE_PY`: brew python3 심볼릭이 사라진 환경(2026-07-10) 대응 — 존재하는 인터프리터로 폴백.

## ⑤ product 34 등록 (buy/products.json) — 07-13
- **34 = 이디야 스페셜에디션 아메리카노 (9번 옵션2)**: slitmCd 2246603712(=9번), option_index=2, url_extra 동일. 제품9 페이지 `[선택 2] 스페셜에디션 아메리카노 150T x2개` 확인 후 등록. cart_plan 에서 옵션 지정용.

## 07-13 데일리런 (참고, 미커밋 데이터)
- Step1: 채널 롯데 N=7, 조합 3·4·8·12·13·22·23, 평균 51%.
- Step2: 10% 5종(2·3·8·9·16, tier).
- 담기: 2번×2→계정1-10, 34(스페셜 아메리카노)×5→계정11-19, **19/19 완료**(11·13·17 재담기로 보강).

## 미커밋 잔존(의도)
- `cart/today.json`·`cart/today_carts.json`·`buy/cart_plan.json`(일일 데이터), backup/temp, logs — daily churn 이라 커밋 제외.
