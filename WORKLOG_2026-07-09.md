# WORKLOG 2026-07-09 (Step2 로그인 근본해결 + check10 simple_range fix + 롯데 결제 삼성카드)

## 요약 (한 줄)
Step 2 로그인 차단을 **"로그인된 9222 CfT 자동 재사용"**으로 근본 해결(하드코딩 제거), check10의 **simple_range 상품 주문서 스킵 버그** 수정, **상품3(하루견과) 10% 오탐** 규명, 현대식품+롯데 카트담기 완료, 롯데 폰결제(오늘 당일카드=**삼성**) #8 완료·#9 수동완료·#10~14 사용자가 집에서 진행.

---

## ① check10 CDP 포트 자동선택 — 하드코딩 제거 (cart/check10.py)
- **문제**: `CDP_PORT="9223"` 하드코딩. `launch-check10-chrome.sh`가 **fresh 프로필**로 9223을 띄우면 세션 없음 → 생짜 ID/PW 로그인 → **Hmall reCAPTCHA v3 차단**("로그인 실패"). Step2가 어제(07-08) 캐시 데이터만 보여줌.
- **수정**: `main()`이 **9223·9222 중 Hmall 로그인된 CfT를 탐지해 재사용**. 로그인된 세션이면 check10 `login()`이 `로그아웃` 감지로 생짜 로그인 skip → reCAPTCHA 회피.
  - `_cdp_alive(port)` + `_hmall_logged_in(context)`(loginForm 진입 후 '로그아웃' 노출 판정) 헬퍼 추가.
  - 후보 = `[CHECK10_CDP_PORT, "9223", "9222"]`(env override 우선). 살아있고 로그인된 첫 포트 사용. 아무데도 없으면 기존 방식(9223 launch + raw login) 폴백.
  - **connect = plain playwright** — patchright는 Chrome 148 connect 시 `Network.setCacheDisabled`로 크래시(실측). CDP attach라 스텔스 무관.
- **실측**: `9223 꺼짐 → 9222 로그인 CfT 재사용 → [OK] 로그인` → 23상품 정상 체크, 시트 `7.09 식품` 생성.

## ② check10 simple_range 주문서 스킵 버그 수정 (cart/check10.py)
- **원인**: `check_payment_flow`(946줄)가 `_find_optimal_qty(benefit_unit, tiers, ...)`에 **tiers만** 넘김. `_find_optimal_qty`는 `if not tiers: return None`. → tier 없는 **단순 10%(simple_ranges)** 상품은 optimal=None → `"≥10% tier unreachable (cap 500K)"`로 **바로구매(주문서) 통째 스킵** → `kakao_price/kakao_final_cost/card_slides/paybacks` 전부 공란. 오늘 10% 상품이 전부 simple_range라 시트의 즉시할인가/실비/페이백 열이 전멸("7.9 식품 미완성"으로 보임).
- **수정**: `simple_ranges`를 **pseudo-tier**(`{min_won, reward_pt=min_won×pct%}`)로 편입해 `_find_optimal_qty`에 함께 넘김(시그니처에 `simple_ranges=` 추가, 내부 `pool = tiers + pseudo`).
- **검증(상품25 석류젤리)**: 수정 전 `kakao_price=null / error=unreachable` → 수정 후 **`kakao_price=55,120 / kakao_final_cost=49,608(10%반영) / card_slides=3 / effective_ten_percent=True / error=None`**. (`267,300×0.93` 류 산식 일치)
- **주의**: pseudo-tier의 `reward_pt`는 min_won 고정값이라 `optimal_tier.effective_pct` 메타는 약간 보수적으로 나올 수 있음(qty 선택엔 무해, 최종 적립/실비는 정확).

## ③ 상품3 하루견과 "하루틴 10%" 오탐 규명 (사용자 지적)
- today.json은 상품3을 `ten_percent=true / 하루틴 10%적립 / prmo P202607016805`로 기록했으나, **라이브 확인 시 상품3 구매혜택 panel에 evntHPointDtl 링크 0개, 본문에 "10%"·"하루틴" 없음**(있는 건 기본 80P 적립 + 현대카드 7% 청구할인).
- 하루틴 이벤트(P202607016805)는 **살아있으나**(7.6~7.12) **① 행사대상=알림수신동의+H.Point 응모(opt-in), ② 행사상품에 하루견과 없음**. check10 실행 시점에 상품3 페이지에 그 링크가 **잠깐** 떠서 잡힌 것(재판정식이 이벤트명 "하루틴 10%적립"의 "10%"로 True 판정).
- **재실행하면 자동 교정**: 지금은 링크 없음 → events 없음 → 재판정식(이벤트명/phrase에 "10%")이 False → 상품3 ✗.
- **결론**: 오늘 진짜 10% = **12개** (10·11·12·13·14·17·20·22·25·28·29·33, 전부 "건강식품 브랜드 특별전" P202607067509 자동적립). 3번 제외.

## ④ 카트담기
- **현대 식품**(`CART_ONLY=true buy/run.py`, cart_plan.json): **28 시서스→계정 8~13, 25 석류젤리→계정 14~19, 각 ×2**. 결과 **12/12** ✓. (어제 옛 플랜 잔여였던 계정 1~7 카트는 clear됨.)
- **롯데 설화수 조합3**(e탄력3 ×2 + f윤90 ×2, `_lotte_cart_fill.py 3 8 14`): **계정 8~14, 7/7** ✓. 로그인 캡차 GCV 자동해결, 비번팝업 `dismiss_popup` "30일간 보이지 않기" 처리 확인.

## ⑤ 롯데 폰결제 (오늘 당일카드 = 삼성카드 7%, 일반결제/모니모 금융인증서)
- 결제 시작=루트 `buy.py 롯데 <계정>` → `phone_auto.lotte_homeshopping_buy`. `detect_card_lotte`가 청구할인 배너에서 **삼성카드** 자동감지(어제는 롯데카드). 삼성=일반결제(카드번호 직접+CVC+pin6+모니모 금융인증서 cert_pw6, ARS 회피).
- **#8**: 완료(paid:true). 단 purchase_ledger에 amount/order_no **null**(스크랩 gap, card=삼성만 기록). 금액 526,781원(주문서 실측).
- **#9**: 인증서 6자리 **셔플 키패드 OCR 오독**으로 `cert_pw6` 오입력 → `wait_text('성공')` 20s 타임아웃 → `PAY_FAIL@order_complete`(롯데앱 복귀 실패, 미결제 상태로 폰이 인증서 비번칸에 멈춤). **중복결제 위험 없음**(인증서 미통과=미결제). → **사용자 수동 완료**. (⚠️ 스샷상 폰은 주문결제 화면으로 돌아와 있었음 — 최종 완료 재확인 권장.)
- **#10~14**: 미완. **사용자가 집에서 진행 예정** (같은 삼성카드 경로).

---

## 코드 변경 (이번 커밋)
- `cart/check10.py`: ①포트 자동선택(하드코딩 제거) + ②simple_range pseudo-tier 편입.
- `buy/checkout_kakao.py`(신규): **카카오페이 날 PC 결제** — 로그인→카트 전체선택→구매하기→**H.Point 전액사용**→카카오페이 캐러셀 선택→**결제하기 클릭까지**, 카카오페이 인증만 본폰 수동. 기본 INSPECT(안누름)/`--go` 실행. plain playwright(148 안전). *(어제 07-08 카카오페이 날 대응으로 작성했던 것, 이번에 커밋)*

## 알려진 이슈 / 다음
- **삼성 인증서 6자리 셔플 OCR 오독** — #9에서 발생. #10~14에서 재발 가능 → 막히면 인증서 단계만 수동. (근본: `_tap_shuffle` cert 키패드 판독 개선 필요.)
- **buy.py 성공 시 자식 로그 미출력** — 감지 카드/주문번호 확인은 `secrets/purchase_ledger.json` 또는 폰 화면으로. (실패 시에만 로그 출력.)
- **purchase_ledger 롯데 order_no/amount null** — 주문완료 화면 스크랩 gap. 카드명만 안정 기록.
- **today.json 상품3 오탐 잔존** — check10 재실행하면 자동 ✗(위 ③). 시트도 재생성 권장.
- **미커밋 잔존(의도)**: `cart/today.json`·`cart/today_carts.json`·`buy/cart_plan.json`(일일 데이터), 그리고 07-05 런처 3종(별건, 이번 커밋 제외).
