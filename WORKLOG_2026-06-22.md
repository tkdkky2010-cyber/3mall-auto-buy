# WORKLOG 2026-06-22

## 일일 실행 (Step 1·2 + 롯데 결제)

### Step 1 (3사 공급률 + 카트플랜)
- 갤러리아 GWP 판독(6.16–6.30, 40/70만 3·6세트) 후 완주.
- ⚠️ Chrome 9222 탭 0개 → galleria attach 실패 → 빈 탭 생성 후 재실행으로 해결.
- **샘플 단가 변경 반영(계산 재처리)**: 사용자가 재고현황 시트(`1aDJ…DSZTxU` 재고현황 E열) s35 3,000→2,500 / s36 5,000→4,500 / s38 신규 11,100 변경.
  - 브라우저 재크롤 없이 `rate-check/_recompute_samples_0622.py`로 갤러리아 새 추증·GWP(6세트 100,200→94,200)를 Hmall·롯데 섹션에 적용 → 순/공급률 재계산. 요약표 K/L/M + cart_plan(O열) 갱신.
  - 결과: 공급률 전반 +0.01 안팎 상승. 롯데 카트플랜 7건 동일 조합(3·4·8·12·13·22·23), 평균 0.4767→0.4882.

### Step 2 (Hmall 10% 적립 체크)
- 10%적립 6 / 적립없음 12 / 에러 4(판매중단). 오늘은 대부분 "더블위크 현대카드 7%".

### 롯데홈쇼핑 폰결제 (g2+h1, 당일카드 KB 7%, 각 522,037원)
- 카트필(PC) 1~7, 7~14 → 폰 결제(ADB, 카메라 0).
- **결제 완료 7계정: #1·7·8·9·10·13·14** (사용자 선택). 뷰티포인트 적립 #9·10·13·14 스샷+모달문구 검증, #1·7·8은 구버전 로깅(스샷 없음 — 2~3일 후 아모레 계정 수동확인 권장).
- 주문번호 확인분: #8 K42849 / #9 K43243 / #10 K44433 / #14 K45515.

## 코드 수정 (버그/정리)

### 1. 주문번호 None 버그 fix
- 원인: KB/현대/PAYCO/삼성 결제가 **주문완료 첫 프레임에서 즉시 return** → 번호 지연렌더를 못 기다려 `order=None` (pay_loca만 이미 고쳐져 있었음 = 중복코드 발산).
- fix: 공용 헬퍼 `_poll_order_complete(timeout)` (confirmed 후 번호 잡힐 때까지 폴링) 신설 → 5개 결제경로(loca/kb/hyundai/payco/samsung) 전부 통일. 한쪽만 고쳐지는 회귀 차단.

### 2. 뷰티포인트 적립 진실 로깅
- `claim_beauty_point`: 완료판정을 **OCR 텍스트 1개에 `적립`+`완료` 동시 포함**으로 엄격화(주문완료 "주문이 완료" false-positive 차단). 실제 모달 문구 로그 기록 + 계정별 스샷 `/tmp/_lt_beauty_{idx}.png` + ✅/⏭️/❌ 명확 출력. idx 인자 추가.

### 3. 카메라 영구 폐기 (롯데·hmall 전부 ADB)
- **오판 정정**: 옛 `LOTTE_PAYMENT_RUNBOOK.md`/`PHASE_3B_SETUP.md` 보고 "롯데 결제에 카메라 필수"로 착각해 결제를 막았으나, 실제 6월부터 100% ADB(FLAG_SECURE PIN도 `uiautomator dump`). 문서·메모리 정정.
- 삭제(고아/문서): `camera_probe.py`·`keypad_ocr_test.py`·`continuity_probe.swift`·`workflow.py`·`buy/phase3/camera.py`·`PHASE_3B_SETUP.md`·`SYSTEM_READY.md`·`TOMORROW.md`.
- **`flow_runner` 카메라 영구 차단**: `__init__`에서 `use_camera=False` 하드코딩(인자·`FLOW_USE_CAMERA` env·step override 무시), `_cap()` ADB 전용. → 어떤 step·카드·명령도 카메라 활성화 불가(검증: `use_camera=True`+`FLOW_USE_CAMERA=1`에도 False). 나머지 `if self.use_camera:` 분기는 상수-False inert(실행 0).
- `LOTTE_PAYMENT_RUNBOOK.md`: §0 환경·Phase 4 PIN·함정표를 카메라→ADB dump 핸들러로 정정.

## TODO / 다음
- ⚠️ **hmall(현대몰) 삼성카드 결제**: 정본 `hmall_hyundai_buy.pay_samsung`(ADB)은 **hmall 첫 라이브 미검증**. 다음에 현대몰 삼성 상황 나오면 #1 한 건 end-to-end 관찰 검증 먼저(롯데 #12 절차). → 검증되면 inert 카메라 죽은코드 물리삭제 가능.
- #1·7·8 뷰티 적립 2~3일 후 아모레 계정 확인.
