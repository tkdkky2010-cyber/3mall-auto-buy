# PRIVATE — 계정/세션 정보 절대 git 노출 금지
hmall_config.json
lotte.json
galleria.json
credentials.json
gen-lang-*.json
lotte_address_map.json
3mall-secrets-*.zip

# 동적 상품 ID (월 갱신, 민감)
hsmaster/config/sulwhasoo-ids.json

# 빌드/런타임 산출물
hsmaster/node_modules/
hsmaster/dist/
hsmaster/data/

# codegen·probe artifacts (평문 비밀번호 흔적 가능) — .sh 파일은 commit 허용
hsmaster/scripts/*.js
hsmaster/scripts/*.mjs
hsmaster/scripts/*.ts

# 계정별 persistent browser profiles (cookies, session)
profiles/

# Phase 3-B 폰 자동화 — 캡처 이미지 (보안 키패드 / 결제 화면 / 카드비번 포함 가능)
phone_auto/_tmp/
phone_auto/__pycache__/
phone_auto/screens/
card_pins.json
secrets/

# 로그·OS 노이즈
*.log
.DS_Store

# Chrome 확장 인증키 (배포용 아님)
lotte-dialog-blocker.pem


# GCP Vision API key (gitignored)
gcp-vision-key.json
mall-ocr-*.json

# 파이썬 캐시 / 임시 / MCP 산출물 (전 디렉토리 — 자동 재생성, 추적 불필요)
__pycache__/
_tmp/
.playwright-mcp/
