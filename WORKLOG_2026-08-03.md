# WORKLOG 2026-08-03 — 롯데 조합3 담기(#1~#7) + 폰결제(#2~#7) 완주 / 광세일 이벤트 사전검증 / #1 로그인 실패 근본원인

## 0. 전날(8/2) 기록 부재 — 구매대장으로 역추적

**8/2 세션은 워크로그가 없다.** `secrets/purchase_ledger.json` 으로만 역추적했다.

| 시각 | # | 계정 | 금액 | 카드 | 주문번호 |
|---|--:|---|--:|---|---|
| 15:20 | 2 | kms3945 | 510,602 | 삼성 | *(미기록)* |
| 15:34 | 4 | owyura | 510,602 | 삼성 | *(미기록)* |
| 17:44 | 16 | kimmk2008 | 510,602 | 삼성 | 2026-08-02-I20794 |
| 18:01 | 3 | kgi5907 | 510,602 | 삼성 | 2026-08-02-I21621 |
| 18:11 | 5 | yr5326 | 510,602 | 삼성 | 2026-08-02-I22042 |
| 18:21 | 15 | joyoungjin1220 | 510,602 | 삼성 | 2026-08-02-I22245 |

전부 조합3. **order_no 없음 ≠ 결제실패** — `lotte_homeshopping_buy.py:1281` 의 `record_combo` 는
`pay.ok` 이후에만 호출되므로, #2·#4 는 결제 성공 + 주문번호 OCR 실패다.
(8/1 미완이던 #15·#16 롯데카드 한도초과는 8/2 에 삼성카드로 해소됨.)

## 1. ★ 광세일 적립 이벤트 — 담기 전 상품별 사전검증

사용자 지시: "오늘 적립 이벤트는 광세일 최대 3만원. 조합3 모든 제품에 붙어있는지 확인하고 담아라."

`evt_no=296569` — **[8월]광세일 패션/리빙 상품 최대 20% 적립 (8/3~6)**

| 코드 | 상품 | goods_no | 상세 적립배너 | tier |
|---|---|---|:--:|---|
| e | 탄력3종 | 2923406968 | ✅ | 5만/10만/20만/30만 → 1만/1.5만/2만/**3만** |
| f | 윤조에센스90 | 2091578259 | ✅ | 동일 evt_no |

이벤트 페이지 행사안내 실측 조건:

- **"상품상세 페이지 내에서 적립배너가 확인되는 상품에 한함"** → **배너 존재 자체가 대상 판정 기준.**
  배너명이 "패션/리빙" 이라 설화수가 대상이 아닌 것처럼 보이지만, 두 상품 상세에 실제로 붙어 있으므로 대상이다.
- **"롯데홈쇼핑 모바일 앱(APP)으로 구매 후 신청"** + **"직접 앱 실행 후 주문 건에 한함(포털 검색 유입 제외)"**
  → 기존 폰앱 플로우 그대로 충족.
- **인당 최대 3만원**, 구매/신청기간 모두 8.3~8.6.
- ⚠️ **"중복 적립 행사 상품으로 확인될 시 적립 취소"** — 같은 상세에 `L.POINT 100배 적립` 배너가 공존한다.
  이번엔 광세일만 신청(그쪽 응모 안 함).

검증 스크립트는 `rate-check/lotte.py` 의 `_COLLECT_JS`/`_VERIFY_JS` 를 그대로 재사용해 상품 2개를 각각 조회.
(`lotte.py` 본체는 store-wide 가정으로 **b 1개만** 조회한다 — 제품별 차이가 의심되면 이번처럼 개별 확인해야 한다.
자동메모리 `lotte-per-product-card-inconsistency` 와 같은 계열의 리스크.)

## 2. 담기 결과 — `_lotte_cart_fill.py 3 1 7`

| # | 계정 | 결과 |
|--:|---|---|
| 1 | tkdkky2002 | ✗ `[LOGIN] 자격증명 거부` |
| 2~7 | kms3945 / kgi5907 / owyura / yr5326 / ybkim8225@gmail.com / lee6040314@naver.com | ✓ 전부 `[OK] 담기 검증 — 카트 2건` |

- #4~#7 은 비번변경 캠페인 팝업 발생 → `[popup] '30일간 보이지 않기' 클릭 (img-alt)` 로 정상 처리. **재발 없음.**
- 쿠폰은 두 상품 모두 `downloaded`.
- ⚠️ 담기 검증은 **품목 수(2건)만** 확인하고 수량(각 2개)은 검증하지 않는다. 실제 교차확인은 폰 결제 금액에서 이뤄진다.

## 3. ★★ #1 tkdkky2002 로그인 실패 — 근본원인 규명 (사용자 가설 반증)

8/1 에 이어 8/3 도 실패. 사용자 가설: **"기존에 입력돼 있던 비번 뒤에 덧붙여 입력해서 그렇다. 다 지우고 새로 써야 한다."**

실측(추측 패치 금지 원칙대로 DOM 부터 확인):

```
로그인 페이지 로드 직후 (아무것도 입력 안 함):
   login_id = lee6040314@naver.com     ← Chrome autofill
   password = (11자)                    ← Chrome autofill, 다른 계정 자격증명
   memSave  = on
```

→ **autofill 프리필은 사실이다.** 하지만 Playwright `.fill()` 은 **덮어쓰기**라 잔존하지 않는다:

- `fill('PWTEST1234!')` 직후 password 값 = `PWTEST1234!` (11자) — 프리필 값 완전 대체 확인.
- 두 필드를 **명시적으로 비운 뒤** `lotte.json` 값 입력 → 제출 직전 DOM 값:
  `id=tkdkky2002`, `pw_len=10`, **sha1 앞 8자리가 `lotte.json` 저장값과 동일, 문자열 비교 `일치=True`**.
- 캡차 미표시. 그 값 그대로 제출 → `pop_loginfailure` + *"아이디와 비밀번호를 확인하고 다시 로그인 해주세요."*

**결론: 입력 오염이 아니라 `lotte.json` 의 #1 비밀번호 자체가 서버에서 거부된다.**
같은 코드로 #2~#7 은 전부 로그인 성공했다. → **사용자가 실제 비밀번호를 확인해 줘야 해결된다.**

> 교훈: "덧입력" 류의 가설은 DOM 값 + 해시 대조로 **1회 측정이면 참/거짓이 갈린다.**
> 코드를 고치기 전에 값부터 재는 게 항상 싸다.

## 4. 폰 결제 결과 — `lotte_homeshopping_buy 2 3 4 5 6 7 combo=3`

폰 무선 ADB: pair `192.168.0.8:34725` (코드 263296) → connect `192.168.0.8:38231`.
`_resolve_serial()` 이 mDNS 엔트리보다 무선 ip:port 를 우선 고정 (기기 2줄로 보여도 동일 폰).

**당일카드 = 롯데 7% 청구할인** (어제 삼성에서 바뀜). `detect_card` 자동감지로 6건 모두 동일 선택,
경로는 `다른결제수단 > 신용카드 > 카드선택`. 8/1 에 발생했던 롯데카드 이용한도 초과는 **재발하지 않음**.

| # | 계정 | 주문번호 | 금액 | 뷰티 | 광세일 적립 |
|--:|---|---|--:|:--:|:--:|
| 2 | kms3945 | 20260803199942 | 513,565 | ✓ | ✓ |
| 3 | kgi5907 | 2026-08-03-J00522 | 513,565 | ✓ | ✓ |
| 4 | owyura | 2026-08-03-J00742 | 513,565 | ✓ | ✓ |
| 5 | yr5326 | 2026-08-03-J00908 | 513,565 | ✓ | ✓ |
| 6 | ybkim8225@gmail.com | 2026-08-03-J01648 | 513,565 | ✓ | ✓ |
| 7 | lee6040314@naver.com | 2026-08-03-J02224 | 513,565 | ✓ | ✓ |

**6/6 성공, 실패 0건. 합계 3,081,390원.** 뷰티포인트 6/6(`뷰티포인트 적립신청이 완료되었습니다.`),
구매사은 6/6 → 계정당 3만원 × 6 = **18만원 적립 예정**. 6건 전부 구매대장 기록 완료.

> #2 주문번호만 형식이 다르다(`20260803199942` vs `2026-08-03-J00522`). 결제는 정상.

## 5. 발견된 갭 (미수정)

- **`secrets/beauty_point_ledger.json` 이 롯데 구매를 반영하지 않는다.** 기록 코드가 `hmall_hyundai_buy.py:67` 에만 있어
  **6/18 이후로 멈춰 있다.** 18만P 목표(자동메모리 `beauty-point-target`) 추적이 어긋난다 → 롯데 경로에도 동일 기록 필요.
- **9222 Chrome 페이지 0개 → CDP 연결 실패**가 이번 세션에 2회 발생(카트필 종료 후 탭이 전부 닫힘).
  `PUT /json/new` 로 탭 생성 후 재연결하면 해결. 진단은 `/json/list` 빈 배열
  (에러 메시지 `Browser context management is not supported` 는 오해 유발).

## 6. 다음 할 일

- [ ] **#1 tkdkky2002 롯데 비밀번호 확인** → `lotte.json` 갱신 → 담기+결제 (광세일 8/6 까지 가능)
- [ ] 롯데 경로 뷰티포인트 대장 기록 추가 (`beauty_point_ledger.json`)
- [ ] 8/1 잔여: `galleria.py` 리디렉트 abort 가드 / `buy/sulwhasoo.py LOTTE_PRODUCTS` 에 `n` 추가(#24~27 담기용)
