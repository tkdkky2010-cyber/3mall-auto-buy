# 2026-09-03 워크로그

## 0. 사용자 지시 (문장 그대로)

> "오늘 1-6 계정 결제해 돈이 없어서 나머지는 식품 결제해야 도리 거 같아 / 172.30.1.44:44133 연결해서 구매진행"
> "오늘 설화수 하나카드로 결제가능해? 롯데카드로 하지말고"
> "뉴케어가 오늘 판매중지야 그래서 오늘 식품카트플랜에는 뉴케어 빼"
> "설화수는 계정별 7분간격없음 바로바로처리해"
> "한도 150 추가해놨어 결제 게속해"
> "하나카드 한도없어서 3번부터 결제안되는거엿거든"

식품 범위는 **AskUserQuestion 으로 확인 → 사용자가 "9월 표 그대로" 선택**.
("나머지(#7~19) 식품" 지시가 9월 정본표의 계정 지정(견과 #1~5 / 이디야 #11~15)과 어긋나
 그대로 실행하면 계획이 깨져서 물었다.)

## 1. 결과 — 현대몰 설화수 조합25 #1~#6 **6/6 결제 완료**

카드 = **하나** (전 계정), H.Point 전액사용 + 하나 5% 즉시할인 적용.

| # | 아이디 | 주문번호 | 실결제 |
|--:|---|---|--:|
| 1 | tkdkky2002 | 20260903091957 | 501,011 |
| 2 | 1plu1mall1 | 20260903093342 | 500,992 |
| 3 | kgi5907@daum.net | 20260903097489 | 501,087 |
| 4 | 1plus1mall0 | 20260903094895 | 501,125 |
| 5 | 1plus1mall1 | 20260903095259 | 501,097 |
| 6 | 1plus1mall4 | 20260903095683 | 500,603 |

합계 **약 3,005,915원 / 6건**. 매니페스트 `paid` #1~6 전부 True.

## 2. ★근본버그 — 같은 할인율 카드가 2장인 날 카드선택이 조용히 빗나간다 (수정함)

### 증상
`#3 => SELECT_CARD_FAIL:하나:하나 캐러셀 탭 후 결제수단에 '하나카드' 미확인 — 오결제 방지 중단`
2회 연속 같은 자리. 그런데 로그엔 **`하나 탭 #1` 라인이 아예 없었다**(= 탭이 안 나갔다).

### 원인
오늘 카드할인 캐러셀에 **롯데 5% / 하나 5% 가 나란히, 할인 후 금액이 완전히 동일(501,087원)**.
`hmall_select_card_discount` 의 선택 판정은 **"캐러셀 행 금액 == 결제버튼 금액"** 뿐이라,
**롯데가 기본선택된 상태에서도 그 등식이 참**이 된다 → "하나 이미 선택됨"으로 오판하고 return.
`_verify_pay_method` 가 결제수단에서 '하나카드' 부재를 잡아 결제는 막혔다(가드는 정상 작동).

fail_audit 스샷(`182624_현대_3.png`)이 결정적 증거 — 롯데 박스만 빨간 테두리+체크,
하나는 회색 체크, 결제수단은 '롯데카드'.

> 이 버그는 코드 주석에 **이미 예고돼 있었다**: "오늘은 두 금액이 같아 안 드러났을 뿐 latent bug".
> 8/5 에 2열 캐러셀 오매칭을 고치며 적어둔 것이 오늘 다른 형태로 발현했다.

### 수정 (2파일, 최소 변경)
1. `phone_auto/flow_runner.py` `hmall_select_card_discount` — `force_tap` 옵션 신설.
   금액일치 판정을 **1회만** 건너뛰고 강제 탭한다(그 뒤론 기존 판정 그대로).
2. `phone_auto/hmall_hyundai_buy.py` `select_card` — 캐러셀 통과 + `_verify_pay_method` 실패면
   **미선택이 확정된 상태**이므로 토글 보호를 풀고 `force_tap=True` 로 1회 재시도.

⚠️ 캐러셀 행은 **토글**이라 무조건 force_tap 을 켜면 선택된 카드를 해제한다.
   **결제수단 카드명으로 미선택을 확정한 뒤에만** 켤 것.

라이브 검증: `[card] 하나 캐러셀 통과했으나 결제수단은 딴 카드 → 강제 탭 1회 재시도`
→ `force_tap=1` → `하나 탭 #1 @ (730,1271) (캐러셀 501087 vs 결제 501087 강제)` → 결제 완주.

## 3. #3 은 그 뒤 **하나카드 한도 초과**로 한 번 더 막혔다

카드선택 수정 후 결제까지 갔으나 `ORDER_NOT_COMPLETE:주문완료 미확인(timeout)`.
화면엔 `주문번호 20260903096967 / 결제가 진행중입니다` 가 떠 있었다.

### ★"진행중" 화면을 성공으로 읽지 않은 것이 맞았다 — 실제로 미성사
마이페이지 실측으로 확정(부재가 아니라 **양성 증거**로):

| 확인 항목 | 값 | 해석 |
|---|---|---|
| 주문접수 / 결제완료 | **0 / 0** | 9/3 주문 접수 자체가 없음 |
| 포인트 | **8,040P 그대로** | 주문서에서 쓰려던 H.Point 미차감 |
| 최신 주문 | 2026.09.02 | 9/3 주문 없음 |
| 장바구니 배지 | **2** | 카트 미비움 |

사용자 확인: **"하나카드 한도없어서 3번부터 결제안되는거엿거든"** → 선결제 1,543,404원으로
한도 확보(하나Pay 알림 18:34) 후 재실행 → `20260903097489` 완주.

> 교훈: 현대몰 주문완료 화면에 **`결제가 진행중입니다` 변형**이 있다. 판독기가 '주문완료'만
> 찾으면 timeout 난다. **주문번호는 그 화면에도 찍히지만, 그 번호가 곧 성사는 아니다** —
> 승인 거절이어도 번호는 발번된다. 성사 판정은 **주문내역 상태 + 포인트 차감**으로 한다.

## 4. 계정 간 7분 간격 제거 (코드 수정 없이)

사용자 지시로 설화수는 간격 없이 연속 처리. `_account_gap` 에 이미 있던
`HMALL_ACCOUNT_GAP_SEC=0` 우회를 사용 — **코드는 건드리지 않았다.**
#1·#2 는 7분 간격으로 돌았고, #3~6 부터 간격 0 으로 재실행했다(계정당 약 2분).

## 5. 오늘 실행 명령 (정본)

```
adb connect 172.30.1.44:44133
HMALL_ACCOUNT_GAP_SEC=0 CLAUDE_OCR_ONLY=1 ANDROID_SERIAL=172.30.1.44:44133 \
  python3 -u -m phone_auto.hmall_hyundai_buy 하나 3 4 5 6 combo=25
```
- 폰이 mDNS(`adb-...._tcp`) 와 IP 두 줄로 잡혀 `ANDROID_SERIAL` 을 IP 로 고정했다.
  (`_resolve_serial` 은 무선(IP) 우선이라 자동으로도 맞지만 명시했다.)
- 하나카드는 **핸드셰이크가 필요 없다** — `input_pin(sequential_logo)` 로 로고칸 순서매핑
  6자리 자동입력. `CLAUDE_OCR_ONLY=1` 을 켜뒀지만 요청이 한 번도 뜨지 않았다.

## 6. 미해결 — 뷰티포인트 적립 **6/6 전부 실패**

`beauty_fail:재인증 버튼 없음` ×5, `beauty_fail:no orderComplete target` ×1.
주문완료 화면 only(now-or-never) 항목이라 사후 복구가 어려울 수 있다. **원인 미규명 — 다음 세션 과제.**
(추정 금지: 재인증 버튼 자체가 사라진 건지, 화면 전환이 빨라 놓친 건지 확인 안 됐다.)

## 7. 대장 금액 이슈 (보정 안 함 — 사용자 방침)

- #2: `[ledger] ⚠️ rate 탭 '9.3' 없음/조회실패 → 금액 미상 기록` → `조합25 x1 ?원`
- 나머지: 대장 `498,550원` vs 실결제 `501,0xx원` 불일치
자동메모리 `ledger-amount-not-required` 대로 **수량만 맞으면 되고 금액 보정은 하지 않는다.**

## 8. 오늘 식품 카트플랜 — 뉴케어 제외

사용자: "뉴케어가 오늘 판매중지야". `buy/cart_plan.json` 을 아래로 교체
(이전 파일은 `buy/cart_plan.json.bak-20260903` 로 백업).

| product | 상품 | qty | accounts |
|---|---|--:|---|
| 2 | 하루견과 초록색 100봉 | 2 | 1–5 |
| 3 | 하루견과 갈색 100봉 | 2 | 1–5 |
| 9 | 이디야 오리지널(option_index 1) | 2 | 11–15 |

★**순서 주의** — `run.py` 는 담기 전에 `clear_cart` 를 돌린다. #1~5 는 같은 날 설화수를
먼저 결제하므로 **설화수 결제 완주 후에** 담아야 한다. 먼저 담으면 설화수 카트가 지워진다.

## 9. 남은 일

- [ ] 식품 담기 `#11~15` (이디야) — `run.py 11-15`
- [ ] 식품 결제 (#1~5 견과 2종, #11~15 이디야)
- [ ] 뷰티포인트 적립 실패 원인 규명
- [ ] 9월 표 잔량 재계산 — 오늘 뉴케어를 못 사서 9/2~9/17 16일 스케줄에 **하루 구멍**이 생겼다
      (표엔 버퍼가 없다. 9/18 이후 5일에 밀어넣을 수 있는지 확인 필요.)
