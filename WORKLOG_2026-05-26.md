# 2026-05-26 작업 기록

## 1. 사건: python3.13 즉시 사망 (코드서명) — 미해결

`bash step2.sh` 실행 시 `python3 cart/check10.py`가 트레이스백 없이 즉시 `Killed: 9`(rc 137)로 죽음.

### 진단 (확정)
- 크래시 리포트: `SIGKILL (Code Signature Invalid)`, `namespace: CODESIGNING` (`~/Library/Logs/DiagnosticReports/Python-*.ips`)
- 샌드박스 / MDM / EDR / Santa **전부 없음**, 셸에 sandbox-exec 래퍼 없음 → **OS 자체**가 죽임
- `cp /bin/echo /tmp/e2 && /tmp/e2 hi` → rc 137 ⇒ **adhoc 서명/복사된 바이너리를 OS가 전역 거부 중** (시스템 문제 확정)
- 정상: `/usr/bin/python3`(3.9, Apple 플랫폼), `/opt/homebrew/bin/python3`(3.14, Developer ID)
- 사망: `/Library/Frameworks/Python.framework/.../3.13`(adhoc 서명 상태, dylib 5/22 수정됨), `~/venv`(3.13 기반)
- 마지막 부팅 5/22 13:03, 이후 재부팅 없음. `/Library/Updates`에 **오늘(5/26 09:49) 스테이징된 업데이트** 존재
- 결론: 같은 커널·같은 파일인데 "어제는 됨" → **오늘 런타임 trust-cache/보안정책이 엄격해진 것**(스테이징 업데이트 연관 추정). python.org 3.13이 정품 PSF Developer ID가 아닌 adhoc이라 새 정책에 걸림
- `codesign --force --sign -` adhoc 재서명 시도 → **효과 없음**(정책이 adhoc 자체를 거부)

### 오늘 처리
- **homebrew 3.14로 우회 실행** (deps 전부 보유 확인) → step2 정상 완료
- 기본 `python3`/`~/venv`는 **고치지 않음(보류)** — 사용자 선택

### 앞으로 (go-forward)
1. **재부팅** 먼저 — 스테이징 업데이트/trust-cache 과도기면 원상복구 가능성 큼. 후 `python3 --version` 확인
2. 안 되면 **python.org 3.13 재설치** (정품 서명 복구). site-packages 보통 유지, 의심 시 `pip install -r buy/requirements.txt`
3. 급하면 즉시 우회: `/opt/homebrew/bin/python3 cart/check10.py && /opt/homebrew/bin/python3 cart/show.py`
4. 빠른 진단 한 줄: `cp /bin/echo /tmp/e2 && /tmp/e2 hi; echo $?` (137이면 OS 전역 거부)

> 메모리 런북: `~/.claude/.../memory/codesign_kill_runbook.md`

## 2. step2 (Hmall 10% 적립 체크) — 완료 (rc 0)

- 실행: homebrew 3.14, 계정 #1 tkdkky2002, CDP 9223
- 결과 저장 위치: `cart/today.json` + Google Sheet **`5.26 식품` 탭**(신규 생성, A1:T27, 25개 상품)
- 요약: **10% 적립 15 / 적립없음 4 / 에러(판매중단) 6**
  - 적립 ✓(15): 1,10,12,13,14,16,17,20,22,25,26,28,29,30,32
  - 적립없음 ✗(4): 2,3(하루견과 출석적립), 9(이디야카페인), 31(닥터린)
  - ERR 판매중단(6): 6 레놉티, 7 락토핏, 8 이디야디카페인, 11 라메종드미엘, 15 셀게이트글루타치온, 23 셀게이트컬리케일
- 확인 필요: ERR 6건은 일시품절/단종 구분 안 됨 — 평소 품목이면 눈으로 확인. 17·25·26·29는 tier=0 `≥10% tier unreachable(cap 500K)` 표시(구간상품 계산 특성, 에러 아님)
