# WORKLOG 2026-08-28 — 롯데 #1~7 조합12 결제 7/7 완주 / 쿠폰 누락 버그 2건 + 하나카드 롯데 경로 신설

## 0. 사용자 지시 (문장 그대로)

1. "238603 172.30.1.60:44185 페어링코드 / 포트번호 36933 / 롯데 1-7번 장바구니 담겨져있는거 오늘 NH야,
   핸드세이크로 해가지고 결제 마쳐줘 / 이전에 항상 핸드세이크로 했어, 코드로 진행하는데 그 사이에
   핸드세이크가 스크린샷만 도와주면 돼"
2. "왤케오래걸림?"
3. "2번,3번 왜 쿠폰 안받았음?"
4. "3번은 내가 비밀번호 입력하고 가만있길래 결제완료 눌렀는데 보니까 쿠폰 적용안되어있던데?"
5. "주문 내가 취소했어"
6. "결제 취소했음 2-7번 아이디 들어가서 장바구니는 그대로 냅두고 상품별로 쿠폰 다시 받고 다시 2-7결제해 /
   이번에는 하나카드로해 2,3번 결제를 농협으로했었는데 취소한주문건 다시 동일한카드로 주문하면
   할인헤택사라짐 오늘하나카드도 할인대상이니까 그렇게해"
7. "2,3 장바구니 다시담아야겠네 4,5,6,7 은 결제안햇으니 장바구니그대로남겨져있음 / 쿠폰 다 제대로받고 결제진행해"
8. "쿠폰적용 제대로해 빼먹지말고"

---

## 1. 최종 결과 — **7/7 완주, 실돈 3,800,062원**

| # | 계정 | 주문번호 | 금액 | 카드 | 뷰티 |
|---|---|---|---|---|---|
| 1 | tkdkky2002 | 2026-08-28-C28245 | 543,857 | NH | ✓ 5,467P |
| 2 | kms3945 | 2026-08-28-C29418 | 543,857 | 하나 | ✓ |
| 3 | kgi5907 | 2026-08-28-C29340 | 543,479 | 하나 | ✓ |
| 4 | owyura | 2026-08-28-C29523 | 540,669 | 하나 | ✓ |
| 5 | yr5326 | 2026-08-28-C29885 | 543,857 | 하나 | ✓ |
| 6 | ybkim8225@gmail.com | 2026-08-28-C29964 | 540,486 | 하나 | ✓ |
| 7 | lee6040314@naver.com | 2026-08-28-C30896 | 543,857 | 하나 | ✓ |

전 계정 **할인쿠폰 2 + 플러스쿠폰 14%·12% + 포인트 전액** 적용 확인. 구매사은은 7계정 모두
'최대 N% 적립' 카드 미발견(광세일 행사상품 아님) — 정상 SKIP.

폐기된 주문(사용자 취소): #2 C28434(630,000) · #3(NH, 사용자가 직접 결제완료 후 취소).

---

## 2. ★쿠폰 누락 버그 2건 — 계정당 8~9만원이 조용히 새고 있었다

### 2-1. `_scroll_to` 단방향 스크롤 (지시 3의 답)

`_scroll_to()` 는 **아래로만** 스크롤했다. 할인쿠폰 '선택완료' 직후 화면이 이미 플러스쿠폰
**아래**에 있으면 8회 헛스크롤 후 `'플러스쿠폰 섹션 없음'` 으로 **조용히 스킵**한다.
에러가 아니라 정상 SKIP(ok:True)이라 그대로 결제까지 갔다.

- #1 은 우연히 화면 위치가 맞아 성공(543,857원) / **#2·#3 은 누락돼 630,000원**.
- 포인트 `used:0` 도 같은 원인(`use_all_points` 도 아래로만 훑음).

→ **양방향 수정**: 한 방향으로 못 찾으면 반대방향으로 한 번 더 훑는다.
   `use_all_points` 의 자체 스크롤 루프도 동일하게 양방향화.

### 2-2. 섹션 라디오 탭 후 **낡은 좌표**로 '변경 >' 탐색 (지시 8 직전에 잡음)

`sec = _scroll_to("플러스쿠폰")` → `tap(sec)` → `_coupon_change_btn(sec)`.
라디오를 탭하면 화면이 **리플로우**되는데 탭 **전** 좌표(sec)로 버튼을 찾아 놓친다.
그리고 그걸 `'변경 없음(받은 쿠폰 X)'` = **쿠폰 없는 계정**으로 해석해 또 조용히 스킵.

- **2026-08-28 #5 yr5326 실측: 플러스쿠폰 26장 보유인데 '받은 쿠폰 X'** 로 스킵.
  627,197원(정상 543,857원)에 결제되기 **직전에 모니터로 잡아 중단** — 결제 실행 전이라 취소 불필요.
- → `_coupon_change_btn` 실패 시 **섹션 재탐색으로 좌표 갱신 후 재시도**. 같은 구조인
  `set_discount_coupons` 에도 동일 적용.

### 2-3. 교훈 — "조용한 SKIP" 이 진짜 사고다

두 버그 다 **에러가 안 난다**. `ok:True` + skip 문구로 넘어가 결제까지 간다.
로그를 안 보면 금액으로만 티가 난다(정가 700,000 / 할인쿠폰만 630,000 / 정상 543,xxx).
→ 결제 전 **`혜택 적용` 로그의 applied 수치를 반드시 확인**할 것. 0이면 결제 중단.
   이번엔 `applied: 0` 감지 전용 모니터를 따로 걸어 #5 를 결제 전에 세웠다.

---

## 3. 하나카드 롯데 경로 신설 — `pay_lotte_hana` (첫 라이브 검증 ✅)

롯데 `buy_one` 분기엔 하나가 **아예 없어** `UNVERIFIED_CARD:하나` 로 정지했다.
현대몰 `pay_hana`(검증됨) + 롯데 `pay_lotte_kb` 패턴을 합쳐 신설. 카드앱 구간은 몰 무관이라
`hana_card.json flow_payment[16:22]` 재사용(22 이후는 hmall 전용 '결제완료' 탭이라 제외).

경로: (원)결제하기 → 하나 SDK '하나Pay 하나카드 결제' 박스 → 하나앱(com.hanaskcard.paycla)
 → '다음' → nFilter pin6(`source=sequential_logo`) → 롯데 복귀 주문완료 폴링.

- PIN 은 **로고칸 밝기 검출 + 순서매핑**이라 숫자 OCR 자체를 안 한다(8↔0 혼동 없음) →
  **핸드셰이크 불필요**. NH(카드번호 직접입력)와 달리 하나는 앱카드라 판독할 키패드가 없다.

### ⚠️ 하나 보안앱 '재실행' 경고 = 이번 유일한 실패모드

하나앱 진입 즉시 **"안정적인 이용을 위해 앱을 다시 실행해주세요"** 팝업이 '다음' 버튼을 덮어
`step 3 tap_dump_text '다음' 미발견 (timeout 8s)` 로 죽는다. **#5·#7 에서 재현.**

- `am force-stop com.hanaskcard.paycla` 만으로는 **안 풀렸다**(#7 2차도 동일 실패).
- **먹힌 것 = 시간 간격.** #5 는 정리 후 몇 분 뒤 재시도해 통과, #7 은 **90초 대기** 후 통과.
  → 짧은 간격의 연속 하나앱 호출을 보안모듈이 막는 것으로 보인다.
  → 코드엔 결제 전 force-stop 을 넣어뒀고(무해), **연속 실패 시엔 1~2분 쉬었다 재시도**할 것.
- PIN 입력(step 5) **전** 단계라 이 실패는 **항상 미결제**. 재시도 안전.

---

## 4. 취소된 주문의 카트·쿠폰 (지시 6·7)

- **결제된 카트는 취소해도 복원되지 않는다.** #2·#3 은 카트 0건 → `buy/sulwhasoo.py lotte <idx> 12` 로 재담기.
  (#4~7 은 미결제라 카트 2건 그대로 유지 — 실조회로 확인)
- 쿠폰도 취소와 함께 사라져 다시 받아야 한다. 롯데는 **쿠폰을 담기 시점의 상품 상세페이지에서** 받으므로
  카트 화면엔 받을 자리가 없다(`applyCoupons` 는 롯데에선 no-op).
  → **`buy/recollect_coupons.py` 신설**: 카트를 건드리지 않고 상품페이지만 재방문해 쿠폰받기 →
    전체 다운로드 → 카트 건수 검증까지. `python3 buy/recollect_coupons.py 2 3 4 5 6 7 --codes=e,h`

---

## 5. 결제 여부 판정 — 카트 건수가 주문내역보다 빨랐다

PAY_FAIL 후 재시도 전 실조회(표준 절차). 이번엔 `myLotte/orderList.lotte` 본문이 동적 로딩이라
주문번호가 안 읽혔다(빈 결과 = 조회 실패, 신뢰 금지). 대신 **카트 건수**가 결정적이었다:

- 결제 성공 → 카트 **비워짐**(#2·#3 실측) / 미결제 → 카트 **2건 유지**(#5·#7 실측)
- #5·#7 모두 "카트 2건 유지" + "하나앱이 PIN 단계 전에 죽음" **두 증거 일치** → 미결제 확정 후 재시도.

---

## 6. 소요 시간 (지시 2의 답)

- NH(#1) = **약 6~8분/계정**: 자동화 3~4분 + 키패드 핸드셰이크 6칸(카드번호 3칸·CVC·비번)
  칸마다 재셔플이라 스크린샷 왕복 6회.
- 하나 = **약 4~5분/계정**: 앱카드라 키패드 왕복 없음. 하나가 확실히 빠르다.

---

## 7. 변경 파일

| 파일 | 변경 |
|---|---|
| `phone_auto/lotte_homeshopping_buy.py` | `_scroll_to` 양방향 / `use_all_points` 양방향 / `set_plus_coupons`·`set_discount_coupons` 변경버튼 재탐색 / `pay_lotte_hana` 신설 + buy_one 하나 분기 + HANA_FLOW import |
| `buy/recollect_coupons.py` | **신설** — 카트 보존 쿠폰 재수령 |
| `cart/today_carts.json` | #1~7 paid/order/card 기록 |

## 8. 남은 것 / 다음에

- [ ] 구매사은 적립: 7계정 모두 미발견(광세일 행사상품 아닌 것으로 보임) — 필요 시 수동 확인
- [ ] 하나 보안앱 경고: 계정 간 **대기시간**을 코드에 넣을지 검토(현재는 실패 시 수동으로 1~2분)
- [ ] `myLotte/orderList.lotte` 동적로딩 → 주문내역 실조회 헬퍼가 빈 결과를 반환. 카트 건수 병행 판정 권장
- [ ] 8/23 H.Point 이벤트(P202608074038) 미신청 16계정 — 사용자 결정 대기(이월)
