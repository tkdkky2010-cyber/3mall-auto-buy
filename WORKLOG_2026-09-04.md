# 2026-09-04 워크로그

## 0. 사용자 지시 (문장 그대로)

> "스텝 1,2 실행해"
> "오늘 담기 진행 (견과 #1~5, 이디야 #11~15, 뉴케어 제외) 이거해줘 / 그리고 9.1 1-5번 계정 구매 사진처럼했어"
> "결제 진행해"
> "오늘 현대카드로 해"
> "BC카드 자꾸 쓰는데 현대카드로만써"
> "현대카드 한도 비워놨어"
> "게정에 포인트있어서 그랬던거니까 오늘 구매한 전체계정 들어가서 살펴봐, 초록색 갈색 둘다 2개씪샀는지 체크, 이디야 샀는지도 체크"
> "계정별 구매 수량 체크하고, 못산거 다 사"
> "2개씩샀는데 78000원 이야?"

## 1. 결과 — 식품 15/15 결제 완료 (**전부 현대카드**)

주문내역 실측으로 **계정별 수량까지 검증 완료**. 대장 = 주문내역 일치.

| 계정 | 초록견과 ×2 | 갈색견과 ×2 |
|---|---|---|
| 1 tkdkky2002 | 20260904081626 | 20260904082558 |
| 2 1plu1mall1 | 20260904090071 | 20260904083407 |
| 3 kgi5907@daum.net | 20260904084368 | 20260904084976 |
| 4 1plus1mall0 | 20260904085485 | 20260904086158 |
| 5 1plus1mall1 | 20260904090292 | 20260904090464 |

| 계정 | 이디야 오리지널 ×2 (62,662원) |
|---|---|
| 11 kimgaeun04 | 20260904093342 |
| 12 Jinhwa4553 | 20260904092079 |
| 13 gcd0327 | 20260904092474 |
| 14 johwajeong | 20260904092738 |
| 15 jyj041220 | 20260904093064 |

**대장 15행 / 카드승인 합계 1,538,194원.** 뉴케어(40)는 step2 실측 `ERR 판매중단` — **이틀째** 제외.

## 2. ★근본버그 — `then=` 2차 주문이 CLI 카드 지정을 상속하지 않는다 (수정함)

> 사용자 지시: **"BC카드 자꾸 쓰는데 현대카드로만써"**

`main()` 은 1차를 `_order(card_override, ...)` 로 부르는데 2차는 `_order(kcard or None, ...)` 였다.
`then=시그니처오리지널` 처럼 `:카드` 를 안 붙이면 `kcard=""` → `None` → **당일카드 자동감지로 폴백**.

오늘은 **BC 와 현대가 같은 할인율로 공동 1위**라 `detect_card` 가 주문마다 다른 쪽을 골랐다:

| 계정 | 1차 | 2차 감지 | 2차 사용 |
|---|---|---|---|
| 1·3·4 | 현대 | 현대 | 현대 (우연히 맞음) |
| **5** | 현대 | **BC** | **BC** ← 페이북 PIN 화면 직전에 발견·중단 |

→ `r = _order(kcard or card_override, [kw], ...)` 로 수정. 단계 지정 > CLI 지정 > 자동감지.
재실행에서 `[#5] 당일카드 감지=BC, 사용=현대` 로 검증.

★**금액이 같은 카드가 2장이면 자동감지는 비결정적**이다. 카드를 지정했으면 **모든 주문 단계에
그 지정이 전달되는지** 확인할 것. 어제(9/3)의 "같은 할인율 2장" 버그와 **같은 뿌리, 다른 자리**다.

## 3. ★내가 틀린 것 — 주문내역 목록 금액은 **총액이 아니라 개당 단가**다

사용자 질문 **"2개씩샀는데 78000원 이야?"** 로 드러났다.

마이페이지 주문내역 목록은 `수량: 2개 / 77,999 원` 처럼 보이지만, 그 금액은 **단가**다.
주문상세(`selectOrdPTCPup?ordNo=…`)를 열어야 진짜 결제 흐름이 나온다 — #1 초록 실측:

```
주문금액 179,800 (89,900×2)
할인    -23,801  (쿠폰 17,980 + 카드즉시할인 5,821)
결제금액 155,999   ← 77,999 × 2
H.Point  -45,400
카드승인 110,599   ← 대장에 기록된 값
```

나는 단가를 총액으로 읽고 **"실제 결제액이 대장보다 훨씬 낮다"** 고 보고했다. **틀렸다.**
대장 금액(카드승인액)은 처음부터 정확했다. 사용자가 **"계정에 포인트있어서 그랬던거니까"** 라고
먼저 짚어줬는데도 나는 계속 오독을 붙들고 있었다.

★교훈: **목록 화면의 숫자는 무엇의 값인지 모른다.** 상세로 한 계층 더 들어가기 전에는
총액이라고 단정하지 말 것. (9/2 §13 "한 계층 더 볼 것" 과 같은 교훈을 또 어겼다.)

## 4. ★내가 놓친 것 — #11 결제를 핸드셰이크 미응답으로 실패시켰다

현대카드는 주문마다 **키패드 판독 2회**(PIN 6자리 + 카드비번 4자리)가 필요하다.
그런데 §3 금액 조사에 매달리는 동안 #11 요청을 못 봐서 **240초 타임아웃**:
`EXC:input_pin(adb): 로컬+클로드 모두 PIN digit 못잡음`.

결제된 건 없어 재실행으로 복구했지만, **결제 실행 중에는 판독 대기가 최우선**이다.
조사는 결제가 멈춘 뒤에 한다.

> 참고: 롯데·하나는 자동입력이라 핸드셰이크가 없다. **현대는 매 주문 2회** — 15주문이면 최대 30회다.
> 판독 헬퍼 `scratchpad/kp.py` 를 만들어 응답 작성 시간을 줄였다(배열은 매번 눈으로 확인 후 pin/pw 선택).

## 5. 대장 정정 2건

- **9/1 누락 20행 보충** (사용자가 사진으로 확인해줌). 카드·주문번호는 `WORKLOG_2026-09-01.md` §1
  실측 표를 그대로 썼다. 금액은 **#1 만** 사진으로 품목별이 확인돼 채웠고, #2~5 는 계정 합계만
  알려져 있어 **공란**으로 뒀다 — 품목별로 쪼개면 지어낸 값이 된다.
- **9/3 설화수 #2**(20260903093342) 시트에만 1행 보충 (로컬 JSON 엔 이미 있었다).
- **9/4 주문 20260904083407 상품명 정정** — `초록색` 으로 기록됐으나 주문내역상 **갈색**.
  금액 92,150 은 포인트 47,300 차감 후 **카드승인액이라 그대로 뒀다**(금액 보정 금지 방침).
  ★안 고치면 #2 가 `초록 4개 / 갈색 0개` 로 집계돼 **다음 날 잔량 계산이 틀어진다.**

## 6. step1 / step2

- **step1 1차 실패** — 어제 카트 검수 때 Chrome 9222 에 남은 **Hmall 로그인 + reCAPTCHA 탭**을
  갤러리아가 CDP attach 하며 잡아 120초 read timeout. 탭을 `about:blank` 로 비우고 재실행 → 통과.
  ★**세션 끝에 CDP 탭을 정리해두면 다음 날 step1 이 안 막힌다.**
- step1 완료(13:52): 갤러리아 27조합 · inventory 새 버전 `c` · Hmall · 롯데 · 카트플랜 `O1:V32` · 미러 133행.
- 경고 3건 전부 정상: 단가 25건 불일치(시트 우선), 재고 4조합 차이(사은품 구성 변경 → append-only),
  조합번호 색칠 9개(`HIGHLIGHT_COMBOS` 하드코딩 고정값과 일치 — 플랜 기준으로 안 바뀜).
- step2 완료: 10%적립 13 / 적립없음 1(루솔) / **판매중단 3(28·39·40 뉴케어)**.

## 7. 남은 일

- [ ] **뉴케어(40) 이틀째 판매중단.** 9월 표는 #1~12 각 1개 × 9/2~9/17(버퍼 0). 9/3·9/4 이틀이
      밀려 9/5~9/17 13일로는 목표 16개에 **3개 부족**. 9/18~9/23 에 밀어넣을지 판단 필요.
- [ ] 9월 표 잔량(9/4 마감 기준): 초록·갈색 #1~5 각 **10개 완료 / 잔량 7**, 창 9/5~9/8 4일 ×2 = 8 (여유 1).
      이디야 #11~15 **8개 완료 / 잔량 8**, 창 9/5~9/8 4일 ×2 = 8 → **딱 맞음, 버퍼 0.**
- [ ] `#11 kimgaeun04` 웹 로그인이 오늘 차단됐다(`로그인에 실패하였습니다. 다른 로그인 수단을 이용바랍니다`).
      폰 앱 로그인은 정상이라 결제엔 지장 없었으나, 반복 로그인 검수 시 주의.
- [ ] `hmall_inquiry.py` 수정 + `hmall_inquiry_reset.py` — **내 작업 아님**, 손대지 않고 그대로 둠.

## 8. 이 세션에서 배운 것

| 내가 단정한 것 | 실제 |
|---|---|
| "주문내역 금액 = 총액" | **개당 단가**였다. 상세로 들어가야 결제 흐름이 보인다 (§3) |
| "실제 결제액이 대장보다 훨씬 낮다" | 대장이 처음부터 맞았다. 내 오독 (§3) |
| "#2 는 선택과 다른 상품이 결제됐다" | 초록이 **아직 결제 안 된 것**뿐. 상품명 1건만 오기록 (§5) |
| "카드를 지정했으니 전 주문이 그 카드다" | 2차 주문이 지정을 안 물려받아 BC 로 갈 뻔했다 (§2) |
| "판독은 조사하다 잠깐 미뤄도 된다" | 240초 타임아웃 = 결제 실패 (§4) |

**공통 처방 — 화면의 숫자든 로그의 성공이든, 그것이 무엇의 값인지 확인하기 전에는 결론내지 말 것.**
그리고 **돈이 움직이는 동안에는 다른 조사를 시작하지 말 것.**
