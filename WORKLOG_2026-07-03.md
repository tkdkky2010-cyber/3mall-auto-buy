# WORKLOG 2026-07-03 (식품 단독결제 — 미완, 집에서 이어할 것)

## 요약 (한 줄)
7.03 식품 단독결제 **15/19 완료**, **16~19 미결제**로 안전 중단(폰 결제 전 카트담기 지점). 재개 = `python3 buy/food_buy.py 16-19` (완료분 자동 skip). ⚠️ **12·13·14 적립(H.Point) 재확인 필요**.

## Step 2 (오늘 아침)
- `bash step2.sh` → `cart/today.json`(date 2026-07-03) + 시트 `7.03 식품` 생성.
- 오늘 10% 대상 10개: `[10,12,13,14,20,22,25,28,29,33]`. 이벤트 = "건강은 金이다 최대 10%적립"(flat simple_range 3만~2백만 10%, tier 없음 → 즉시할인가/실비 "—").

## 식품 구매 플랜 (사용자 스크린샷대로, qty 전부 2)
| 상품# | 상품명 | 계정 |
|---|---|---|
| 28 | 스키니랩 시서스 다이어트 11박 | 1,2,3,4,5,16,17 |
| 25 | 유기농 석류젤리 9박(90개) | 6,7,8,9,10,18,19 |
| 33 | GRN 붓기캔디 | 11,12,13,14,15 |
- SoT = `buy/cart_plan.json` (오늘 이 파일에 기록. 매일 이 파일만 갈아끼우면 됨).

## 진행 상태 (실돈)
- **결제완료 1~15** (`buy/_food_done.json` 기록). 
- **미결제 16(28)·17(28)·18(25)·19(25)**.
- 16번이 카트담기 단계일 때(폰 결제 전) `pkill`로 안전 중단 → 결제 도중 아님, 중복결제 위험 없음.

## ⚠️ 적립(H.Point) 재확인 TODO — 12·13·14
- 폰 재부팅 직후 이 3계정의 **적립신청 로그에 "로그인에 실패하였습니다. 다른…"** 텍스트 섞임(True로 보고됐지만 실반영 불확실). **결제는 확실히 됨.**
- 1~11·15는 적립 로그 깨끗(`✓ 신청 완료`). 
- 재확인/재신청: `apply_hpoint`는 idempotent(already_done 처리)라 재실행 안전. 12·13·14 적립만 다시 태우면 됨(PC 9222만 필요, 폰 불필요). prmo는 `cart/today.json`에서 자동 로드.

## 이번 세션 코드 변경 (⚠️ 커밋 안 함, uncommitted)
1. **`buy/cart_plan.json`** — 오늘 계정별 플랜(위 표). untracked 파일.
2. **`buy/food_buy.py`** — 하드코딩 PLAN 제거. `cart_plan.json` items 역매핑 → `PLAN`(계정→상품) + `QTY_BY`((계정,상품)→수량). QTY=2 하드코딩도 제거(플랜에서 읽음).
   - `pc_cart_set`: 단일상품 임시카트를 **`buy/_cart_plan_food_tmp.json`**에 쓰고 run.py에 `CART_PLAN_FILE` env로 전달 → 마스터 cart_plan.json 안 덮어씀(재개 시 PLAN 훼손 방지).
3. **`buy/run.py`** 3곳:
   - `PLAN_FILE = Path(os.environ.get("CART_PLAN_FILE") or (ROOT/"cart_plan.json"))` — env override(하위호환).
   - connect 직전 **`ensure_chrome(int(CDP_PORT))`** 추가 → 9222 죽음/탭0 자동복구(check10과 동일 패턴, 규칙 feedback_chrome_auto_launch 준수).
   - `login()`에 **비밀번호 변경 캠페인(pwdChangePup) 처리** 추가 → "90일 후 변경하기"(연기)로 닫고 재확인. ⚠️'지금 변경하기' 절대 금지.

## 이번 세션 사건/원인 (추측 아님, 실측)
- **9222 Chrome 탭0/사망** → `connect_over_cdp: Browser context management is not supported`. run.py가 ensure_chrome 미호출이던 게 원인 → 위 픽스로 해결.
- **폰 재부팅**(사용자, 12번 `LOGIN_FAIL:logout 실패 이전계정 안풀림` 멈춤 해소용). 재연결: `adb pair 172.30.1.83:<pairport> <code>` → `adb connect 172.30.1.83:36431` → **IP 중복 disconnect, mDNS(`adb-RFCNC0WRL9Z-…_adb-tls-connect._tcp`)만 유지**(안 그러면 bare adb "more than one device"). 재부팅 후 12번 결제 성공.
- **15번 카트 로그인 실패의 진짜 원인 = 비밀번호 변경 캠페인**(`/mo/cob/pwdChangePup`, 계정별 90일 주기 → 12~14는 안 걸리고 15만 걸림). login()이 "로그아웃" 텍스트만 보고 오판 → 위 픽스로 해결. 16~19도 걸리면 자동 처리.

## 재개 절차 (집에서)
1. 폰 무선 디버깅 재연결(adb pair+connect+mDNS dedup) — 위 참고.
2. `python3 buy/food_buy.py 16-19` (1~15 자동 skip, 계정 간 7분). 9222는 ensure_chrome로 자동 launch.
3. 완료 후 **12·13·14 적립 재확인/재신청**.
4. (선택) 코드 변경 커밋: cart_plan.json / food_buy.py / run.py.
