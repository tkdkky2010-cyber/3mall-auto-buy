# WORKLOG 2026-06-10 — 롯데 결제 **올스탑(설화수 품절)** + Step 2(Hmall 10%) 완료

폰: SM-G9960 무선 adb (페어링 172.30.1.95, connect :46273). PC Chrome 9222/9223. ⚠️실돈.
요약: 새벽 카트필된 롯데 15~20 결제 시도 → #15 카나리에서 **자음생2종(g) 품절** 발각 → 사용자 지시 **오늘 설화수 올스탑**(저녁 재고 입고 후 결정). 대신 **Step 2(check10)** 진행/완료.

## 1. 롯데 폰결제 15~20 — 카나리 #15 후 올스탑 ⏸️
새벽(03:32~03:46) PC 카트필 최종상태 (매번 clear 후 재담기 = 마지막 유효):
| # | 아이디 | 조합 | 구성 |
|---|---|---|---|
| 15·16 | joyoungjin1220 / kimmk2008 | 조합14 | g자생2×2 + h자생크림×1 |
| 17·18 | miu1838 / nami8225 | 조합23 (원래 조합17 2회 FATAL→23 대체) | h자생크림×3 |
| 19·20 | ybkim9960 / yeonsuk0428 | 조합23 | h자생크림×3 |

- **#15 라이브 카나리**: 로그인✓ 카트✓ 주소/쿠폰/포인트✓ 당일카드=삼성5%✓ → **카드번호 키패드에서 PAY_FAIL**.
- **카트 금액 216,002원 = 조합14(롯데 결제예상 533,264원) 아님**. CDP 크롬으로 #15 카트 직접 검증 → **카트 라인 1개뿐 = h(자음생크림리치세트, goods_no 2719761746)×1**.
- 원인 = **자음생2종(g)이 품절**이라 카트필 시 g×2가 안 담기고 h×1만 들어감 (카트필 코드 정상). 216,002 = h×1 롯데가와 정확히 일치.
- 사용자 결정: **오늘 설화수(롯데 결제) 전부 보류, 저녁 재고 입고 보고 재개**. #17·#18은 재개 시 조합23 유지로 확정.

### 1-a. 카드번호 키패드 OCR ROI 드리프트 fix (`phone_auto/hmall_hyundai_buy.py`)
- 앱 업데이트로 롯데 일반결제 카드번호 셔플키패드가 **화면 중앙으로 reflow**(윗줄 숫자 y≈1035). 기존 `KEYPAD_ROI_Y=(0.45,0.98)`(상단=y1080)이 **윗줄(1·2·3·4·5)을 잘라먹어** 2·3·4·5 누락 → `_tap_shuffle` 매핑 실패.
- 캡처 이미지 실측 검증: 중앙 band `(0.38,0.52)` + **2엔진(vision+gcv)**로 0~9 전부 정확 매핑(입력완료 y1260 배제, 4엔진은 입력완료를 '2'로 오독하는 stray 있어 2엔진 우선).
- fix = `_tap_shuffle` **ROI 에스컬레이션**: 기존 하단(`KEYPAD_ROI_Y`) → 신규 `KEYPAD_ROI_MID=(0.38,0.52)` 순차 시도. hmall(하단 키패드) 안 깨고 신 lotte 처리. **재개 시 라이브 미검증 — 첫 결제로 확인 필요.**
- 검증도구 신규: `buy/_verify_cart.py`(계정별 카트 goods_no 검증 — `<tr>` 안 hidden input goods_no/goods_nm/ord_qty/nl_prc 파싱), `buy/_dump_cart.py`(카트 DOM 덤프 1회성).

## 2. Step 2 (Hmall 우수스토어 10% 적립 체크) — 완료 ✅
`bash step2.sh` → `cart/check10.py`(9223 자동 launch) + `cart/show.py`. today.json 6/9자라 오늘 미실행 상태였음.
- 결과: **10%적립 16 / 적립없음 1 / 판매중단 5**. `cart/today.json`(date 2026-06-10) + 시트 `6.10 식품!A1:S24` 입력.
- **10% 적립 16개 ID**: `[2,3,9,10,11,12,13,14,16,17,20,22,25,28,29,32]`
- **적립없음 1**: 오라틱스 구강유산균(출석적립만).
- **판매중단 5**: 레놉티(6)·락토핏(7)·이디야 디카페인(8)·셀게이트 글루타치온30p(15)·셀게이트 컬리케일6박(23). check10이 `error:판매중단` 정상 감지. ⚠️ ERR=일시오류 아님(재실행 2회 동일) — **today.json에 판매중단으로 기록됨**.
- 정정: ERR을 일시 page-load 오류로 오판해 1회 불필요 재실행함(결과 동일, today.json 영향 없음).

## 미결/다음
- 저녁 설화수 재고 입고 확인 → 품절 풀린 조합 기준 롯데 15~20 결제 재개. 재개 시 키패드 ROI fix 라이브 검증.
- 판매중단 5종은 입고 시 자동 복구(check10 재실행으로 반영).
